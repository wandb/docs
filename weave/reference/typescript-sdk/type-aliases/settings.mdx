---
title: "Settings"
description: "TypeScript SDK reference"
---

> **Settings** = `object`

<sub>Source: [src/settings.ts:6](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/settings.ts#L6)</sub>

## Properties

### attributes

> `readonly` **attributes**: `Record`\<`string`, `any`\>

A map of attributes applied to every trace produced by this client.

<sub>Source: [src/settings.ts:17](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/settings.ts#L17)</sub>

***

### genai

> `readonly` **genai**: `object`

<sub>Source: [src/settings.ts:34](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/settings.ts#L34)</sub>

#### batchOptions

> `optional` **batchOptions**: `BufferConfig`

`BatchSpanProcessor` configuration. Ignored unless `spanProcessor === 'batch'`.

#### spanProcessor

> `optional` **spanProcessor**: `"batch"` \| `"simple"` \| `SpanProcessor`

How GenAI spans are exported.

- `'batch'` (default): `BatchSpanProcessor`, suitable for production
  agents and long-lived processes.
- `'simple'`: `SimpleSpanProcessor`, one HTTP POST per span. Useful for
  tests and short-lived CLIs where deterministic flush matters more than
  throughput.
- `SpanProcessor` instance: a user-supplied processor. The caller owns
  its lifecycle; the Weave OTLP exporter targeting `/agents/otel/v1/traces`
  is not used.

***

### printCallLink

> `readonly` **printCallLink**: `boolean`

Prints links in terminal to Weave UI for ops.

<sub>Source: [src/settings.ts:12](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/settings.ts#L12)</sub>

#### Default

`true`

***

### useCallsComplete

> **useCallsComplete**: `boolean`

Sends finished calls to the `calls/complete` endpoint (start+end paired
client-side) instead of the legacy `call/upsert_batch` path.

<sub>Source: [src/settings.ts:32](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/settings.ts#L32)</sub>

#### Default

`true`

***

### useOTelV2

> **useOTelV2**: `boolean`

Routes OTel-capable integrations through their OTel variant.

<sub>Source: [src/settings.ts:24](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/settings.ts#L24)</sub>

#### Default

`true`