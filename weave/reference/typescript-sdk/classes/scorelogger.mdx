---
title: "ScoreLogger"
description: "TypeScript SDK reference"
---

ScoreLogger manages scoring for a single prediction.
Returned from EvaluationLogger.logPrediction().

<sub>Source: [src/evaluationLogger.ts:330](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/evaluationLogger.ts#L330)</sub>

## Example

```ts twoslash
// @noErrors
const pred = await ev.logPrediction(example, output);
await pred.logScore("accuracy", 0.95);
await pred.logScore("relevance", 0.8);
await pred.finish(); // Finalizes the prediction
```

## Constructors

### Constructor

> **new ScoreLogger**(`evalLogger`): `ScoreLogger`

<sub>Source: [src/evaluationLogger.ts:338](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/evaluationLogger.ts#L338)</sub>

#### Parameters

<ParamField path="evalLogger" type="EvaluationLogger" required>
  See [`EvaluationLogger`](./evaluationlogger).
</ParamField>

#### Returns

`ScoreLogger`

## Accessors

### isFinishCalled

#### Get Signature

> **get** **isFinishCalled**(): `boolean`

Check if finish() has been called.
Used by EvaluationLogger to detect unfinished predictions.

<sub>Source: [src/evaluationLogger.ts:368](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/evaluationLogger.ts#L368)</sub>

##### Returns

`boolean`

## Methods

### finish()

> **finish**(): `Promise`\<`void`\>

Finish the scoring process for the prediction.
Finalizes the predict_and_score call with accumulated scores.
Updates incremental aggregates and frees memory.

<sub>Source: [src/evaluationLogger.ts:470](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/evaluationLogger.ts#L470)</sub>

#### Returns

`Promise`\<`void`\>

***

### logScore()

> **logScore**(`scorerName`, `score`): `Promise`\<`void`\>

Log a score for this prediction.
Creates a scorer call as a child of predict_and_score.

<sub>Source: [src/evaluationLogger.ts:379](https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/evaluationLogger.ts#L379)</sub>

#### Parameters

<ParamField path="scorerName" type="string" required>
  Name of the scorer (e.g., "accuracy", "f1_score")
</ParamField>

<ParamField path="score" type="any" required>
  The score value
</ParamField>

#### Returns

`Promise`\<`void`\>