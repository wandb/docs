---
title: "ネストされた関数のトレース"
description: "W&B トレースを使用して深くネストされた Call 構造をトラッキングする方法を学びます"
---

LLM を活用したアプリケーションには、複数の LLM の Call と、監視が重要な追加のデータ処理および検証ロジックが含まれる場合があります。多くのアプリケーションで一般的な深くネストされた Call 構造においても、`weave.op()` をトレースしたい各関数に追加することで、Weave は親子関係をネストされた関数全体にわたってトラッキングします。

以下のコードは[クイックスタートの例](/ja/weave/quickstart)をベースに、LLM から返されたアイテムの数をカウントし、それらを上位レベルの関数でラップするロジックを追加しています。また、この例では `weave.op()` を使用して、すべての関数、その Call の順序、および親子関係をトレースしています。

<Tabs>
  <Tab title="Python">
    ```python {1,7,26,32,42}
    import weave
    import json
    from openai import OpenAI

    client = OpenAI()

    @weave.op()
    def extract_dinos(sentence: str) -> dict:
        response = client.chat.completions.create(
            model="gpt-4o",
            messages=[
                {
                    "role": "system",
                    "content": """Extract any dinosaur `name`, their `common_name`, \
    names and whether its `diet` is a herbivore or carnivore, in JSON format."""
                },
                {
                    "role": "user",
                    "content": sentence
                }
                ],
                response_format={ "type": "json_object" }
            )
        return response.choices[0].message.content

    @weave.op()
    def count_dinos(dino_data: dict) -> int:
        # 返されたリストのアイテム数をカウントする
        k = list(dino_data.keys())[0]
        return len(dino_data[k])

    @weave.op()
    def dino_tracker(sentence: str) -> dict:
        # LLM を使用して恐竜を抽出する
        dino_data = extract_dinos(sentence)

        # 返された恐竜の数をカウントする
        dino_data = json.loads(dino_data)
        n_dinos = count_dinos(dino_data)
        return {"n_dinosaurs": n_dinos, "dinosaurs": dino_data}

    weave.init('jurassic-park')

    sentence = """I watched as a Tyrannosaurus rex (T. rex) chased after a Triceratops (Trike), \
    both carnivore and herbivore locked in an ancient dance. Meanwhile, a gentle giant \
    Brachiosaurus (Brachi) calmly munched on treetops, blissfully unaware of the chaos below."""

    result = dino_tracker(sentence)
    print(result)
    ```

    **ネストされた関数**

    上記のコードを実行すると、**Traces** ページに2つのネストされた関数 (`extract_dinos` と `count_dinos`) の入力と出力、および自動的にログされた OpenAI のトレースが表示されます。

    ![選択した Call の詳細パネルと中央のトレース ツリー パネルを表示するネストされた Weave Traces ページ](/images/tutorial_tracing_2_nested_dinos.png)
  </Tab>

  <Tab title="TypeScript">
    ```typescript
    import OpenAI from 'openai';
    import * as weave from 'weave';

    const openai = new OpenAI();

    const extractDinos = weave.op(async (sentence: string) => {
      const response = await openai.chat.completions.create({
        model: 'gpt-4o',
        messages: [
          {
            role: 'system',
            content:
              'Extract any dinosaur `name`, their `common_name`, names and whether its `diet` is a herbivore or carnivore, in JSON format.',
          },
          {role: 'user', content: sentence},
        ],
        response_format: {type: 'json_object'},
      });
      return response.choices[0].message.content;
    });

    const countDinos = weave.op(async (dinoData: string) => {
      const parsed = JSON.parse(dinoData);
      return Object.keys(parsed).length;
    });

    const dinoTracker = weave.op(async (sentence: string) => {
      const dinoData = await extractDinos(sentence);
      const nDinos = await countDinos(dinoData);
      return {nDinos, dinoData};
    });

    async function main() {
      await weave.init('jurassic-park');

      const sentence = `I watched as a Tyrannosaurus rex (T. rex) chased after a Triceratops (Trike),
            both carnivore and herbivore locked in an ancient dance. Meanwhile, a gentle giant
            Brachiosaurus (Brachi) calmly munched on treetops, blissfully unaware of the chaos below.`;

      const result = await dinoTracker(sentence);
      console.log(result);
    }

    main();

    ```

    **ネストされた関数**

    上記のコードを実行すると、**Traces** ページに2つのネストされた関数 (`extract_dinos` と `count_dinos`) の入力と出力、および自動的にログされた OpenAI のトレースが表示されます。

    {/* TODO: Update to TS screenshot */}

    ![選択した Call の詳細パネルと中央のトレース ツリー パネルを表示するネストされた Weave Traces ページ](/images/tutorial_tracing_2_nested_dinos.png)
  </Tab>
</Tabs>

<div id="tracking-metadata">
  ## メタデータのトラッキング
</div>

`weave.attributes` コンテキストマネージャーを使用し、呼び出し時にトラッキングするメタデータの辞書を渡すことで、メタデータをトラッキングすることができます。

上記の例を続けます：

<Tabs>
  <Tab title="Python">
    ```python lines {1,10-11}
    import weave

    weave.init('jurassic-park')

    sentence = """I watched as a Tyrannosaurus rex (T. rex) chased after a Triceratops (Trike), \
    both carnivore and herbivore locked in an ancient dance. Meanwhile, a gentle giant \
    Brachiosaurus (Brachi) calmly munched on treetops, blissfully unaware of the chaos below."""

    # 以前に定義した関数と一緒にメタデータをトラッキングする
    with weave.attributes({'user_id': 'lukas', 'env': 'production'}):
        result = dino_tracker(sentence)
    ```
  </Tab>

  <Tab title="TypeScript">
    ```plaintext
    この機能はまだ TypeScript では使用できません。
    ```
  </Tab>
</Tabs>

<Note>
  メタデータは、ユーザー ID やコードの環境ステータス (開発、ステージング、または本番) など、実行時にトラッキングすることをお勧めします。

  system prompt などのシステム設定をトラッキングするには、[Weave Models](/ja/weave/guides/core-types/models) を使用することをお勧めします。
</Note>

属性の使用方法の詳細については、[属性の定義とログ](/ja/weave/guides/tools/attributes)を参照してください。

<div id="whats-next">
  ## 次のステップ
</div>

* [App Versioning チュートリアル](/ja/weave/tutorial-weave_models)に従って、アドホックなプロンプト、モデル、アプリケーションの変更を取得し、バージョン管理して整理しましょう。