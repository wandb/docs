---
title: "login"
description: "TypeScript SDK リファレンス"
---

<div id="login">
  # login
</div>

▸ **login**(`apiKey`, `host?`): `Promise`\<`void`\>

指定されたAPIキーを使用して Weights & Biases (W&B) にログインします。
この関数は、今後の利用のために認証情報を netrc ファイルに保存しようとしますが、
ファイルシステムに書き込めない場合でも実行を続行します。

<div id="parameters">
  #### パラメータ
</div>

| Name | Type | Description |
| :------ | :------ | :------ |
| `apiKey` | `string` | W&B の APIキー。 |
| `host?` | `string` | （オプション）ホスト名（通常はカスタム W&B サーバーを使用している場合にのみ必要です）。 |

<div id="returns">
  #### 戻り値
</div>

`Promise`\<`void`\>

`Throws`

APIキーが指定されていない場合、または Weave trace サーバーへの接続を検証できない場合にスローされます。

<div id="defined-in">
  #### 定義元
</div>

[clientApi.ts:23](https://github.com/wandb/weave/blob/1aee4006a95d913addb45881dfc950de7ce7b0bd/sdks/node/src/clientApi.ts#L23)

___