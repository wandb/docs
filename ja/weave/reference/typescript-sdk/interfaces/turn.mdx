---
title: "ターン"
description: "TypeScript SDK リファレンス"
---

import { GitHubLink } from '/snippets/ja/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/genai/turn.ts#L98" />

agent invocation です。通常は、単一のユーザーメッセージに応答するための処理をラップします。`invoke_agent` span を発行し、そのターンにおける trace のルートとして機能します。常に `ROOT_CONTEXT` の下で開始されるため、OTel でインストルメントされた他のライブラリから誤って親を継承することはありません。

`weave.startTurn()` (または `conversation.startTurn()`) によって作成され、`end()` で終了します。1 つの非同期チェーン内で実行中にできるターンは 1 つだけです。子 (LLM、Tool、SubAgent) は `startLLM`、`startTool`、`startSubagent` の各 method を通じて attach されます。

## 例 {#examples}

```ts twoslash
// @noErrors
const turn = weave.startTurn();

try {
  const llm = turn.startLLM({model: 'gpt-4o', providerName: 'openai'});
  // ...
  llm.end();
} finally {
  turn.end();
}
```

```ts twoslash
// @noErrors
const turn = weave.startTurn({
  model: 'gpt-4o',
  agentName: 'research-bot',
  agentId: 'research-bot-prod',
  agentDescription: 'Looks up facts on Wikipedia.',
  agentVersion: '1.4.2',
  userMessage: 'What is the weather in Tokyo?',
  systemInstructions: ['You are a helpful weather bot.'],
  startTime: new Date('2026-05-29T10:00:00.000Z'),
});

try {
  const llm = turn.startLLM({model: 'gpt-4o', providerName: 'openai'});
  // ...
  llm.end();
} finally {
  turn.end();
}
```

## 継承元 {#extends}

* `SpanBase`

## アクセサー {#accessors}

### エージェント名 {#agentname}

#### Get Signature {#get-signature}

> **get** **agentName**(): `string`

##### 戻り値 {#returns}

`string`

***

### モデル {#model}

#### Get Signature {#get-signature-2}

> **get** **model**(): `string`

##### 戻り値 {#returns-2}

`string`

## メソッド {#methods}

### ~~addEvent()~~ {#addevent}

<Warning>
  **非推奨。** 代わりに [setAttributes](#setattributes) でこのデータを記録してください。
  OpenTelemetry は Span Event API (`Span.addEvent`) を段階的に廃止しています。この
  メソッドは引き続き動作し、既存の span event データも有効なままです。
  [https://opentelemetry.io/blog/2026/deprecating-span-events/](https://opentelemetry.io/blog/2026/deprecating-span-events/) を参照してください。
</Warning>

> **addEvent**(`name`, `attributes?`, `startTime?`): `this`

名前付きイベントを span に追加します。コンテキストの圧縮、ツールループの検出、guardrail のトリガーなど、
span 以外の時点を記録する際に便利です。`end()` の後は警告を出し、
何も行いません。OTel の `Span.addEvent` と同様の動作をします。

#### パラメーター {#parameters}

<ParamField path="name" type="string" required />

<ParamField path="attributes" type="Attributes" />

<ParamField path="startTime" type="TimeInput" />

#### 戻り値 {#returns-3}

`this`

#### 例 {#example}

```ts twoslash
// @noErrors
span.addEvent('context_compacted', {removedMessages: 12});
```

#### 継承元 {#inherited-from}

`SpanBase.addEvent`

***

### end() {#end}

> **end**(`opts?`): `void`

現在のフィールド値を読み取り (`start`以降に`record()`で行った変更を反映) 、span を終了します。冪等です。失敗としてマークするには`error`を渡します。終了時刻を過去に設定するには`endTime`を渡します。

#### パラメーター {#parameters-2}

<ParamField path="opts" type="object">
  <Expandable title="プロパティ" defaultOpen>
    <ParamField path="endTime" type="TimeInput" />

    <ParamField path="error" type="Error" />
  </Expandable>
</ParamField>

#### 戻り値 {#returns-4}

`void`

***

### record() {#record}

> **record**(`opts`): `this`

変更可能なフィールドの任意のサブセットを一括で設定します。マージではなく置換されます。
provider 呼び出しの戻り値を受け取った後に、すべてを一度に割り当てる場合に便利です。

#### パラメーター {#parameters-3}

<ParamField path="opts" type="object" required>
  <Expandable title="プロパティ" defaultOpen>
    <ParamField path="agentDescription" type="string" />

    <ParamField path="agentId" type="string" />

    <ParamField path="agentName" type="string" />

    <ParamField path="agentVersion" type="string" />

    <ParamField path="messages" type="Message[]">
      [`Message`](./message) を参照してください。
    </ParamField>

    <ParamField path="model" type="string" />

    <ParamField path="outputMessages" type="Message[]">
      [`Message`](./message) を参照してください。
    </ParamField>

    <ParamField path="systemInstructions" type="string[]" />
  </Expandable>
</ParamField>

#### 戻り値 {#returns-5}

`this`

***

### ~~setAttribute()~~ {#setattribute}

<Warning>
  **非推奨。** 代わりに、Python
  SDK の `set_attributes` および OTel の `Span.setAttributes` に対応する [setAttributes](#setattributes) を使用してください。既存の単一属性の呼び出し元が引き続き動作するよう、薄い
  alias として残されています。これを備えているのは `ターン` のみで、ほかの emitter に単数形はありません。
</Warning>

> **setAttribute**(`key`, `value`): `this`

#### パラメーター {#parameters-4}

<ParamField path="key" type="string" required />

<ParamField path="value" type="AttributeValue" required />

#### 戻り値 {#returns-6}

`this`

***

### setAttributes() {#setattributes}

> **setAttributes**(`attributes`): `this`

span に複数の属性を一度に設定します。`end()` の後に呼び出すと警告が表示され、何も実行されません。OTel の `Span.setAttributes` (および Python SDK の
`set_attributes`) と同様です。

#### パラメーター {#parameters-5}

<ParamField path="attributes" type="Attributes" required />

#### 戻り値 {#returns-7}

`this`

#### 例 {#example-2}

```ts twoslash
// @noErrors
span.setAttributes({'weave.tag': 'prod', 'gen_ai.response.id': id});
```

#### 継承元 {#inherited-from-2}

`SpanBase.setAttributes`

***

### startLLM() {#startllm}

> **startLLM**(`opts`): [`LLM`](./llm)

この ターン 内で子 LLM span を開始します。

#### パラメーター {#parameters-6}

<ParamField path="opts" type="LLMInit" required>
  [`LLMInit`](./llminit) を参照してください。
</ParamField>

#### 戻り値 {#returns-8}

[`LLM`](./llm)

***

### startSubagent() {#startsubagent}

> **startSubagent**(`opts`): [`SubAgent`](./subagent)

この ターン 内で子 SubAgent の span を開始します。

#### パラメーター {#parameters-7}

<ParamField path="opts" type="SubAgentInit" required>
  [`SubAgentInit`](./subagentinit) を参照してください。
</ParamField>

#### 戻り値 {#returns-9}

[`SubAgent`](./subagent)

***

### startTool() {#starttool}

> **startTool**(`opts`): [`Tool`](./tool)

この ターン 内で子 Tool span を開始します。

#### パラメーター {#parameters-8}

<ParamField path="opts" type="ToolInit" required>
  [`ToolInit`](./toolinit) を参照してください。
</ParamField>

#### 戻り値 {#returns-10}

[`Tool`](./tool)