---
title: "LLM"
description: "TypeScript SDK リファレンス"
---

import { GitHubLink } from '/snippets/ja/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/genai/llm.ts#L90" />

LLM Call。`gen_ai.*` 属性を含む `chat` span を生成します。

`weave.startLLM()` (または `turn.startLLM()`) で作成し、`end()` で終了します。非同期コンテキストでは、一度にアクティブにできる LLM は 1 つのみです。`startTool` / `startSubagent` を使用して、その配下に tool/subagent Call をネストします。

`inputMessages` / `outputMessages` / `usage` / `reasoning` は直接設定するか、ヘルパー関数 (`output`、`think`、`attachMedia`、`record`) を使用して設定します。

記録されたすべてのデータは、`end()` 時に span にフラッシュされます。

## 例 {#examples}

```ts twoslash
// @noErrors
const llm = weave.startLLM({model: 'gpt-4o-mini', providerName: 'openai'});

try {
  llm.inputMessages = [{role: 'user', content: prompt}];
  const resp = await openai.chat.completions.create({...});
  llm.output(resp.choices[0].message.content ?? '');
  llm.record({usage: {inputTokens: resp.usage?.prompt_tokens}});
} finally {
  llm.end();
}
```

```ts twoslash
// @noErrors
const llm = weave.startLLM({
  model: 'gpt-4o-mini',
  providerName: 'openai',
  systemInstructions: ['You are a helpful weather bot.'],
  startTime: new Date('2026-05-29T10:00:00.000Z'),
});

try {
  // ... LLM を呼び出し、llm.outputMessages / usage を設定 ...
} finally {
  llm.end();
}
```

## 継承元 {#extends}

* `SpanBase`

## プロパティ {#properties}

### inputMessages {#inputmessages}

> **inputMessages**: [`Message`](./message)[] = `[]`

モデルに送信される入力メッセージです。`end()` の呼び出し時に `gen_ai.input.messages` にフラッシュされます。

***

### モデル {#model}

> `readonly` **モデル**: `string`

***

### outputMessages {#outputmessages}

> **outputMessages**: [`Message`](./message)[] = `[]`

モデルが返すアシスタント メッセージ。`end()` 時に
`gen_ai.output.messages` にフラッシュされます。

***

### providerName {#providername}

> `readonly` **providerName**: `string`

***

### 推論 {#reasoning}

> `optional` **推論**: [`Reasoning`](./reasoning)

思考の連鎖の内容です。シリアル化時に、最後のアシスタント メッセージに
ReasoningPart として組み込まれます。

***

### usage {#usage}

> **usage**: [`Usage`](./usage) = `{}`

トークン数とキャッシュ統計です。`end()` 時に `gen_ai.usage.*` へフラッシュされます。

## メソッド {#methods}

### ~~addEvent()~~ {#addevent}

<Warning>
  **非推奨。** 代わりに [setAttributes](#setattributes) を使用してこのデータを記録してください。
  OpenTelemetry は Span Event API (`Span.addEvent`) を段階的に廃止しています。この
  メソッドは引き続き使用でき、既存の span event データも有効です。
  [https://opentelemetry.io/blog/2026/deprecating-span-events/](https://opentelemetry.io/blog/2026/deprecating-span-events/) を参照してください。
</Warning>

> **addEvent**(`name`, `attributes?`, `startTime?`): `this`

名前付きイベントを span に追加します。コンテキストの圧縮、tool ループの検出、guardrail の発動など、
span 以外の時点をマークする場合に役立ちます。`end()` の後に呼び出すと警告が表示され、
何も実行されません。OTel の `Span.addEvent` に対応します。

#### パラメーター {#parameters}

<ParamField path="name" type="string" required />

<ParamField path="attributes" type="Attributes" />

<ParamField path="startTime" type="TimeInput" />

#### 戻り値 {#returns}

`this`

#### 例 {#example}

```ts twoslash
// @noErrors
span.addEvent('context_compacted', {removedMessages: 12});
```

#### 継承元 {#inherited-from}

`SpanBase.addEvent`

***

### attachMedia() {#attachmedia}

> **attachMedia**(`opts`): `this`

LLM Call に添付するメディアを準備します。
`content` (インライン base64 バイト) 、`uri` (URI 参照) 、または `fileId`
(事前にアップロードされた file ID) のいずれか 1 つだけを指定します。添付は `end()` 時に
`inputMessages` 内の最後のユーザーメッセージに追加されます。

#### パラメーター {#parameters-2}

<ParamField path="opts" type="{…} | {…} | {…}" required>
  [`Modality`](../type-aliases/modality)、[`Modality`](../type-aliases/modality)、[`Modality`](../type-aliases/modality) を参照してください。

  完全なタイプ: `{ content: string; mimeType: string; modality: Modality; } | { modality: Modality; uri: string; } | { fileId: string; mimeType?: string; modality: Modality; }`
</ParamField>

#### 戻り値 {#returns-2}

`this`

***

### attachMediaUrl() {#attachmediaurl}

> **attachMediaUrl**(`url`, `opts`): `this`

`attachMedia({uri, modality})` の簡易版です。

#### パラメーター {#parameters-3}

<ParamField path="url" type="string" required />

<ParamField path="opts" type="object" required>
  <Expandable title="プロパティ" defaultOpen>
    <ParamField path="modality" type="Modality" required>
      [`Modality`](../type-aliases/modality) を参照してください。
    </ParamField>
  </Expandable>
</ParamField>

#### 戻り値 {#returns-3}

`this`

***

### end() {#end}

> **end**(`opts?`): `void`

蓄積された状態をフラッシュして span を閉じます。冪等です。失敗としてマークするには `error` を渡し、終了時刻を過去に設定するには `endTime` を渡します。

#### パラメーター {#parameters-4}

<ParamField path="opts" type="object">
  <Expandable title="プロパティ" defaultOpen>
    <ParamField path="endTime" type="TimeInput" />

    <ParamField path="error" type="Error" />
  </Expandable>
</ParamField>

#### 戻り値 {#returns-4}

`void`

***

### output() {#output}

> **output**(`content`): `this`

レスポンスにアシスタント メッセージを追加します。

#### パラメーター {#parameters-5}

<ParamField path="content" type="string" required />

#### 戻り値 {#returns-5}

`this`

***

### record() {#record}

> **record**(`opts`): `this`

変更可能なフィールドの任意のサブセットを一括で設定します。マージではなく置換されます。
provider 呼び出しの結果が返された後に、すべてを一度に割り当てる場合に便利です。

#### パラメーター {#parameters-6}

<ParamField path="opts" type="object" required>
  <Expandable title="プロパティ" defaultOpen>
    <ParamField path="finishReasons" type="string[]" />

    <ParamField path="inputMessages" type="Message[]">
      [`Message`](./message) を参照してください。
    </ParamField>

    <ParamField path="mediaAttachments" type="({…} | {…} | {…})[]">
      [`Modality`](../type-aliases/modality)、[`Modality`](../type-aliases/modality)、[`Modality`](../type-aliases/modality) を参照してください。

      完全なタイプ: `({ content: string; mimeType: string; modality: Modality; } | { modality: Modality; uri: string; } | { fileId: string; mimeType?: string; modality: Modality; })[]`
    </ParamField>

    <ParamField path="outputMessages" type="Message[]">
      [`Message`](./message) を参照してください。
    </ParamField>

    <ParamField path="outputType" type="string" />

    <ParamField path="reasoning" type="Reasoning">
      [`Reasoning`](./reasoning) を参照してください。
    </ParamField>

    <ParamField path="responseId" type="string" />

    <ParamField path="responseModel" type="string" />

    <ParamField path="usage" type="Usage">
      [`Usage`](./usage) を参照してください。
    </ParamField>
  </Expandable>
</ParamField>

#### 戻り値 {#returns-6}

`this`

***

### setAttributes() {#setattributes}

> **setAttributes**(`attributes`): `this`

span に複数の属性をまとめて設定します。`end()` の呼び出し後は警告を出力し、
何も実行しません。OTel の `Span.setAttributes` (および Python SDK の
`set_attributes`) と同様です。

#### パラメーター {#parameters-7}

<ParamField path="attributes" type="Attributes" required />

#### 戻り値 {#returns-7}

`this`

#### 例 {#example-2}

```ts twoslash
// @noErrors
span.setAttributes({'weave.tag': 'prod', 'gen_ai.response.id': id});
```

#### 継承元 {#inherited-from-2}

`SpanBase.setAttributes`

***

### startSubagent() {#startsubagent}

> **startSubagent**(`opts`): [`SubAgent`](./subagent)

この LLM 内にネストされた子 SubAgent の span を開始します。

#### パラメーター {#parameters-8}

<ParamField path="opts" type="SubAgentInit" required>
  [`SubAgentInit`](./subagentinit) を参照してください。
</ParamField>

#### 戻り値 {#returns-8}

[`SubAgent`](./subagent)

***

### startTool() {#starttool}

> **startTool**(`opts`): [`Tool`](./tool)

この LLM 内にネストされた子 Tool span を開始します。

#### パラメーター {#parameters-9}

<ParamField path="opts" type="ToolInit" required>
  [`ToolInit`](./toolinit) を参照してください。
</ParamField>

#### 戻り値 {#returns-9}

[`Tool`](./tool)

***

### think() {#think}

> **think**(`content`): `this`

モデルの推論/思考の連鎖 コンテンツを設定または追加します。内容は
`this.reasoning.content` に蓄積されます。シリアル化時には `ReasoningPart` として最後のアシスタント メッセージに
組み込まれ、Python SDK のオンザワイヤ形式に一致します。

#### パラメーター {#parameters-10}

<ParamField path="content" type="string" required />

#### 戻り値 {#returns-10}

`this`