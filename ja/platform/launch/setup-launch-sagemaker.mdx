---
title: 'チュートリアル: SageMaker で W&B Launch をセットアップする'
description: "ECR、S3、IAM のセットアップ手順を使用して、Amazon SageMaker にトレーニング ジョブを送信するように W&B Launch を設定します。"
---

W&amp;B Launch を使用して Amazon SageMaker に Launch ジョブを送信し、SageMaker プラットフォーム上で提供済みまたはカスタムのアルゴリズムを使って機械学習 モデルをトレーニングできます。SageMaker はコンピュートリソースの起動と解放を自動で行うため、EKS クラスターを持たない Teams にとっても有力な選択肢となります。

Amazon SageMaker に接続された W&amp;B Launch キューに送信された Launch ジョブは、[CreateTrainingJob API](https://docs.aws.amazon.com/sagemaker/latest/APIReference/API_CreateTrainingJob.html) を使って SageMaker Training Job として実行されます。`CreateTrainingJob` API に渡される引数は、Launch キューの設定で制御します。

Amazon SageMaker は [Docker イメージを使ってトレーニング ジョブを実行します](https://docs.aws.amazon.com/sagemaker/latest/dg/your-algorithms-training-algo-dockerfile.html)。SageMaker が取得するイメージは Amazon Elastic Container Registry (ECR) に保存されている必要があります。つまり、トレーニングに使用するイメージは ECR 上に保存されていなければなりません。

<Note>
  このガイドでは SageMaker Training Job の実行方法を説明します。Amazon SageMaker 上に推論用の モデルをデプロイする方法については、[この Launch ジョブの例](https://github.com/wandb/launch-jobs/tree/main/jobs/deploy_to_sagemaker_endpoints)を参照してください。
</Note>

<div id="prerequisites">
  ## 前提条件
</div>

作業を開始する前に、次の前提条件を満たしていることを確認してください。

* [Launch agent に Docker イメージのビルドを任せるかどうかを決める。](#decide-if-you-want-the-launch-agent-to-build-a-docker-images)
* [AWS リソースを設定し、S3、ECR、および SageMaker の IAM ロールに関する情報を取得する。](#set-up-aws-resources)
* [Launch agent 用の IAM ロールを作成する](#create-an-iam-role-for-launch-agent)。

<div id="decide-if-you-want-the-launch-agent-to-build-a-docker-images">
  ### Launch エージェントに Docker イメージのビルドを任せるかどうかを決める
</div>

W&B Launch エージェントに Docker イメージのビルドを任せるかどうかを決めます。選択肢は次の 2 つです。 

* Launch エージェントに Docker イメージをビルドさせ、そのイメージを Amazon ECR にプッシュし、あなたの代わりに [SageMaker Training](https://docs.aws.amazon.com/sagemaker/latest/APIReference/API_CreateTrainingJob.html) ジョブを送信させます。このオプションは、トレーニングコードを素早く反復的に更新する ML エンジニアにとって設定をシンプルにできます。  
* Launch エージェントに、トレーニングまたは推論スクリプトを含む既存の Docker イメージを使用させます。このオプションは既存の CI システムとの相性が良好です。このオプションを選択する場合は、Docker イメージを手動で Amazon ECR 上のコンテナレジストリにアップロードする必要があります。

<div id="set-up-aws-resources">
  ### AWS リソースをセットアップする
</div>

利用する AWS リージョンで、次の AWS リソースが設定されていることを確認します。

1. コンテナイメージを保存するための [ECR repository](https://docs.aws.amazon.com/AmazonECR/latest/userguide/repository-create.html)。
2. SageMaker Training ジョブの入力および出力を保存するための、1 つ以上の [S3 buckets](https://docs.aws.amazon.com/AmazonS3/latest/userguide/create-bucket-overview.html)。
3. SageMaker がトレーニング ジョブを実行し、Amazon ECR および Amazon S3 と連携できるようにするための、Amazon SageMaker 用 IAM ロール。

これらのリソースの ARN を控えておいてください。後で [Launch queue 設定](#configure-launch-queue-for-sagemaker) を定義する際に ARN が必要になります。

<div id="create-a-iam-policy-for-launch-agent">
  ### Launch agent 用の IAM ポリシーを作成する
</div>

1. AWS の IAM 画面から新しいポリシーを作成します。
2. JSON ポリシーエディタに切り替え、ユースケースに応じて以下のポリシーを貼り付けてください。`<>` で囲まれている値は自分の値に置き換えてください:

<Tabs>
<Tab title="エージェントが事前構築済み Docker イメージを送信する場合">
   ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Effect": "Allow",
           "Action": [
             "logs:DescribeLogStreams",
             "SageMaker:AddTags",
             "SageMaker:CreateTrainingJob",
             "SageMaker:DescribeTrainingJob"
           ],
           "Resource": "arn:aws:sagemaker:<region>:<account-id>:*"
         },
         {
           "Effect": "Allow",
           "Action": "iam:PassRole",
           "Resource": "arn:aws:iam::<account-id>:role/<RoleArn-from-queue-config>"
         },
       {
           "Effect": "Allow",
           "Action": "kms:CreateGrant",
           "Resource": "<ARN-OF-KMS-KEY>",
           "Condition": {
             "StringEquals": {
               "kms:ViaService": "SageMaker.<region>.amazonaws.com",
               "kms:GrantIsForAWSResource": "true"
             }
           }
         }
       ]
     }
   ```
</Tab>
<Tab title="エージェントが Docker イメージをビルドして送信する場合">
   ```json
     {
       "Version": "2012-10-17",
       "Statement": [
         {
           "Effect": "Allow",
           "Action": [
             "logs:DescribeLogStreams",
             "SageMaker:AddTags",
             "SageMaker:CreateTrainingJob",
             "SageMaker:DescribeTrainingJob"
           ],
           "Resource": "arn:aws:sagemaker:<region>:<account-id>:*"
         },
         {
           "Effect": "Allow",
           "Action": "iam:PassRole",
           "Resource": "arn:aws:iam::<account-id>:role/<RoleArn-from-queue-config>"
         },
          {
         "Effect": "Allow",
         "Action": [
           "ecr:CreateRepository",
           "ecr:UploadLayerPart",
           "ecr:PutImage",
           "ecr:CompleteLayerUpload",
           "ecr:InitiateLayerUpload",
           "ecr:DescribeRepositories",
           "ecr:DescribeImages",
           "ecr:BatchCheckLayerAvailability",
           "ecr:BatchDeleteImage"
         ],
         "Resource": "arn:aws:ecr:<region>:<account-id>:repository/<repository>"
       },
       {
         "Effect": "Allow",
         "Action": "ecr:GetAuthorizationToken",
         "Resource": "*"
       },
       {
           "Effect": "Allow",
           "Action": "kms:CreateGrant",
           "Resource": "<ARN-OF-KMS-KEY>",
           "Condition": {
             "StringEquals": {
               "kms:ViaService": "SageMaker.<region>.amazonaws.com",
               "kms:GrantIsForAWSResource": "true"
             }
           }
         }
       ]
     }
   ```
</Tab>
</Tabs>

3. **Next** をクリックします。
4. ポリシーに名前と説明を設定します。
5. **Create policy** をクリックします。

<div id="create-an-iam-role-for-launch-agent">
  ### Launch エージェント用の IAM ロールを作成する
</div>

Launch エージェントが Amazon SageMaker トレーニングジョブを作成できるようにするには、権限が必要です。以下の手順に従って IAM ロールを作成します。

1. AWS の IAM コンソールから新しいロールを作成します。 
2. **Trusted Entity** に **AWS Account**（または組織のポリシーに合った別のオプション）を選択します。
3. 権限画面をスクロールし、先ほど作成したポリシーを選択します。 
4. ロールに名前と説明を付けます。
5. **Create role** を選択します。
6. ロールの ARN をメモします。Launch エージェントをセットアップする際に、この ARN を指定します。

IAM ロールの作成については、[AWS Identity and Access Management Documentation](https://docs.aws.amazon.com/IAM/latest/UserGuide/introduction.html) を参照してください。

<Note>
* Launch エージェントにイメージのビルドも行わせたい場合は、追加で必要な権限について [Advanced agent set up](./setup-agent-advanced) を参照してください。
* Amazon SageMaker キューに対する `kms:CreateGrant` 権限は、関連付けられた ResourceConfig に VolumeKmsKeyId が指定されていて、かつ関連付けられたロールにこのアクションを許可するポリシーが存在しない場合にのみ必要です。
</Note>

<div id="configure-launch-queue-for-sagemaker">
  ## SageMaker 用の Launch キューを設定する
</div>

次に、W&B App で SageMaker をコンピュート リソースとして使用するキューを作成します。

1. [Launch App](https://wandb.ai/launch) に移動します。
3. **Create Queue** ボタンをクリックします。
4. キューを作成したい **Entity** を選択します。
5. **Name** フィールドにキュー名を入力します。
6. **Resource** として **SageMaker** を選択します。
7. **Configuration** フィールド内で、SageMaker ジョブに関する情報を指定します。デフォルトでは、W&B によって YAML と JSON の `CreateTrainingJob` リクエストボディが自動的に入力されます。
   ```json
   {
     "RoleArn": "<REQUIRED>", 
     "ResourceConfig": {
         "InstanceType": "ml.m4.xlarge",
         "InstanceCount": 1,
         "VolumeSizeInGB": 2
     },
     "OutputDataConfig": {
         "S3OutputPath": "<REQUIRED>"
     },
     "StoppingCondition": {
         "MaxRuntimeInSeconds": 3600
     }
   }
   ```

少なくとも次の項目を指定する必要があります。

- `RoleArn` : SageMaker 実行 IAM ロールの ARN（[prerequisites](#prerequisites) を参照）。Launch **agent** の IAM ロールと混同しないでください。
- `OutputDataConfig.S3OutputPath` : SageMaker の出力先を指定する Amazon S3 URI。
- `ResourceConfig`: リソース設定の必須指定。リソース設定のオプションは [こちら](https://docs.aws.amazon.com/sagemaker/latest/APIReference/API_ResourceConfig.html) で説明されています。
- `StoppingCondition`: トレーニングジョブの停止条件の必須指定。オプションは [こちら](https://docs.aws.amazon.com/sagemaker/latest/APIReference/API_StoppingCondition.html) で説明されています。

7. **Create Queue** ボタンをクリックします。

<div id="set-up-the-launch-agent">
  ## Launch エージェントをセットアップする
</div>

このセクションでは、エージェントをどこにデプロイできるか、またデプロイ先に応じてどのように設定するかについて説明します。

Amazon SageMaker キュー用の Launch エージェントのデプロイ先には、[いくつかの選択肢](#decide-where-to-run-the-launch-agent) があります。ローカルマシン、EC2 インスタンス、または EKS クラスターで実行できます。Launch エージェントをデプロイする場所に応じて、[適切に設定](#configure-a-launch-agent) してください。

<div id="decide-where-to-run-the-launch-agent">
  ### Launch agent をどこで実行するかを決める
</div>

本番ワークロードで、すでに EKS クラスターを保有しているお客様には、この Helm chart を使って Launch agent を EKS クラスターにデプロイすることを W&B は推奨します。

本番ワークロードで、まだ EKS クラスターがない場合は、EC2 インスタンスを利用するのが良い選択肢です。Launch agent のインスタンスは常に稼働し続けますが、`t2.micro` サイズの EC2 インスタンス以上のスペックは必要なく、比較的安価です。

検証用途や個人での利用ケースでは、ローカルマシン上で Launch agent を実行することで、素早く始めることができます。

ご自身のユースケースに応じて、以下のタブに記載された手順に従い、Launch agent を適切に設定してください。 

<Tabs>
<Tab title="EKS">
W&B は、EKS クラスターに agent をインストールする際に、[W&B managed helm chart](https://github.com/wandb/helm-charts/tree/main/charts/launch-agent) を使用することを強く推奨します。
</Tab>
<Tab title="EC2">
Amazon EC2 ダッシュボードに移動し、次の手順を実行します。

1. **Launch instance** をクリックします。
2. **Name** フィールドに名前を入力します。必要に応じてタグを追加します。
2. **Instance type** から、EC2 コンテナ用のインスタンスタイプを選択します。1vCPU と 1GiB のメモリを超えるスペックは不要です（例: t2.micro）。 
3. **Key pair (login)** フィールドで、組織用のキーペアを作成します。このキーペアは後のステップで、SSH クライアントを使用して [EC2 インスタンスに接続](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/connect.html) する際に使用します。
2. **Network settings** で、組織に適切なセキュリティグループを選択します。 
3. **Advanced details** を展開します。**IAM instance profile** で、前の手順で作成した launch agent 用の IAM ロールを選択します。
2. **Summary** フィールドを確認します。問題なければ **Launch instance** を選択します。 

AWS 上の EC2 ダッシュボード左側のパネルから **Instances** に移動します。作成した EC2 インスタンスが稼働中であることを（**Instance state** 列を参照して）確認します。EC2 インスタンスが起動していることを確認したら、ローカルマシンのターミナルに移動し、以下を実行します。

1. **Connect** を選択します。 
2. **SSH client** タブを選択し、表示される手順に従って EC2 インスタンスに接続します。
3. EC2 インスタンス上で、次のパッケージをインストールします:
   ```bash
   sudo yum install python311 -y && python3 -m ensurepip --upgrade && pip3 install wandb && pip3 install wandb[launch]
   ```
4. 次に、EC2 インスタンス上で Docker をインストールして起動します:
   ```bash
   sudo yum update -y && \
   sudo yum install -y docker python3 && \
   sudo systemctl start docker && \
   sudo systemctl enable docker && \
   sudo usermod -a -G docker ec2-user
   
   newgrp docker
   ```

これで Launch agent の設定に進めます。
</Tab>
<Tab title="Local machine">
ローカルマシン上でポーリングしている Launch agent にロールを関連付けるには、`~/.aws/config` および `~/.aws/credentials` にある AWS の設定ファイルを使用します。前のステップで launch agent 用に作成した IAM ロール ARN を指定します。
 
   ```yaml title="~/.aws/config"
   [profile SageMaker-agent]
   role_arn = arn:aws:iam::<account-id>:role/<agent-role-name>
   source_profile = default                                                                   
   ```

   ```yaml title="~/.aws/credentials"
   [default]
   aws_access_key_id=<access-key-id>
   aws_secret_access_key=<secret-access-key>
   aws_session_token=<session-token>
   ```

セッショントークンには、関連付けられているプリンシパルに応じて、1 時間または 3 日という [最大有効期間](https://docs.aws.amazon.com/cli/latest/reference/sts/get-session-token.html#description) があることに注意してください。
</Tab>
</Tabs>

<div id="configure-a-launch-agent">
  ### launch agent を設定する
</div>

`launch-config.yaml` という名前の YAML 設定ファイルで launch agent を設定します。

デフォルトでは、W&amp;B は `~/.config/wandb/launch-config.yaml` 内の設定ファイルを探します。`-c` フラグを付けて launch agent を起動する際に、別のディレクトリを指定することもできます。

次の YAML スニペットは、エージェントの主要な設定オプションの指定方法を示しています。

```yaml title="launch-config.yaml"
max_jobs: -1
queues:
  - <queue-name>
environment:
  type: aws
  region: <your-region>
registry:
  type: ecr
  uri: <ecr-repo-arn>
builder: 
  type: docker

```

次に `wandb launch-agent` コマンドでエージェントを起動します

<div id="optional-push-your-launch-job-docker-image-to-amazon-ecr">
  ## （オプション）Launch ジョブ用 Docker イメージを Amazon ECR にプッシュする
</div>

<Note>
  このセクションは、Launch エージェントがトレーニングまたは推論のロジックを含む既存の Docker イメージを使用している場合にのみ対象となります。 [Launch エージェントがどのように動作するかには 2 つのオプションがあります。](#decide-if-you-want-the-launch-agent-to-build-a-docker-images)
</Note>

Launch ジョブを含む Docker イメージを Amazon ECR リポジトリにプッシュします。イメージベースのジョブを使用する場合は、新しい Launch ジョブを送信する前に、その Docker イメージが ECR レジストリ内に存在している必要があります。

{/* 画像の完全な URI は、たとえばジョブ作成時に次のように指定できます。

  ```bash
  wandb job create image <your-image-uri> --p <project> ...
  ``` */}

{/* ## W&B から Launch ジョブを実行する

  W&B GUI を開くと、SageMaker Launch キューが有効になっています。UI や CLI からこのキューにジョブを投入できます。 */}
