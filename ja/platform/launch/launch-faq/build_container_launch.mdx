---
title: W&B にコンテナをビルドしてほしくありません。それでも Launch を使えますか？
---

事前にビルド済みの Docker イメージを起動するには、次のコマンドを実行します。`<>` で囲まれたプレースホルダーを、ご自身の環境に合わせた情報に置き換えてください。

```bash
wandb launch -d <docker-image-uri> -q <queue-name> -E <entrypoint>
```

このコマンドは job を作成して run を開始します。

イメージから job を作成するには、次のコマンドを使用します。

```bash
wandb job create image <image-name> -p <project> -e <entity>
```
