---
title: 組織のアクティビティを表示
description: "デプロイタイプ別に、W&B の組織ダッシュボードでユーザーのステータス、アクティビティ、使用状況の傾向を表示します。"
---

このページでは、W&amp;B 組織のアクティビティを表示する方法を説明します。続行するには、デプロイタイプを選択してください。

<div id="view-user-status-and-activity">
  ## ユーザーのステータスとアクティビティを確認する
</div>

<Tabs>
  <Tab title="専用クラウド / セルフマネージド">
    1. **Organization Dashboard** にアクセスします。

       * **専用クラウド**: `https://<org-name>.io/org/dashboard/`。`<org-name>` を組織名に置き換えます。
       * **セルフマネージド**: `https://<YOUR-W&B-SERVER-IP>/org/dashboard`。`<YOUR-W&B-SERVER-IP>` をデプロイ環境の IP アドレスに置き換えます。

       **Users** タブがデフォルトで開き、組織内のすべてのユーザーが一覧表示されます。
    2. ユーザーのステータスで一覧を並べ替えるには、**Last Active** 列のヘッダーをクリックします。各ユーザーのステータスは次のいずれかです。
       * **招待保留中**: 招待は送信されましたが、まだ承諾されていません。
       * **タイムスタンプ**: ユーザーが招待を承諾し、少なくとも 1 回はサインインしています。タイムスタンプは直近のアクティビティを示します。
       * **無効化済み**: 管理者がそのユーザーのアクセス権を取り消しました。
       * **ステータスなし**: ハイフンで示されます。ユーザーは以前はアクティブでしたが、過去 6 か月間はアクティブではありません。
    3. ユーザーの **Last Active** フィールドにカーソルを合わせると、追加日と合計アクティブ日数を確認できます。

    次のいずれかを行うユーザーは *active* とみなされます。

    * W&amp;B にログインする。
    * W&amp;B App のいずれかのページを開く。
    * run をログする。
    * SDK を使用して実験をトラッキングする。
    * 何らかの方法で W&amp;B サーバーとやり取りする。
  </Tab>

  <Tab title="Multi-tenant Cloud">
    1. [**Members** ページ](https://wandb.ai/account-settings/wandb/members/)を開きます。表には組織内のすべてのユーザーが表示されます。
    2. **Last Active** 列のヘッダーをクリックして、ユーザーのステータスで並べ替えます。各ユーザーのステータスは次のいずれかです。
       * **タイムスタンプ**: ユーザーが少なくとも 1 回はサインインしています。タイムスタンプは直近のアクティビティを示します。
       * **ステータスなし**: ハイフンで示されます。ユーザーはその組織内でまだアクティブになっていません。

    2025 年 5 月 8 日以降に、組織を対象とする監査可能なアクションを実行したユーザーは *active* とみなされます。完全な一覧については、**Audit logging** の [Actions](/ja/platform/hosting/monitoring-usage/audit-logging#actions) を参照してください。
  </Tab>
</Tabs>

<div id="view-activity-over-time">
  ## 期間ごとのアクティビティを表示する
</div>

<Tabs>
  <Tab title="専用クラウド / セルフマネージド">
    指定した期間中に何人のユーザーがアクティブだったかを確認するには、**Activity** タブを使用します。

    1. **Organization Dashboard** を開きます。[ユーザーアクティビティを表示する](#view-user-activity)を参照してください。
    2. **Activity** をクリックします。
    3. 次のプロットを確認します。
       * **Total active users**: 選択した期間中のユニークなアクティブユーザー数 (デフォルトは 3 か月) 。
       * **Users active over time**: 期間中のアクティブユーザー数の推移 (デフォルトは 6 か月) 。ポイントにカーソルを合わせると、その日時点の正確な数を確認できます。

    レポート期間を変更するには、プロット上部のドロップダウンを使用します。選択肢は、過去 30 日間、過去 3 か月、過去 6 か月、過去 12 か月、全期間です。
  </Tab>

  <Tab title="Multi-tenant Cloud">
    集計されたアクティビティを表示するには、**Activity Dashboard** を使用します。

    1. W&amp;B App の右上にあるユーザーアイコンをクリックします。
    2. **Account** の下の **Users** をクリックします。
    3. ユーザーの表の上にある Activity Panel を確認します。
       * **Active user count**: 選択した期間中のユニークなアクティブユーザー数 (デフォルトは 3 か月) 。
       * **Weekly active users**: 週ごとのアクティブユーザー数。
       * **Most active user**: アクティブ日数と最終アクティブ日で順位付けされた上位 10 人のユーザー。
    4. 日付範囲を変更するには、右上の日付ピッカーをクリックして新しい値を選択します。7 日、30 日 (デフォルト) 、90 日、6 か月、12 か月から選択でき、すべてのプロットが自動的に更新されます。
  </Tab>
</Tabs>

<div id="export-user-details">
  ## ユーザー詳細をエクスポートする
</div>

**Users** タブから、各ユーザーの詳細 (ユーザー名、メールアドレス、最終アクティブ時刻、ロールなど) が一覧された CSV をダウンロードできます。

<Tabs>
  <Tab title="専用クラウド / セルフマネージド">
    1. **Users** タブで、**Invite new user** の横にある **action (<Icon icon="ellipsis" iconType="solid" />)** メニューをクリックします。
    2. **Export as CSV** をクリックします。

    ### CSV エクスポートのスキーマ

    CSV エクスポートでは、区切り文字としてカンマ (`,`) を使用し、文字列は二重引用符で囲まれ、以下の列を定義するヘッダー行が含まれます。

    * `"Name"`
    * `"Username"`
    * `"Org Role"`
    * `"Models Seat"`
    * `"Weave Access"`
    * `"Email"`
    * `"Teams"`
    * `"Last Active"`
  </Tab>

  <Tab title="Multi-tenant Cloud">
    1. **Users** タブで、右上にある **action (<Icon icon="ellipsis" iconType="solid" />)** メニューをクリックします。
    2. **Export as CSV** を選択してファイルをダウンロードします。

    ### CSV エクスポートのスキーマ

    CSV エクスポートでは、区切り文字としてカンマ (`,`) を使用し、文字列は二重引用符で囲まれ、以下の列を定義するヘッダー行が含まれます。

    * `"Name"`
    * `"Username"`
    * `"Last Active"`
    * `"Role"`
    * `"Email"`
    * `"Teams"`
    * `"Status"`
    * `"Number of Reports"`
    * `"Number of Runs"`
    * `"Number of active days"`
    * `"Models Seat"`
    * `"Weave Access"`
  </Tab>
</Tabs>