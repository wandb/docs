---
title: オーディオ
kind: クラス
namespace: wandb.sdk.data_types.audio
---

import { GitHubLink } from '/snippets/ja/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/sdk/data_types/audio.py#L19" />

## <Badge color="yellow" size="lg" shape="rounded">クラス</Badge> wandb.Audio {#class-wandbaudio}

オーディオクリップを扱う W&amp;B クラスです。

```python
(data_or_path: 'str | pathlib.Path | list | np.ndarray',
sample_rate: 'int | None' = None,
caption: 'str | None' = None)
```

## 引数 {#args}

<ResponseField name="data_or_path" type="str | pathlib.Path | list | np.ndarray">
  オーディオファイルへのパス、またはオーディオデータの NumPy 配列。
</ResponseField>

<ResponseField name="sample_rate" type="int | None">
  サンプルレート。生のオーディオデータの NumPy 配列を渡す場合は必須です。
</ResponseField>

<ResponseField name="caption" type="str | None">
  オーディオとともに表示するキャプション。
</ResponseField>

## メソッド {#methods}

### <Badge color="blue" size="lg" shape="rounded">メソッド</Badge> Audio.durations() {#method-audiodurations}

```python
(audio_list)
```

オーディオファイルの再生時間を計算します。

##### 引数 {#arguments}

<ResponseField name="audio_list" type="_empty">
  説明は提供されていません。
</ResponseField>

### <Badge color="blue" size="lg" shape="rounded">メソッド</Badge> Audio.sample_rates() {#method-audiosample_rates}

```python
(audio_list)
```

オーディオファイルのサンプルレートを取得します。

##### 引数 {#arguments-2}

<ResponseField name="audio_list" type="_empty">
  説明はありません。
</ResponseField>