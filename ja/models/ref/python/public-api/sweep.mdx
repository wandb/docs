---
title: Sweep
kind: クラス
namespace: wandb.apis.public.sweeps
---

import { GitHubLink } from '/snippets/ja/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/sweeps.py#L272" />

## <kbd>クラス</kbd> wandb.apis.public.Sweep

```python
(service_api: 'ServiceApi',
entity: 'str',
project: 'str',
sweep_id: 'str',
attrs: 'Mapping[str, Any] | None' = None)
```

sweep に関連付けられた run の一覧。

<div id="args">
  ## 引数
</div>

* `service_api`:
* `entity`:
* `project`:
* `sweep_id`:
* `attrs`: 

<div id="attributes">
  ## 属性
</div>

* `runs` (`Runs`): run のリスト
* `id` (`str`): Sweep ID
* `project` (`str`): この sweep が属するプロジェクトの名
* `config` (`dict`): sweep の設定を含む辞書
* `state` (`str`): sweep の状態。&quot;Finished&quot;、&quot;Failed&quot;、&quot;Crashed&quot;、または &quot;Running&quot; のいずれかになります。
* `expected_run_count` (`int`): この sweep で想定される run 数

<div id="properties">
  ## プロパティ
</div>

### <kbd>プロパティ</kbd> config

この sweep で使用される設定。

### <kbd>プロパティ</kbd> entity

sweep に関連付けられた entity。

### <kbd>プロパティ</kbd> expected_run_count

このsweepで想定されるrun数を返します。run数が無限の場合は None を返します。

### <kbd>プロパティ</kbd> name

sweep の名前。

以下の優先順位で、存在する最初の名前を返します。

1. ユーザーが編集した表示名
2. 作成時に設定された名前
3. Sweep ID

### <kbd>プロパティ</kbd> order

sweep の並び順のキーを返します。

### <kbd>プロパティ</kbd> path

プロジェクトのパスを返します。

このパスは、entity、プロジェクト名、sweep ID からなるリストです。

### <kbd>プロパティ</kbd> url

sweep の URL。

sweep の URL は、entity、プロジェクト、文字列 `"sweeps"`、および sweep ID.run&#95;id から生成されます。SaaS ユーザーの場合、形式は `https://wandb.ai/entity/project/sweeps/sweeps_ID` です。

### <kbd>プロパティ</kbd> username

非推奨です。代わりに `Sweep.entity` を使用してください。

<div id="methods">
  ## メソッド
</div>

### <kbd>メソッド</kbd> Sweep.agent()

```python
self,
agent_id: 'str'
```

ID を指定して、この sweep のエージェントをクエリします。

<div id="arguments">
  ##### 引数
</div>

* `agent_id`: 検索するエージェントの ID。

### <kbd>メソッド</kbd> Sweep.agents()

```python
self
```

この sweep に含まれるすべてのエージェントの一覧をクエリします。

### <kbd>メソッド</kbd> Sweep.best_run()

```python
(self, order=None)
```

設定で定義されたメトリクス、または指定された順序でソートされた最良の run を返します。

<div id="arguments">
  ##### 引数
</div>

* `order`: 

### <kbd>メソッド</kbd> Sweep.get()

```python
(api: 'Api',
entity: 'str | None' = None,
project: 'str | None' = None,
sid: 'str | None' = None,
order: 'str | None' = None,
query: 'str | None' = None, **kwargs)
```

クラウドバックエンドに対してクエリを実行します。

<div id="arguments">
  ##### 引数
</div>

* `api`: W&amp;B API インスタンス。
* `entity`: project を所有する entity (ユーザー名またはチーム)。
* `project`: sweep を取得する元のプロジェクト名。
* `sid`: クエリ対象の sweep ID。
* `order`: sweep の run が返される順序。
* `query`: クエリの実行に使用するクエリ。
* `kwargs`: 

### <kbd>メソッド</kbd> Sweep.to_html()

```python
self,
height: 'int' = 420,
hidden: 'bool' = False
```

この sweep を表示する iframe を含む HTML を生成します。

<div id="arguments">
  ##### 引数
</div>

* `height`:
* `hidden`: 