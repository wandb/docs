---
title: Sweeps でコードロギングを有効にするにはどうすればよいですか？
---

Sweeps のコードロギングを有効にするには、W&amp;B の run を初期化した後に `wandb.log_code()` を呼び出します。これは、W&amp;B のプロファイル設定でコードロギングを有効にしている場合でも必要な操作です。高度なコードロギングについては、[こちらの `wandb.log_code()` のドキュメント](/ja/models/ref/python/experiments/run/#log_code)を参照してください。