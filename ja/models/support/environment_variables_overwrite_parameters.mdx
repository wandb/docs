---
title: 環境変数は wandb.init() に渡したパラメータを上書きしますか？
---

`wandb.init` に渡した引数は環境変数よりも優先されます。環境変数が設定されていない場合に、システムのデフォルト以外のデフォルトディレクトリを設定したい場合は、`wandb.init(dir=os.getenv("WANDB_DIR", my_default_override))` を使用してください。