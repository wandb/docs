---
title: 最終評価精度のような、時間の経過とともに変化しないメトリクスをログに記録するにはどうすればよいですか？
---

`run.log({'final_accuracy': 0.9})` を使用すると、最終的な精度（final accuracy）が正しく更新されます。デフォルトでは、 `run.log({'final_accuracy': <value>})` は `run.settings['final_accuracy']` を更新し、その値は Runs テーブルに反映されます。