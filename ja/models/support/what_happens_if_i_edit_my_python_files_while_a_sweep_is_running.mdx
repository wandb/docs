---
title: スイープの実行中に Python ファイルを編集した場合はどうなりますか？
---

スイープの実行中の挙動は次のとおりです。

- スイープが使用している `train.py` スクリプトが変更されても、スイープは元の `train.py` を使い続けます。
- `train.py` スクリプトが参照しているファイル、たとえば `helper.py` スクリプト内のヘルパー関数などが変更された場合、スイープは更新後の `helper.py` を使い始めます。