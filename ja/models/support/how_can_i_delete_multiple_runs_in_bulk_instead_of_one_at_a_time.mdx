---
title: 複数の run を一度に削除するにはどうすればよいですか？
---

[public API](/ja/models/ref/python/public-api/api) を使用すると、複数の run を 1 回の操作で削除できます。

```python
import wandb

api = wandb.Api()
runs = api.runs('<entity>/<project>')
for run in runs:
    if <condition>:
        run.delete()
```
