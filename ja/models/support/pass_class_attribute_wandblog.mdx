---
title: wandb.Run.log() にクラス属性を渡すとどうなりますか？
---

`wandb.Run.log()` にクラス属性を渡すのは避けてください。属性は、ネットワーク呼び出しが実行される前に変更される可能性があります。メトリクスをクラス属性として保持する場合は、`wandb.Run.log()` を呼び出した時点の属性値と記録されるメトリクスが一致するように、ディープコピーを使用してください。