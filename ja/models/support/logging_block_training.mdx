---
title: ログ記録は学習をブロックしますか？
---

「ログ関数は遅延実行されますか？ ローカルの処理を実行している間に、結果の送信がネットワーク経由で W&amp;B のサーバーに届くことに依存したくありません。」

`wandb.log` 関数はローカルファイルに1行を書き込み、ネットワーク呼び出しをブロックしません。`wandb.init` を呼び出すと、同じマシン上で新しいプロセスが起動されます。このプロセスはファイルシステムの変更を監視し、Web サービスと非同期に通信するため、ローカルでの処理は中断されることなく継続できます。