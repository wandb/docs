---
title: コードをテストするときに wandb を無効化できますか？
---

テスト時に W&B を no-operation (NOOP) として動作させるには、`wandb.init(mode="disabled")` を使用するか、`WANDB_MODE=disabled` を設定します。

<Note>
`wandb.init(mode="disabled")` を使用しても、W&B が `WANDB_CACHE_DIR` にアーティファクトを保存することは防げません。
</Note>