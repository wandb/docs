---
title: "InitStartError: wandb プロセスとの通信中にエラーが発生しました"
keywords: ["Experiments", "Run Crashes"]
---

このエラーは、ライブラリがサーバーにデータを同期するプロセスの起動中に問題に遭遇したことを示しています。

次の回避策で、特定の環境におけるこの問題を解決できます。

<Tabs>
  <Tab title="Linux と OS X">
    ```python
    wandb.init(settings=wandb.Settings(start_method="fork"))
    ```
  </Tab>

  <Tab title="Google Colab">
    バージョン `0.13.0` より前では、次を使用してください。

    ```python
    wandb.init(settings=wandb.Settings(start_method="thread"))
    ```
  </Tab>
</Tabs>

***

{/* ---- AUTO-GENERATED: tab badges ----
  front matter のキーワードをもとに、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Experiments](/ja/support/models/tags/experiments)</Badge><Badge stroke shape="pill" color="orange" size="md">[Run Crashes](/ja/support/models/tags/run-crashes)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
