---
title: "AWS Batch、ECS などのクラウド インフラストラクチャーで W&B Sweeps を使用できますか？"
keywords: ["Sweeps", "AWS"]
---

任意の W&amp;B Sweep エージェントが `sweep_id` にアクセスできるように `sweep_id` を公開するには、これらのエージェントが `sweep_id` を読み取って実行できる method を実装します。

たとえば、Amazon EC2 インスタンスを起動し、その上で `wandb agent` を実行します。SQS キューを使用して `sweep_id` を複数の EC2 インスタンスにブロードキャストします。その後、各インスタンスはキューから `sweep_id` を取得して、プロセスを開始できます。

***

{/* ---- AUTO-GENERATED: tab badges ----
  front matter 内のキーワードに基づき、scripts/knowledgebase-nav/generate_tags.py によって管理されます。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Sweeps](/ja/support/models/tags/sweeps)</Badge><Badge stroke shape="pill" color="orange" size="md">[AWS](/ja/support/models/tags/aws)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
