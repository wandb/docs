---
title: "自分の run にログしたデータに、プログラムから直接アクセスするにはどうすればよいですか？"
keywords: ["Experiments"]
---

history オブジェクトは、`wandb.log` でログしたメトリクスを追跡します。API を使用して history オブジェクトにアクセスできます。

```python
api = wandb.Api()
run = api.run("username/project/run_id")
print(run.history())
```

***

{/* ---- 自動生成: タブバッジ ----
  フロントマター内の keywords をもとに、scripts/knowledgebase-nav/generate_tags.py で管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Experiments](/ja/support/models/tags/experiments)</Badge>

{{/* ---- 自動生成終了: タブバッジ ---- */}}
