---
title: "sweep で最良の run のアーティファクトを取得するにはどうすればよいですか？"
keywords: ["Artifacts"]
---

sweep で最も高いパフォーマンスを示した run からアーティファクトを取得するには、次のコードを使用します。

```python
api = wandb.Api()
sweep = api.sweep("entity/project/sweep_id")
runs = sorted(sweep.runs, key=lambda run: run.summary.get("val_acc", 0), reverse=True)
best_run = runs[0]
for artifact in best_run.logged_artifacts():
    artifact_path = artifact.download()
    print(artifact_path)
```

***

{/* ---- 自動生成: タブバッジ ----
  Managed by scripts/knowledgebase-nav/generate_tags.py from keywords in front matter.
  Do not edit between these markers by hand.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Artifacts](/ja/support/models/tags/artifacts)</Badge>

{{/* ---- 自動生成ここまで: タブバッジ ---- */}}
