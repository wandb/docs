---
title: "同じマシンでアカウントを切り替えるにはどうすればよいですか？"
keywords: ["環境変数"]
---

同じマシンで 2 つの W&amp;B アカウントを管理するには、両方の APIキー をファイルに保存します。リポジトリでは、次のコードを使用してキーを安全に切り替え、シークレットキーがソース管理にチェックインされないようにします。

```python
if os.path.exists("~/keys.json"):
    os.environ["WANDB_API_KEY"] = json.loads("~/keys.json")["work_account"]
```

***

{/* ---- 自動生成: タブバッジ ----
  フロントマター内のキーワードをもとに、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[環境変数](/ja/support/models/tags/environment-variables)</Badge>

{/* ---- 自動生成: タブバッジの終了 ---- */}
