---
title: "Sweeps でカスタム CLI コマンドを使用するにはどうすればよいですか?"
keywords: ["Sweeps"]
featured: true
---

トレーニング設定でコマンドライン引数を渡す場合は、W&amp;B Sweeps でカスタム CLI コマンドを使用できます。

以下の例では、コードスニペットで、ユーザーが `train.py` という Python スクリプトを実行してトレーニングし、そのスクリプトが解析する値を渡す bash ターミナルを示しています。

```bash
/usr/bin/env python train.py -b \
    your-training-config \
    --batchsize 8 \
    --lr 0.00001
```

カスタムコマンドを実装するには、YAMLファイルの`command`キーを変更します。前の例に基づくと、設定は次のようになります：

```yaml
program:
  train.py
method: grid
parameters:
  batch_size:
    value: 8
  lr:
    value: 0.0001
command:
  - ${env}
  - python
  - ${program}
  - "-b"
  - your-training-config
  - ${args}
```

`${args}` キーは、sweep 設定内のすべてのパラメーターを、`argparse` 向けに `--param1 value1 --param2 value2` の形式で展開します。

`argparse` 以外の追加の引数が必要な場合は、以下を実装します。

```python
parser = argparse.ArgumentParser()
args, unknown = parser.parse_known_args()
```

<Note>
  環境によっては、`python` が Python 2 を指す場合があります。Python 3 を確実に呼び出すには、コマンド設定で `python3` を使用してください。

  ```yaml
  program:
    script.py
  command:
    - ${env}
    - python3
    - ${program}
    - ${args}
  ```
</Note>

***

{/* ---- AUTO-GENERATED: tab badges ----
  フロントマター内のキーワードを基に、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Sweeps](/ja/support/models/tags/sweeps)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
