---
title: "最終評価の精度のように、時間が経っても変化しないメトリクスをログするにはどうすればよいですか？"
keywords: ["Runs"]
---

`run.log({'final_accuracy': 0.9})` を使用すると、最終精度が正しく更新されます。デフォルトでは、`run.log({'final_accuracy': <value>})` によって `run.settings['final_accuracy']` が更新され、Runs 表にその値が反映されます。

***

<Badge stroke shape="pill" color="orange" size="md">[Runs](/ja/support/models/tags/runs)</Badge>