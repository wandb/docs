---
title: "`Est. Runs` 列とは何ですか？"
keywords: ["Sweeps", "ハイパーパラメーター"]
---

W&amp;B は、離散的な探索空間を持つ W&amp;B Sweep を作成するときに、生成される Runs の推定数を表示します。この合計は、探索空間のデカルト積を表しています。

たとえば、次の探索空間について考えてみましょう。

<Frame>
  <img src="/images/sweeps/sweeps_faq_whatisestruns_1.png" alt="推定 Runs 列" />
</Frame>

この場合、デカルト積は 9 になります。W&amp;B はこの値を App UI に推定 run 数 (**Est. Runs**) として表示します。

<Frame>
  <img src="/images/sweeps/spaces_sweeps_faq_whatisestruns_2.webp" alt="Sweep の run 推定" />
</Frame>

推定 Run 数をプログラムから取得するには、W&amp;B SDK 内の Sweep オブジェクトの `expected_run_count` 属性を使用します。

```python
sweep_id = wandb.sweep(
    sweep_configs, project="your_project_name", entity="your_entity_name"
)
api = wandb.Api()
sweep = api.sweep(f"your_entity_name/your_project_name/sweeps/{sweep_id}")
print(f"EXPECTED RUN COUNT = {sweep.expected_run_count}")
```

***

{/* ---- AUTO-GENERATED: tab badges ----
  フロントマターのキーワードに基づき、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Sweeps](/ja/support/models/tags/sweeps)</Badge><Badge stroke shape="pill" color="orange" size="md">[ハイパーパラメーター](/ja/support/models/tags/hyperparameter)</Badge>

{/* ---- 自動生成: タブバッジ ---- */}
