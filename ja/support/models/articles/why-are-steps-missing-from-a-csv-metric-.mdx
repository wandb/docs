---
title: "CSVメトリクスのエクスポートで step が欠落するのはなぜですか？"
keywords: ["Experiments", "Runs"]
---

エクスポートの制限により、CSV として、または `run.history` API を使用して run history 全体をエクスポートできない場合があります。完全な run history に 액세스するには、Parquet 形式を使用して run history アーティファクトをダウンロードしてください。

```python
import wandb
import pandas as pd

run = wandb.init()
artifact = run.use_artifact('<entity>/<project>/<run-id>-history:v0', type='wandb-history')
artifact_dir = artifact.download()
df = pd.read_parquet('<path to .parquet file>')
```

***

{/* ---- AUTO-GENERATED: tab badges ----
  フロントマター内のキーワードから scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Experiments](/ja/support/models/tags/experiments)</Badge><Badge stroke shape="pill" color="orange" size="md">[Runs](/ja/support/models/tags/runs)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
