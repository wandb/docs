---
title: "Files タブに表示されないファイルを確認するにはどうすればよいですか？"
keywords: ["Experiments"]
---

Files タブには、最大 10,000 個のファイルが表示されます。すべてのファイルをダウンロードするには、[public API](/ja/models/ref/python/public-api/api) を使用してください。

```python
import wandb

api = wandb.Api()
run = api.run('<entity>/<project>/<run_id>')
run.file('<file>').download()

for f in run.files():
    if <condition>:
        f.download()
```

***

{/* ---- AUTO-GENERATED: tab badges ----
  scripts/knowledgebase-nav/generate_tags.py により、フロントマター内のキーワードに基づいて管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Experiments](/ja/support/models/tags/experiments)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
