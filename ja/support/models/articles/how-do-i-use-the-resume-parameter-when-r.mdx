---
title: "W&B で run を再開する際に resume パラメーターを使用するにはどうすればよいですか？"
keywords: ["再開"]
---

W&amp;B で `resume` パラメーターを使用するには、`wandb.init()` で `entity`、`project`、`id` を指定したうえで、`resume` 引数を設定します。`resume` 引数には `"must"` または `"allow"` を指定できます。

```python
  run = wandb.init(entity="your-entity", project="your-project", id="your-run-id", resume="must")
```

***

{/* ---- 自動生成: タブバッジ ----
  Managed by scripts/knowledgebase-nav/generate_tags.py from keywords in front matter.
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[再開](/ja/support/models/tags/resuming)</Badge>

{/* ---- 自動生成終了: タブバッジ ---- */}
