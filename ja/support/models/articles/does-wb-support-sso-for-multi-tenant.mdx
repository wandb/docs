---
title: "W&B は Multi-tenant で SSO をサポートしていますか?"
keywords: ["セキュリティ"]
---

W&amp;B は、Auth0 を介して Multi-tenant 環境向けのシングルサインオン (SSO) をサポートしています。SSO インテグレーションは、Okta や Azure AD など、OIDC 準拠の任意のアイデンティティプロバイダーと互換性があります。OIDC プロバイダーを設定するには、次の step に従ってください。

* アイデンティティプロバイダーで Single Page Application (SPA) を作成します。
* `grant_type` を `implicit` フローに設定します。
* コールバック URI を `https://wandb.auth0.com/login/callback` に設定します。

**W&amp;B の要件**

設定が完了したら、アプリケーションの `Client ID` と `Issuer URL` を添えて、カスタマーサクセスマネージャー (CSM) に連絡してください。W&amp;B は、これらの情報を使用して Auth0 接続を確立し、SSO を有効化します。

***

{/* ---- AUTO-GENERATED: tab badges ----
  front matter のキーワードに基づき、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[セキュリティ](/ja/support/models/tags/security)</Badge>

{/* ---- 自動生成終了: タブバッジ ---- */}
