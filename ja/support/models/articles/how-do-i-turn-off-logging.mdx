---
title: "ログをオフにするにはどうすればよいですか？"
keywords: ["ログ"]
---

コマンド `wandb offline` は環境変数 `WANDB_MODE=offline` を設定し、データがリモートの W&amp;B サーバーに同期されるのを防ぎます。この操作はすべてのプロジェクトに影響し、W&amp;B サーバーにデータがログされなくなります。

警告メッセージを抑制するには、次のコードを使用します。

```python
import logging

logger = logging.getLogger("wandb")
logger.setLevel(logging.WARNING)
```

***

{/* ---- 自動生成: タブバッジ ----
  front matter のキーワードに基づき、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[ログ](/ja/support/models/tags/logs)</Badge>

{/* ---- 自動生成: タブバッジここまで ---- */}
