---
title: "既存のrunにアーティファクトをログするにはどうすればよいですか?"
keywords: ["Artifacts"]
---

場合によっては、アーティファクトを以前にログされたrunの出力としてマークする必要があります。この場合は、古いrunを再初期化し、次のように新しいアーティファクトをログします。

```python
with wandb.init(id="existing_run_id", resume="allow") as run:
    artifact = wandb.Artifact("artifact_name", "artifact_type")
    artifact.add_file("my_data/file.txt")
    run.log_artifact(artifact)
```

***

{/* ---- AUTO-GENERATED: tab badges ----
  フロントマター内のキーワードから scripts/knowledgebase-nav/generate_tags.py によって管理されます。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Artifacts](/ja/support/models/tags/artifacts)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
