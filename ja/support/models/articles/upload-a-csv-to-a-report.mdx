---
title: "レポートに CSV をアップロードする"
keywords: ["Reports"]
---

レポートに CSV をアップロードするには、`wandb.Table` 形式を使用します。Python スクリプトで CSV を読み込み、`wandb.Table` オブジェクトとしてログします。これにより、データがレポート内に表として表示されます。

***

{/* ---- 自動生成: タブバッジ ----
  front matter のキーワードを基に、scripts/knowledgebase-nav/generate_tags.py で管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Reports](/ja/support/models/tags/reports)</Badge>

{/* ---- 自動生成終了: タブバッジ ---- */}
