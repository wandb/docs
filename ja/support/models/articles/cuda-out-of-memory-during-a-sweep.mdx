---
title: "sweep 中に `Cuda out of memory` が発生した場合はどうすればよいですか？"
keywords: ["Sweeps", "Run Crashes"]
---

sweep 中に `Cuda out of memory` が発生する場合は、プロセスベースの実行を使用するようにコードをリファクタリングしてください。コードを Python スクリプトとして書き直し、Python SDK ではなく CLI から sweep エージェントを呼び出します。

1. トレーニング ロジックを Python スクリプト (たとえば `train.py`) に追加します。

   ```python
   if __name__ == "__main__":
       train()
   ```

2. YAML の sweep 設定でそのスクリプトを参照します。

   ```yaml
   program: train.py
   method: bayes
   metric:
     name: validation_loss
     goal: maximize
   parameters:
     learning_rate:
       min: 0.0001
       max: 0.1
     optimizer:
       values: ["adam", "sgd"]
   ```

3. CLI で sweep を初期化します。

   ```shell
   wandb sweep config.yaml
   ```

4. CLI で sweep エージェントを起動します。`sweep_ID` は前の手順で返された ID に置き換えてください。

   ```shell
   wandb agent sweep_ID
   ```

Python SDK (`wandb.agent`) ではなく CLI ベースのエージェント (`wandb agent`) を使用すると、各 run がそれぞれ独立したメモリ割り当てを持つ別個のプロセスとして実行されるため、CUDA メモリが run 間で蓄積するのを防げます。

詳細は, [Sweeps のトラブルシューティング](/ja/models/sweeps/troubleshoot-sweeps/)を参照してください。

***

{/* ---- AUTO-GENERATED: tab badges ----
  フロントマター内のキーワードに基づき、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Sweeps](/ja/support/models/tags/sweeps)</Badge><Badge stroke shape="pill" color="orange" size="md">[run の異常終了](/ja/support/models/tags/run-crashes)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}