---
title: "Trace Data が切り取られる"
keywords: ["Trace Data"]
---

Weave UI では、大きな Trace Data が一部切り取られる場合があります。この問題は、デフォルトのトレース出力が生のカスタム Python オブジェクトであるため、Weave がシリアライズできないのです。

大きな Trace Data が切り取られないようにするには、すべての Trace Data を返す文字列の辞書を定義してください。

```python
import weave

class MyObj:
    def __init__(self, x: int):
        self.x = x

    def __repr__(self):
        return f"MyObj(x={self.x})"

    def to_dict(self):
        return {"x": self.x}

@weave.op()
def make_my_obj():
    x = "s" * 10_000
    return MyObj(x)
```

***

{/* ---- AUTO-GENERATED: tab badges ----
  front matter のキーワードに基づき、scripts/knowledgebase-nav/generate_tags.py によって管理されています。
  これらのマーカーの間は手動で編集しないでください。
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Trace Data](/ja/support/weave/tags/trace-data)</Badge>

{/* ---- END AUTO-GENERATED: tab badges ---- */}
