---
title: Launch 작업 보기
description: "W&B App 및 CLI에서 Launch 작업의 세부 정보를 보고, 사용 가능한 작업 목록과 작업 상태를 확인합니다."
keywords: ["작업 상태", "작업 목록", "작업 검사", "CLI 작업 목록"]
---

이 페이지에서는 큐의 Launch 작업 정보를 확인하는 방법을 설명합니다. W&amp;B App에서 작업 세부 정보를 살펴보고, CLI에서 작업 목록을 확인하고, 큐에 있는 run의 상태를 해석할 수 있습니다.

<div id="view-jobs">
  ## 작업 보기
</div>

큐의 작업을 보려면 W&amp;B App을 사용합니다:

1. [https://wandb.ai/home](https://wandb.ai/home)에서 W&amp;B App으로 이동합니다.
2. 왼쪽 사이드바의 **Applications** 섹션에서 **Launch**를 선택합니다.
3. **All entities** 드롭다운을 선택한 다음, Launch 작업이 속한 entity를 선택합니다.
4. Launch Application 페이지에서 접을 수 있는 UI를 펼쳐 해당 큐에 추가된 작업 목록을 확인합니다.

<Note>
  Launch 에이전트는 Launch 작업을 실행할 때 run을 생성합니다. 즉, 목록의 각 run은 해당 큐의 특정 작업에 해당합니다.
</Note>

예를 들어, 다음 이미지는 `job-source-launch_demo-canonical`이라는 작업에서 생성된 두 개의 run을 보여줍니다. 이 작업은 `Start queue`라는 큐에 있습니다. 큐의 첫 번째 run 이름은 `resilient-snowball`이고 두 번째 run 이름은 `earthy-energy-165`입니다.

<Frame>
  <img src="/images/launch/launch_jobs_status.png" alt="Launch 작업 상태 뷰" />
</Frame>

W&amp;B App UI에서는 Launch 작업으로 생성된 run에 대해 다음과 같은 추가 정보를 확인할 수 있습니다.

* **Run**: 해당 작업에 할당된 W&amp;B run의 이름입니다.
* **Job ID**: 작업의 이름입니다.
* **Project**: run이 속한 프로젝트의 이름입니다.
* **Status**: 큐에 추가된 run의 상태입니다.
* **Author**: run을 생성한 W&amp;B entity입니다.
* **Creation date**: 큐가 생성된 타임스탬프입니다.
* **Start time**: 작업이 시작된 타임스탬프입니다.
* **Duration**: 작업의 run이 완료되는 데 걸린 시간(초)입니다.

<div id="list-jobs">
  ## 작업 목록 보기
</div>

W&amp;B CLI를 사용해 프로젝트에 있는 작업 목록을 확인하세요. `wandb job list` 명령어를 사용하고, `--project` 및 `--entity` 플래그로 각각 Launch 작업이 속한 프로젝트 이름과 entity 이름을 지정하세요.

`[ENTITY]`는 W&amp;B entity로 바꾸고, `[PROJECT-NAME]`은 Launch 작업이 속한 프로젝트 이름으로 바꾸세요.

```bash
wandb job list --entity [ENTITY] --project [PROJECT-NAME]
```

<div id="check-the-status-of-a-job">
  ## 작업 상태 확인
</div>

큐에 들어간 각 run에는 Launch 라이프사이클에서 현재 위치를 나타내는 상태가 있습니다. 다음 표는 큐에 들어간 run이 가질 수 있는 상태를 정의합니다.

| 상태           | 설명                                                                       |
| ------------ | ------------------------------------------------------------------------ |
| **Idle**     | 활성 에이전트가 없는 큐에 run이 있습니다.                                                |
| **Queued**   | 에이전트가 처리할 때까지 큐에서 대기 중인 run입니다.                                          |
| **Pending**  | 에이전트가 run을 가져갔지만 아직 시작하지 않았습니다. 클러스터에서 사용할 수 있는 리소스가 없어 이런 상태가 될 수 있습니다. |
| **Running**  | run이 현재 실행 중입니다.                                                         |
| **Killed**   | 사용자가 작업을 중지했습니다.                                                         |
| **Crashed**  | run이 데이터 전송을 중단했거나 정상적으로 시작되지 않았습니다.                                     |
| **Failed**   | run이 0이 아닌 종료 코드와 함께 종료되었거나 시작하지 못했습니다.                                  |
| **Finished** | 작업이 성공적으로 완료되었습니다.                                                       |