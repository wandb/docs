---
title: CSV 메트릭 내보내기에서 스텝이 누락되는 이유는 무엇인가요?
---

내보내기 제한 때문에 전체 run 기록이 CSV로, 또는 `run.history` API를 사용해 내보내지 못할 수도 있습니다. 전체 run 기록에 액세스하려면 Parquet 형식으로 run history 아티팩트를 다운로드하세요:

```python
import wandb
import pandas as pd

with wandb.init() as run:
    artifact = run.use_artifact('<entity>/<project>/<run-id>-history:v0', type='wandb-history')
    artifact_dir = artifact.download()
    df = pd.read_parquet('<path to .parquet file>')
```
