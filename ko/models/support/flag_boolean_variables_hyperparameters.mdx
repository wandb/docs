---
title: 하이퍼파라미터로 불리언 변수를 플래그로 사용할 수 있나요?
---

설정의 command 섹션에서 `${args_no_boolean_flags}` 매크로를 사용하여 하이퍼파라미터를 불리언 플래그로 전달할 수 있습니다. 이 매크로는 불리언 하이퍼파라미터를 자동으로 플래그로 포함합니다. `param`이 `True`이면 command에 `--param` 플래그가 전달됩니다. `param`이 `False`이면 해당 플래그는 생략됩니다.