---
title: 불리언(boolean) 변수를 하이퍼파라미터로 플래그 지정할 수 있나요?
---

설정의 command 섹션에서 `${args_no_boolean_flags}` 매크로를 사용하여 하이퍼파라미터를 불리언(boolean) 플래그로 전달하세요. 이 매크로는 불리언 파라미터를 플래그로 자동 포함합니다. 만약 `param` 이 `True` 이면 코맨드에 `--param` 이 추가됩니다. `param` 이 `False` 이면 해당 플래그는 생략됩니다.