---
title: Sweep 가 실행 중인 동안 Python 파일을 수정하면 어떻게 되나요?
---

Sweeps 가 실행되는 동안:
- 스윕 이 사용하는 `train.py` 스크립트 가 변경되어도, Sweeps 는 원래의 `train.py`를 계속 사용합니다.
- `train.py` 스크립트 가 참조하는 파일들(예: `helper.py` 스크립트 의 헬퍼 함수 등)이 변경되면, Sweeps 는 업데이트된 `helper.py`를 사용하기 시작합니다.