---
title: 스윕이 실행 중일 때 Python 파일을 수정하면 어떻게 되나요?
---

스윕이 실행 중인 동안에는 다음과 같습니다.

- 스윕에서 사용하는 `train.py` 스크립트가 변경되더라도, 스윕은 원래의 `train.py`를 계속 사용합니다.
- `train.py` 스크립트에서 참조하는 파일(예: `helper.py` 스크립트에 정의된 헬퍼 함수)이 변경되면, 스윕은 변경된 `helper.py`를 사용하기 시작합니다.