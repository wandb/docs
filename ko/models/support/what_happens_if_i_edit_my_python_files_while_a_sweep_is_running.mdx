---
title: 스위프가 실행 중일 때 Python 파일을 수정하면 어떻게 되나요?
---

스위프가 실행 중일 때:

- 스위프에서 사용하는 `train.py` 스크립트가 변경되더라도 스위프는 원래의 `train.py`를 계속 사용합니다.
- `helper.py` 스크립트의 헬퍼 함수처럼 `train.py` 스크립트가 참조하는 파일이 변경되면, 스위프는 변경된 `helper.py`를 사용하기 시작합니다.