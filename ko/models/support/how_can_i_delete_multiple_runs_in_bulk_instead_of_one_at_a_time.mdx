---
title: 여러 run을 한 번에 일괄 삭제할 수 있나요?
---

[public API](/ko/models/ref/python/public-api/api)를 사용해 여러 run을 한 번의 작업으로 삭제할 수 있습니다:

```python
import wandb

api = wandb.Api()
runs = api.runs('<entity>/<project>')
for run in runs:
    if <condition>:
        run.delete()
```
