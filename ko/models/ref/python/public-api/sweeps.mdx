---
title: Sweeps
namespace: public_apis_namespace
python_object_type: class
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/sweeps.py" />


## <kbd>class</kbd> `Sweeps`

`Sweep` 객체 컬렉션을 순회하는 지연(lazy) 이터레이터입니다.

**예시:**

```python
from wandb.apis.public import Api

sweeps = Api().project(name="project_name", entity="entity").sweeps()

# 스윕을 반복하며 세부 정보 출력
for sweep in sweeps:
     print(f"Sweep name: {sweep.name}")
     print(f"Sweep ID: {sweep.id}")
     print(f"Sweep URL: {sweep.url}")
     print("----------")
```


### <kbd>method</kbd> `Sweeps.__init__`

```python
__init__(
    client: 'RetryingClient',
    entity: 'str',
    project: 'str',
    per_page: 'int' = 50
) → Sweeps
```

`Sweep` 객체의 이터러블 컬렉션입니다.

**Args:**

* `client`:  W&amp;B를 쿼리하는 데 사용되는 API 클라이언트입니다.
* `entity`:  스윕을 보유한 엔티티입니다.
* `project`:  스윕을 포함하는 프로젝트입니다.
* `per_page`:  API에 대한 각 요청당 가져올 스윕의 개수입니다.

***


### <kbd>속성</kbd> Sweeps.length

---