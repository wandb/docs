---
title: ArtifactTypes
kind: class
namespace: wandb.apis.public.artifacts
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/artifacts.py#L144" />

## <Badge color="yellow" size="lg" shape="rounded">Class</Badge> wandb.apis.public.ArtifactTypes

특정 프로젝트의 `ArtifactType` 객체를 순회하는 lazy 이터레이터입니다.

<div id="methods">
  ## 메서드
</div>

### <Badge color="blue" size="lg" shape="rounded">방법</Badge> RelayPaginator.convert_objects()

```python
self
```

마지막으로 가져온 응답 데이터를 순회되는 객체로 변환합니다.

### <Badge color="blue" size="lg" shape="rounded">방법</Badge> Paginator.**next**()

```python
self
```

이터레이터의 다음 항목을 반환합니다. 더 이상 항목이 없으면 StopIteration 예외를 발생시킵니다

### <Badge color="blue" size="lg" shape="rounded">방법</Badge> Paginator.update_variables()

```python
self
```

다음 페이지를 가져오기 위한 쿼리 변수를 업데이트합니다.