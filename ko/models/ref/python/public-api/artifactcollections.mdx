---
title: ArtifactCollections
kind: 클래스
namespace: wandb.apis.public.artifacts
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/artifacts.py#L330" />

<div id="class-wandbapispublicartifactcollections">
  ## <Badge color="yellow" size="lg" shape="rounded">클래스</Badge> wandb.apis.public.ArtifactCollections
</div>

프로젝트의 특정 유형에 속한 artifact collection입니다.

<div id="methods">
  ## 메서드
</div>

<div id="method-relaypaginatorconvert_objects">
  ### <Badge color="blue" size="lg" shape="rounded">방법</Badge> RelayPaginator.convert_objects()
</div>

```python
self
```

마지막으로 가져온 응답 데이터를 순회되는 객체로 변환합니다.

<div id="method-paginatornext">
  ### <Badge color="blue" size="lg" shape="rounded">방법</Badge> Paginator.**next**()
</div>

```python
self
```

이터레이터의 다음 항목을 반환합니다. 더 이상 항목이 없으면 StopIteration 예외를 발생시킵니다.

<div id="method-paginatorupdate_variables">
  ### <Badge color="blue" size="lg" shape="rounded">방법</Badge> Paginator.update_variables()
</div>

```python
self
```

다음 페이지를 가져오기 위한 쿼리 변수를 업데이트합니다.