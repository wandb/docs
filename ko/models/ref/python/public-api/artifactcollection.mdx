---
title: ArtifactCollection
kind: class
namespace: wandb.apis.public.artifacts
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/artifacts.py#L476" />

## <kbd>class</kbd> wandb.apis.public.ArtifactCollection

관련된 아티팩트를 그룹으로 나타내는 아티팩트 컬렉션입니다.

<div id="properties">
  ## 속성
</div>

### <kbd>속성</kbd> aliases

이 컬렉션에 포함된 모든 artifact 버전의 별칭입니다.

### <kbd>속성</kbd> created_at

아티팩트 컬렉션이 생성된 날짜입니다.

### <kbd>속성</kbd> 설명

아티팩트 컬렉션의 설명입니다.

### <kbd>속성</kbd> entity

프로젝트를 소유하는 entity(사용자 또는 팀)입니다.

### <kbd>속성</kbd> id

아티팩트 컬렉션의 고유 식별자입니다.

### <kbd>속성</kbd> 이름

아티팩트 컬렉션의 이름입니다.

### <kbd>속성</kbd> 프로젝트

아티팩트 컬렉션이 포함된 프로젝트입니다.

### <kbd>속성</kbd> tags

아티팩트 컬렉션에 연결된 tags입니다.

### <kbd>속성</kbd> type

아티팩트 컬렉션의 유형입니다.

### <kbd>속성</kbd> updated_at

아티팩트 컬렉션이 마지막으로 업데이트된 날짜입니다.

<div id="methods">
  ## 메서드
</div>

### <kbd>방법</kbd> ArtifactCollection.artifacts()

```python
self,
per_page: 'int' = 50,
start: 'str | None' = None
```

컬렉션에 있는 모든 아티팩트를 조회합니다.

<div id="arguments">
  ##### 인수
</div>

* `per_page`: 페이지당 가져올 아티팩트 수입니다.
* `start`: 이전 paginator의 `.cursor` 속성에서 가져온, 이전 쿼리를 다시 시작하기 위한 페이지네이션 커서입니다.

### <kbd>방법</kbd> ArtifactCollection.change_type()

```python
self,
new_type: 'str'
```

사용 중단되었습니다. 대신 `save`로 유형을 직접 변경하세요.

<div id="arguments">
  ##### 인수
</div>

* `new_type`: 

### <kbd>방법</kbd> ArtifactCollection.delete()

```python
self
```

아티팩트 컬렉션 전체를 삭제합니다.

### <kbd>방법</kbd> ArtifactCollection.is_sequence()

```python
self
```

아티팩트 컬렉션이 시퀀스인지 반환합니다.

### <kbd>방법</kbd> ArtifactCollection.save()

```python
self
```

아티팩트 컬렉션의 변경 사항을 저장합니다.