---
title: IncompleteRunHistoryError
kind: 클래스
namespace: wandb.apis.public.runhistory.downloads
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/runhistory/downloads.py#L18" />

<div id="class-wandbapispublicincompleterunhistoryerror">
  ## <Badge color="yellow" size="lg" shape="rounded">클래스</Badge> wandb.apis.public.IncompleteRunHistoryError
</div>

run 이력에 누락된 내용이 있을 때 발생합니다.

불완전한 이력은 일부 데이터가 아직 parquet 파일로 내보내지지 않은 경우에 발생하며, 일반적으로 run이 아직 진행 중이기 때문입니다.

<div id="methods">
  ## 메서드
</div>

<div id="method-baseexceptionadd_note">
  ### <Badge color="blue" size="lg" shape="rounded">방법</Badge> BaseException.add_note()
</div>

```python
(self, object, /)
```

Exception.add&#95;note(note) --
예외에 메모를 추가합니다

<div id="arguments">
  ##### 인수
</div>

<ResponseField name="object" type="_empty">
  설명이 없습니다.
</ResponseField>

<div id="method-baseexceptionwith_traceback">
  ### <Badge color="blue" size="lg" shape="rounded">방법</Badge> BaseException.with_traceback()
</div>

```python
(self, object, /)
```

Exception.with&#95;traceback(tb) --
self의 **traceback**를 tb로 설정한 후 self를 반환합니다.

<div id="arguments-2">
  ##### 인수
</div>

<ResponseField name="object" type="_empty">
  설명이 없습니다.
</ResponseField>