---
title: IncompleteRunHistoryError
kind: 클래스
namespace: wandb.apis.public.runhistory.downloads
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/runhistory/downloads.py#L18" />

## <kbd>클래스</kbd> wandb.apis.public.IncompleteRunHistoryError

run 이력에 누락된 내용이 있을 때 발생합니다.

불완전한 이력은 일부 데이터가 아직 parquet 파일로 내보내지지 않은 경우에 발생하며, 일반적으로 run이 아직 진행 중이기 때문입니다.

<div id="methods">
  ## 메서드
</div>

### <kbd>방법</kbd> BaseException.add_note()

```python
(self, object, /)
```

Exception.add&#95;note(note) --
예외에 메모를 추가합니다

<div id="arguments">
  ##### 인수
</div>

* `object`: 

### <kbd>방법</kbd> BaseException.with_traceback()

```python
(self, object, /)
```

Exception.with&#95;traceback(tb) --
self의 **traceback**를 tb로 설정한 후 self를 반환합니다.

<div id="arguments">
  ##### 인수
</div>

* `object`: 