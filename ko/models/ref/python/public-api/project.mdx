---
title: 프로젝트
kind: 클래스
namespace: wandb.apis.public.projects
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/projects.py#L142" />

## <kbd>클래스</kbd> wandb.apis.public.Project

```python
service_api: 'ServiceApi',
entity: 'str',
project: 'str',
attrs: 'Mapping[str, Any]'
```

프로젝트는 Runs의 네임스페이스입니다.

<div id="args">
  ## 매개변수
</div>

* `service_api`: W&amp;B를 쿼리하는 데 사용하는 서비스 API입니다.
* `entity`: 프로젝트를 소유한 entity입니다.
* `project`: 쿼리할 프로젝트의 이름입니다.
* `attrs`: 프로젝트의 속성입니다.

<div id="properties">
  ## 속성
</div>

### <kbd>속성</kbd> id

### <kbd>속성</kbd> owner

프로젝트 소유자를 User 객체로 반환합니다.

### <kbd>속성</kbd> path

프로젝트의 경로를 반환합니다. 경로는 entity와 프로젝트 이름을 포함하는 목록입니다.

### <kbd>속성</kbd> url

프로젝트의 URL을 반환합니다.

<div id="methods">
  ## 메서드
</div>

### <kbd>방법</kbd> Project.artifacts_types()

```python
self,
per_page: 'int' = 50
```

이 프로젝트에 연결된 모든 artifact 유형을 반환합니다.

<div id="arguments">
  ##### 인수
</div>

* `per_page`: 

### <kbd>방법</kbd> Project.collections()

```python
self,
filters: 'Mapping[str, Any] | None' = None,
order: 'str | None' = None,
per_page: 'int' = 50
```

이 프로젝트와 연결된 모든 artifact collection을 반환합니다.

<div id="arguments">
  ##### 인수
</div>

* `filters`: 쿼리에 적용할 필터의 선택적 매핑입니다.
* `order`: 결과 순서를 지정하는 선택적 문자열입니다. `order` 앞에 `+`를 붙이면 오름차순(기본값)이고, `-`를 붙이면 내림차순입니다.
* `per_page`: 페이지당 가져올 artifact collection의 수입니다. 기본값은 50입니다.

### <kbd>방법</kbd> Project.sweeps()

```python
self,
per_page: 'int' = 50,
filters: 'dict[str, Any] | None' = None,
order: 'str | None' = None
```

이 프로젝트의 Sweeps를 페이지로 나누어 반환합니다.

<div id="arguments">
  ##### 인수
</div>

* `per_page`: API 요청당 가져올 sweep 수입니다.
* `filters`: (dict) Runs 필터를 사용해 특정 sweep을 조회하기 위한 쿼리입니다. 자세한 내용은 wandb/apis/public/api.py:runs를 참조하세요.
* `order`: 