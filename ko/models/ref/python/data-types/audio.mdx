---
title: 오디오
kind: 클래스
namespace: wandb.sdk.data_types.audio
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/sdk/data_types/audio.py#L19" />

<div id="class-wandbaudio">
  ## <Badge color="yellow" size="lg" shape="rounded">클래스</Badge> wandb.Audio
</div>

오디오 클립용 W&amp;B 클래스입니다.

```python
(data_or_path: 'str | pathlib.Path | list | np.ndarray',
sample_rate: 'int | None' = None,
caption: 'str | None' = None)
```

<div id="args">
  ## 인수
</div>

<ResponseField name="data_or_path" type="str | pathlib.Path | list | np.ndarray">
  오디오 파일 경로 또는 오디오 데이터가 포함된 NumPy 배열입니다.
</ResponseField>

<ResponseField name="sample_rate" type="int | None">
  원시 NumPy 오디오 데이터 배열을 전달할 때 필요한 샘플 레이트입니다.
</ResponseField>

<ResponseField name="caption" type="str | None">
  오디오와 함께 표시할 캡션입니다.
</ResponseField>

<div id="methods">
  ## 메서드
</div>

<div id="method-audiodurations">
  ### <Badge color="blue" size="lg" shape="rounded">메서드</Badge> Audio.durations()
</div>

```python
(audio_list)
```

오디오 파일의 길이를 계산합니다.

<div id="arguments">
  ##### 인수
</div>

<ResponseField name="audio_list" type="_empty">
  설명이 없습니다.
</ResponseField>

<div id="method-audiosample_rates">
  ### <Badge color="blue" size="lg" shape="rounded">메서드</Badge> Audio.sample_rates()
</div>

```python
(audio_list)
```

오디오 파일의 샘플 레이트를 조회합니다.

<div id="arguments-2">
  ##### 인수
</div>

<ResponseField name="audio_list" type="_empty">
  설명이 없습니다.
</ResponseField>