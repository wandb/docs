---
title: Object3D
kind: 클래스
namespace: wandb.sdk.data_types.object_3d
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/sdk/data_types/object_3d.py#L245" />

## <kbd>클래스</kbd> wandb.Object3D

```python
data_or_path: 'np.ndarray | str | pathlib.Path | TextIO | dict',
caption: 'str | None' = None, **kwargs: 'str | FileFormat3D | None'
```

3D 포인트 클라우드용 W&amp;B 클래스입니다.

<div id="args">
  ## 인수
</div>

* `data_or_path`: Object3D는 파일 또는 numpy 배열로 초기화할 수 있습니다.
* `caption`: 객체를 표시할 때 함께 표시되는 캡션입니다.
* `kwargs`: 

<div id="methods">
  ## 메서드
</div>

### <kbd>방법</kbd> Object3D.from_file()

```python
data_or_path: 'TextIO | str',
file_type: 'FileFormat3D | None' = None
```

파일 또는 스트림을 사용해 Object3D를 초기화합니다.

<div id="arguments">
  ##### 인수
</div>

* `data_or_path`: 파일 경로 또는 `TextIO` 스트림입니다.

### <kbd>방법</kbd> Object3D.from_numpy()

```python
data: 'np.ndarray'
```

NumPy 배열에서 Object3D를 초기화합니다.

### <kbd>방법</kbd> Object3D.from_point_cloud()

```python
points: 'Sequence[Point]',
boxes: 'Sequence[Box3D]',
vectors: 'Sequence[Vector3D] | None' = None,
point_cloud_type: 'PointCloudType' = 'lidar/beta'
```

Python 객체로부터 Object3D를 초기화합니다.

<div id="arguments">
  ##### 인수
</div>

* `points`: 포인트 클라우드의 점입니다.
* `boxes`: 포인트 클라우드에 레이블을 지정하기 위한 3D 바운딩 박스입니다. 박스는 포인트 클라우드 시각화에 표시됩니다.
* `vectors`: 각 벡터는 포인트 클라우드 시각화에 표시됩니다. 바운딩 박스의 방향을 나타내는 데 사용할 수 있습니다. 기본값은 None입니다.