---
title: controller()
kind: 함수
namespace: wandb.sdk.wandb_sweep
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/sdk/wandb_sweep.py#L96" />

## <Badge color="blue" size="lg" shape="rounded">함수</Badge> wandb.controller() {#function-wandbcontroller}

공개 스윕 컨트롤러 생성자입니다.

```python
sweep_id_or_config: 'str | dict | None' = None,
entity: 'str | None' = None,
project: 'str | None' = None
```

## 인수 {#args}

<ResponseField name="sweep_id_or_config" type="str | dict | None">
  설명이 없습니다.
</ResponseField>

<ResponseField name="entity" type="str | None">
  설명이 없습니다.
</ResponseField>

<ResponseField name="project" type="str | None">
  설명이 없습니다.
</ResponseField>

## 예시 {#examples}

```python
import wandb

tuner = wandb.controller(...)
print(tuner.sweep_config)
print(tuner.sweep_id)
tuner.configure_search(...)
tuner.configure_stopping(...)
```