---
title: string
---

## 체이닝할 수 있는 Ops {#chainable-ops}

### <a id="string-notEqual" />`string-notEqual` {#string-notequal}

두 값이 서로 다른지 확인합니다.

| 인수    | 설명             |
| :---- | :------------- |
| `lhs` | 비교할 첫 번째 값입니다. |
| `rhs` | 비교할 두 번째 값입니다. |

#### 반환 값 {#return-value}

두 값이 서로 다른지 여부입니다.

### <a id="string-add" />`string-add` {#string-add}

두 [문자열](/ko/models/ref/query-panel/string)을 연결합니다

| 인수    | 설명                                         |
| :---- | :----------------------------------------- |
| `lhs` | 첫 번째 [문자열](/ko/models/ref/query-panel/string) |
| `rhs` | 두 번째 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-2}

이어 붙인 [string](/ko/models/ref/query-panel/string)

### <a id="string-equal" />`string-equal` {#string-equal}

두 값이 같은지 확인합니다.

| 인수    | 설명             |
| :---- | :------------- |
| `lhs` | 비교할 첫 번째 값입니다. |
| `rhs` | 비교할 두 번째 값입니다. |

#### 반환 값 {#return-value-3}

두 값이 동일한지 여부입니다.

### <a id="string-append" />`string-append` {#string-append}

[string](/ko/models/ref/query-panel/string)에 접미사를 덧붙입니다.

| 인수       | 설명                                                |
| :------- | :------------------------------------------------ |
| `str`    | 접미사를 덧붙일 [string](/ko/models/ref/query-panel/string) |
| `suffix` | 덧붙일 접미사                                           |

#### 반환 값 {#return-value-4}

접미사가 추가된 [string](/ko/models/ref/query-panel/string)

### <a id="string-contains" />`string-contains` {#string-contains}

[string](/ko/models/ref/query-panel/string)에 부분 문자열이 포함되어 있는지 확인합니다

| 인수    | 설명                                           |
| :---- | :------------------------------------------- |
| `str` | 확인할 [string](/ko/models/ref/query-panel/string) |
| `sub` | 포함되어 있는지 확인할 부분 문자열                          |

#### 반환 값 {#return-value-5}

[string](/ko/models/ref/query-panel/string)에 부분 문자열이 포함되어 있는지 여부

### <a id="string-endsWith" />`string-endsWith` {#string-endswith}

[string](/ko/models/ref/query-panel/string)이 지정된 접미사로 끝나는지 확인합니다

| 인수       | 설명                                           |
| :------- | :------------------------------------------- |
| `str`    | 확인할 [string](/ko/models/ref/query-panel/string) |
| `suffix` | 확인할 접미사                                      |

#### 반환 값 {#return-value-6}

[string](/ko/models/ref/query-panel/string)이 해당 접미사로 끝나는지 여부

### <a id="string-findAll" />`string-findAll` {#string-findall}

[string](/ko/models/ref/query-panel/string)에서 부분 문자열이 나타나는 모든 경우를 찾습니다

| 인수    | 설명                                                  |
| :---- | :-------------------------------------------------- |
| `str` | 부분 문자열을 찾을 [string](/ko/models/ref/query-panel/string) |
| `sub` | 찾을 부분 문자열                                           |

#### 반환 값 {#return-value-7}

[string](/ko/models/ref/query-panel/string)에서 부분 문자열이 위치한 인덱스 *목록*

### <a id="string-isAlnum" />`string-isAlnum` {#string-isalnum}

[string](/ko/models/ref/query-panel/string)이 영숫자로만 이루어져 있는지 확인합니다

| 인수    | 설명                                           |
| :---- | :------------------------------------------- |
| `str` | 확인할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-8}

[string](/ko/models/ref/query-panel/string)이 영숫자로만 이루어져 있는지 여부

### <a id="string-isAlpha" />`string-isAlpha` {#string-isalpha}

[string](/ko/models/ref/query-panel/string)이 알파벳 문자로만 이루어져 있는지 확인합니다

| 인수    | 설명                                           |
| :---- | :------------------------------------------- |
| `str` | 확인할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-9}

[string](/ko/models/ref/query-panel/string)이 알파벳으로만 구성되었는지 여부

### <a id="string-isNumeric" />`string-isNumeric` {#string-isnumeric}

[string](/ko/models/ref/query-panel/string)이 숫자형인지 확인합니다

| 인수    | 설명                                           |
| :---- | :------------------------------------------- |
| `str` | 확인할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-10}

해당 [string](/ko/models/ref/query-panel/string)이 숫자형인지 여부

### <a id="string-lStrip" />`string-lStrip` {#string-lstrip}

앞의 공백 제거

| 인수    | 설명                                                      |
| :---- | :------------------------------------------------------ |
| `str` | 앞의 공백을 제거할 [string](/ko/models/ref/query-panel/string)입니다. |

#### 반환 값 {#return-value-11}

공백이 제거된 [string](/ko/models/ref/query-panel/string).

### <a id="string-len" />`string-len` {#string-len}

[string](/ko/models/ref/query-panel/string)의 길이를 반환합니다

| 인수    | 설명                                               |
| :---- | :----------------------------------------------- |
| `str` | 길이를 확인할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-12}

[string](/ko/models/ref/query-panel/string)의 길이

### <a id="string-lower" />`string-lower` {#string-lower}

[string](/ko/models/ref/query-panel/string)을 소문자로 변환합니다.

| 인수    | 설명                                                |
| :---- | :------------------------------------------------ |
| `str` | 소문자로 변환할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-13}

소문자로 된 [string](/ko/models/ref/query-panel/string)

### <a id="string-partition" />`string-partition` {#string-partition}

[string](/ko/models/ref/query-panel/string)을 [string](/ko/models/ref/query-panel/string) &#95;목록&#95;으로 나눕니다

| 인수    | 설명                                          |
| :---- | :------------------------------------------ |
| `str` | 나눌 [string](/ko/models/ref/query-panel/string) |
| `sep` | 나눌 때 사용할 구분자                                |

#### 반환 값 {#return-value-14}

구분자 앞의 [string](/ko/models/ref/query-panel/string), 구분자 자체, 그리고 구분자 뒤의 [string](/ko/models/ref/query-panel/string)로 이루어진 [strings](/ko/models/ref/query-panel/string)의 *목록*

### <a id="string-prepend" />`string-prepend` {#string-prepend}

[string](/ko/models/ref/query-panel/string) 앞에 접두사를 붙입니다

| Argument | 설명                                               |
| :------- | :----------------------------------------------- |
| `str`    | 접두사를 붙일 [string](/ko/models/ref/query-panel/string) |
| `prefix` | 앞에 붙일 접두사                                        |

#### 반환 값 {#return-value-15}

접두사가 앞에 붙은 [string](/ko/models/ref/query-panel/string)

### <a id="string-rStrip" />`string-rStrip` {#string-rstrip}

문자열 끝의 공백 제거

| 인수    | 설명                                                   |
| :---- | :--------------------------------------------------- |
| `str` | 공백을 제거할 [string](/ko/models/ref/query-panel/string)입니다. |

#### 반환 값 {#return-value-16}

공백이 제거된 [string](/ko/models/ref/query-panel/string).

### <a id="string-replace" />`string-replace` {#string-replace}

[string](/ko/models/ref/query-panel/string) 내에서 부분 문자열이 나타나는 모든 부분을 대체합니다.

| 인수       | 설명                                               |
| :------- | :----------------------------------------------- |
| `str`    | 내용을 대체할 [string](/ko/models/ref/query-panel/string) |
| `sub`    | 대체할 부분 문자열                                       |
| `newSub` | 기존 부분 문자열을 대체할 부분 문자열                            |

#### 반환값 {#return-value-17}

치환된 [string](/ko/models/ref/query-panel/string)

### <a id="string-slice" />`string-slice` {#string-slice}

시작 인덱스와 끝 인덱스를 기준으로 [string](/ko/models/ref/query-panel/string)에서 부분 문자열을 잘라냅니다.

| 인수      | 설명                                           |
| :------ | :------------------------------------------- |
| `str`   | 잘라낼 [string](/ko/models/ref/query-panel/string) |
| `begin` | 부분 문자열의 시작 인덱스                               |
| `end`   | 부분 문자열의 끝 인덱스                                |

#### 반환 값 {#return-value-18}

부분 문자열

### <a id="string-split" />`string-split` {#string-split}

[문자열](/ko/models/ref/query-panel/string)을 [문자열](/ko/models/ref/query-panel/string) &#95;목록&#95;으로 분할합니다.

| 인수    | 설명                                        |
| :---- | :---------------------------------------- |
| `str` | 분할할 [문자열](/ko/models/ref/query-panel/string) |
| `sep` | 분할 기준이 되는 구분자                             |

#### 반환 값 {#return-value-19}

[문자열](/ko/models/ref/query-panel/string)의 *목록*

### <a id="string-startsWith" />`string-startsWith` {#string-startswith}

[string](/ko/models/ref/query-panel/string)이 접두사로 시작하는지 확인합니다.

| 인수       | 설명                                           |
| :------- | :------------------------------------------- |
| `str`    | 확인할 [string](/ko/models/ref/query-panel/string) |
| `prefix` | 확인할 접두사                                      |

#### 반환 값 {#return-value-20}

[string](/ko/models/ref/query-panel/string)이 접두어로 시작하는지 여부

### <a id="string-strip" />`string-strip` {#string-strip}

[string](/ko/models/ref/query-panel/string) 양쪽 끝의 공백을 제거합니다.

| 인수    | 설명                                                   |
| :---- | :--------------------------------------------------- |
| `str` | 공백을 제거할 [string](/ko/models/ref/query-panel/string)입니다. |

#### 반환 값 {#return-value-21}

공백이 제거된 [string](/ko/models/ref/query-panel/string).

### <a id="string-upper" />`string-upper` {#string-upper}

[string](/ko/models/ref/query-panel/string)을 대문자로 변환합니다.

| 매개변수  | 설명                                                |
| :---- | :------------------------------------------------ |
| `str` | 대문자로 변환할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-22}

대문자로 변환된 [문자열](/ko/models/ref/query-panel/string)

### <a id="string-levenshtein" />`string-levenshtein` {#string-levenshtein}

두 [string](/ko/models/ref/query-panel/string) 사이의 레벤슈타인 거리를 계산합니다.

| 인수     | 설명                                                |
| :----- | :------------------------------------------------ |
| `str1` | 첫 번째 [string](/ko/models/ref/query-panel/string)입니다. |
| `str2` | 두 번째 [string](/ko/models/ref/query-panel/string)입니다. |

#### 반환 값 {#return-value-23}

두 [문자열](/ko/models/ref/query-panel/string) 간의 레벤슈타인 거리입니다.

### <a id="local-sqlconnection" />`local-sqlconnection` {#local-sqlconnection}

안녕하세요

| 인수     | 설명    |
| :----- | :---- |
| `conf` | 안녕하세요 |
| `size` | 안녕하세요 |

#### 반환 값 {#return-value-24}

hello

## Ops 목록 {#list-ops}

### <a id="string-notEqual-list" />`string-notEqual` {#string-notequal-2}

두 값이 서로 다른지 확인합니다.

| 인수    | 설명             |
| :---- | :------------- |
| `lhs` | 비교할 첫 번째 값입니다. |
| `rhs` | 비교할 두 번째 값입니다. |

#### 반환 값 {#return-value-25}

두 값이 서로 다른지 여부입니다.

### <a id="string-add-list" />`string-add` {#string-add-2}

두 [문자열](/ko/models/ref/query-panel/string)을 연결합니다

| 인수    | 설명                                         |
| :---- | :----------------------------------------- |
| `lhs` | 첫 번째 [문자열](/ko/models/ref/query-panel/string) |
| `rhs` | 두 번째 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-26}

이어 붙인 [string](/ko/models/ref/query-panel/string)

### <a id="string-equal-list" />`string-equal` {#string-equal-2}

두 값이 같은지 확인합니다.

| 인수    | 설명             |
| :---- | :------------- |
| `lhs` | 비교할 첫 번째 값입니다. |
| `rhs` | 비교할 두 번째 값입니다. |

#### 반환 값 {#return-value-27}

두 값이 동일한지 여부입니다.

### <a id="string-append-list" />`string-append` {#string-append-2}

[문자열](/ko/models/ref/query-panel/string)에 접미사를 덧붙입니다.

| 인수       | 설명                                             |
| :------- | :--------------------------------------------- |
| `str`    | 접미사를 덧붙일 [문자열](/ko/models/ref/query-panel/string) |
| `suffix` | 덧붙일 접미사                                        |

#### 반환 값 {#return-value-28}

접미사가 추가된 [string](/ko/models/ref/query-panel/string)

### <a id="string-contains-list" />`string-contains` {#string-contains-2}

[문자열](/ko/models/ref/query-panel/string)에 부분 문자열이 포함되어 있는지 확인합니다.

| 인수    | 설명                                        |
| :---- | :---------------------------------------- |
| `str` | 확인할 [문자열](/ko/models/ref/query-panel/string) |
| `sub` | 확인할 부분 문자열                                |

#### 반환 값 {#return-value-29}

[string](/ko/models/ref/query-panel/string)에 부분 문자열이 포함되어 있는지 여부

### <a id="string-endsWith-list" />`string-endsWith` {#string-endswith-2}

[string](/ko/models/ref/query-panel/string)이 지정한 접미사로 끝나는지 확인합니다

| 인수       | 설명                                           |
| :------- | :------------------------------------------- |
| `str`    | 확인할 [string](/ko/models/ref/query-panel/string) |
| `suffix` | 끝부분이 일치하는지 확인할 접미사                           |

#### 반환 값 {#return-value-30}

[string](/ko/models/ref/query-panel/string)이 해당 접미사로 끝나는지 여부

### <a id="string-findAll-list" />`string-findAll` {#string-findall-2}

[문자열](/ko/models/ref/query-panel/string)에서 하위 문자열이 나타나는 모든 위치를 찾습니다.

| 인수    | 설명                                               |
| :---- | :----------------------------------------------- |
| `str` | 하위 문자열을 찾을 [문자열](/ko/models/ref/query-panel/string) |
| `sub` | 찾을 하위 문자열                                        |

#### 반환 값 {#return-value-31}

[string](/ko/models/ref/query-panel/string)에서 부분 문자열이 위치한 인덱스 *목록*

### <a id="string-isAlnum" />`string-isAlnum` {#string-isalnum-2}

[문자열](/ko/models/ref/query-panel/string)이 영숫자로만 이루어져 있는지 확인합니다

| 인수    | 설명                                        |
| :---- | :---------------------------------------- |
| `str` | 확인할 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-32}

[string](/ko/models/ref/query-panel/string)이 영숫자로만 이루어져 있는지 여부

### <a id="string-isAlpha-list" />`string-isAlpha` {#string-isalpha-2}

[문자열](/ko/models/ref/query-panel/string)이 알파벳으로만 구성되어 있는지 확인합니다

| 인수    | 설명                                        |
| :---- | :---------------------------------------- |
| `str` | 확인할 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-33}

[string](/ko/models/ref/query-panel/string)이 알파벳으로만 구성되었는지 여부

### <a id="string-isNumeric-list" />`string-isNumeric` {#string-isnumeric-2}

[문자열](/ko/models/ref/query-panel/string)이 숫자인지 확인합니다.

| 인수    | 설명                                        |
| :---- | :---------------------------------------- |
| `str` | 확인할 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-34}

해당 [string](/ko/models/ref/query-panel/string)이 숫자형인지 여부

### <a id="string-lStrip-list" />`string-lStrip` {#string-lstrip-2}

선행 공백을 제거합니다

| 인수    | 설명                                               |
| :---- | :----------------------------------------------- |
| `str` | 제거할 [string](/ko/models/ref/query-panel/string)입니다. |

#### 반환 값 {#return-value-35}

공백이 제거된 [string](/ko/models/ref/query-panel/string).

### <a id="string-len-list" />`string-len` {#string-len-2}

[문자열](/ko/models/ref/query-panel/string)의 길이를 반환합니다

| 인수    | 설명                                            |
| :---- | :-------------------------------------------- |
| `str` | 길이를 확인할 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-36}

[string](/ko/models/ref/query-panel/string)의 길이

### <a id="string-lower-list" />`string-lower` {#string-lower-2}

[string](/ko/models/ref/query-panel/string)을 소문자로 변환합니다

| 인수    | 설명                                                |
| :---- | :------------------------------------------------ |
| `str` | 소문자로 변환할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-37}

소문자로 된 [string](/ko/models/ref/query-panel/string)

### <a id="string-partition-list" />`string-partition` {#string-partition-2}

[string](/ko/models/ref/query-panel/string)을 [string](/ko/models/ref/query-panel/string) &#95;목록&#95;으로 나눕니다

| 인수    | 설명                                          |
| :---- | :------------------------------------------ |
| `str` | 나눌 [string](/ko/models/ref/query-panel/string) |
| `sep` | 나눌 때 사용할 구분자                                |

#### 반환 값 {#return-value-38}

구분자 앞의 [string](/ko/models/ref/query-panel/string), 구분자 자체, 그리고 구분자 뒤의 [string](/ko/models/ref/query-panel/string)로 이루어진 [strings](/ko/models/ref/query-panel/string)의 *목록*

### <a id="string-prepend-list" />`string-prepend` {#string-prepend-2}

[문자열](/ko/models/ref/query-panel/string) 앞에 접두사를 추가합니다.

| 인수       | 설명                                             |
| :------- | :--------------------------------------------- |
| `str`    | 접두사를 추가할 [문자열](/ko/models/ref/query-panel/string) |
| `prefix` | 앞에 추가할 접두사                                     |

#### 반환 값 {#return-value-39}

접두사가 앞에 붙은 [string](/ko/models/ref/query-panel/string)

### <a id="string-rStrip" />`string-rStrip` {#string-rstrip-2}

문자열 끝의 공백을 제거합니다

| 인수    | 설명                                               |
| :---- | :----------------------------------------------- |
| `str` | 공백을 제거할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-40}

공백이 제거된 [string](/ko/models/ref/query-panel/string).

### <a id="string-replace-list" />`string-replace` {#string-replace-2}

[string](/ko/models/ref/query-panel/string)에서 하위 문자열이 나타나는 모든 부분을 대체합니다

| 인수       | 설명                                               |
| :------- | :----------------------------------------------- |
| `str`    | 내용을 대체할 [string](/ko/models/ref/query-panel/string) |
| `sub`    | 대체할 하위 문자열                                       |
| `newSub` | 기존 하위 문자열을 대체할 새 하위 문자열                          |

#### 반환값 {#return-value-41}

치환된 [string](/ko/models/ref/query-panel/string)

### <a id="string-slice-list" />`string-slice` {#string-slice-2}

[string](/ko/models/ref/query-panel/string)을 시작 인덱스와 끝 인덱스를 기준으로 부분 문자열로 잘라냅니다

| 인수      | 설명                                           |
| :------ | :------------------------------------------- |
| `str`   | 잘라낼 [string](/ko/models/ref/query-panel/string) |
| `begin` | 부분 문자열의 시작 인덱스                               |
| `end`   | 부분 문자열의 끝 인덱스                                |

#### 반환 값 {#return-value-42}

부분 문자열

### <a id="string-split-list" />`string-split` {#string-split-2}

[문자열](/ko/models/ref/query-panel/string)을 [문자열](/ko/models/ref/query-panel/string) &#95;목록&#95;으로 분할합니다.

| 인수    | 설명                                        |
| :---- | :---------------------------------------- |
| `str` | 분할할 [문자열](/ko/models/ref/query-panel/string) |
| `sep` | 분할 기준이 되는 구분자                             |

#### 반환 값 {#return-value-43}

[문자열](/ko/models/ref/query-panel/string)의 *목록*

### <a id="string-startsWith-list" />`string-startsWith` {#string-startswith-2}

[string](/ko/models/ref/query-panel/string)이 지정한 접두사로 시작하는지 확인합니다

| 인수       | 설명                                           |
| :------- | :------------------------------------------- |
| `str`    | 확인할 [string](/ko/models/ref/query-panel/string) |
| `prefix` | 확인할 접두사                                      |

#### 반환 값 {#return-value-44}

[string](/ko/models/ref/query-panel/string)이 접두어로 시작하는지 여부

### <a id="string-strip-list" />`string-strip` {#string-strip-2}

[string](/ko/models/ref/query-panel/string)의 양쪽 끝 공백을 제거합니다.

| 인수    | 설명                                               |
| :---- | :----------------------------------------------- |
| `str` | 공백을 제거할 [string](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-45}

공백이 제거된 [string](/ko/models/ref/query-panel/string).

### <a id="string-upper-list" />`string-upper` {#string-upper-2}

[문자열](/ko/models/ref/query-panel/string)을 대문자로 변환합니다

| 인수    | 설명                                             |
| :---- | :--------------------------------------------- |
| `str` | 대문자로 변환할 [문자열](/ko/models/ref/query-panel/string) |

#### 반환 값 {#return-value-46}

대문자로 변환된 [문자열](/ko/models/ref/query-panel/string)

### <a id="string-levenshtein-list" />`string-levenshtein` {#string-levenshtein-2}

두 [string](/ko/models/ref/query-panel/string) 사이의 레벤슈타인 거리를 계산합니다.

| 인수     | 설명                                                |
| :----- | :------------------------------------------------ |
| `str1` | 첫 번째 [string](/ko/models/ref/query-panel/string)입니다. |
| `str2` | 두 번째 [string](/ko/models/ref/query-panel/string)입니다. |

#### 반환 값 {#return-value-47}

두 [문자열](/ko/models/ref/query-panel/string) 간의 레벤슈타인 거리입니다.