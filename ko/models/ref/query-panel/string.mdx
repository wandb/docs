---
title: string
---

## Chainable Ops
### <a id="string-notEqual"></a>`string-notEqual`

두 값의 불일치 여부를 결정합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 비교할 첫 번째 값. |
| `rhs` | 비교할 두 번째 값. |

#### 반환 값
두 값이 일치하지 않는지 여부.

### <a id="string-add"></a>`string-add`

두 [strings](/models/ref/query-panel/string/) 을 연결합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 첫 번째 [string](/models/ref/query-panel/string) |
| `rhs` | 두 번째 [string](/models/ref/query-panel/string) |

#### 반환 값
연결된 [string](/models/ref/query-panel/string)

### <a id="string-equal"></a>`string-equal`

두 값의 일치 여부를 결정합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 비교할 첫 번째 값. |
| `rhs` | 비교할 두 번째 값. |

#### 반환 값
두 값이 일치하는지 여부.

### <a id="string-append"></a>`string-append`

[string](/models/ref/query-panel/string) 뒤에 접미사를 추가합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 접미사를 추가할 [string](/models/ref/query-panel/string) |
| `suffix` | 추가할 접미사 |

#### 반환 값
접미사가 추가된 [string](/models/ref/query-panel/string)

### <a id="string-contains"></a>`string-contains`

[string](/models/ref/query-panel/string) 에 부분 문자열이 포함되어 있는지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |
| `sub` | 검색할 부분 문자열 |

#### 반환 값
[string](/models/ref/query-panel/string) 에 부분 문자열이 포함되어 있는지 여부

### <a id="string-endsWith"></a>`string-endsWith`

[string](/models/ref/query-panel/string) 이 특정 접미사로 끝나는지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |
| `suffix` | 확인할 접미사 |

#### 반환 값
[string](/models/ref/query-panel/string) 이 해당 접미사로 끝나는지 여부

### <a id="string-findAll"></a>`string-findAll`

[string](/models/ref/query-panel/string) 에서 부분 문자열이 나타나는 모든 위치를 찾습니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 부분 문자열을 찾을 [string](/models/ref/query-panel/string) |
| `sub` | 찾을 부분 문자열 |

#### 반환 값
[string](/models/ref/query-panel/string) 내 부분 문자열 인덱스의 _list_

### <a id="string-isAlnum"></a>`string-isAlnum`

[string](/models/ref/query-panel/string) 이 영숫자(alphanumeric)인지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 이 영숫자인지 여부

### <a id="string-isAlpha"></a>`string-isAlpha`

[string](/models/ref/query-panel/string) 이 알파벳인지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 이 알파벳인지 여부

### <a id="string-isNumeric"></a>`string-isNumeric`

[string](/models/ref/query-panel/string) 이 숫자인지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 이 숫자인지 여부

### <a id="string-lStrip"></a>`string-lStrip`

앞부분의 공백을 제거합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 공백을 제거할 [string](/models/ref/query-panel/string). |

#### 반환 값
공백이 제거된 [string](/models/ref/query-panel/string).

### <a id="string-len"></a>`string-len`

[string](/models/ref/query-panel/string) 의 길이를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 길이를 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 의 길이

### <a id="string-lower"></a>`string-lower`

[string](/models/ref/query-panel/string) 을 소문자로 변환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 소문자로 변환할 [string](/models/ref/query-panel/string) |

#### 반환 값
소문자로 변환된 [string](/models/ref/query-panel/string)

### <a id="string-partition"></a>`string-partition`

[string](/models/ref/query-panel/string) 을 [strings](/models/ref/query-panel/string/) 의 _list_ 로 파티션합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 분할할 [string](/models/ref/query-panel/string) |
| `sep` | 분할 기준이 되는 구분자 |

#### 반환 값
[strings](/models/ref/query-panel/string/) 의 _list_: 구분자 앞의 [string](/models/ref/query-panel/string), 구분자 자체, 그리고 구분자 뒤의 [string](/models/ref/query-panel/string)

### <a id="string-prepend"></a>`string-prepend`

[string](/models/ref/query-panel/string) 앞에 접두사를 추가합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 접두사를 추가할 [string](/models/ref/query-panel/string) |
| `prefix` | 추가할 접두사 |

#### 반환 값
접두사가 추가된 [string](/models/ref/query-panel/string)

### <a id="string-rStrip"></a>`string-rStrip`

뒷부분의 공백을 제거합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 공백을 제거할 [string](/models/ref/query-panel/string). |

#### 반환 값
공백이 제거된 [string](/models/ref/query-panel/string).

### <a id="string-replace"></a>`string-replace`

[string](/models/ref/query-panel/string) 에서 부분 문자열이 나타나는 모든 곳을 교체합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 내용을 교체할 [string](/models/ref/query-panel/string) |
| `sub` | 교체될 기존 부분 문자열 |
| `newSub` | 기존 부분 문자열을 대체할 새로운 부분 문자열 |

#### 반환 값
내용이 교체된 [string](/models/ref/query-panel/string)

### <a id="string-slice"></a>`string-slice`

시작 및 종료 인덱스를 기반으로 [string](/models/ref/query-panel/string) 을 부분 문자열로 슬라이스합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 슬라이스할 [string](/models/ref/query-panel/string) |
| `begin` | 부분 문자열의 시작 인덱스 |
| `end` | 부분 문자열의 종료 인덱스 |

#### 반환 값
슬라이스된 부분 문자열

### <a id="string-split"></a>`string-split`

[string](/models/ref/query-panel/string) 을 [strings](/models/ref/query-panel/string/) 의 _list_ 로 분할합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 분할할 [string](/models/ref/query-panel/string) |
| `sep` | 분할 기준이 되는 구분자 |

#### 반환 값
[strings](/models/ref/query-panel/string/) 의 _list_

### <a id="string-startsWith"></a>`string-startsWith`

[string](/models/ref/query-panel/string) 이 특정 접두사로 시작하는지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |
| `prefix` | 확인할 접두사 |

#### 반환 값
[string](/models/ref/query-panel/string) 이 해당 접두사로 시작하는지 여부

### <a id="string-strip"></a>`string-strip`

[string](/models/ref/query-panel/string) 의 양 끝에서 공백을 제거합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 공백을 제거할 [string](/models/ref/query-panel/string). |

#### 반환 값
공백이 제거된 [string](/models/ref/query-panel/string).

### <a id="string-upper"></a>`string-upper`

[string](/models/ref/query-panel/string) 을 대문자로 변환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 대문자로 변환할 [string](/models/ref/query-panel/string) |

#### 반환 값
대문자로 변환된 [string](/models/ref/query-panel/string)

### <a id="string-levenshtein"></a>`string-levenshtein`

두 [strings](/models/ref/query-panel/string/) 간의 레벤슈타인 거리(Levenshtein distance)를 계산합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str1` | 첫 번째 [string](/models/ref/query-panel/string). |
| `str2` | 두 번째 [string](/models/ref/query-panel/string). |

#### 반환 값
두 [strings](/models/ref/query-panel/string/) 간의 레벤슈타인 거리.


## List Ops
### <a id="string-notEqual"></a>`string-notEqual`

두 값의 불일치 여부를 결정합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 비교할 첫 번째 값. |
| `rhs` | 비교할 두 번째 값. |

#### 반환 값
두 값이 일치하지 않는지 여부.

### <a id="string-add"></a>`string-add`

두 [strings](/models/ref/query-panel/string/) 을 연결합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 첫 번째 [string](/models/ref/query-panel/string) |
| `rhs` | 두 번째 [string](/models/ref/query-panel/string) |

#### 반환 값
연결된 [string](/models/ref/query-panel/string)

### <a id="string-equal"></a>`string-equal`

두 값의 일치 여부를 결정합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 비교할 첫 번째 값. |
| `rhs` | 비교할 두 번째 값. |

#### 반환 값
두 값이 일치하는지 여부.

### <a id="string-append"></a>`string-append`

[string](/models/ref/query-panel/string) 뒤에 접미사를 추가합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 접미사를 추가할 [string](/models/ref/query-panel/string) |
| `suffix` | 추가할 접미사 |

#### 반환 값
접미사가 추가된 [string](/models/ref/query-panel/string)

### <a id="string-contains"></a>`string-contains`

[string](/models/ref/query-panel/string) 에 부분 문자열이 포함되어 있는지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |
| `sub` | 검색할 부분 문자열 |

#### 반환 값
[string](/models/ref/query-panel/string) 에 부분 문자열이 포함되어 있는지 여부

### <a id="string-endsWith"></a>`string-endsWith`

[string](/models/ref/query-panel/string) 이 특정 접미사로 끝나는지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |
| `suffix` | 확인할 접미사 |

#### 반환 값
[string](/models/ref/query-panel/string) 이 해당 접미사로 끝나는지 여부

### <a id="string-findAll"></a>`string-findAll`

[string](/models/ref/query-panel/string) 에서 부분 문자열이 나타나는 모든 위치를 찾습니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 부분 문자열을 찾을 [string](/models/ref/query-panel/string) |
| `sub` | 찾을 부분 문자열 |

#### 반환 값
[string](/models/ref/query-panel/string) 내 부분 문자열 인덱스의 _list_

### <a id="string-isAlnum"></a>`string-isAlnum`

[string](/models/ref/query-panel/string) 이 영숫자(alphanumeric)인지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 이 영숫자인지 여부

### <a id="string-isAlpha"></a>`string-isAlpha`

[string](/models/ref/query-panel/string) 이 알파벳인지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 이 알파벳인지 여부

### <a id="string-isNumeric"></a>`string-isNumeric`

[string](/models/ref/query-panel/string) 이 숫자인지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 이 숫자인지 여부

### <a id="string-lStrip"></a>`string-lStrip`

앞부분의 공백을 제거합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 공백을 제거할 [string](/models/ref/query-panel/string). |

#### 반환 값
공백이 제거된 [string](/models/ref/query-panel/string).

### <a id="string-len"></a>`string-len`

[string](/models/ref/query-panel/string) 의 길이를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 길이를 확인할 [string](/models/ref/query-panel/string) |

#### 반환 값
[string](/models/ref/query-panel/string) 의 길이

### <a id="string-lower"></a>`string-lower`

[string](/models/ref/query-panel/string) 을 소문자로 변환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 소문자로 변환할 [string](/models/ref/query-panel/string) |

#### 반환 값
소문자로 변환된 [string](/models/ref/query-panel/string)

### <a id="string-partition"></a>`string-partition`

[string](/models/ref/query-panel/string) 을 [strings](/models/ref/query-panel/string/) 의 _list_ 로 파티션합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 분할할 [string](/models/ref/query-panel/string) |
| `sep` | 분할 기준이 되는 구분자 |

#### 반환 값
[strings](/models/ref/query-panel/string/) 의 _list_: 구분자 앞의 [string](/models/ref/query-panel/string), 구분자 자체, 그리고 구분자 뒤의 [string](/models/ref/query-panel/string)

### <a id="string-prepend"></a>`string-prepend`

[string](/models/ref/query-panel/string) 앞에 접두사를 추가합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 접두사를 추가할 [string](/models/ref/query-panel/string) |
| `prefix` | 추가할 접두사 |

#### 반환 값
접두사가 추가된 [string](/models/ref/query-panel/string)

### <a id="string-rStrip"></a>`string-rStrip`

뒷부분의 공백을 제거합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 공백을 제거할 [string](/models/ref/query-panel/string). |

#### 반환 값
공백이 제거된 [string](/models/ref/query-panel/string).

### <a id="string-replace"></a>`string-replace`

[string](/models/ref/query-panel/string) 에서 부분 문자열이 나타나는 모든 곳을 교체합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 내용을 교체할 [string](/models/ref/query-panel/string) |
| `sub` | 교체될 기존 부분 문자열 |
| `newSub` | 기존 부분 문자열을 대체할 새로운 부분 문자열 |

#### 반환 값
내용이 교체된 [string](/models/ref/query-panel/string)

### <a id="string-slice"></a>`string-slice`

시작 및 종료 인덱스를 기반으로 [string](/models/ref/query-panel/string) 을 부분 문자열로 슬라이스합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 슬라이스할 [string](/models/ref/query-panel/string) |
| `begin` | 부분 문자열의 시작 인덱스 |
| `end` | 부분 문자열의 종료 인덱스 |

#### 반환 값
슬라이스된 부분 문자열

### <a id="string-split"></a>`string-split`

[string](/models/ref/query-panel/string) 을 [strings](/models/ref/query-panel/string/) 의 _list_ 로 분할합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 분할할 [string](/models/ref/query-panel/string) |
| `sep` | 분할 기준이 되는 구분자 |

#### 반환 값
[strings](/models/ref/query-panel/string/) 의 _list_

### <a id="string-startsWith"></a>`string-startsWith`

[string](/models/ref/query-panel/string) 이 특정 접두사로 시작하는지 확인합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 확인할 [string](/models/ref/query-panel/string) |
| `prefix` | 확인할 접두사 |

#### 반환 값
[string](/models/ref/query-panel/string) 이 해당 접두사로 시작하는지 여부

### <a id="string-strip"></a>`string-strip`

[string](/models/ref/query-panel/string) 의 양 끝에서 공백을 제거합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 공백을 제거할 [string](/models/ref/query-panel/string). |

#### 반환 값
공백이 제거된 [string](/models/ref/query-panel/string).

### <a id="string-upper"></a>`string-upper`

[string](/models/ref/query-panel/string) 을 대문자로 변환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str` | 대문자로 변환할 [string](/models/ref/query-panel/string) |

#### 반환 값
대문자로 변환된 [string](/models/ref/query-panel/string)

### <a id="string-levenshtein"></a>`string-levenshtein`

두 [strings](/models/ref/query-panel/string/) 간의 레벤슈타인 거리(Levenshtein distance)를 계산합니다.

| 인수 | 설명 |
| :--- | :--- |
| `str1` | 첫 번째 [string](/models/ref/query-panel/string). |
| `str2` | 두 번째 [string](/models/ref/query-panel/string). |

#### 반환 값
두 [strings](/models/ref/query-panel/string/) 간의 레벤슈타인 거리.
