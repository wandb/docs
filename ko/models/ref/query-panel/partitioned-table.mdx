---
title: partitioned-table
---

## 체이닝 가능한 Ops {#chainable-ops}

### <a id="asset-file" />`asset-file` {#asset-file}

asset의 &#95;file&#95;을 반환합니다

| 인수      | 설명    |
| :------ | :---- |
| `asset` | asset |

#### 반환 값 {#return-value}

asset의 *file*

### <a id="partitionedtable-file" />`partitionedtable-file` {#partitionedtable-file}

&#95;partitioned-table&#95;의 &#95;file&#95;을 반환합니다

| 인수                 | 설명                  |
| :----------------- | :------------------ |
| `partitionedTable` | *partitioned-table* |

#### 반환 값 {#return-value-2}

&#95;partitioned-table&#95;의 *file*

### <a id="partitionedtable-rows" />`partitionedtable-rows` {#partitionedtable-rows}

&#95;partitioned-table&#95;의 행을 반환합니다

| 인수                 | 설명                         |
| :----------------- | :------------------------- |
| `partitionedTable` | 행을 조회할 *partitioned-table* |

#### 반환 값 {#return-value-3}

&#95;partitioned-table&#95;의 행

## Ops 목록 {#list-ops}

### <a id="asset-file-list" />`asset-file` {#asset-file-2}

asset의 &#95;file&#95;을 반환합니다

| 인수      | 설명    |
| :------ | :---- |
| `asset` | asset |

#### 반환 값 {#return-value-4}

asset의 *file*