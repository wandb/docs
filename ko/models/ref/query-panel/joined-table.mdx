---
title: joined-table
---

## 연결 가능한 Ops {#chainable-ops}

### <a id="asset-file" />`asset-file` {#asset-file}

asset의 &#95;file&#95;을 반환합니다

| 인수      | 설명    |
| :------ | :---- |
| `asset` | asset |

#### 반환값 {#return-value}

asset의 *file*

### <a id="joinedtable-file" />`joinedtable-file` {#joinedtable-file}

&#95;joined-table&#95;의 &#95;file&#95;을 반환합니다.

| 인수            | 설명             |
| :------------ | :------------- |
| `joinedTable` | *joined-table* |

#### 반환값 {#return-value-2}

&#95;joined-table&#95;의 *file*

### <a id="joinedtable-rows" />`joinedtable-rows` {#joinedtable-rows}

&#95;joined-table&#95;의 행들을 반환합니다

| 인수            | 설명                                      |
| :------------ | :-------------------------------------- |
| `joinedTable` | *joined-table*                          |
| `leftOuter`   | 오른쪽 테이블에 일치하는 행이 없어도 왼쪽 테이블의 행을 포함할지 여부 |
| `rightOuter`  | 왼쪽 테이블에 일치하는 행이 없어도 오른쪽 테이블의 행을 포함할지 여부 |

#### 반환값 {#return-value-3}

&#95;joined-table&#95;의 각 행

## Ops 목록 {#list-ops}

### <a id="asset-file-list" />`asset-file` {#asset-file-2}

asset의 &#95;file&#95;을 반환합니다

| 인수      | 설명    |
| :------ | :---- |
| `asset` | asset |

#### 반환값 {#return-value-4}

asset의 *file*