---
title: 불리언(boolean)
---

## Chainable Ops
### <a id="and"></a>`and`

두 값의 논리 `and` 연산 결과를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 첫 번째 이진 값 |
| `rhs` | 두 번째 이진 값 |

#### 반환 값
두 값의 논리 `and` 연산 결과

### <a id="or"></a>`or`

두 값의 논리 `or` 연산 결과를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 첫 번째 이진 값 |
| `rhs` | 두 번째 이진 값 |

#### 반환 값
두 값의 논리 `or` 연산 결과

### <a id="boolean-not"></a>`boolean-not`

값의 논리적 역(inverse)을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `bool` | 불리언 값 |

#### 반환 값
값의 논리적 역

### <a id="boolean-not"></a>`boolean-not`

값의 논리적 역을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `bool` | 불리언 값 |

#### 반환 값
값의 논리적 역


## List Ops
### <a id="and"></a>`and`

두 값의 논리 `and` 연산 결과를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 첫 번째 이진 값 |
| `rhs` | 두 번째 이진 값 |

#### 반환 값
두 값의 논리 `and` 연산 결과

### <a id="or"></a>`or`

두 값의 논리 `or` 연산 결과를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `lhs` | 첫 번째 이진 값 |
| `rhs` | 두 번째 이진 값 |

#### 반환 값
두 값의 논리 `or` 연산 결과

### <a id="boolean-not"></a>`boolean-not`

값의 논리적 역을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `bool` | 불리언 값 |

#### 반환 값
값의 논리적 역

### <a id="boolean-not"></a>`boolean-not`

값의 논리적 역을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `bool` | 불리언 값 |

#### 반환 값
값의 논리적 역