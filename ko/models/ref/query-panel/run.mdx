---
title: run
---

## Chainable Ops
### <a id="run-config"></a>`run-config`

[run](/models/ref/query-panel/run) 의 config _typedDict_ 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 config _typedDict_

### <a id="run-createdAt"></a>`run-createdAt`

[run](/models/ref/query-panel/run) 이 생성된 날짜와 시간을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 이 생성된 날짜와 시간

### <a id="run-heartbeatAt"></a>`run-heartbeatAt`

[run](/models/ref/query-panel/run) 의 마지막 하트비트 날짜와 시간을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 마지막 하트비트 날짜와 시간

### <a id="run-history"></a>`run-history`

[run](/models/ref/query-panel/run) 의 로그 히스토리를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 로그 히스토리

### <a id="run-jobType"></a>`run-jobType`

[run](/models/ref/query-panel/run) 의 job 유형 을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 job 유형

### <a id="run-loggedArtifactVersion"></a>`run-loggedArtifactVersion`

지정된 이름과 에일리어스 에 대해 [run](/models/ref/query-panel/run) 에 의해 로그 된 [artifactVersion](/models/ref/query-panel/artifact-version/) 을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |
| `artifactVersionName` | [artifactVersion](/models/ref/query-panel/artifact-version/) 의 name:alias |

#### 반환 값
지정된 이름과 에일리어스 에 대해 [run](/models/ref/query-panel/run) 에 의해 로그 된 [artifactVersion](/models/ref/query-panel/artifact-version/)

### <a id="run-loggedArtifactVersions"></a>`run-loggedArtifactVersions`

[run](/models/ref/query-panel/run) 에 의해 로그 된 모든 [artifactVersions](/models/ref/query-panel/artifact-version/) 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 에 의해 로그 된 [artifactVersions](/models/ref/query-panel/artifact-version/)

### <a id="run-name"></a>`run-name`

[run](/models/ref/query-panel/run) 의 이름을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 이름

### <a id="run-runtime"></a>`run-runtime`

[run](/models/ref/query-panel/run) 의 런타임(초 단위)을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 런타임(초 단위)

### <a id="run-summary"></a>`run-summary`

[run](/models/ref/query-panel/run) 의 summary _typedDict_ 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 summary _typedDict_

### <a id="run-usedArtifactVersions"></a>`run-usedArtifactVersions`

[run](/models/ref/query-panel/run) 에 의해 사용된 모든 [artifactVersions](/models/ref/query-panel/artifact-version/) 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 에 의해 사용된 [artifactVersions](/models/ref/query-panel/artifact-version/)

### <a id="run-user"></a>`run-user`

[run](/models/ref/query-panel/run) 의 [user](/models/ref/query-panel/user/) 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 [user](/models/ref/query-panel/user/)


## List Ops
### <a id="run-config"></a>`run-config`

[run](/models/ref/query-panel/run) 의 config _typedDict_ 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 config _typedDict_

### <a id="run-createdAt"></a>`run-createdAt`

[run](/models/ref/query-panel/run) 이 생성된 날짜와 시간을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 이 생성된 날짜와 시간

### <a id="run-heartbeatAt"></a>`run-heartbeatAt`

[run](/models/ref/query-panel/run) 의 마지막 하트비트 날짜와 시간을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 마지막 하트비트 날짜와 시간

### <a id="run-history"></a>`run-history`

[run](/models/ref/query-panel/run) 의 로그 히스토리를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 로그 히스토리

### <a id="run-jobType"></a>`run-jobType`

[run](/models/ref/query-panel/run) 의 job 유형 을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 job 유형

### <a id="run-loggedArtifactVersion"></a>`run-loggedArtifactVersion`

지정된 이름과 에일리어스 에 대해 [run](/models/ref/query-panel/run) 에 의해 로그 된 [artifactVersion](/models/ref/query-panel/artifact-version/) 을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |
| `artifactVersionName` | [artifactVersion](/models/ref/query-panel/artifact-version/) 의 name:alias |

#### 반환 값
지정된 이름과 에일리어스 에 대해 [run](/models/ref/query-panel/run) 에 의해 로그 된 [artifactVersion](/models/ref/query-panel/artifact-version/)

### <a id="run-loggedArtifactVersions"></a>`run-loggedArtifactVersions`

[run](/models/ref/query-panel/run) 에 의해 로그 된 모든 [artifactVersions](/models/ref/query-panel/artifact-version/) 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 에 의해 로그 된 [artifactVersions](/models/ref/query-panel/artifact-version/)

### <a id="run-name"></a>`run-name`

[run](/models/ref/query-panel/run) 의 이름을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 이름

### <a id="run-runtime"></a>`run-runtime`

[run](/models/ref/query-panel/run) 의 런타임(초 단위)을 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 런타임(초 단위)

### <a id="run-summary"></a>`run-summary`

[run](/models/ref/query-panel/run) 의 summary _typedDict_ 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 의 summary _typedDict_

### <a id="run-usedArtifactVersions"></a>`run-usedArtifactVersions`

[run](/models/ref/query-panel/run) 에 의해 사용된 모든 [artifactVersions](/models/ref/query-panel/artifact-version/) 를 반환합니다.

| 인수 | 설명 |
| :--- | :--- |
| `run` | [run](/models/ref/query-panel/run) |

#### 반환 값
[run](/models/ref/query-panel/run) 에 의해 사용된 [artifactVersions](/models/ref/query-panel/artifact-version/)