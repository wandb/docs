---
description: 머신 러닝 실험에서 플롯을 생성하고 추적합니다.
title: 실험에서 플롯 생성 및 추적
---

W&amp;B Models에서 `wandb.plot`의 메서드를 사용하면 트레이닝 중 시간에 따라 변화하는 차트를 포함해 `wandb.Run.log()`로 차트를 추적할 수 있습니다. 맞춤형 차트 프레임워크에 대해 자세히 알아보려면 [맞춤형 차트 워크스루](/ko/models/app/features/custom-charts/walkthrough/)를 참조하세요.

<div id="basic-charts">
  ### 기본 차트
</div>

W&amp;B 차트를 생성하려면 다음과 같이 하세요.

1. `wandb.Table` 객체를 생성하고 시각화할 데이터를 추가하세요.
2. W&amp;B의 내장 [헬퍼 함수](/ko/models/ref/python/custom-charts) 중 하나를 사용해 플롯을 생성하세요.
3. `wandb.Run.log()`로 플롯을 로깅하세요.

다음 기본 차트는 메트릭과 결과를 기본적으로 시각화하는 데 사용할 수 있습니다.

<Tabs>
  <Tab title="선">
    임의의 축에 표시되는, 서로 연결되고 순서가 있는 점 목록으로 구성된 맞춤형 선형 플롯을 기록합니다.

    ```python
    import wandb

    with wandb.init() as run:
        data = [[x, y] for (x, y) in zip(x_values, y_values)]
        table = wandb.Table(data=data, columns=["x", "y"])
        run.log(
            {
                "my_custom_plot_id": wandb.plot.line(
                    table, "x", "y", title="Custom Y versus X line plot"
                )
            }
        )
    ```

    이를 사용하면 임의의 두 차원에 대한 곡선을 로깅할 수 있습니다. 두 값 목록을 서로 대응시켜 플로팅하는 경우에는 각 목록의 값 개수가 정확히 일치해야 합니다. 예를 들어, 각 점에는 x와 y가 모두 있어야 합니다.

    <Frame>
      <img src="/images/track/line_plot.png" alt="맞춤형 선형 플롯" />
    </Frame>

    자세한 내용은 [Creating Custom Line Plots With W&amp;B](https://wandb.ai/wandb/plots/reports/Custom-Line-Plots--VmlldzoyNjk5NTA) 리포트를 참조하세요.

    [코드 실행](https://tiny.cc/custom-charts)
  </Tab>

  <Tab title="산점도">
    임의의 x축과 y축에 대한 점 (x, y) 목록으로 맞춤형 산점도를 로깅합니다.

    ```python
    import wandb

    with wandb.init() as run:
        data = [[x, y] for (x, y) in zip(class_x_scores, class_y_scores)]
        table = wandb.Table(data=data, columns=["class_x", "class_y"])
        run.log({"my_custom_id": wandb.plot.scatter(table, "class_x", "class_y")})
    ```

    이를 사용해 임의의 두 차원에서 산점도 점을 로깅할 수 있습니다. 두 값 목록을 서로 대응시켜 그리는 경우에는 두 목록의 값 개수가 정확히 일치해야 합니다. 예를 들어, 각 점에는 x와 y가 모두 있어야 합니다.

    <Frame>
      <img src="/images/track/demo_scatter_plot.png" alt="맞춤형 산점도" />
    </Frame>

    자세한 내용은 [W&amp;B로 맞춤형 산점도 만들기](https://wandb.ai/wandb/plots/reports/Custom-Scatter-Plots--VmlldzoyNjk5NDQ) 리포트를 참조하세요.

    [코드 실행](https://tiny.cc/custom-charts)
  </Tab>

  <Tab title="막대">
    몇 줄만으로 기본 제공되는 맞춤형 막대 차트(레이블이 있는 값 목록을 막대로 표시)를 기록합니다:

    ```python
    import wandb

    with wandb.init() as run:
        data = [[label, val] for (label, val) in zip(labels, values)]
        table = wandb.Table(data=data, columns=["label", "value"])
        run.log(
            {
                "my_bar_chart_id": wandb.plot.bar(
                    table, "label", "value", title="Custom bar chart"
                )
            }
        )
    ```

    이를 사용해 임의의 막대 차트를 로깅할 수 있습니다. 목록의 레이블 수와 값 수는 정확히 일치해야 합니다. 각 데이터 포인트에는 레이블과 값이 모두 있어야 합니다.

    <Frame>
      <img src="/images/track/basic_charts_bar.png" alt="맞춤형 막대 차트" />
    </Frame>

    자세한 내용은 [Custom Bar Charts](https://wandb.ai/wandb/plots/reports/Custom-Bar-Charts--VmlldzoyNzExNzk) 리포트를 참조하세요.

    [코드 실행](https://tiny.cc/custom-charts)
  </Tab>

  <Tab title="Histogram">
    몇 줄만으로 기본 지원되는 맞춤형 히스토그램(값 목록을 개수 또는 발생 빈도에 따라 빈으로 분류)을 기록합니다. 예측 신뢰도 점수 목록(`scores`)이 있다면, 다음과 같이 분포를 시각화할 수 있습니다:

    ```python
    import wandb

    with wandb.init() as run:
        data = [[s] for s in scores]
        table = wandb.Table(data=data, columns=["scores"])
        run.log({"my_histogram": wandb.plot.histogram(table, "scores", title="Histogram")})
    ```

    이를 사용해 임의의 히스토그램을 기록할 수 있습니다. `data`는 목록의 목록으로, 행과 열로 이루어진 2차원 배열을 지원하기 위한 형식입니다.

    <Frame>
      <img src="/images/track/demo_custom_chart_histogram.png" alt="맞춤형 히스토그램" />
    </Frame>

    자세한 내용은 [Creating Custom Histograms With W&amp;B](https://wandb.ai/wandb/plots/reports/Custom-Histograms--VmlldzoyNzE0NzM) 리포트를 참조하세요.

    [코드 실행](https://tiny.cc/custom-charts)
  </Tab>

  <Tab title="멀티라인">
    하나의 x-y 축에 여러 선이나 서로 다른 x-y 좌표 쌍 목록 여러 개를 그립니다:

    ```python
    import wandb

    with wandb.init() as run:
        run.log(
            {
                "my_custom_id": wandb.plot.line_series(
                    xs=[0, 1, 2, 3, 4],
                    ys=[[10, 20, 30, 40, 50], [0.5, 11, 72, 3, 41]],
                    keys=["metric Y", "metric Z"],
                    title="Two Random Metrics",
                    xname="x units",
                )
            }
        )
    ```

    x와 y 점의 개수는 정확히 일치해야 합니다. 여러 y 값 목록에 맞춰 하나의 x 값 목록을 제공하거나, 각 y 값 목록에 대해 별도의 x 값 목록을 제공할 수 있습니다.

    <Frame>
      <img src="/images/track/basic_charts_histogram.png" alt="멀티라인 플롯" />
    </Frame>

    자세한 내용은 [Custom Multi-Line Plots](https://wandb.ai/wandb/plots/reports/Custom-Multi-Line-Plots--VmlldzozOTMwMjU) 리포트를 참조하세요.
  </Tab>
</Tabs>

<div id="model-evaluation-charts">
  ### 모델 평가 차트
</div>

이러한 프리셋 차트에는 `wandb.plot()` 메서드가 내장되어 있어, 스크립트에서 차트를 바로 로깅하고 UI에서 원하는 정보를 정확하게 확인할 수 있습니다.

<Tabs>
  <Tab title="정밀도-재현율 곡선">
    한 줄로 [정밀도-재현율 곡선](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.precision_recall_curve.html#sklearn.metrics.precision_recall_curve)을 생성하세요:

    ```python
    import wandb
    with wandb.init() as run:
        # ground_truth는 정답 레이블 목록이고, predictions는 예측 점수 목록입니다.
        # 예를 들어, ground_truth = [0, 1, 1, 0]이고 predictions = [0.1, 0.4, 0.35, 0.8]입니다.
        ground_truth = [0, 1, 1, 0]
        predictions = [0.1, 0.4, 0.35, 0.8]
        run.log({"pr": wandb.plot.pr_curve(ground_truth, predictions)})
    ```

    코드에서 다음 항목에 액세스할 수 있을 때마다 이를 log할 수 있습니다:

    * 예시 집합에 대한 모델의 예측 점수(`predictions`)
    * 해당 예시들에 대응하는 정답 레이블(`ground_truth`)
    * (선택) 레이블 또는 클래스 이름의 list. 예를 들어, 레이블 인덱스 0이 cat, 1이 dog, 2가 bird를 의미하는 경우 `labels=["cat", "dog", "bird"]`
    * (선택) plot에서 시각화할 레이블의 하위 집합(여전히 list 형식)

    <Frame>
      <img src="/images/track/model_eval_charts_precision_recall.png" alt="정밀도-재현율 곡선" />
    </Frame>

    자세한 내용은 [Plot Precision Recall Curves With W&amp;B](https://wandb.ai/wandb/plots/reports/Plot-Precision-Recall-Curves--VmlldzoyNjk1ODY) 리포트를 참조하세요.

    [코드 실행](https://colab.research.google.com/drive/1mS8ogA3LcZWOXchfJoMrboW3opY1A8BY?usp=sharing)
  </Tab>

  <Tab title="ROC 곡선">
    한 줄로 [ROC curve](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_curve.html#sklearn.metrics.roc_curve)를 생성하세요:

    ```python
    import wandb

    with wandb.init() as run:
        # ground_truth는 실제 레이블 목록이고, predictions는 예측 점수 목록입니다.
        # 예시: ground_truth = [0, 1, 1, 0], predictions = [0.1, 0.4, 0.35, 0.8]
        ground_truth = [0, 1, 1, 0]
        predictions = [0.1, 0.4, 0.35, 0.8]
        run.log({"roc": wandb.plot.roc_curve(ground_truth, predictions)})
    ```

    코드에서 다음 항목에 액세스할 수 있을 때마다 이를 로그할 수 있습니다:

    * 예시 집합에 대한 모델의 예측 점수(`predictions`)
    * 해당 예시에 대응하는 실제 정답 레이블(`ground_truth`)
    * (선택) 레이블 또는 클래스 이름의 목록. 예를 들어, 레이블 인덱스 0이 cat, 1이 dog, 2가 bird를 의미한다면 `labels=["cat", "dog", "bird"]`입니다.
    * (선택) 플롯에 시각화할 이러한 레이블의 하위 집합(여전히 목록 형식)

    <Frame>
      <img src="/images/track/demo_custom_chart_roc_curve.png" alt="ROC 곡선" />
    </Frame>

    자세한 내용은 [Plot ROC Curves With W&amp;B](https://wandb.ai/wandb/plots/reports/Plot-ROC-Curves--VmlldzoyNjk3MDE) 리포트를 참조하세요.

    [코드 실행](https://colab.research.google.com/github/wandb/examples/blob/master/colabs/wandb-log/Plot_ROC_Curves_with_W%26B.ipynb)
  </Tab>

  <Tab title="혼동 행렬">
    한 줄로 다중 클래스 [혼동 행렬](https://scikit-learn.org/stable/auto_examples/model_selection/plot_confusion_matrix.html)을 생성하세요:

    ```python
    import wandb

    cm = wandb.plot.confusion_matrix(
        y_true=ground_truth, preds=predictions, class_names=class_names
    )

    with wandb.init() as run:
        run.log({"conf_mat": cm})
    ```

    코드에서 다음 항목에 액세스할 수 있는 곳이라면 어디에서나 이를 log할 수 있습니다:

    * 예시 집합에 대한 모델의 예측 레이블(`preds`) 또는 정규화된 확률 점수(`probs`). 확률의 shape은 (예시 수, 클래스 수)여야 합니다. 확률이나 예측 중 하나만 제공할 수 있으며, 둘 다 제공할 수는 없습니다.
    * 해당 예시들에 대응하는 실제 정답 레이블(`y_true`).
    * `class_names`에 string으로 된 레이블 또는 클래스 이름의 전체 목록. 예를 들어 인덱스 0이 `cat`, 1이 `dog`, 2가 `bird`인 경우 `class_names=["cat", "dog", "bird"]`입니다.

    <Frame>
      <img src="/images/experiments/confusion_matrix.png" alt="혼동 행렬" />
    </Frame>

    자세한 내용은 [Confusion Matrix: 사용 및 예시](https://wandb.ai/wandb/plots/reports/Confusion-Matrix--VmlldzozMDg1NTM) 리포트를 참조하세요.

    [코드 실행](https://colab.research.google.com/github/wandb/examples/blob/master/colabs/wandb-log/Log_a_Confusion_Matrix_with_W%26B.ipynb)
  </Tab>
</Tabs>

<div id="interactive-custom-charts">
  ### 대화형 맞춤형 차트
</div>

완전히 맞춤 설정하려면 기본 제공 [맞춤형 차트 프리셋](/ko/models/app/features/custom-charts/walkthrough/)을 조정하거나 새 프리셋을 생성한 다음 차트를 저장하세요. 차트 ID를 사용하면 스크립트에서 해당 맞춤형 프리셋으로 데이터를 바로 로깅할 수 있습니다.

```python
import wandb
# Create a table with the columns to plot.
table = wandb.Table(data=data, columns=["step", "height"])

# Map from the table's columns to the chart's fields.
fields = {"x": "step", "value": "height"}

# Use the table to populate the new custom chart preset.
# To use your own saved chart preset, change the vega_spec_name.
# To edit the title, change the string_fields.
my_custom_chart = wandb.plot_table(
    vega_spec_name="carey/new_chart",
    data_table=table,
    fields=fields,
    string_fields={"title": "Height Histogram"},
)

with wandb.init() as run:
    # Log the custom chart.
    run.log({"my_custom_chart": my_custom_chart})
```

[코드 실행하기](https://tiny.cc/custom-charts)

<div id="matplotlib-and-plotly-plots">
  ### Matplotlib 및 Plotly 플롯
</div>

`wandb.plot()`으로 W&amp;B [맞춤형 차트](/ko/models/app/features/custom-charts/walkthrough/)를 사용하는 대신 [matplotlib](https://matplotlib.org/)와 [Plotly](https://plotly.com/)로 생성한 차트를 W&amp;B에 로깅할 수 있습니다.

```python
import wandb
import matplotlib.pyplot as plt

with wandb.init() as run:
    # 간단한 matplotlib 플롯을 생성합니다.
    plt.figure()
    plt.plot([1, 2, 3, 4])
    plt.ylabel("some interesting numbers")

    # W&B에 플롯을 로깅합니다.
    run.log({"chart": plt})
```

`matplotlib` 플롯 또는 figure 객체를 `wandb.Run.log()`에 전달하면 됩니다. 기본적으로 플롯은 [Plotly](https://plot.ly/) 플롯으로 변환됩니다. 플롯을 이미지로 로깅하고 싶다면, 플롯을 `wandb.Image`에 전달하면 됩니다. Plotly 차트도 직접 전달할 수 있습니다.

<Note>
  &quot;You attempted to log an empty plot&quot;와 같은 오류가 발생하면, `fig = plt.figure()`로 플롯과 분리된 figure를 별도로 저장한 뒤, `wandb.Run.log()` 호출에서 `fig`를 로깅하면 됩니다.
</Note>

<div id="log-custom-html-to-wb-tables">
  ### 맞춤형 HTML을 W&amp;B 테이블에 로깅하기
</div>

W&amp;B는 Plotly와 Bokeh에서 생성한 대화형 차트를 HTML로 로깅하고 테이블에 추가하는 기능을 지원합니다.

<div id="log-plotly-figures-to-tables-as-html">
  #### HTML로 Plotly figure를 테이블에 로깅하기
</div>

Plotly의 대화형 차트를 HTML로 변환한 다음 W&amp;B 테이블에 로깅할 수 있습니다.

```python
import wandb
import plotly.express as px

# 새 run을 초기화합니다.
with wandb.init(project="log-plotly-fig-tables", name="plotly_html") as run:

    # 테이블을 생성합니다.
    table = wandb.Table(columns=["plotly_figure"])

    # Plotly figure의 경로를 생성합니다.
    path_to_plotly_html = "./plotly_figure.html"

    # Plotly figure 예시입니다.
    fig = px.scatter(x=[0, 1, 2, 3, 4], y=[0, 1, 4, 9, 16])

    # Plotly figure를 HTML로 저장합니다.
    # auto_play를 False로 설정하면 애니메이션 Plotly 차트가
    # 테이블에서 자동으로 재생되지 않습니다.
    fig.write_html(path_to_plotly_html, auto_play=False)

    # Plotly figure를 HTML 파일로 테이블에 추가합니다.
    table.add_data(wandb.Html(path_to_plotly_html))

    # 테이블을 로깅합니다.
    run.log({"test_table": table})
```

<div id="log-bokeh-figures-to-tables-as-html">
  #### Bokeh figure를 HTML로 변환해 테이블에 로깅하기
</div>

대화형 Bokeh 차트를 HTML로 변환해 W&amp;B 테이블에 로깅할 수 있습니다.

```python
from scipy.signal import spectrogram
import holoviews as hv
import panel as pn
from scipy.io import wavfile
import numpy as np
from bokeh.resources import INLINE

hv.extension("bokeh", logo=False)
import wandb


def save_audio_with_bokeh_plot_to_html(audio_path, html_file_name):
    sr, wav_data = wavfile.read(audio_path)
    duration = len(wav_data) / sr
    f, t, sxx = spectrogram(wav_data, sr)
    spec_gram = hv.Image((t, f, np.log10(sxx)), ["Time (s)", "Frequency (hz)"]).opts(
        width=500, height=150, labelled=[]
    )
    audio = pn.pane.Audio(wav_data, sample_rate=sr, name="Audio", throttle=500)
    slider = pn.widgets.FloatSlider(end=duration, visible=False)
    line = hv.VLine(0).opts(color="white")
    slider.jslink(audio, value="time", bidirectional=True)
    slider.jslink(line, value="glyph.location")
    combined = pn.Row(audio, spec_gram * line, slider).save(html_file_name)


html_file_name = "audio_with_plot.html"
audio_path = "hello.wav"
save_audio_with_bokeh_plot_to_html(audio_path, html_file_name)

wandb_html = wandb.Html(html_file_name)

with wandb.init(project="audio_test") as run:
    my_table = wandb.Table(columns=["audio_with_plot"], data=[[wandb_html]])
    run.log({"audio_table": my_table})
```
