---
title: run 삭제
description: W&B App을 사용하여 W&B 프로젝트에서 run을 삭제하는 방법을 알아보세요.
---

W&B App을 사용하여 프로젝트에서 하나 이상의 run을 삭제할 수 있습니다.

1. 삭제하려는 run이 포함된 프로젝트로 이동합니다.
2. **Runs** 탭을 선택합니다.
3. 삭제하려는 run 옆의 체크박스를 선택합니다.
4. 테이블 위에 있는 **Delete** 버튼(휴지통 아이콘)을 선택합니다.
5. 표시되는 드로어에서 **Delete**를 선택합니다.

<Note>
run ID는 run이 삭제된 이후에도 다시 사용할 수 없습니다. 동일한 ID를 사용하면 해당 run은 오류와 함께 실패합니다.
</Note>

<Note>
많은 수의 run이 포함된 프로젝트의 경우, 검색 창에서 정규식(Regex)을 사용해 삭제하려는 run을 필터링하거나, 필터 버튼을 사용해 상태, 태그, 기타 속성에 따라 run을 필터링할 수 있습니다.
</Note>

<div id="run-deletion-flowchart">
  ### run 삭제 플로우차트
</div>

다음 다이어그램은 관련 아티팩트와 Registry 링크 처리까지 포함한 run 삭제 전체 프로세스를 보여 줍니다.

```mermaid
graph TB
    Start([사용자가 run 삭제<br/>시작]) --> RunSelect[삭제할 Runs<br/>선택]
    
    RunSelect --> DeletePrompt{연결된 Artifacts<br/>삭제?}
    
    DeletePrompt -->|아니오| DeleteRunOnly[Run만 삭제<br/><br/>- Run 메타데이터 제거됨<br/>- 아티팩트는 계속 사용 가능<br/>- 아티팩트에 계속 접근 가능]
    
    DeletePrompt -->|예| CheckArtifacts[연결된 아티팩트<br/>확인]
    
    CheckArtifacts --> HasRegistry{Model Registry에<br/>연결된 Artifacts?}
    
    HasRegistry -->|예| RegistryWarning[⚠️ 경고<br/><br/>Registry 모델이 삭제됩니다<br/>프로덕션 별칭 영향받음]
    HasRegistry -->|아니오| DirectDelete
    
    RegistryWarning --> ConfirmRegistry{Registry 모델<br/>삭제 확인?}
    
    ConfirmRegistry -->|아니오| DeleteRunOnly
    ConfirmRegistry -->|예| DirectDelete[Run + Artifacts 삭제<br/><br/>- Run 메타데이터 제거됨<br/>- 아티팩트 영구 삭제됨<br/>- Registry 링크 제거됨<br/>- 복구 불가]
    
    DeleteRunOnly --> PartialEnd([Run 삭제됨<br/>Artifacts 보존됨])
    DirectDelete --> FullEnd([Run + Artifacts<br/>영구 삭제됨])
    
    style Start fill:#e1f5fe,stroke:#333,stroke-width:2px,color:#000
    style DeletePrompt fill:#fff3e0,stroke:#333,stroke-width:2px,color:#000
    style RegistryWarning fill:#ffecb3,stroke:#333,stroke-width:2px,color:#000
    style DirectDelete fill:#ffebee,stroke:#333,stroke-width:2px,color:#000
    style DeleteRunOnly fill:#e8f5e9,stroke:#333,stroke-width:2px,color:#000
    style PartialEnd fill:#c8e6c9,stroke:#333,stroke-width:2px,color:#000
    style FullEnd fill:#ffcdd2,stroke:#333,stroke-width:2px,color:#000
```

<Warning>
  run을 삭제할 때 연결된 아티팩트를 함께 삭제하도록 선택하면, 해당 아티팩트는 영구적으로 삭제되며 나중에 run을 복원하더라도 복구할 수 없습니다. 여기에는 Registry에 연결된 아티팩트도 포함됩니다.
</Warning>
