---
description: W&B를 사용하여 Cohere 모델을 파인튜닝하는 방법.
title: Cohere 파인튜닝
---

W&B를 사용하면 Cohere 모델의 파인튜닝 지표와 구성을 로깅하여 모델 성능을 분석하고 이해할 수 있으며, 그 결과를 동료들과 공유할 수 있습니다.

[Cohere의 이 가이드](https://docs.cohere.com/page/convfinqa-finetuning-wandb)에서 파인튜닝 실행을 시작하는 방법에 대한 전체 예제를 확인할 수 있고, [Cohere API 문서](https://docs.cohere.com/reference/createfinetunedmodel#request.body.settings.wandb)는 여기에서 확인할 수 있습니다.

<div id="log-your-cohere-fine-tuning-results">
  ## Cohere 파인튜닝 결과 로깅하기
</div>

W&B 워크스페이스에 Cohere 파인튜닝 로깅을 추가하려면:

1. W&B API 키, W&B `entity`, `project` 이름으로 `WandbConfig`를 생성합니다. API 키는 https://wandb.ai/settings 에서 생성하세요.

2. 이 config를 모델 이름, 데이터셋, 하이퍼파라미터와 함께 `FinetunedModel` 객체에 전달해 파인튜닝 실행을 시작합니다.


    ```python
    from cohere.finetuning import WandbConfig, FinetunedModel

    # W&B 정보를 사용해 config 생성
    wandb_ft_config = WandbConfig(
        api_key="<wandb_api_key>",
        entity="my-entity", # 제공된 API 키와 연결된 유효한 엔터티여야 합니다
        project="cohere-ft",
    )

    ...  # 데이터셋과 하이퍼파라미터를 설정합니다

    # cohere에서 파인튜닝 실행을 시작합니다
    cmd_r_finetune = co.finetuning.create_finetuned_model(
      request=FinetunedModel(
        name="command-r-ft",
        settings=Settings(
          base_model=...
          dataset_id=...
          hyperparameters=...
          wandb=wandb_ft_config  # 여기에서 W&B config를 전달합니다
        ),
      ),
    )
    ```

3. 생성한 W&B 프로젝트에서 모델의 파인튜닝 학습 및 검증 지표와 하이퍼파라미터를 확인합니다.

    <Frame>
        <img src="/images/integrations/cohere_ft.png" alt="Cohere 파인튜닝 대시보드"  />
    </Frame>

<div id="organize-runs">
  ## 실행 정리
</div>

W&B 실행은 자동으로 정리되며, 작업 유형, 베이스 모델, learning rate 및 기타 하이퍼파라미터와 같은 모든 설정 매개변수를 기준으로 필터링하거나 정렬할 수 있습니다.

또한 실행 이름을 바꾸고, 메모를 추가하거나 태그를 달아 실행들을 그룹화할 수 있습니다.

<div id="resources">
  ## 참고 자료
</div>

* [Cohere 파인튜닝 예제](https://github.com/cohere-ai/notebooks/blob/kkt_ft_cookbooks/notebooks/finetuning/convfinqa_finetuning_wandb.ipynb)