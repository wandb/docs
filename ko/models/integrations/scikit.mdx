---
title: Scikit-Learn
description: "W&B를 사용하여 실험 추적과 자동화된 플롯 로깅으로 scikit-learn 모델 성능을 시각화하고 비교하세요."
---

import ApiKeyCreateStreamlined from "/snippets/ko/_includes/api-key-create-streamlined.mdx";

몇 줄의 코드만으로 wandb를 사용해 scikit-learn 모델의 성능을 시각화하고 비교할 수 있습니다. [예제 사용해 보기 →](https://wandb.me/scikit-colab)

<div id="get-started">
  ## 시작하기
</div>

<div id="sign-up-and-create-an-api-key">
  ### 가입하고 API 키 생성하기
</div>

API 키는 당신의 컴퓨터를 W&amp;B에 인증합니다. 사용자 프로필에서 API 키를 생성할 수 있습니다.

<ApiKeyCreateStreamlined />

1. 오른쪽 상단의 사용자 프로필 아이콘을 클릭하세요.
2. **User Settings**를 선택한 후 **API Keys** 섹션으로 스크롤하세요.

<div id="install-the-wandb-library-and-log-in">
  ### `wandb` 라이브러리를 설치하고 로그인하기
</div>

로컬 환경에 `wandb` 라이브러리를 설치하고 로그인하려면:

<Tabs>
  <Tab title="명령줄">
    1. `WANDB_API_KEY` [환경 변수](/ko/models/track/environment-variables/)를 자신의 API 키로 설정합니다.

       ```bash
       export WANDB_API_KEY=<your_api_key>
       ```

    2. `wandb` 라이브러리를 설치한 다음 로그인합니다.

       ```shell
       pip install wandb

       wandb login
       ```
  </Tab>

  <Tab title="Python">
    ```bash
    pip install wandb
    ```

    ```python
    import wandb
    wandb.login()
    ```
  </Tab>

  <Tab title="Python 노트북">
    ```notebook
    !pip install wandb

    import wandb
    wandb.login()
    ```
  </Tab>
</Tabs>

<div id="log-metrics">
  ### 메트릭 로깅
</div>

```python
import wandb

wandb.init(project="visualize-sklearn") as run:

  y_pred = clf.predict(X_test)
  accuracy = sklearn.metrics.accuracy_score(y_true, y_pred)

  # 메트릭을 시간에 따라 로깅할 때는 run.log를 사용하세요
  run.log({"accuracy": accuracy})

  # 또는 트레이닝 종료 시 최종 메트릭을 로깅하려면 run.summary도 사용할 수 있습니다
  run.summary["accuracy"] = accuracy
```

<div id="make-plots">
  ### 플롯 생성하기
</div>

<div id="step-1-import-wandb-and-initialize-a-new-run">
  #### 1단계: wandb를 임포트하고 새 런을 초기화하세요
</div>

```python
import wandb

run = wandb.init(project="visualize-sklearn")
```

<div id="step-2-visualize-plots">
  #### 2단계: 플롯 시각화
</div>

<div id="individual-plots">
  #### 개별 플롯
</div>

모델을 트레이닝하고 예측을 수행한 후 wandb에서 예측을 분석하기 위한 플롯을 생성할 수 있습니다. 아래 **지원 플롯** 섹션을 참조하여 지원되는 차트의 전체 목록을 확인하세요.

```python
# 단일 플롯 시각화
wandb.sklearn.plot_confusion_matrix(y_true, y_pred, labels)
```

<div id="all-plots">
  #### 모든 플롯
</div>

W&amp;B에는 `plot_classifier`와 같은 함수들이 있어서 여러 관련 플롯을 그립니다:

```python
# 모든 분류기 플롯 시각화
wandb.sklearn.plot_classifier(
    clf,
    X_train,
    X_test,
    y_train,
    y_test,
    y_pred,
    y_probas,
    labels,
    model_name="SVC",
    feature_names=None,
)

# 모든 회귀 플롯
wandb.sklearn.plot_regressor(reg, X_train, X_test, y_train, y_test, model_name="Ridge")

# 모든 클러스터링 플롯
wandb.sklearn.plot_clusterer(
    kmeans, X_train, cluster_labels, labels=None, model_name="KMeans"
)

run.finish()
```

<div id="existing-matplotlib-plots">
  #### 기존 Matplotlib 플롯
</div>

Matplotlib에서 생성된 플롯도 W&amp;B 대시보드에 로깅할 수 있습니다. 이를 위해 먼저 `plotly`를 설치해야 합니다.

```bash
pip install plotly
```

마지막으로, 플롯은 다음과 같이 W&amp;B 대시보드에 로깅할 수 있습니다:

```python
import matplotlib.pyplot as plt
import wandb

with wandb.init(project="visualize-sklearn") as run:

  # 여기서 plt.plot(), plt.scatter() 등을 모두 수행하세요.
  # ...

  # plt.show()를 하는 대신에:
  run.log({"plot": plt})
```

<div id="supported-plots">
  ## 지원되는 플롯
</div>

<div id="learning-curve">
  ### 학습 곡선
</div>

<Frame>
  <img src="/images/integrations/scikit_learning_curve.png" alt="Scikit-learn 학습 곡선" />
</Frame>

서로 다른 크기의 데이터셋으로 모델을 트레이닝하고, 트레이닝 및 테스트 세트 각각에 대해 교차 검증 점수와 데이터셋 크기의 관계를 나타내는 플롯을 생성합니다.

`wandb.sklearn.plot_learning_curve(model, X, y)`

* model (clf or reg): 학습이 완료된 회귀기 또는 분류기를 입력으로 받습니다.
* X (arr): 데이터셋 특성.
* y (arr): 데이터셋 레이블.

<div id="roc">
  ### ROC
</div>

<Frame>
  <img src="/images/integrations/scikit_roc.png" alt="Scikit-learn ROC 곡선" />
</Frame>

ROC 곡선은 참 양성률(TPR, y축)을 거짓 양성률(FPR, x축)에 대해 나타냅니다. 이상적인 점수는 TPR = 1, FPR = 0으로, 왼쪽 상단 지점에 해당합니다. 일반적으로 ROC 곡선 아래 면적(AUC-ROC)을 계산하며, AUC-ROC 값이 클수록 성능이 더 우수합니다.

`wandb.sklearn.plot_roc(y_true, y_probas, labels)`

* y&#95;true (arr): 테스트 세트 레이블.
* y&#95;probas (arr): 테스트 세트 예측 확률.
* labels (list): 타겟 변수(y)에 대한 명명된 레이블.

<div id="class-proportions">
  ### 클래스 비율
</div>

<Frame>
  <img src="/images/integrations/scikic_class_props.png" alt="Scikit-learn 분류 속성" />
</Frame>

트레이닝 세트와 테스트 세트에서 타겟 클래스의 분포를 플롯합니다. 불균형 클래스를 감지하고 하나의 클래스가 모델에 과도한 영향을 미치지 않도록 확인하는 데 유용합니다.

`wandb.sklearn.plot_class_proportions(y_train, y_test, ['dog', 'cat', 'owl'])`

* y&#95;train (arr): 트레이닝 세트 레이블.
* y&#95;test (arr): 테스트 세트 레이블.
* labels (list): 타겟 변수(y)에 대한 명명된 레이블.

<div id="precision-recall-curve">
  ### 정밀도-재현율 곡선
</div>

<Frame>
  <img src="/images/integrations/scikit_precision_recall.png" alt="Scikit-learn 정밀도-재현율 곡선" />
</Frame>

서로 다른 임곗값에 대해 정밀도와 재현율 사이의 트레이드오프를 계산합니다. 곡선 아래 면적이 높을수록 높은 재현율과 높은 정밀도를 나타냅니다. 여기서 높은 정밀도는 낮은 거짓 양성률과 관련되며, 높은 재현율은 낮은 거짓 음성률과 관련됩니다.

두 점수가 모두 높으면 분류기가 정확한 결과(높은 정밀도)를 반환하면서 전체 양성 사례의 대부분을 찾아내고 있음을 의미합니다(높은 재현율). PR 곡선은 클래스 불균형이 심할 때 특히 유용합니다.

`wandb.sklearn.plot_precision_recall(y_true, y_probas, labels)`

* y&#95;true (arr): 테스트 세트의 레이블.
* y&#95;probas (arr): 테스트 세트의 예측 확률.
* labels (list): 타겟 변수(y)에 대한 레이블 이름 목록.

<div id="feature-importances">
  ### 특성 중요도
</div>

<Frame>
  <img src="/images/integrations/scikit_feature_importances.png" alt="Scikit-learn 특성 중요도 차트" />
</Frame>

분류 작업에서 각 특성의 중요도를 평가하고 플로팅합니다. 트리처럼 `feature_importances_` 속성을 가진 분류기에서만 작동합니다.

`wandb.sklearn.plot_feature_importances(model, ['width', 'height, 'length'])`

* model (clf): 학습이 완료된 분류기를 입력으로 받습니다.
* feature&#95;names (list): 특성 이름. 특성 인덱스를 해당 이름으로 대체하여 플롯을 더 쉽게 읽을 수 있게 합니다.

<div id="calibration-curve">
  ### 캘리브레이션 곡선
</div>

<Frame>
  <img src="/images/integrations/scikit_calibration_curve.png" alt="Scikit-learn 캘리브레이션 곡선" />
</Frame>

분류기의 예측 확률이 얼마나 잘 보정되어 있는지와 보정되지 않은 분류기를 보정하는 방법을 플롯으로 표시합니다. 기준 로지스틱 회귀 모델, 인수로 전달된 모델, 그리고 해당 모델의 isotonic 보정과 sigmoid 보정을 통해 추정된 예측 확률을 비교합니다.

캘리브레이션 곡선이 대각선에 가까울수록 더 좋습니다. 뒤집힌 시그모이드 모양의 곡선은 과적합된 분류기를, 시그모이드 모양의 곡선은 과소적합된 분류기를 나타냅니다. 모델의 isotonic 및 sigmoid 보정을 트레이닝하여 곡선을 비교하면, 모델이 과적합 또는 과소적합인지 여부를 파악할 수 있으며, 이 경우 어떤 보정 방식(sigmoid 또는 isotonic)이 이를 수정하는 데 도움이 될지 알 수 있습니다.

자세한 내용은 [sklearn의 문서](https://scikit-learn.org/stable/auto_examples/calibration/plot_calibration_curve.html)를 확인하세요.

`wandb.sklearn.plot_calibration_curve(clf, X, y, 'RandomForestClassifier')`

* model (clf): 학습이 완료된 분류기를 입력으로 받습니다.
* X (arr): 트레이닝 세트의 특성.
* y (arr): 트레이닝 세트의 레이블.
* model&#95;name (str): 모델 이름. 기본값은 &#39;Classifier&#39;입니다.

<div id="confusion-matrix">
  ### 혼동 행렬
</div>

<Frame>
  <img src="/images/integrations/scikit_confusion_matrix.png" alt="Scikit-learn 혼동 행렬" />
</Frame>

분류 정확도를 평가하기 위해 혼동 행렬을 계산합니다. 모델 예측의 품질을 평가하고 모델이 틀린 예측의 패턴을 찾는 데 유용합니다. 대각선은 모델이 올바르게 예측한 경우를 나타내며, 예를 들어 실제 레이블이 예측 레이블과 일치하는 경우입니다.

`wandb.sklearn.plot_confusion_matrix(y_true, y_pred, labels)`

* y&#95;true (arr): 테스트 세트 레이블.
* y&#95;pred (arr): 테스트 세트 예측 레이블.
* labels (list): 타겟 변수(y)의 명명된 레이블.

<div id="summary-metrics">
  ### 요약 메트릭
</div>

<Frame>
  <img src="/images/integrations/scikit_summary_metrics.png" alt="Scikit-learn 요약 메트릭" />
</Frame>

* 분류 요약 메트릭을 계산합니다. 예: `mse`, `mae`, `r2` 점수.
* 회귀 요약 메트릭을 계산합니다. 예: `f1`, 정확도, 정밀도, 재현율.

`wandb.sklearn.plot_summary_metrics(model, X_train, y_train, X_test, y_test)`

* model (clf or reg): 학습이 완료된 회귀 모델 또는 분류 모델을 입력으로 받습니다.
* X (arr): 트레이닝 세트 특성.
* y (arr): 트레이닝 세트 레이블.
  * X&#95;test (arr): 테스트 세트 특성.
* y&#95;test (arr): 테스트 세트 레이블.

<div id="elbow-plot">
  ### 엘보 플롯
</div>

<Frame>
  <img src="/images/integrations/scikit_elbow_plot.png" alt="Scikit-learn 엘보 플롯" />
</Frame>

클러스터 수에 따른 설명 분산의 백분율을 측정하고 플롯하며, 트레이닝 시간을 함께 표시합니다. 최적의 클러스터 수를 선택하는 데 유용합니다.

`wandb.sklearn.plot_elbow_curve(model, X_train)`

* model (클러스터러): 학습이 완료된 클러스터러를 입력받습니다.
* X (arr): 트레이닝 세트 특성.

<div id="silhouette-plot">
  ### 실루엣 플롯
</div>

<Frame>
  <img src="/images/integrations/scikit_silhouette_plot.png" alt="Scikit-learn 실루엣 플롯" />
</Frame>

하나의 클러스터 내 각 점이 이웃 클러스터의 점들과 얼마나 가까운지 측정하고 플롯합니다. 클러스터들의 두께는 클러스터 크기를 나타냅니다. 수직선은 모든 점의 평균 실루엣 점수를 나타냅니다.

+1에 가까운 실루엣 계수는 해당 샘플이 이웃 클러스터들로부터 멀리 떨어져 있음을 나타냅니다. 0 값은 샘플이 두 이웃 클러스터 간의 결정 경계에 있거나 매우 가까이 있음을 나타내며, 음수 값은 해당 샘플들이 잘못된 클러스터에 할당되었을 가능성이 있음을 나타냅니다.

일반적으로 모든 실루엣 클러스터 점수가 평균 이상(빨간 선을 넘어)이고 가능한 한 1에 가까워야 합니다. 또한 데이터의 기본 패턴을 반영하는 클러스터 크기를 선호합니다.

`wandb.sklearn.plot_silhouette(model, X_train, ['spam', 'not spam'])`

* model (clusterer): 학습이 완료된 클러스터러를 입력으로 받습니다.
* X (arr): 트레이닝 세트 특성.
  * cluster&#95;labels (list): 클러스터 레이블 이름. 클러스터 인덱스를 해당 이름으로 대체하여 플롯을 더 읽기 쉽게 만듭니다.

<div id="outlier-candidates-plot">
  ### 이상치 후보 플롯
</div>

<Frame>
  <img src="/images/integrations/scikit_outlier_plot.png" alt="Scikit-learn 이상치 플롯" />
</Frame>

회귀 모델에 대한 데이터 포인트의 영향을 Cook의 거리를 통해 측정합니다. 영향력이 크게 왜곡된 인스턴스들은 잠재적인 이상치일 수 있습니다. 이상치 탐지에 유용합니다.

`wandb.sklearn.plot_outlier_candidates(model, X, y)`

* model (regressor): 학습이 완료된 분류기를 입력으로 받습니다.
* X (arr): 트레이닝 세트 특성.
* y (arr): 트레이닝 세트 레이블.

<div id="residuals-plot">
  ### 잔차 플롯
</div>

<Frame>
  <img src="/images/integrations/scikit_residuals_plot.png" alt="Scikit-learn 잔차 플롯" />
</Frame>

예측 타겟 값(y축) 대 실제 타겟 값과 예측 타겟 값의 차이(x축)를 측정하고 플롯하며, 잔차 오차의 분포도 함께 표시합니다.

일반적으로 잘 적합된 모델의 잔차는 무작위로 분포되어야 합니다. 좋은 모델은 데이터셋의 대부분 현상을 설명하며 무작위 오차만 남기기 때문입니다.

`wandb.sklearn.plot_residuals(model, X, y)`

* model (regressor): 학습이 완료된 회귀기를 입력으로 받습니다.
* X (arr): 트레이닝 세트 특성입니다.
* y (arr): 트레이닝 세트 레이블입니다.

  질문이 있으시면 [Slack 커뮤니티](https://wandb.me/slack)에서 기꺼이 답변해 드리겠습니다.

<div id="example">
  ## 예제
</div>

* [Colab에서 실행](https://wandb.me/scikit-colab): 시작에 도움이 되는 단순한 노트북.