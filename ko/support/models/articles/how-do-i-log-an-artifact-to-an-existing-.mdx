---
title: "기존 run에 아티팩트를 로깅하려면 어떻게 하나요?"
keywords: ["Artifacts"]
---

가끔은 아티팩트를 이전에 로그된 run의 출력으로 표시해야 할 수 있습니다. 이 경우 이전 run을 다시 초기화한 다음, 다음과 같이 새 아티팩트를 로그합니다:

```python
with wandb.init(id="existing_run_id", resume="allow") as run:
    artifact = wandb.Artifact("artifact_name", "artifact_type")
    artifact.add_file("my_data/file.txt")
    run.log_artifact(artifact)
```

***

{/* ---- 자동 생성: 탭 배지 ----
  프런트매터의 키워드를 기반으로 scripts/knowledgebase-nav/generate_tags.py에서 관리됩니다.
  이 마커 사이의 내용은 직접 수정하지 마세요.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Artifacts](/ko/support/models/tags/artifacts)</Badge>

{/* ---- 자동 생성 종료: 탭 배지 ---- */}
