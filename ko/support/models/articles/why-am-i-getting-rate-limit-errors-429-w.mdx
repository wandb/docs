---
title: "W&B Inference에서 요청 속도 제한 오류(429)가 발생하는 이유는 무엇인가요?"
keywords: ["Inference"]
---

요청 속도 제한 오류(429)는 동시성 제한을 초과할 때 발생합니다.

**오류:** &quot;요청에 대한 동시성 제한에 도달했습니다&quot;

**해결 방법:**

* 병렬 요청 수를 줄이세요
* 요청 사이에 지연을 추가하세요
* 지수 백오프를 구현하세요
* 참고: 요청 속도 제한은 W&amp;B 프로젝트별로 적용됩니다

<div id="best-practices-to-avoid-rate-limits">
  ## 요청 속도 제한을 피하기 위한 모범 사례
</div>

1. **지수 백오프를 적용한 재시도 로직 구현:**
   ```python
   import time

   def retry_with_backoff(func, max_retries=3):
       for i in range(max_retries):
           try:
               return func()
           except Exception as e:
               if "429" in str(e) and i < max_retries - 1:
                   time.sleep(2 ** i)
               else:
                   raise
   ```

2. **병렬 요청 대신 배치 처리 사용**

3. **W&amp;B Billing 페이지에서 사용량 모니터링**

<div id="default-spending-caps">
  ## 기본 지출 상한
</div>

* **Pro accounts:** 월 $6,000
* **Enterprise accounts:** 연 $700,000

한도를 조정하려면 계정 담당자 또는 지원팀에 문의하세요.

***

<Badge stroke shape="pill" color="orange" size="md">[Inference](/ko/support/models/tags/inference)</Badge>