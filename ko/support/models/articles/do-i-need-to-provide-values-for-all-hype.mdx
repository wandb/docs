---
title: "W&B Sweep에서 모든 하이퍼파라미터 값을 지정해야 하나요? 기본값을 설정할 수 있나요?"
keywords: ["Sweeps"]
---

`(run.config())`를 사용하면 스윕 설정에서 하이퍼파라미터 이름과 값을 가져올 수 있으며, 이는 사전처럼 동작합니다.

스윕에 속하지 않은 run의 경우, `wandb.init()`의 `config` 인자에 사전을 전달해 `wandb.Run.config()` 값을 설정합니다. 스윕에서는 `wandb.init()`에 제공한 모든 설정이 기본값으로 사용되며, 스윕이 이를 재정의할 수 있습니다.

명시적인 동작을 위해 `rwandb.Run.config.setdefaults()`를 사용하세요. 다음 코드 스니펫은 두 방법을 모두 보여줍니다.

<Tabs>
  <Tab title="wandb.init()">
    ```python
    # 하이퍼파라미터 기본값 설정
    config_defaults = {"lr": 0.1, "batch_size": 256}

    # run을 시작하고
    # 스윕이 재정의할 수 있는 기본값 제공
    with wandb.init(config=config_defaults) as run:
        # 여기에 트레이닝 코드를 추가
        ...
    ```
  </Tab>

  <Tab title="config.setdefaults()">
    ```python
    # 하이퍼파라미터 기본값 설정
    config_defaults = {"lr": 0.1, "batch_size": 256}

    # run 시작
    with wandb.init() as run:
        # 스윕에서 설정하지 않은 값 업데이트
        run.config.setdefaults(config_defaults)

        # 여기에 트레이닝 코드를 추가
    ```
  </Tab>
</Tabs>

***

<Badge stroke shape="pill" color="orange" size="md">[Sweeps](/ko/support/models/tags/sweeps)</Badge>