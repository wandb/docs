---
title: "W&B에서 run을 재개할 때 resume 파라미터는 어떻게 사용하나요?"
keywords: ["재개"]
---

W&amp;B에서 `resume` 파라미터를 사용하려면 `entity`, `project`, `id`를 지정하고 `wandb.init()`의 `resume` 인수를 설정하세요. `resume` 인수에는 `"must"` 또는 `"allow"` 값을 사용할 수 있습니다.

```python
  run = wandb.init(entity="your-entity", project="your-project", id="your-run-id", resume="must")
```

***

{/* ---- AUTO-GENERATED: tab badges ----
  프런트 매터의 키워드를 기반으로 scripts/knowledgebase-nav/generate_tags.py 스크립트에서 관리됩니다.
  이 표시 사이의 내용은 직접 수정하지 마세요.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[재개](/ko/support/models/tags/resuming)</Badge>

{/* ---- END AUTO-GENERATED: 탭 배지 ---- */}
