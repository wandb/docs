---
title: "withAttributes"
description: "TypeScript SDK 레퍼런스"
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

```ts
withAttributes<T>(attrs: Record<string, any>, fn: () => T | Promise<T>): T | Promise<T>
```

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/clientApi.ts#L238" />

현재 실행 컨텍스트에 속성을 추가하여 `fn` 내부에서 생성된 모든 calls가
자동으로 이를 상속하도록 합니다. 속성은 트레이스 서버의 call 기록에
기록되며 Weave UI/필터링에 표시되므로 request ID, tenant, 실험 등으로 run에 태그를 지정하는 데
적합합니다.

예시:

```ts twoslash
// @noErrors
await withAttributes({requestId: 'abc'}, async () => {
  await myOp();
});
```

<div id="type-parameters">
  ## 유형 매개변수
</div>

<ParamField path="T" />

<div id="parameters">
  ## 파라미터
</div>

<ParamField path="attrs" type="Record<string, any>" required />

<ParamField path="fn" type="() => T | Promise<T>" required />

<div id="returns">
  ## 반환값
</div>

`T` | `Promise`&lt;`T`&gt;