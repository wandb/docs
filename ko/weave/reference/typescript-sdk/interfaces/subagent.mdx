---
title: "SubAgent"
description: "TypeScript SDK 레퍼런스"
---

import { GitHubLink } from '/snippets/ko/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/genai/subagent.ts#L84" />

## 확장 {#extends}

* `SpanBase`

## 접근자 {#accessors}

### 모델 {#model}

#### 조회 시그니처 {#get-signature}

> **get** **model**(): `string`

##### 반환값 {#returns}

`string`

***

### name {#name}

#### 조회 시그니처 {#get-signature-2}

> **get** **name**(): `string`

##### 반환값 {#returns-2}

`string`

## 메서드 {#methods}

### ~~addEvent()~~ {#addevent}

<Warning>
  **사용 중단.** 대신 [setAttributes](#setattributes)를 통해 이 데이터를 기록하세요.
  OpenTelemetry는 Span Event API(`Span.addEvent`)를 단계적으로 폐지하고 있습니다. 이
  방법은 계속 작동하며 기존 span 이벤트 데이터도 유효하게 유지됩니다.
  [https://opentelemetry.io/blog/2026/deprecating-span-events/](https://opentelemetry.io/blog/2026/deprecating-span-events/)를 참조하세요.
</Warning>

> **addEvent**(`name`, `attributes?`, `startTime?`): `this`

span에 이름이 지정된 이벤트를 추가합니다. 컨텍스트 압축, 도구 루프 감지 또는
가드레일 트리거와 같은 span 외 시점을 표시하는 데 유용합니다. `end()` 이후에는 경고를 표시하고
아무 작업도 수행하지 않습니다. OTel `Span.addEvent`와 동일하게 동작합니다.

#### 매개변수 {#parameters}

<ParamField path="name" type="string" required />

<ParamField path="attributes" type="Attributes" />

<ParamField path="startTime" type="TimeInput" />

#### 반환값 {#returns-3}

`this`

#### 예시 {#example}

```ts twoslash
// @noErrors
span.addEvent('context_compacted', {removedMessages: 12});
```

#### 상속됨 {#inherited-from}

`SpanBase.addEvent`

***

### end() {#end}

> **end**(`opts?`): `void`

현재 필드 값을 조회하여(`start` 이후 `record()`를 통해 변경된 사항 반영) span을 종료합니다. 멱등성을 보장합니다. 실패로 표시하려면 `error`를 전달하고, 종료 시간을 과거로 설정하려면 `endTime`을 전달하세요.

#### 매개변수 {#parameters-2}

<ParamField path="opts" type="object">
  <Expandable title="속성" defaultOpen>
    <ParamField path="endTime" type="TimeInput" />

    <ParamField path="error" type="Error" />
  </Expandable>
</ParamField>

#### 반환값 {#returns-4}

`void`

***

### record() {#record}

> **record**(`opts`): `this`

변경 가능한 필드의 일부를 일괄 설정합니다. 병합하지 않고 기존 값을 대체합니다.
입력 및 출력 메시지는 span이 종료될 때 직렬화됩니다.

#### 매개변수 {#parameters-3}

<ParamField path="opts" type="object" required>
  <Expandable title="속성" defaultOpen>
    <ParamField path="agentDescription" type="string" />

    <ParamField path="agentId" type="string" />

    <ParamField path="agentVersion" type="string" />

    <ParamField path="messages" type="Message[]">
      [`Message`](./message)를 참조하세요.
    </ParamField>

    <ParamField path="model" type="string" />

    <ParamField path="name" type="string" />

    <ParamField path="outputMessages" type="Message[]">
      [`Message`](./message)를 참조하세요.
    </ParamField>

    <ParamField path="systemInstructions" type="string[]" />
  </Expandable>
</ParamField>

#### 반환값 {#returns-5}

`this`

***

### setAttributes() {#setattributes}

> **setAttributes**(`attributes`): `this`

span에 여러 속성을 한 번에 설정합니다. `end()` 호출 후에는 경고를 표시하며 아무 작업도 수행하지 않습니다.
OTel `Span.setAttributes`(및 Python SDK의 `set_attributes`)와 동일하게 작동합니다.

#### 매개변수 {#parameters-4}

<ParamField path="attributes" type="Attributes" required />

#### 반환값 {#returns-6}

`this`

#### 예시 {#example-2}

```ts twoslash
// @noErrors
span.setAttributes({'weave.tag': 'prod', 'gen_ai.response.id': id});
```

#### 상속됨 {#inherited-from-2}

`SpanBase.setAttributes`

***

### startLLM() {#startllm}

> **startLLM**(`opts`): [`LLM`](./llm)

이 SubAgent 하위에 중첩된 자식 LLM span을 시작합니다.

#### 매개변수 {#parameters-5}

<ParamField path="opts" type="LLMInit" required>
  [`LLMInit`](./llminit)을 참조하세요.
</ParamField>

#### 반환값 {#returns-7}

[`LLM`](./llm)

***

### startSubagent() {#startsubagent}

> **startSubagent**(`opts`): `SubAgent`

이 SubAgent 하위에 중첩된 SubAgent span을 시작합니다.

#### 매개변수 {#parameters-6}

<ParamField path="opts" type="SubAgentInit" required>
  [`SubAgentInit`](./subagentinit)를 참조하세요.
</ParamField>

#### 반환값 {#returns-8}

`SubAgent`

***

### startTool() {#starttool}

> **startTool**(`opts`): [`Tool`](./tool)

이 SubAgent 하위에 중첩된 자식 도구 span을 시작합니다.

#### 매개변수 {#parameters-7}

<ParamField path="opts" type="ToolInit" required>
  [`ToolInit`](./toolinit)을 참조하세요.
</ParamField>

#### 반환값 {#returns-9}

[`Tool`](./tool)