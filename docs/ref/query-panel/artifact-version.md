# artifactVersion

## Chainable Ops
<h3 id="artifactVersion-aliases"><code>artifactVersion-aliases</code></h3>

Returns the aliases for a [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The aliases for a [artifactVersion](artifact-version)

<h3 id="artifactVersion-createdAt"><code>artifactVersion-createdAt</code></h3>

Returns the datetime at which the [artifactVersion](artifact-version) was created

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The datetime at which the [artifactVersion](artifact-version) was created

<h3 id="artifactVersion-file"><code>artifactVersion-file</code></h3>

Returns the _file_ of the [artifactVersion](artifact-version) for the given path

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |
| `path` | The path of the _file_ |

#### Return Value
The _file_ of the [artifactVersion](artifact-version) for the given path

<h3 id="artifactVersion-files"><code>artifactVersion-files</code></h3>

Returns the _list_ of _files_ of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The _list_ of _files_ of the [artifactVersion](artifact-version)

<h3 id="artifactVersion-link"><code>artifactVersion-link</code></h3>

Returns the url for a [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The url for a [artifactVersion](artifact-version)

<h3 id="artifactVersion-metadata"><code>artifactVersion-metadata</code></h3>

Returns the [artifactVersion](artifact-version) metadata dictionary

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The [artifactVersion](artifact-version) metadata dictionary

<h3 id="artifactVersion-name"><code>artifactVersion-name</code></h3>

Returns the name of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The name of the [artifactVersion](artifact-version)

<h3 id="artifactVersion-size"><code>artifactVersion-size</code></h3>

Returns the size of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The size of the [artifactVersion](artifact-version)

<h3 id="artifactVersion-usedBy"><code>artifactVersion-usedBy</code></h3>

Returns the [runs](run) that use the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The [runs](run) that use the [artifactVersion](artifact-version)

<h3 id="artifactVersion-versionId"><code>artifactVersion-versionId</code></h3>

Returns the versionId of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The versionId of the [artifactVersion](artifact-version)


## List Ops
<h3 id="artifactVersion-aliases"><code>artifactVersion-aliases</code></h3>

Returns the aliases for a [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The aliases for a [artifactVersion](artifact-version)

<h3 id="artifactVersion-createdAt"><code>artifactVersion-createdAt</code></h3>

Returns the datetime at which the [artifactVersion](artifact-version) was created

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The datetime at which the [artifactVersion](artifact-version) was created

<h3 id="artifactVersion-file"><code>artifactVersion-file</code></h3>

Returns the _file_ of the [artifactVersion](artifact-version) for the given path

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |
| `path` | The path of the _file_ |

#### Return Value
The _file_ of the [artifactVersion](artifact-version) for the given path

<h3 id="artifactVersion-files"><code>artifactVersion-files</code></h3>

Returns the _list_ of _files_ of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The _list_ of _files_ of the [artifactVersion](artifact-version)

<h3 id="artifactVersion-link"><code>artifactVersion-link</code></h3>

Returns the url for a [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The url for a [artifactVersion](artifact-version)

<h3 id="artifactVersion-metadata"><code>artifactVersion-metadata</code></h3>

Returns the [artifactVersion](artifact-version) metadata dictionary

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The [artifactVersion](artifact-version) metadata dictionary

<h3 id="artifactVersion-name"><code>artifactVersion-name</code></h3>

Returns the name of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The name of the [artifactVersion](artifact-version)

<h3 id="artifactVersion-size"><code>artifactVersion-size</code></h3>

Returns the size of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The size of the [artifactVersion](artifact-version)

<h3 id="artifactVersion-usedBy"><code>artifactVersion-usedBy</code></h3>

Returns the [runs](run) that use the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The [runs](run) that use the [artifactVersion](artifact-version)

<h3 id="artifactVersion-versionId"><code>artifactVersion-versionId</code></h3>

Returns the versionId of the [artifactVersion](artifact-version)

| Argument |  |
| :--- | :--- |
| `artifactVersion` | A [artifactVersion](artifact-version) |

#### Return Value
The versionId of the [artifactVersion](artifact-version)

