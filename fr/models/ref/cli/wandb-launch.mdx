---
title: "wandb launch"
---

{/*
  Pour ajouter un contenu d’introduction à cette commande :
  1. Créez le fichier d’extrait : /snippets/_includes/cli/wandb-launch.mdx
  2. Ajoutez votre contenu d’introduction à ce fichier
  3. Supprimez l’intégralité de ce bloc de commentaire et ne conservez que les deux lignes ci-dessous :

  import WandbLaunch from "/snippets/_includes/cli/wandb-launch.mdx";

  <WandbLaunch/>

  L’extrait sera détecté automatiquement lors de la prochaine régénération.
  */}

Lancez ou placez un job W&amp;B dans la file d&#39;attente. Voir https://wandb.me/launch


<div id="usage">
  ## Utilisation
</div>

```bash
wandb launch [OPTIONS]
```


<div id="options">
  ## Options
</div>

| Option                  | Description                                                                                                                                                                                                                                                                                                                                                                                                                               |
| :---------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `--uri`, `-u`           | Chemin local ou URI d’un dépôt Git à lancer. Si cette option est fournie, cette commande crée un job à partir de l’URI spécifiée.                                                                                                                                                                                                                                                                                                         |
| `--job`, `-j`           | Nom du job à lancer. Si cette option est fournie, Launch ne nécessite pas d’URI.                                                                                                                                                                                                                                                                                                                                                          |
| `--entry-point`, `-E`   | Point d’entrée dans le projet. [par défaut : main]. Si le point d’entrée est introuvable, le système tente d’exécuter comme script le fichier du projet portant le nom spécifié, en utilisant `python` pour les fichiers .py et le shell par défaut (spécifié par la variable d’environnement $SHELL) pour les fichiers .sh. Si cette option est fournie, elle remplace la valeur `entrypoint` transmise via un fichier de configuration. |
| `--build-context`       | Chemin vers le contexte de build dans le code source. Par défaut, utilise la racine du code source. Compatible uniquement avec -u.                                                                                                                                                                                                                                                                                                        |
| `--name`                | Nom du run sous lequel lancer le run. S’il n’est pas spécifié, un nom de run aléatoire sera utilisé. Si cette option est fournie, elle remplace le nom transmis via un fichier de configuration.                                                                                                                                                                                                                                          |
| `--entity`, `-e`        | Nom de l’entité cible à laquelle le nouveau run sera envoyé. Par défaut, utilise l’entité définie dans le dossier local wandb/settings. Si cette option est fournie, elle remplace la valeur d’entité transmise via un fichier de configuration.                                                                                                                                                                                          |
| `--project`, `-p`       | Nom du projet cible auquel le nouveau run sera envoyé. Par défaut, utilise le nom du projet donné par l’URI source ou, pour les runs GitHub, le nom du dépôt Git. Si cette option est fournie, elle remplace la valeur du projet transmise via un fichier de configuration.                                                                                                                                                               |
| `--resource`, `-r`      | Ressource d’exécution à utiliser pour le run. Valeurs prises en charge : &#39;local-process&#39;, &#39;local-container&#39;, &#39;kubernetes&#39;, &#39;sagemaker&#39;, &#39;gcp-vertex&#39;. Ce paramètre est désormais requis lors d’un envoi vers une file d’attente sans configuration de ressource. Si cette option est fournie, elle remplace la valeur de ressource transmise via un fichier de configuration.                     |
| `--docker-image`, `-d`  | Image Docker spécifique à utiliser. Au format nom:tag. Si cette option est fournie, elle remplace la valeur de l’image Docker transmise via un fichier de configuration.                                                                                                                                                                                                                                                                  |
| `--base-image`, `-B`    | Image Docker dans laquelle exécuter le code du job. Incompatible avec --docker-image.                                                                                                                                                                                                                                                                                                                                                     |
| `--config`, `-c`        | Chemin vers un fichier JSON (doit se terminer par &#39;.json&#39;) ou chaîne JSON qui sera transmis comme configuration de Launch. Détermine comment le run lancé sera configuré.                                                                                                                                                                                                                                                      |
| `--set-var`, `-v`       | Définit les valeurs des variables de modèle pour les files d’attente avec liste d’autorisation activée, sous forme de paires clé-valeur, par ex. `--set-var key1=value1 --set-var key2=value2`                                                                                                                                                                                                                                            |
| `--queue`, `-q`         | Nom de la file d’attente de runs vers laquelle envoyer. Si aucune n’est spécifiée, lance directement un seul run. Si fournie sans argument (`--queue`), utilise par défaut la file d’attente &#39;default&#39;. Sinon, si un nom est fourni, la file d’attente de runs spécifiée doit exister dans le projet et l’entité indiqués.                                                                                                        |
| `--async`               | Indicateur permettant d’exécuter le job de manière asynchrone. La valeur par défaut est false ; autrement dit, sauf si --async est défini, wandb launch attend la fin du job. Cette option est incompatible avec --queue ; les options asynchrones lors d’une exécution avec un agent doivent être définies sur wandb launch-agent. (par défaut : False)                                                                                  |
| `--resource-args`, `-R` | Chemin vers un fichier JSON (doit se terminer par &#39;.json&#39;) ou chaîne JSON qui sera transmis comme arguments de ressource à la ressource de calcul. Le contenu exact à fournir varie selon le backend d’exécution. Voir la documentation pour la structure de ce fichier.                                                                                                                                                          |
| `--dockerfile`, `-D`    | Chemin vers le Dockerfile utilisé pour build le job, relatif à la racine du job                                                                                                                                                                                                                                                                                                                                                           |
| `--priority`, `-P`      | Lorsque --queue est fourni, définit la priorité du job. Les jobs Launch avec la priorité la plus élevée sont traités en premier. L’ordre, de la priorité la plus élevée à la plus faible, est : critical, high, medium, low                                                                                                                                                                                                               |