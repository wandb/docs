---
title: controller()
kind: fonction
namespace: wandb.sdk.wandb_sweep
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/wandb/blob/main/wandb/sdk/wandb_sweep.py#L100" />

## <kbd>fonction</kbd> wandb.controller

```python
sweep_id_or_config: 'str | dict | None' = None,
entity: 'str | None' = None,
project: 'str | None' = None
```

Constructeur public du contrôleur de balayage.

<div id="args">
  ## Arguments
</div>

* `sweep_id_or_config`:
* `entity`:
* `project`: 

<div id="examples">
  ## Exemples
</div>

```python
import wandb

tuner = wandb.controller(...)
print(tuner.sweep_config)
print(tuner.sweep_id)
tuner.configure_search(...)
tuner.configure_stopping(...)
```