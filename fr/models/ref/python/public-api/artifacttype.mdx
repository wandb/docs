---
title: ArtifactType
kind: classe
namespace: wandb.apis.public.artifacts
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/artifacts.py#L204" />

## <Badge color="yellow" size="lg" shape="rounded">classe</Badge> wandb.apis.public.ArtifactType {#class-wandbapispublicartifacttype}

Un objet artifact qui correspond à une requête basée sur le type spécifié.

## Propriétés {#properties}

<ResponseField name="id" type="str">
  L’ID unique du type d’artifact.
</ResponseField>

<ResponseField name="name" type="str">
  Le nom du type d’artifact.
</ResponseField>

## Méthodes {#methods}

### <Badge color="blue" size="lg" shape="rounded">méthode</Badge> ArtifactType.collection() {#method-artifacttypecollection}

```python
self,
name: 'str'
```

Obtenir une collection d&#39;artifacts spécifique à partir de son nom.

##### Arguments {#arguments}

<ResponseField name="name" type="str">
  Le nom de la collection d&#39;artifacts à récupérer.
</ResponseField>

### <Badge color="blue" size="lg" shape="rounded">méthode</Badge> ArtifactType.collections() {#method-artifacttypecollections}

```python
self,
filters: 'Mapping[str, Any] | None' = None,
order: 'str | None' = None,
per_page: 'int' = 50,
start: 'str | None' = None
```

Obtenir toutes les collections d&#39;artifacts associées à ce type d&#39;artifact.

##### Arguments {#arguments-2}

<ResponseField name="filters" type="Mapping[str, Any] | None">
  Mappage facultatif des filtres à appliquer à la requête.
</ResponseField>

<ResponseField name="order" type="str | None">
  Chaîne facultative permettant de spécifier l’ordre des résultats. S’il est préfixé par &#39;+&#39;, le tri est croissant (par défaut). S’il est préfixé par &#39;-&#39;, le tri est décroissant. Par défaut, l’ordre utilisé est l’ID de la collection, par ordre décroissant.
</ResponseField>

<ResponseField name="per_page" type="int">
  Le nombre de collections d&#39;artifacts à récupérer par page. La valeur par défaut est 50.
</ResponseField>

<ResponseField name="start" type="str | None">
  Curseur de pagination pour reprendre une requête antérieure, capturé à partir de l’attribut `.cursor` d’un paginateur précédent.
</ResponseField>