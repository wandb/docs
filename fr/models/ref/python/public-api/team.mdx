---
title: Team
kind: classe
namespace: wandb.apis.public.teams
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/apis/public/teams.py#L61" />

<div id="class-wandbapispublicteam">
  ## <Badge color="yellow" size="lg" shape="rounded">Classe</Badge> wandb.apis.public.Team
</div>

Classe représentant une équipe W&amp;B.

Cette classe fournit des méthodes pour gérer les équipes W&amp;B, notamment pour créer des équipes,
inviter des membres et gérer des comptes de service. Elle hérite de Attrs
pour gérer les attributs de l’équipe.

```python
(service_api: 'ServiceApi',
name: 'str',
attrs: 'Mapping[str, Any] | None' = None)
```

<div id="args">
  ## Arguments
</div>

<ResponseField name="service_api" type="ServiceApi">
  Instance de l’API de service à utiliser pour interroger W&amp;B.
</ResponseField>

<ResponseField name="name" type="str">
  Nom de l’équipe
</ResponseField>

<ResponseField name="attrs" type="Mapping[str, Any] | None">
  Dictionnaire facultatif des attributs de l’équipe
</ResponseField>

<div id="examples">
  ## Exemples
</div>

Recherchez une équipe et invitez un membre.

```python
import wandb

api = wandb.Api()
team = api.team("my-team")
team.invite("user@example.com")
```

Créez une équipe et ajoutez un compte de service.

```python
import wandb

api = wandb.Api()
team = api.create_team("my-team")
team.create_service_account("CI service account")
```

<div id="methods">
  ## Méthodes
</div>

<div id="method-teamcreate">
  ### <Badge color="blue" size="lg" shape="rounded">méthode</Badge> Team.create()
</div>

```python
api: 'Api',
team: 'str',
admin_username: 'str | None' = None
```

Créer une nouvelle équipe.

<div id="arguments">
  ##### Arguments
</div>

<ResponseField name="api" type="Api">
  (`Api`) L&#39;instance d&#39;API à utiliser
</ResponseField>

<ResponseField name="team" type="str">
  (str) Le nom de la équipe
</ResponseField>

<ResponseField name="admin_username" type="str | None">
  (str) nom d&#39;utilisateur facultatif de l&#39;administrateur de la équipe ; par défaut, l&#39;utilisateur actuel.
</ResponseField>

<div id="method-teamcreate_service_account">
  ### <Badge color="blue" size="lg" shape="rounded">méthode</Badge> Team.create_service_account()
</div>

```python
self,
description: 'str'
```

Créez un compte de service pour l’équipe.

<div id="arguments-2">
  ##### Arguments
</div>

<ResponseField name="description" type="str">
  (str) Une description de ce compte de service
</ResponseField>

<div id="method-teaminvite">
  ### <Badge color="blue" size="lg" shape="rounded">méthode</Badge> Team.invite()
</div>

```python
self,
username_or_email: 'str',
admin: 'bool' = False
```

Invitez un utilisateur dans une équipe.

<div id="arguments-3">
  ##### Arguments
</div>

<ResponseField name="username_or_email" type="str">
  (str) Le nom d’utilisateur ou l’adresse e-mail de l’utilisateur que vous souhaitez inviter.
</ResponseField>

<ResponseField name="admin" type="bool">
  (bool) Indique si cet utilisateur doit devenir administrateur de la équipe. La valeur par défaut est `False`.
</ResponseField>