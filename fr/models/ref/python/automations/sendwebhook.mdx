---
title: SendWebhook
kind: classe
namespace: wandb.automations.actions
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

<GitHubLink compact url="https://github.com/wandb/wandb/blob/main/wandb/automations/actions.py#L182" />

<div id="class-wandbautomationssendwebhook">
  ## <Badge color="yellow" size="lg" shape="rounded">Classe</Badge> wandb.automations.SendWebhook
</div>

Définit une action d’automatisation qui envoie une requête webhook.

```python
*,
integration_id: Annotated[str, Strict(strict=True)],
requestPayload: Optional[Annotated[dict[str, Any], Json, BeforeValidator(func=<function ensure_json>, json_schema_input_type=PydanticUndefined), PlainSerializer(func=<function to_json>, return_type=PydanticUndefined, when_used='always')]] = None,
action_type: Literal[GENERIC_WEBHOOK] = GENERIC_WEBHOOK
```

<div id="args">
  ## Arguments
</div>

<ResponseField name="integration_id" type="Annotated">
  L’ID de l’intégration webhook qui sera utilisée pour envoyer la requête.
</ResponseField>

<ResponseField name="requestPayload" type="Optional">
  La charge utile, éventuellement avec des variables de template, à envoyer dans la requête webhook.
</ResponseField>

<ResponseField name="action_type" type="Literal">
  Aucune description fournie.
</ResponseField>

<div id="methods">
  ## Méthodes
</div>

<div id="method-sendwebhookfrom_integration">
  ### <Badge color="blue" size="lg" shape="rounded">méthode</Badge> SendWebhook.from_integration()
</div>

```python
integration: 'WebhookIntegration', *,
payload: 'JsonEncoded[dict[str, Any]] | None' = None
```

Définissez une action webhook qui envoie à l’intégration (webhook) spécifiée.

<div id="arguments">
  ##### Arguments
</div>

<ResponseField name="integration" type="WebhookIntegration">
  Aucune description fournie.
</ResponseField>

<ResponseField name="payload" type="JsonEncoded[dict[str, Any]] | None">
  Aucune description fournie.
</ResponseField>