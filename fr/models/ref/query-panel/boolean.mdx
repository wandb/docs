---
title: booléen
---

## Ops enchaînables {#chainable-ops}

### <a id="and" />`and` {#and}

Renvoie le résultat du `and` logique entre les deux valeurs

| Argument | Description             |
| :------- | :---------------------- |
| `lhs`    | Première valeur binaire |
| `rhs`    | Deuxième valeur binaire |

#### Valeur de retour {#return-value}

Le `and` logique entre les deux valeurs

### <a id="or" />`or` {#or}

Renvoie le `or` logique entre les deux valeurs

| Argument | Description             |
| :------- | :---------------------- |
| `lhs`    | Première valeur binaire |
| `rhs`    | Deuxième valeur binaire |

#### Valeur de retour {#return-value-2}

Le `or` logique entre les deux valeurs

### <a id="boolean-not" />`boolean-not` {#boolean-not}

Renvoie l’inverse logique de la valeur

| Argument | Description         |
| :------- | :------------------ |
| `bool`   | La valeur booléenne |

#### Valeur de retour {#return-value-3}

L’inverse logique de la valeur

### <a id="boolean-not-2" />`boolean-not` {#boolean-not-2}

Renvoie l’inverse logique de la valeur

| Argument | Description         |
| :------- | :------------------ |
| `bool`   | La valeur booléenne |

#### Valeur de retour {#return-value-4}

L’inverse logique de la valeur

## Ops sur les listes {#list-ops}

### <a id="and-list" />`and` {#and-2}

Renvoie le résultat du `and` logique entre les deux valeurs

| Argument | Description             |
| :------- | :---------------------- |
| `lhs`    | Première valeur binaire |
| `rhs`    | Seconde valeur binaire  |

#### Valeur de retour {#return-value-5}

Le `and` logique entre les deux valeurs

### <a id="or-list" />`or` {#or-2}

Renvoie le résultat du `or` logique entre les deux valeurs

| Argument | Description             |
| :------- | :---------------------- |
| `lhs`    | Première valeur binaire |
| `rhs`    | Deuxième valeur binaire |

#### Valeur de retour {#return-value-6}

Le `or` logique entre les deux valeurs

### <a id="boolean-not-list" />`boolean-not` {#boolean-not-3}

Renvoie l’inverse logique de la valeur

| Argument | Description         |
| :------- | :------------------ |
| `bool`   | La valeur booléenne |

#### Valeur de retour {#return-value-7}

L’inverse logique de la valeur

### <a id="boolean-not-2-list" />`boolean-not` {#boolean-not-4}

Renvoie l’inverse logique de la valeur

| Argument | Description         |
| :------- | :------------------ |
| `bool`   | La valeur booléenne |

#### Valeur de retour {#return-value-8}

L’inverse logique de la valeur