---
title: joined-table
---

## Ops enchaînables {#chainable-ops}

### <a id="asset-file" />`asset-file` {#asset-file}

Renvoie le *fichier* de l’asset

| Argument | Description |
| :------- | :---------- |
| `asset`  | L’asset     |

#### Valeur de retour {#return-value}

Le *fichier* de l’asset

### <a id="joinedtable-file" />`joinedtable-file` {#joinedtable-file}

Renvoie le *fichier* d’un *joined-table*

| Argument      | Description       |
| :------------ | :---------------- |
| `joinedTable` | Le *joined-table* |

#### Valeur de retour {#return-value-2}

La *fichier* d’une *joined-table*

### <a id="joinedtable-rows" />`joinedtable-rows` {#joinedtable-rows}

Renvoie les lignes d’une *joined-table*

| Argument      | Description                                                                                                         |
| :------------ | :------------------------------------------------------------------------------------------------------------------ |
| `joinedTable` | La *joined-table*                                                                                                   |
| `leftOuter`   | Indique s’il faut inclure les lignes du tableau de gauche qui n’ont pas de correspondance dans le tableau de droite |
| `rightOuter`  | Indique s’il faut inclure les lignes du tableau de droite qui n’ont pas de correspondance dans le tableau de gauche |

#### Valeur de retour {#return-value-3}

Les lignes du *joined-table*

## Lister les Ops {#list-ops}

### <a id="asset-file-list" />`asset-file` {#asset-file-2}

Renvoie le *fichier* associé à la ressource

| Argument | Description  |
| :------- | :----------- |
| `asset`  | La ressource |

#### Valeur de retour {#return-value-4}

Le *fichier* de l’asset