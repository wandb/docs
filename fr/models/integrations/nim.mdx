---
title: Job de déploiement pour le microservice NVIDIA NeMo Inference
description: "Déployez un artefact de modèle W&B sur un microservice NVIDIA NeMo Inference à l'aide de W&B Launch pour l'inférence de modèles à grande échelle."
---

Déployez un artefact de modèle depuis W&amp;B sur un microservice NVIDIA NeMo Inference. Pour cela, utilisez W&amp;B Launch. W&amp;B Launch convertit les artefacts de modèle au format NVIDIA NeMo Model et les déploie sur un serveur NIM/Triton en cours d’exécution.

W&amp;B Launch accepte actuellement les types de modèles compatibles suivants :

1. [Llama2](https://llama.meta.com/llama2/)
2. [StarCoder](https://github.com/bigcode-project/starcoder)
3. NV-GPT (bientôt disponible)

<Note>
  Le temps de déploiement varie selon le modèle et le type de machine. La configuration de base de Llama2-7b prend environ 1 minute sur l’instance Google Cloud `a2-ultragpu-1g`.
</Note>

<div id="quickstart">
  ## Démarrage rapide
</div>

1. [Créez une file d&#39;attente Launch](/fr/platform/launch/add-job-to-queue/) si vous n&#39;en avez pas déjà une. Voir ci-dessous un exemple de configuration de file d&#39;attente.

   ```yaml
   net: host
   gpus: all # peut être un ensemble spécifique de GPU ou `all` pour tout utiliser
   runtime: nvidia # nécessite également le runtime de conteneur nvidia
   volume:
     - model-store:/model-store/
   ```

   <Frame>
     <img src="/images/integrations/nim1.png" alt="image" />
   </Frame>

2. Créez ce job dans votre projet :

   ```bash
   wandb job create -n "deploy-to-nvidia-nemo-inference-microservice" \
      -e $ENTITY \
      -p $PROJECT \
      -E jobs/deploy_to_nvidia_nemo_inference_microservice/job.py \
      -g andrew/nim-updates \
      git https://github.com/wandb/launch-jobs
   ```

3. Lancez un agent sur votre machine GPU :
   ```bash
   wandb launch-agent -e $ENTITY -p $PROJECT -q $QUEUE
   ```

4. Soumettez le job de déploiement Launch avec les configurations souhaitées depuis l&#39;[interface Launch](https://wandb.ai/launch)
   1. Vous pouvez également le soumettre via la CLI :
      ```bash
      wandb launch -d gcr.io/playground-111/deploy-to-nemo:latest \
        -e $ENTITY \
        -p $PROJECT \
        -q $QUEUE \
        -c $CONFIG_JSON_FNAME
      ```
      <Frame>
        <img src="/images/integrations/nim2.png" alt="image" />
      </Frame>

5. Vous pouvez suivre le processus de déploiement dans l&#39;interface Launch.
   <Frame>
     <img src="/images/integrations/nim3.png" alt="image" />
   </Frame>

6. Une fois l&#39;opération terminée, vous pouvez immédiatement envoyer une requête curl au point de terminaison pour tester le modèle. Le nom du modèle est toujours `ensemble`.
   ```bash
    #!/bin/bash
    curl -X POST "http://0.0.0.0:9999/v1/completions" \
        -H "accept: application/json" \
        -H "Content-Type: application/json" \
        -d '{
            "model": "ensemble",
            "prompt": "Tell me a joke",
            "max_tokens": 256,
            "temperature": 0.5,
            "n": 1,
            "stream": false,
            "stop": "string",
            "frequency_penalty": 0.0
            }'
   ```