---
title: "Stocker et suivre les versions des prompts"
description: "Récupérer et gérer les versions de vos prompts pour les applications LLM"
---

Après avoir [créé et publié des prompts](/fr/weave/guides/core-types/prompts.mdx), vous pouvez faire référence à des versions spécifiques, les récupérer et les utiliser dans le code comme en production.

Lorsque vous publiez un prompt avec `weave.publish`, W&amp;B Weave crée une version immuable. Chaque version peut être référencée par son nom et son numéro de version, récupérée dans le code avec `weave.ref(...).get()`, et consultée dans l&#39;interface Weave.

<div id="prompt-versions">
  # Versions des prompts
</div>

Dans Weave, les prompts sont stockés sous forme d&#39;**objets versionnés**. Cela vous permet d&#39;itérer en toute sécurité sur vos prompts, tout en garantissant que les évaluations, les expériences et les systèmes de production puissent référencer la version exacte du prompt avec laquelle ils ont été exécutés.

Les versions des prompts sont accessibles à l&#39;aide de références d&#39;objets Weave. Cette page explique comment les versions des prompts sont créées, comment récupérer une version spécifique dans le code, et comment inspecter et comparer les versions dans l&#39;interface Weave.

<div id="how-prompt-versions-are-created">
  ## Comment les versions des prompts sont créées
</div>

Weave suit automatiquement chaque version de vos prompts et crée un historique complet de leur évolution. Ce système de gestion des versions est essentiel pour les flux de travail d&#39;ingénierie de prompts, car il vous permet d&#39;expérimenter en toute sécurité, d&#39;identifier quelles modifications ont amélioré ou dégradé les performances et de revenir facilement à des versions antérieures si nécessaire. Chaque fois que vous publiez un prompt portant le même nom mais avec un contenu différent, Weave crée une nouvelle version tout en conservant toutes les versions précédentes.

Chaque version est immuable. Une fois créée, le contenu de cette version ne peut plus être modifié. Si vous mettez à jour un prompt et le publiez à nouveau, Weave crée une nouvelle version tout en conservant les versions précédentes.

La gestion des versions vous permet de :

* Reproduire des expériences passées.
* Itérer en toute sécurité sur les modifications apportées aux prompts.
* Déployer les mises à jour de prompts de manière contrôlée.

<div id="construct-a-fully-qualified-ref-uri">
  ## Créer une URI de référence pleinement qualifiée
</div>

Les objets Weave peuvent être identifiés de manière unique à l’aide d’une URI de référence pleinement qualifiée.

Une URI de réf. d&#39;objet Weave pleinement qualifiée se présente comme suit :

```text
weave:///<your-team-name>/<your-project-name>/object/<object_name>:<object_version>
```

* *your-team-name*: entité W&amp;B (nom d’utilisateur ou nom de l’équipe)
* *your-project-name*: projet W&amp;B
* *object&#95;name*: nom de l’objet
* *object&#95;version*: soit un hash de version, soit une chaîne comme `v0` ou `v1`, soit un alias comme `:latest`. Tous les objets ont l’alias `:latest`.

Par exemple, un URI complet de prompt Weave ressemble à ceci :

```text
weave:///your-team-name/your-project-name/object/support_prompt:v3
```

<div id="retrieve-a-prompt-version-in-code">
  ## Récupérer une version de prompt dans le code
</div>

Pour récupérer un prompt, créez une référence à son nom et à sa version, puis appelez `.get()` pour le charger. Une référence pointe vers un objet stocké ; `.get()` récupère cet objet pour que vous puissiez l’utiliser dans votre application.

Vous pouvez construire des références de plusieurs façons :

* `weave.ref(<name>)` : récupère la version `:latest` d’un prompt. Nécessite d’appeler `weave.init(...)`.
* `weave.ref(<name>:<version>)` :  récupère la version spécifiée d’un prompt. Nécessite d’appeler `weave.init(...)`.
* `weave.ref(<fully_qualified_ref_uri>)` : récupère le prompt situé à l’[URI de référence pleinement qualifié](#construct-a-fully-qualified-ref-uri) indiquée. Ne nécessite pas d’appeler `weave.init(...)`.

L’exemple suivant charge la version `support_prompt:v3` pour que vous puissiez l’utiliser dans votre application :

```python lines
import weave

weave.init("your-team-name/your-project-name")

prompt = weave.ref("support_prompt:v3").get()
```

<div id="use-prompt-versions-in-production">
  ## Utiliser des versions de prompt en production
</div>

Lorsque vous déployez des prompts dans des systèmes de production, fixez une version de prompt précise au lieu de faire référence à la dernière version. Dans votre application de production, chargez cette version avec `weave.ref(<name>:<version>).get()`.

Le fait de fixer une version garantit que le comportement en production reste stable, même si de nouvelles versions de prompt sont publiées par la suite.

Un flux de travail courant ressemble à ceci :

1. Développez et testez une nouvelle version de prompt.
2. Évaluez le nouveau prompt à l&#39;aide de jeux de données ou de suites d&#39;évaluation.
3. Mettez à jour le système de production pour qu&#39;il fasse référence à la nouvelle version.

Cette approche permet aux équipes d&#39;itérer en toute sécurité sur les prompts sans modifier de manière inattendue le comportement en production.

## <a id="view-prompts" aria-label="Voir et comparer les versions des prompts" />Voir et comparer les versions des prompts

Pour afficher les versions du prompt dans l’interface utilisateur :

1. Accédez à [wandb.ai](https://wandb.ai/) et sélectionnez votre projet.
2. Dans la barre latérale du projet Weave, cliquez sur **Assets**. La page Assets s’ouvre.
3. Sur la page Assets, cliquez sur **Prompts**. La page Prompts s’ouvre et affiche les prompts de votre projet.
4. Dans la colonne **Versions**, cliquez sur **(x) Versions** pour le prompt que vous souhaitez afficher. Une liste des versions du prompt s’ouvre.

![Interface utilisateur de Prompt Assets, montrant les versions du prompt sélectionné.](/weave/guides/core-types/imgs/prompt-object.png)

5. (Facultatif) Vous pouvez comparer différentes versions de prompts en cochant les cases à côté des prompts listés, puis en cliquant sur le bouton **Compare** dans la barre d’outils du tableau. Cela vous permet de voir les différences entre vos prompts.

![Interface utilisateur de Compare prompts montrant les différences entre deux versions de prompt.](/weave/guides/core-types/imgs/prompt-comparison.png)

<div id="add-prompt-tags-and-aliases">
  ## Ajouter des tags et des alias de prompt
</div>

Vous pouvez organiser les versions de prompt à l’aide d’alias et de tags. Ces libellés permettent d’identifier et de référencer des versions spécifiques.

* Alias : un nom unique qui pointe vers une seule version de prompt. Vous pouvez à tout moment faire pointer un alias vers une autre version, ce qui est utile pour des références stables comme production ou staging.
* Tag : un libellé descriptif associé à une version. Une version peut avoir plusieurs tags. Utilisez les tags pour catégoriser et filtrer les versions, par exemple reviewed ou passed-eval.

Pour définir des alias et des tags pour vos prompts dans l’interface utilisateur :

1. Dans la barre latérale du projet Weave, cliquez sur **Assets**. Cela ouvre la page Assets.
2. Sur la page Assets, cliquez sur **Prompts**. Notez que vous pouvez consulter les `Aliases` et `Tags` attribués dans le tableau.
3. Dans le tableau Prompts, cliquez sur le lien du prompt que vous souhaitez modifier.
4. Dans le panneau de détails du prompt sélectionné, la barre de titre affiche le nom du prompt et une version spécifique. Choisissez la version du prompt que vous souhaitez mettre à jour.
5. Dans la barre d’outils du panneau, utilisez les commandes pour ajouter ou supprimer les alias et tags attribués à cette version spécifique.

![Version de prompt sélectionnée dans le panneau de détails du prompt sur la page Prompts, avec les commandes pour Aliases et Tags.](/weave/guides/core-types/imgs/prompt-alias-tags.png)

<div id="modify-tags-and-aliases-in-code">
  ### Modifier les tags et les alias dans le code
</div>

Les tags et les alias peuvent aussi être gérés dans le code pour des tâches telles que :

* Publier des prompts avec des tags et des alias directement lors de la publication.
* Associer des alias à des versions spécifiques et les résoudre pour charger des prompts.
* Ajouter, supprimer ou lister des tags et des alias sur des versions.

L&#39;exemple de code suivant ajoute à votre projet un prompt `my-prompt` avec trois versions, chacune ayant différents tags et alias. Mettez à jour `'your-team-name/your-project-name'` en fonction de votre projet.

<CodeGroup>
  ```python Python lines
  import weave

  // TODO: Définissez 'your-team-name/your-project-name' avec les bonnes valeurs.
  client = weave.init('your-team-name/your-project-name')

  # Publiez deux versions d'un prompt.
  v0_ref = weave.publish(
      weave.StringPrompt("Answer the user's question: {question}"),
      name="my-prompt",
  )
  v1_ref = weave.publish(
      weave.StringPrompt("Answer the user's question helpfully and concisely: {question}"),
      name="my-prompt",
  )

  # --- Alias : pointeurs nommés vers une version spécifique. ---

  # Faites pointer des alias vers une version.
  client.set_aliases(v0_ref, "staging")
  client.set_aliases(v1_ref, "production")

  # Récupérez les alias d'une version.
  client.get_aliases(v1_ref)  # ["production", "latest"]

  # Déplacez un alias en le définissant sur une autre version.
  client.set_aliases(v0_ref, "production")  # Se détache automatiquement de v1.

  # Résolvez un alias pour charger l'objet.
  prompt = weave.ref("my-prompt:production").get()

  # Listez tous les alias du projet.
  client.list_aliases()

  # Supprimez un alias.
  client.remove_aliases(v0_ref, "production")

  # --- Tags : tags appliqués à une version spécifique. ---

  # Ajoutez des tags à une version.
  client.add_tags(v0_ref, ["reviewed", "passed-eval"])
  client.add_tags(v1_ref, ["reviewed", "needs-improvement"])

  # Obtenez les tags d'une version.
  client.get_tags(v0_ref)  # ["passed-eval", "reviewed"]

  # Listez tous les tags distincts du projet.
  client.list_tags()

  # Supprimez des tags d'une version.
  client.remove_tags(v1_ref, ["needs-improvement"])

  # --- Combinaison : récupérez les deux en un appel unique. ---

  tags, aliases = client.get_tags_and_aliases(v0_ref)

  # --- Publiez avec des tags et des alias directement lors de la publication. ---

  ref = weave.publish(
      weave.StringPrompt("Be brief: {question}"),
      name="my-prompt",
      tags=["reviewed"],
      aliases=["production"],
  )

  ```

  ```plaintext TypeScript lines
  Cette fonctionnalité n'est pas encore disponible dans le SDK TypeScript.
  ```
</CodeGroup>

Lorsque vous exécutez l&#39;exemple, Weave affiche des liens vers vos objets dans le tableau de bord Weave. Suivez ces liens pour voir vos prompts.