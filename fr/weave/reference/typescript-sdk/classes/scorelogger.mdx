---
title: "ScoreLogger"
description: "Référence du SDK TypeScript"
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/evaluationLogger.ts#L330" />

ScoreLogger gère l’attribution d’un score à une seule prédiction.
Renvoyé par EvaluationLogger.logPrediction().

## Exemple {#example}

```ts twoslash
// @noErrors
const pred = await ev.logPrediction(example, output);
await pred.logScore("accuracy", 0.95);
await pred.logScore("relevance", 0.8);
await pred.finish(); // Finalise la prédiction
```

## Constructeur {#constructor}

> **new ScoreLogger**(`evalLogger`) : `ScoreLogger`

### Paramètres {#parameters}

<ParamField path="evalLogger" type="EvaluationLogger" required>
  Voir [`EvaluationLogger`](./evaluationlogger).
</ParamField>

### Renvoie {#returns}

`ScoreLogger`

## Accesseurs {#accessors}

### isFinishCalled {#isfinishcalled}

#### Signature d’obtention {#get-signature}

> **get** **isFinishCalled**(): `boolean`

Vérifie si finish() a été appelé.
Utilisé par EvaluationLogger pour détecter les prédictions non finalisées.

##### Renvoie {#returns-2}

`boolean`

## Méthodes {#methods}

### finish() {#finish}

> **finish**(): `Promise`&lt;`void`&gt;

Termine le processus d’attribution d’un score de la prédiction.
Finalise l’appel `predict_and_score` avec les scores accumulés.
Met à jour les agrégats incrémentiels et libère la mémoire.

#### Renvoie {#returns-3}

`Promise`&lt;`void`&gt;

***

### logScore() {#logscore}

> **logScore**(`scorerName`, `score`): `Promise`&lt;`void`&gt;

Journalisez un score pour cette prédiction.
Crée un appel de scorer enfant de predict&#95;and&#95;score.

#### Paramètres {#parameters-2}

<ParamField path="scorerName" type="string" required>
  Nom du scorer (par exemple : &quot;accuracy&quot;, &quot;f1&#95;score&quot;)
</ParamField>

<ParamField path="score" type="any" required>
  La valeur du score
</ParamField>

#### Renvoie {#returns-4}

`Promise`&lt;`void`&gt;