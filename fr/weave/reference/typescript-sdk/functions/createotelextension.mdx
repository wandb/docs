---
title: "createOtelExtension"
description: "Référence du SDK TypeScript"
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

```ts
createOtelExtension(opts?: object): (pi) => void
```

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/integrations/piCodingAgent.ts#L658" />

Crée une extension pour l&#39;agent de code pi qui génère des spans OTEL sur
l&#39;ensemble du cycle de vie de l&#39;agent, conformément aux conventions sémantiques GenAI.

Lorsque `weave.init(...)` a été appelé, les spans sont automatiquement exportés
vers le serveur de traces Weave à l&#39;adresse `/agents/otel/v1/traces`. Sinon, transmettez un
`tracer` personnalisé dans `opts`.

<div id="parameters">
  ## Paramètres
</div>

<ParamField path="opts" type="object">
  <Expandable title="propriétés" defaultOpen>
    <ParamField path="captureContent" type="boolean" />

    <ParamField path="tracer" type="Tracer" />
  </Expandable>
</ParamField>

<div id="returns">
  ## Renvoie
</div>

(`pi`) =&gt; `void`

<div id="example">
  ## Exemple
</div>

```typescript twoslash
// @noErrors
const resourceLoader = new DefaultResourceLoader({
  extensionFactories: [createOtelExtension()],
});
```