---
title: "op"
description: "Référence du SDK TypeScript"
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

<div id="call-signature">
  ## Signature d’appel
</div>

```ts
op<T>(fn: T, options?: object): Op<T>
```

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/op.ts#L392" />

<div id="type-parameters">
  ### Paramètres de type
</div>

<ParamField path="T" type="extends (...args) => any" />

<div id="parameters">
  ### Paramètres
</div>

<ParamField path="fn" type="T" required />

<ParamField path="options" type="object">
  <Expandable title="propriétés" defaultOpen>
    <ParamField path="attributes" type="Record<string, any>">
      Attributs par défaut appliqués à chaque appel créé par cette op. Les intégrations
      renseignent cette valeur pour marquer la provenance `attributes.integration`. Fusionnés sous `kind`/
      `color`, qui ont la priorité en cas de conflit de clé.
    </ParamField>

    <ParamField path="bindThis" type="WeaveObject">
      Voir [`WeaveObject`](../classes/weaveobject).
    </ParamField>

    <ParamField path="callDisplayName" type="(...args) => string" />

    <ParamField path="isDecorator" type="boolean" />

    <ParamField path="name" type="string" />

    <ParamField path="opColor" type="string">
      Couleur personnalisée de l’opération dans l’interface utilisateur
    </ParamField>

    <ParamField path="opKind" type="string">
      Type d’opération (par exemple, &#39;llm&#39;, &#39;agent&#39;, &#39;tool&#39;, &#39;search&#39;) utilisé pour la catégorisation dans l’interface utilisateur
    </ParamField>

    <ParamField path="originalFunction" type="T" />

    <ParamField path="parameterNames" type="&#x22;useParam0Object&#x22; | string[]" />

    <ParamField path="shouldAdoptThis" type="boolean" />

    <ParamField path="streamReducer" type="object">
      Type complet : `{ finalizeFn: (state) => void; initialStateFn: () => any; reduceFn: (state, chunk) => any; }`

      <Expandable title="propriétés" defaultOpen>
        <ParamField path="finalizeFn" type="(state) => void" required />

        <ParamField path="initialStateFn" type="() => any" required />

        <ParamField path="reduceFn" type="(state, chunk) => any" required />
      </Expandable>
    </ParamField>

    <ParamField path="summarize" type="(result) => Record<string, any>" />
  </Expandable>
</ParamField>

<div id="returns">
  ### Renvoie
</div>

[`Op`](../type-aliases/op)&lt;`T`&gt;

<div id="call-signature-2">
  ## Signature d’appel
</div>

```ts
op<T>(thisArg: any, fn: T, options?: object): Op<T>
```

<div id="type-parameters-2">
  ### Paramètres de type
</div>

<ParamField path="T" type="extends (...args) => any" />

<div id="parameters-2">
  ### Paramètres
</div>

<ParamField path="thisArg" type="any" required />

<ParamField path="fn" type="T" required />

<ParamField path="options" type="object">
  <Expandable title="propriétés" defaultOpen>
    <ParamField path="attributes" type="Record<string, any>">
      Attributs par défaut appliqués à chaque appel créé par cette op. Les intégrations
      renseignent cette valeur pour inscrire la provenance `attributes.integration`. Ils sont fusionnés sous `kind`/
      `color`, qui l’emportent en cas de conflit de clé.
    </ParamField>

    <ParamField path="bindThis" type="WeaveObject">
      Voir [`WeaveObject`](../classes/weaveobject).
    </ParamField>

    <ParamField path="callDisplayName" type="(...args) => string" />

    <ParamField path="isDecorator" type="boolean" />

    <ParamField path="name" type="string" />

    <ParamField path="opColor" type="string">
      Couleur personnalisée de l’opération dans l’interface utilisateur
    </ParamField>

    <ParamField path="opKind" type="string">
      Le type d’opération (par exemple, &#39;llm&#39;, &#39;agent&#39;, &#39;tool&#39;, &#39;search&#39;) utilisé pour la catégorisation dans l’interface utilisateur
    </ParamField>

    <ParamField path="originalFunction" type="T" />

    <ParamField path="parameterNames" type="&#x22;useParam0Object&#x22; | string[]" />

    <ParamField path="shouldAdoptThis" type="boolean" />

    <ParamField path="streamReducer" type="object">
      Type complet : `{ finalizeFn: (state) => void; initialStateFn: () => any; reduceFn: (state, chunk) => any; }`

      <Expandable title="propriétés" defaultOpen>
        <ParamField path="finalizeFn" type="(state) => void" required />

        <ParamField path="initialStateFn" type="() => any" required />

        <ParamField path="reduceFn" type="(state, chunk) => any" required />
      </Expandable>
    </ParamField>

    <ParamField path="summarize" type="(result) => Record<string, any>" />
  </Expandable>
</ParamField>

<div id="returns-2">
  ### Renvoie
</div>

[`Op`](../type-aliases/op)&lt;`T`&gt;

<div id="call-signature-3">
  ## Signature d&#39;appel
</div>

```ts
op(target: object, propertyKey: string | symbol, descriptor: TypedPropertyDescriptor<any>): TypedPropertyDescriptor<any>
```

<div id="parameters-3">
  ### Paramètres
</div>

<ParamField path="target" type="object" required />

<ParamField path="propertyKey" type="string | symbol" required />

<ParamField path="descriptor" type="TypedPropertyDescriptor<any>" required />

<div id="returns-3">
  ### Renvoie
</div>

`TypedPropertyDescriptor`&lt;`any`&gt;

<div id="call-signature-4">
  ## Signature d’appel
</div>

```ts
op<T>(value: T, context: object): Op<T>
```

<div id="type-parameters-3">
  ### Paramètres de type
</div>

<ParamField path="T" type="extends (...args) => any" />

<div id="parameters-4">
  ### Paramètres
</div>

<ParamField path="value" type="T" required />

<ParamField path="context" type="object" required>
  <Expandable title="propriétés" defaultOpen>
    <ParamField path="access" type="{ get: Function; has: boolean; }">
      <Expandable title="propriétés" defaultOpen>
        <ParamField path="get" required />

        <ParamField path="has" required />
      </Expandable>
    </ParamField>

    <ParamField path="class" type="Function" />

    <ParamField path="kind" type="&#x22;method&#x22;" required />

    <ParamField path="metadata" type="any" />

    <ParamField path="name" type="string | symbol" required />

    <ParamField path="private" type="boolean" />

    <ParamField path="static" type="boolean" />

    <ParamField path="addInitializer" />
  </Expandable>
</ParamField>

<div id="returns-4">
  ### Renvoie
</div>

[`Op`](../type-aliases/op)&lt;`T`&gt;

<div id="call-signature-5">
  ## Signature d’appel
</div>

```ts
op(options: Partial<OpOptions<any>>): MethodDecorator
```

<div id="parameters-5">
  ### Paramètres
</div>

<ParamField path="options" type="Partial<OpOptions<any>>" required />

<div id="returns-5">
  ### Renvoie
</div>

`MethodDecorator`