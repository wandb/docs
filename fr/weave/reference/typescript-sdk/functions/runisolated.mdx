---
title: "runIsolated"
description: "Référence du SDK TypeScript"
---

import { GitHubLink } from '/snippets/fr/_includes/github-source-link.mdx';

```ts
runIsolated<T>(fn: () => T): T
```

<GitHubLink url="https://github.com/wandb/weave/blob/7c9efdc9fe05ffcaf2430dd652ef9bc5b3ffdfaa/sdks/node/src/genai/context.ts#L54" />

Exécutez `fn` dans un cadre d’état GenAI nouveau et isolé. Toute Conversation / tout tour de conversation / tout LLM
démarré dans `fn` n’existe que dans ce cadre — il n’entre pas en conflit avec
d’autres cadres `runIsolated` exécutés simultanément, et n’est pas propagé
à la chaîne asynchrone externe.

Utilisez cette fonction pour exécuter en toute sécurité des tâches GenAI en parallèle :

```typescript
  await Promise.all([
    weave.runIsolated(async () => { ... }),
    weave.runIsolated(async () => { ... }),
  ]);
```

L’utilisation séquentielle de single-flight ne nécessite pas ce wrapper —
l’état global par défaut du processus s’en charge.

<div id="type-parameters">
  ## Paramètres de type
</div>

<ParamField path="T" />

<div id="parameters">
  ## Paramètres
</div>

<ParamField path="fn" type="() => T" required />

<div id="returns">
  ## Renvoie
</div>

`T`