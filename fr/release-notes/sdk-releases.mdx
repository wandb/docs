---
title: "Versions du SDK W&B"
description: "Consultez les notes de version du SDK Python W&B, y compris les nouvelles fonctionnalités, les correctifs de bug et les changements incompatibles par version."
sidebarTitle: "Toutes les versions"
rss: true
---

Cette page présente les notes de version du SDK Python W&amp;B (package `wandb`). Pour W&amp;B Server, voir les [notes de version de W&amp;B Server](/fr/release-notes/server-releases).

<Update label="0.25.1" description="10 mars 2026">
  ## Ajouts

  * Nouveau volet de journaux de console de run dans la commande `wandb beta leet`. Basculez avec `l`.
  * Nouveau volet de métriques système dans le mode espace de travail multi-run de la commande `wandb beta leet`. Basculez avec `s`.
  * La commande `wandb beta leet` prend désormais en charge le filtrage des métriques système. Basculez avec `\`.
  * `ArtifactType.collections()` prend désormais en charge le filtrage et le tri des collections.
  * Un avertissement est désormais enregistré si `run.log_artifact` ne crée pas de nouvelle version parce que le contenu de l’artefact est identique à celui d’une version existante.
  * La nouvelle méthode `Project.collections()` récupère les collections d’artifacts filtrées et triées d’un projet.
  * La nouvelle commande CLI `wandb purge-cache` nettoie les fichiers mis en cache.

  ## Correctifs

  * Correction d’un interblocage rare provoqué lorsque le ramasse-miettes se déclenche à un moment mal choisi et exécute une méthode `__del__` qui affiche du texte.
  * Correction d’un bug où le chargement de plusieurs runs avec `api.Runs("...")`, puis la tentative d’obtenir l’utilisateur d’un run avec un code comme celui-ci, pouvait échouer avec une `AttributeError` :

    ```python
    for run in runs:
      print(run.user)
    ```
</Update>

<Update label="0.25.0" description="12 février 2026">
  ## Changements notables

  * Python 3.8 n’est plus pris en charge.

  ## Ajouts

  * Le TUI `wandb beta leet` prend désormais en charge les espaces de travail multi-run.
  * Configurez de manière interactive la configuration persistante de `wandb beta leet` avec la commande `wandb beta leet config`.
  * Accédez aux informations utilisateur du propriétaire du projet via la propriété `owner` de `wandb.apis.public.Project`.

  ## Correctifs

  * Correction d’un bug où un agent de balayage pour un balayage supprimé pouvait s’exécuter indéfiniment avec des erreurs 404 répétées.
  * Correction d’un bug où un plantage de `wandb-core` pouvait produire des traces d’erreur extrêmement longues et répétitives dans les anciennes versions de Python.
  * Correction d’une régression introduite dans la v0.24.0 où une synchronisation TensorBoard pouvait s’arrêter après avoir synchronisé 1 Mo de données.
</Update>

<Update label="0.24.2" description="4 février 2026">
  ## Ajouts

  * `wandb.Api()` prend désormais en charge l’authentification fédérée (basée sur JWT).

  ## Correctifs

  * L’URL de téléchargement présignée est désormais actualisée si elle expire pendant le téléchargement de fichiers d’artefact.
</Update>

<Update label="0.24.1" description="29 janvier 2026">
  ## Changements notables

  * Les runs créés avec `wandb==0.24.0` peuvent ne pas parvenir à téléverser certaines données, ce que cette version corrige. Les données manquantes sont stockées dans le fichier `.wandb` du run et peuvent être téléversées à nouveau avec `wandb sync`.

  ## Ajouts

  * `download_history_exports` dans la classe `api.Run` pour télécharger l’historique exporté du run au format de fichier parquet.

  ## Modifications

  * Lorsqu’un fichier de paramètres (tel que `./wandb/settings` ou `~/.config/wandb/settings`) contient un paramètre non valide, tous les fichiers de paramètres sont ignorés et une erreur est affichée.

  ## Correctifs

  * Après `wandb login --host <invalid-url>`, l’utilisation de `wandb login --host <valid-url>` fonctionne normalement.
  * Correction d’un bug où `wandb beta sync` pouvait rester bloqué sur `Syncing...` et ne pas parvenir à charger les informations d’identification.
  * Correction d’un bug pouvant occasionnellement entraîner des données non téléversées. Désormais, toutes les données sont stockées dans le fichier `.wandb` du run et peuvent être téléversées à nouveau avec `wandb sync`.

  Ces régressions ont été introduites dans la version 0.24.0.
</Update>

<Update label="0.24.0" description="13 janvier 2026">
  <Warning>
    Cette version a été retirée de PyPI. N’utilisez pas cette version. Si vous utilisez cette version, passez immédiatement à la v0.24.1 ou à une version plus récente. En raison d’une régression corrigée dans la v0.24.1, les runs créés avec `wandb==0.24.0` risquent de ne pas téléverser certaines données.
  </Warning>

  ## Changements notables

  Cette version supprime le module hérité et obsolète `wandb.beta.workflows`, y compris ses fonctions `log_model()`/`use_model()`/`link_model()`. Il s’agit officiellement d’un changement incompatible.

  ## Ajouts

  * `wandb agent` et `wandb.agent()` acceptent désormais l’indicateur `forward_signals` (CLI : `--forward-signals/-f`) pour relayer SIGINT/SIGTERM et d’autres signaux interceptables de l’agent vers les runs enfants du sweep, ce qui permet des arrêts plus propres lorsque vous interrompez un processus d’agent.
  * `wandb beta sync` prend désormais en charge l’option `--live` pour synchroniser un run pendant son enregistrement.

  ## Suppression

  * Suppression du module obsolète `wandb.beta.workflows`, y compris ses fonctions `log_model()`, `use_model()` et `link_model()`, dont les successeurs modernes sont respectivement les méthodes `Run.log_artifact`, `Run.use_artifact` et `Run.link_artifact`.

  ## Correctif

  * Correction des annotations de type de `Run.__exit__` pour accepter les valeurs `None`, transmises lorsqu’aucune exception n’est levée.
  * Correction de l’erreur `Invalid Client ID digest` lors de la création d’artefacts après l’appel à `random.seed()`. Des ID client pouvaient entrer en collision lorsque l’état aléatoire était initialisé de manière déterministe.
  * Correction d’une erreur CLI lors de l’affichage d’une liste d’artefacts vides.
  * Correction d’une régression lors de l’appel à `api.run()` sur un run Sweeps.
  * Correction du message « View run at » affiché à la fin d’un run, qui n’incluait parfois aucune URL.
  * Les Runs interrogés depuis `wandb.Api()` affichent désormais une représentation textuelle dans les notebooks VSCode au lieu d’une fenêtre HTML défaillante.
</Update>

<Update label="v0.23.1" description="2 décembre 2025">
  ## Ajouts

  * Prise en charge des regex dans les filtres de métriques et d’aperçu des runs de la commande `wandb beta leet`.
  * Inspection des graphiques dans la commande `wandb beta leet` : faites un clic droit et faites glisser pour afficher (x, y) au point de données le plus proche ; maintenez Alt enfoncé pour une inspection synchronisée sur tous les graphiques visibles.
  * L’API Automations prend désormais en charge la création et la modification d’automatisations qui se déclenchent en fonction de l’état des runs.
  * L’API Automations prend désormais en charge les événements d’automatisation zscore de base.
  * Ajout de la méthode `beta_history_scan` aux objets `Run` pour accélérer le balayage de l’historique avec `wandb.Api`.

  ## Modification

  * `wandb.Api()` lève désormais une `UsageError` si `WANDB_IDENTITY_TOKEN_FILE` est défini et qu’aucune clé API explicite n’est fournie.

  ## Obsolète

  * Le mode anonyme est obsolète et émettra des avertissements. Cette obsolescence inclut le paramètre `anonymous`, la variable d’environnement `WANDB_ANONYMOUS`, `wandb.init(anonymous=...)` et les commandes `wandb login --anonymously` et `wandb.login(anonymous=...)`.

  ## Correctif

  * `wandb.Image()` n’affiche plus d’avertissement d’obsolescence.
  * `Registry.description` et `ArtifactCollection.description` ne rejettent plus les chaînes vides.
  * L’instanciation des objets `Artifact` est désormais nettement plus rapide.
  * `wandb.Run.save()` revient désormais aux liens physiques et, si nécessaire, à la copie (en rétrogradant la politique de fichier &#39;live&#39; à &#39;now&#39;, le cas échéant) lorsque les liens symboliques sont désactivés ou indisponibles (par ex. entre volumes ou sans Developer Mode sous Windows).
  * Les alias de collection d’artifacts sont désormais récupérés à la demande lors de l’accès à `ArtifactCollection.aliases` plutôt que lors de l’instanciation d’`ArtifactCollection`, ce qui améliore les performances de `Api.artifact_collections()`, `Api.registries().collections()`, etc.
  * La clé API explicitement fournie dans `wandb.init(settings=wandb.Settings(api_key="..."))` est désormais utilisée pour la connexion. La clé du run est utilisée lors de la journalisation d’artefacts via `run.log_artifact`.
  * L’interface TUI W&amp;B LEET affiche correctement les valeurs négatives des graduations de l’axe Y ainsi que les unités de base et d’affichage de certaines métriques système.
  * Correction d’une rare boucle infinie dans `console_capture.py`.
  * Le téléversement/téléchargement de fichiers respecte désormais `WANDB_X_EXTRA_HTTP_HEADERS`, sauf pour les [artefacts de référence](/fr/models/artifacts/track-external-files).
</Update>

<Update label="v0.23.0" description="11 novembre 2025">
  ## Ajouts

  * Commande expérimentale `wandb beta leet` - Lightweight Experiment Exploration Tool - une interface utilisateur en terminal légère pour consulter localement les runs W&amp;B avec visualisation des métriques en temps réel et surveillance du système.
  * L’API du registre prend désormais en charge la gestion programmatique des utilisateurs et des membres d’équipe de chaque registre.
  * `Registry.id` a été ajouté comme propriété (en lecture seule) des objets `Registry`.

  ## Correctif

  * `Artifact.files()` renvoie désormais un `len()` correct lors du filtrage par le paramètre `names`.
  * Le numérateur de la progression du téléversement de fichier ne dépasse désormais plus occasionnellement la taille totale du fichier.
  * `Artifact.link()` enregistre désormais les artefacts non enregistrés au lieu de lever une erreur, conformément au comportement de `Run.link_artifact()`.
  * L’enregistrement automatique du code fonctionne désormais lors de l’exécution de notebooks ipython dans l’extension notebook Jupyter de VSCode.
  * La journalisation d’un artefact avec des flottants infinis dans `Artifact.metadata` lève désormais une `ValueError` plus tôt, au lieu d’attendre que les nouvelles tentatives de requête expirent.
</Update>

<Update label="v0.22.3" description="28 octobre 2025">
  ## Ajouts

  * Paramètres `console_chunk_max_seconds` et `console_chunk_max_bytes` pour le découpage multipart des fichiers de journaux de console selon la taille et la durée.
  * Les méthodes de requête de l’API du registre (`Api.registries()`, `Registry.{collections,versions}()`, `Api.registries().{collections,versions}()`) acceptent désormais un argument nommé `per_page` pour remplacer la taille de lot par défaut des résultats paginés.

  ## Modifications

  * Les clés API de plus de 40 caractères sont désormais prises en charge.

  ## Correctif

  * `run.config` renvoie désormais correctement un dict lors de l’appel de `artifact.logged_by()` dans la v0.22.1.
  * `wandb.Api(api_key=...)` donne désormais la priorité à la clé API explicitement fournie plutôt qu’aux identifiants mis en cache localement au niveau du thread.
  * Correction d’un interblocage rare dans `console_capture.py`.

    Si vous capturez les traces de pile des threads pendant l’interblocage et voyez le thread `wandb-AsyncioManager-main` bloqué sur une ligne dans `console_capture.py` : ce problème est désormais corrigé.
  * Correction d’un problème où la synchronisation TensorBoard cessait parfois de fonctionner si les fichiers tfevents étaient en cours d’écriture.
  * `Artifact.manifest` diffère le téléchargement **et** la génération de l’URL de téléchargement du manifeste de l’artefact jusqu’à sa première utilisation. Si le manifeste n’a pas été modifié localement, `Artifact.size` et `Artifact.digest` peuvent être renvoyés sans attendre la récupération du manifeste complet.
  * Correction du téléversement des références de dossier GCS via `artifact.add_reference`.
  * Le SDK déduit désormais correctement les chemins des notebooks dans les sessions Jupyter, en utilisant le répertoire racine du serveur, afin que l’enregistrement du code fonctionne dans les sous-répertoires (par ex. `code/nested/<notebook>.ipynb`).

  Voir le [journal des modifications v0.22.3](https://github.com/wandb/wandb/releases/tag/v0.22.3) sur GitHub.
</Update>

<Update label="v0.22.2" description="07 octobre 2025">
  ## Correctif

  * Correction possible de certains cas où le fichier `output.log` n’était pas téléversé.
  * Correction des téléversements de données excessifs lors d’appels répétés à `run.save()` sur des fichiers inchangés.

  Voir le [journal des modifications v0.22.2](https://github.com/wandb/wandb/releases/tag/v0.22.2) sur GitHub.
</Update>

<Update label="v0.22.1" description="29 septembre 2025">
  ## Ajouts

  * Optimisation de la revérification des téléchargements d’artefacts grâce à la mise en cache des sommes de contrôle.
  * Prise en charge du chargement lazy pour `Api().runs()` afin d’améliorer les performances lors du listage des runs. Le nouveau paramètre `lazy=True` (par défaut) charge uniquement les métadonnées essentielles au départ, avec chargement automatique à la demande des champs volumineux comme config et summary lors de l’accès.
  * Ajout de l’option `storage_region` lors de la création d’artefacts. Les utilisateurs peuvent utiliser [CoreWeave AI Object Storage](https://docs.coreweave.com/docs/products/storage/object-storage) en spécifiant `wandb.Artifact(storage_region="coreweave-us")` lors de l’utilisation de wandb.ai pour accélérer le téléversement et le téléchargement d’artefacts sur l’infrastructure de CoreWeave.

  ## Correctif

  * `Api.artifact_exists()` et `Api.artifact_collection_exists()` lèvent désormais une exception lorsqu’elles rencontrent des erreurs de délai d’attente, au lieu de renvoyer `False` (potentiellement à tort).

  Voir le [journal des modifications v0.22.1](https://github.com/wandb/wandb/releases/tag/v0.22.1) sur GitHub.
</Update>

<Update label="v0.22.0" description="18 septembre 2025">
  ## Changements notables

  Cette version met fin à la prise en charge de MacOS 10.

  ## Suppression

  * Suppression d’un build ciblant MacOS 10.x en raison de multiples considérations liées à la sécurité et à la chaîne d’approvisionnement.

  ## Correctif

  * La reprise d’un run avec un autre run actif lèvera désormais une erreur, sauf si vous appelez d’abord `run.finish()`, ou si vous appelez `wandb.init()` avec le paramètre `reinit='create_new'`.
  * Correction de `Api().runs()` pour wandb server &lt;  0.51.0 (lors de l’ajout de `project.internalId` à l’API gql).
  * Sweeps : les scripts de run `command` qui `import readline`, directement ou indirectement (par exemple `import torch` avec Python 3.13), ne devraient plus provoquer d’interblocage.

  Voir le journal des modifications.
</Update>

<Update label="v0.21.4" description="11 septembre 2025">
  ## Ajouts

  * Ajout de l’intégration DSPy : suivez les métriques d’évaluation au fil du temps, consignez les prédictions et l’évolution de la signature du programme dans W&amp;B Tables, et enregistrez les programmes DSPy dans W&amp;B Artifacts (programme complet ou état au format JSON/PKL).

  Voir le journal des modifications.
</Update>

<Update label="v0.21.3" description="30 août 2025">
  ## Modification

  * Mise à jour de la contrainte de dépendance `click`, de `>=7.1` à `>=8.0.1`.

  ## Correctif

  * Le message &quot;Changes to your wandb environment variables will be ignored&quot; n’est plus affiché lorsque rien n’a changé.

  Voir le journal des modifications.
</Update>

<Update label="v0.21.2" description="28 août 2025">
  ## Changements notables

  Cette version lève désormais des erreurs qui étaient auparavant masquées lors des appels à `Artifact.link()` ou `Run.link_artifact()`. Bien que cela évite des échecs non détectés dans ces méthodes, il s’agit aussi d’un changement incompatible.

  ## Ajouts

  * Nouveaux paramètres pour `max_end_of_run_history_metrics` et `max_end_of_run_summary_metrics`.
  * Nouveau module `wandb.integration.weave` pour initialiser automatiquement Weave lorsqu’un run W&amp;B est actif et que `weave` est importé.

  ## Modification

  * Les erreurs rencontrées lors de la liaison d’un artefact ne sont plus masquées, et `Artifact.link()` ainsi que `Run.link_artifact()` ne renvoient plus `None`.
  * Les sections &quot;Run history&quot; et &quot;Run summary&quot; affichées à la fin d’un run sont désormais limitées à 10 métriques chacune.

  ## Correctif

  * Les dataclasses dans la `config` d’un run ne lèvent plus l’erreur `Object of type ... is not JSON serializable` lorsqu’elles contiennent de vraies classes comme champs.
  * `Artifact.link()` et `Run.link_artifact()` devraient être plus rapides sur les versions de serveur 0.74.0+, en nécessitant 4 à 5 requêtes GraphQL bloquantes inutiles en moins.

  Voir le journal des modifications.
</Update>

<Update label="v0.21.1" description="7 août 2025">
  ## Changements notables

  L&#39;ordre par défaut de `Api().runs(...)` et `Api().sweeps(...)` est désormais croissant, en fonction de l&#39;horodatage `created_at` des runs.

  ## Ajouts

  * Prise en charge de l&#39;option de résumé `first` dans `define_metric`.
  * Ajout de la prise en charge de la pagination pour les balayages.
  * Ajout du paramètre `pattern` à `Api().run().files` pour récupérer uniquement les fichiers correspondant à un motif donné depuis le backend W&amp;B.
  * Ajout d&#39;une clé `format` facultative à la JSONSchema d&#39;entrée de Launch pour spécifier une chaîne avec un format secret.

  ## Modifications

  * La propriété `Sweep.name` renvoie désormais le nom d&#39;affichage modifié par l&#39;utilisateur s&#39;il est disponible (sinon, le nom original issu de la configuration du balayage, puis l&#39;ID du balayage comme auparavant).
  * `Api().runs(...)` et `Api().sweeps(...)` renvoient désormais les runs dans l&#39;ordre croissant selon l&#39;horodatage `created_at` des runs.
  * Le téléversement des artefacts contenant de gros fichiers (&gt;2GB) est plus rapide grâce à l&#39;utilisation d&#39;un hachage parallèle sur les systèmes disposant de davantage de cœurs.
  * Suppression de l&#39;implémentation de `__bool__` pour les itérateurs du registre afin de les aligner sur les itérateurs lazy de Python.

  ## Obsolète

  * Le module `wandb.beta.workflows` et son contenu (y compris `log_model()`, `use_model()` et `link_model()`) sont obsolètes et seront supprimés dans une future version.

  ## Correctifs

  * Correction de l&#39;URL d&#39;artefact pour les artefacts du registre d&#39;organisation afin qu&#39;elle soit indépendante du type d&#39;artefact.
  * Les suffixes des noms `InternalArtifact` nettoyés ont été raccourcis à 6 caractères alphanumériques.
  * `wandb.Video` n&#39;affichera pas d&#39;indicateur de progression pendant l&#39;encodage de la vidéo lorsque les variables d&#39;environnement `WANDB_SILENT`/`WANDB_QUIET` sont définies.
  * Correction d&#39;un problème où les registres récupérés avec `api.registries()` comportaient un préfixe supplémentaire `wandb-registry-` dans les champs name et full&#95;name.
  * Correction d&#39;un plantage pouvant survenir lors de l&#39;utilisation de `sync_tensorboard`.
  * `Api().run(...).upload_file` ne génère plus d&#39;erreur lors du téléversement d&#39;un fichier situé à un chemin différent par rapport au répertoire racine fourni.
  * L&#39;appel de la fonction `load()` sur un objet run de l&#39;API publique ne génère plus de `TypeError`.
  * Lorsqu&#39;une fonction de run Sweeps appelée par l&#39;API `wandb.agent()` lève une exception, celle-ci apparaît désormais sur la page des journaux du run. (Auparavant, cela ne se produisait que pour les runs appelés par la commande CLI `wandb agent`.)

  Voir le [journal des modifications de la v0.21.1](https://github.com/wandb/wandb/releases/tag/v0.21.1) sur GitHub.
</Update>

<Update label="v0.21.0" description="1er juillet 2025">
  ## Changements notables

  Cette version supprime l’implémentation héritée du processus `service`. Il s’agit d’un changement incompatible.

  ## Ajouts

  * Paramètre `x_stats_track_process_tree` pour suivre des métriques propres au processus, comme la RSS, le pourcentage de CPU et le nombre de threads utilisés pour l’ensemble de l’arborescence des processus, à partir de `x_stats_pid`. Cela peut être coûteux et est désactivé par défaut.
  * Les notes sont désormais renvoyées au client lors de la reprise d’un run.
  * Ajout de la prise en charge de la création de préréglages de graphiques Vega personnalisés via l’API. Les utilisateurs peuvent désormais définir et téléverser des spécifications de graphiques personnalisées, puis les réutiliser entre les runs avec wandb.plot&#95;table().

  ## Modifications

  * L’appel à `Artifact.link()` n’instancie plus de run factice jetable.
  * `wandb` tente désormais d’utiliser des sockets Unix pour l’IPC au lieu d’écouter sur localhost, ce qui lui permet de fonctionner dans des environnements aux autorisations plus restrictives (comme Databricks).
  * `Api.artifact()` affiche désormais un avertissement lors de la récupération d’artefacts à partir de collections migrées du registre de modèles.
  * La propriété `.length` pour les objets interrogés via `wandb.Api` a été déclarée obsolète. Utilisez `len(...)` à la place.

  ## Suppressions

  * Suppression de l’implémentation Python héritée du processus `service`. L’option `legacy-service` de `wandb.require`, ainsi que les paramètres `x_require_legacy_service` et `x_disable_setproctitle` et les variables d’environnement correspondantes, ont été supprimés et provoqueront désormais une erreur s’ils sont utilisés.

  * Suppression de l’attribut privé `wandb.Run._metadata`. Pour remplacer le nombre de CPU et de GPU détectés automatiquement, ainsi que le type de GPU, utilisez les nouveaux paramètres `x_stats_{cpu_count,cpu_logical_count,gpu_count,gpu_type}`.

  ## Correctif

  * Autorise les URI CoreWeave au format S3 pour les artefacts de référence.
  * Correction d’un bug rare qui rendait Ctrl+C inopérant après l’enregistrement de grandes quantités de données.
  * Respecte les paramètres `silent`, `quiet` et `show_warnings` transmis à une instance de `Run` pour les avertissements émis par le processus `service`.
  * `api.Runs` n’effectue plus un appel d’API pour chaque run chargé depuis W&amp;B.
  * Analyse correctement le paramètre `x_extra_http_headers` depuis la variable d’environnement.
  * `.length` appelle le backend W&amp;B pour charger la longueur des objets lorsqu’aucune donnée n’a été chargée, au lieu de renvoyer `None`.

  Voir le journal des modifications.
</Update>

<Update label="v0.20.1" description="4 juin 2025">
  ## Correctif

  * `wandb.Image()` ne fonctionnait plus dans la version 0.20.0 lorsqu’on lui fournissait des tableaux NumPy avec des valeurs dans l’intervalle [0, 1] ; c’est désormais corrigé.

  * wandb.Table : ajout d’un nouveau paramètre du constructeur, `log_mode`, avec les options `"IMMUTABLE"` et `"MUTABLE"`. Le mode de journalisation `IMMUTABLE` (par défaut) correspond au comportement existant et n’autorise l’enregistrement d’un tableau qu’une seule fois. Le mode de journalisation `MUTABLE` permet d’enregistrer de nouveau le tableau s’il a été modifié. (@domphan-wandb in https://github.com/wandb/wandb/pull/9758)

  * wandb.Table : ajout d’un nouveau `log_mode`, `"INCREMENTAL"`, qui enregistre de façon incrémentielle les nouvelles données ajoutées au tableau. (@domphan-wandb in https://github.com/wandb/wandb/pull/9810)

  Voir le journal des modifications.
</Update>

<Update label="v0.20.0" description="03 juin 2025">
  ## Changements notables

  Cette version supprime la possibilité de désactiver le processus `service`. Il s&#39;agit d&#39;un changement incompatible.

  ## Ajouts

  * Ajout du paramètre `merge` à `Artifact.add_dir` pour permettre l&#39;écrasement des fichiers d&#39;artefact ajoutés précédemment.
  * Prise en charge de `pytorch.tensor` pour les paramètres `masks` et `boxes` lors de la création d&#39;un objet `wandb.Image`.
  * `sync_tensorboard` prend désormais en charge la synchronisation des fichiers tfevents stockés dans S3, GCS et Azure.
    * Les chemins GCS utilisent le format `gs://bucket/path/to/log/dir` et s&#39;appuient sur les identifiants par défaut de l&#39;application, qui peuvent être configurés avec `gcloud auth application-default login`
    * Les chemins S3 utilisent le format `s3://bucket/path/to/log/dir` et s&#39;appuient sur les identifiants par défaut définis via `aws configure`
    * Les chemins Azure utilisent le format `az://account/container/path/to/log/dir` et les identifiants de `az login`, mais exigent également que les variables d&#39;environnement `AZURE_STORAGE_ACCOUNT` et `AZURE_STORAGE_KEY` soient définies. D&#39;autres variables d&#39;environnement sont également prises en charge, voir [ici](https://pkg.go.dev/gocloud.dev@v0.41.0/blob/azureblob#hdr-URLs).
  * Ajout de la prise en charge de `pathlib.Path` pour l&#39;initialisation de certains objets Media.
  * Nouveau paramètre `x_skip_transaction_log` permettant d&#39;ignorer le journal des transactions. Remarque : à utiliser avec prudence, car cela supprime les garanties de récupération.
  * Ajout du paramètre `normalize` à l&#39;initialisation de `wandb.Image` afin de normaliser les valeurs de pixel des Images initialisées avec un tableau numpy ou un tenseur pytorch.

  ## Modification

  * Diverses API lèvent désormais `TypeError` au lieu de `ValueError` ou d&#39;autres erreurs génériques lorsqu&#39;elles reçoivent un argument du mauvais type.
  * Diverses API Artifacts et Automations lèvent désormais `CommError` au lieu de `ValueError` lorsqu&#39;elles rencontrent des erreurs du serveur, afin de faire remonter le message d&#39;erreur du serveur.
  * La méthode `wandb.sdk.wandb_run.Run::save` requiert désormais l&#39;argument `glob_str`.

  ## Suppression

  * Suppression de la possibilité de désactiver le processus `service`. Le paramètre `x_disable_service`/`_disable_service` et la variable d&#39;environnement `WANDB_DISABLE_SERVICE`/`WANDB_X_DISABLE_SERVICE` sont obsolètes et lèveront désormais une erreur s&#39;ils sont utilisés.
  * Suppression de la possibilité d&#39;utiliser `wandb.docker` après avoir uniquement importé `wandb`.
    * `wandb.docker` ne fait pas partie de l&#39;interface publique de `wandb` et est susceptible d&#39;introduire des changements incompatibles. Veuillez ne pas l&#39;utiliser.
  * Suppression de l&#39;argument sans effet `sync` de la fonction `wandb.Run::log`.
  * Suppression de la propriété obsolète `wandb.sdk.wandb_run.Run.mode`.
  * Suppression de la méthode obsolète `wandb.sdk.wandb_run.Run::join`.

  ## Obsolète

  * Le paramètre `start_method` est obsolète et n&#39;a aucun effet ; il est ignoré sans risque.
  * La propriété `Artifact.use_as` et le paramètre `use_as` de `run.use_artifact()` sont obsolètes, car ils n&#39;étaient pas utilisés dans W&amp;B Launch.

  ## Correctif

  * L&#39;appel à `wandb.teardown()` dans un processus enfant d&#39;un processus ayant appelé `wandb.setup()` ne lève plus `WandbServiceNotOwnedError`.
    * Cette erreur pouvait se produire lors de l&#39;utilisation de W&amp;B Sweeps
  * Les runs hors ligne avec branchement demandé (fork ou rewind) se synchronisent correctement.
  * Les exceptions sont désormais enregistrées sous forme de chaîne lorsqu&#39;une exception est levée dans la méthode `wait_until_running` de Job.
  * `wandb.Image` initialisé avec des données tensorflow était normalisé différemment de lorsqu&#39;il était initialisé avec un tableau numpy.
  * L&#39;utilisation de `wandb login` n&#39;affiche plus d&#39;avertissement concernant `wandb.require("legacy-service")`.
  * L&#39;enregistrement d&#39;un `Table` (ou d&#39;autres objets qui créent des artefacts internes) ne lève plus `ValueError` lorsqu&#39;il est effectué depuis un run dont l&#39;ID contient des caractères spéciaux.
  * `wandb.Api` initialisé avec `base_url` respecte désormais l&#39;URL fournie, plutôt que l&#39;URL de la dernière connexion.

  Voir le [journal des modifications v0.20.0](https://github.com/wandb/wandb/releases/tag/v0.20.0) sur GitHub.
</Update>

<Update label="v0.19.11" description="07 mai 2025">
  ## Ajouts

  * Ajout de la création, de la suppression et de la mise à jour des registres dans le SDK.
  * Ajout de la propriété `artifact.is_link` aux artefacts pour déterminer si un artefact est un artefact lié (comme dans le registre) ou un artefact source.
  * Ajout de `artifact.linked_artifacts` pour récupérer tous les artefacts liés à un artefact source, et de `artifact.source_artifact` pour récupérer l’artefact source d’un artefact lié.
  * `run.link_artifact()`, `artifact.link()` et `run.link_model()` renvoient désormais l’artefact lié après la liaison.
  * Téléchargement multipart pour les fichiers d’artefact de plus de 2 Go ; l’utilisateur peut le contrôler directement avec `artifact.download(multipart=True)`.
  * Ajout de la propriété `Project.id` pour obtenir l’ID du projet sur un `wandb.public.Project`.
  * Nouvelle API publique pour les automatisations W&amp;B.
    * Nouveaux sous-modules et classes dans `wandb.automations.*` pour permettre la gestion programmatique des automatisations W&amp;B.
    * `Api.integrations()`, `Api.slack_integrations()`, `Api.webhook_integrations()` pour récupérer les intégrations Slack ou webhook existantes d’une équipe.
    * `Api.create_automation()`, `Api.automation()`/`Api.automations()`, `Api.update_automation()`, `Api.delete_automation()` pour créer, récupérer, modifier et supprimer des automatisations.
  * Création et modification d’automatisations déclenchées par des événements `RUN_METRIC_CHANGE`, c’est-à-dire lors de changements des valeurs de métriques d’un run (deltas absolus ou relatifs).
  * Possibilité de collecter des métriques de profilage pour les GPU Nvidia à l’aide de DCGM. Pour l’activer, définissez la variable d’environnement `WANDB_ENABLE_DCGM_PROFILING` sur `true`. Le service `nvidia-dcgm` doit être en cours d’exécution sur la machine. L’activation de cette fonctionnalité peut entraîner une augmentation de l’utilisation des ressources.

  ## Correctifs

  * `run.log_code` définit correctement la valeur `code_path` dans les configurations du run.
  * Utilisation correcte de `WANDB_CONFIG_DIR` pour déterminer le chemin du fichier de paramètres système.
  * Empêche les noms invalides pour `Artifact` et `ArtifactCollection` (qui les rendraient impossibles à enregistrer), en levant explicitement une `ValueError` lors d’une tentative d’attribution d’un nom invalide.
  * Empêche l’erreur pydantic `ConfigError` dans les environnements Pydantic v1 due à l’absence d’appel à `.model_rebuild()/.update_forward_refs()` sur des types générés avec des champs ForwardRef.
  * `wandb.init()` ne lève plus l’erreur `Permission denied` lorsque le répertoire wandb n’est ni accessible en écriture ni en lecture.
  * L’appel à `file.delete()` sur des fichiers interrogés via `api.Runs(...)` ne lève plus de `CommError`.
    * Bug introduit dans la version 0.19.1

  Consultez le [journal des modifications v0.19.11](https://github.com/wandb/wandb/releases/tag/v0.19.11) sur GitHub.
</Update>

<Update label="v0.19.10" description="22 avril 2025">
  ## Ajouts

  * Le nouveau paramètre `reinit="create_new"` fait en sorte que `wandb.init()` crée un nouveau run même si d’autres runs sont actifs, sans terminer les autres runs (contrairement à `reinit="finish_previous"`). Cela deviendra à terme la valeur par défaut.
  * Ajout de `Artifact.history_step` pour renvoyer l’étape de run la plus proche à laquelle des métriques de l’historique ont été enregistrées pour le run source de l’artefact.
  * Ajout de l’indicateur `data_is_not_path` pour ignorer les vérifications de fichiers lors de l’initialisation de `wandb.Html` avec une chaîne pointant vers un fichier.

  ## Modifications

  * `Artifact.download()` ne lève plus d’erreur lors de l’utilisation de `WANDB_MODE=offline` ou lorsqu’un run hors ligne existe.

  ## Suppressions

  * Suppression de l’argument `-q` / `--quiet` de la magic `wandb` dans IPython / Jupyter ; utilisez plutôt le paramètre de run `quiet`.

  ## Obsolète

  * Les méthodes `wandb.Run` suivantes sont obsolètes au profit de propriétés et seront supprimées dans une prochaine version (@kptkin dans https://github.com/wandb/wandb/pull/8925) :
    * `run.project_name()` est obsolète au profit de `run.project`
    * La méthode `run.get_url()` est obsolète au profit de `run.url`
    * La méthode `run.get_project_url()` est obsolète au profit de `run.project_url`
    * La méthode `run.get_sweep_url()` est obsolète au profit de `run.sweep_url`

  ## Correctifs

  * Correction d’une `ValueError` sous Windows lors de l’exécution d’un script W&amp;B depuis un autre disque.
  * Correction d’un problème où le paramètre `base_url` n’était pas fourni à `wandb.login`.
  * `wandb.Html()` ne lève plus `IsADirectoryError` avec une valeur correspondant à un répertoire sur le système de l’utilisateur.

  Consultez le [journal des modifications v0.19.10](https://github.com/wandb/wandb/releases/tag/v0.19.10) sur GitHub.
</Update>

<Update label="v0.19.9" description="1 avril 2025">
  ## Ajouts

  * Le paramètre `reinit` peut désormais être défini sur `"default"`.
  * Ajout de la prise en charge de la création d’URL de téléchargement de fichiers d’artefact à l’aide du nouveau schéma d’URL, avec le contexte d’appartenance à une collection d’artifacts.

  ## modification

  * Les valeurs booléennes du paramètre `reinit` sont obsolètes ; utilisez plutôt &quot;return&#95;previous&quot; et &quot;finish&#95;previous&quot;.
  * Le logger &quot;wandb&quot; est configuré avec `propagate=False` au moment de l’import, alors qu’auparavant cela se produisait au démarrage d’un run. Cela peut modifier les messages observés par le logger racine dans certains flux de travail.
  * Metaflow nécessite désormais le package `plum-dispatch`.
  * La contrainte de version sur `pydantic` a été assouplie pour prendre en charge à la fois v1 et v2.
  * Les types `pydantic` existants ont été adaptés pour être compatibles avec Pydantic v1.
  * `wandb.init(dir=...)` crée désormais tous les répertoires inexistants dans `dir` si son répertoire parent est accessible en écriture.
  * Le serveur prend désormais en charge la récupération de fichiers d’artefact en fournissant des informations supplémentaires sur la collection ; l’API Artifacts a été mise à jour pour utiliser les nouveaux points de terminaison.
  * Les méthodes paginées (et les paginateurs sous-jacents) qui acceptent un argument `per_page` n’acceptent désormais que des valeurs `int`. Les valeurs `per_page` par défaut sont désormais définies directement dans les signatures de méthode, et le passage explicite de `None` n’est plus pris en charge.

  ## Correctif

  * L’appel à `wandb.init()` dans un notebook termine les runs précédents, comme indiqué précédemment dans la documentation.
    * Bug introduit dans 0.19.0
  * Correction d’une erreur levée lors de la journalisation d’images `jpg`/`jpeg` contenant des données de transparence.
  * `wandb.init(resume_from=...)` fonctionne désormais sans avoir à spécifier explicitement l’`id` du run.
  * La suppression de fichiers avec l’API publique fonctionne de nouveau.
    * Bug introduit dans 0.19.1
  * Correction d’un problème empêchant l’affichage des fichiers multimédias dans l’interface utilisateur lors de l’enregistrement dans un run avec un bucket de stockage personnalisé.

  Consultez le [journal des modifications v0.19.9](https://github.com/wandb/wandb/releases/tag/v0.19.9) sur GitHub.
</Update>

<Update label="v0.19.8" description="4 mars 2025">
  ## Correctif

  * Les chemins des fichiers multimédias contenant des caractères spéciaux (?, *, ], [ ou ) n’entraînent plus l’échec du téléversement de fichiers dans `wandb-core`.

  ## modification

  * Le moniteur système échantillonne désormais les métriques toutes les 15 secondes par défaut, au lieu de 10 secondes.

  Consultez le [journal des modifications v0.19.8](https://github.com/wandb/wandb/releases/tag/v0.19.8) sur GitHub.
</Update>

<Update label="v0.19.7" description="21 février 2025">
  ## Ajouts

  * API de recherche du registre.

  ## modification

  * La contrainte sur moviepy a été modifiée en &gt;=1.0.0.
  * `wandb.init()` affiche des informations plus détaillées, en particulier lorsqu’il reste bloqué à réessayer après des erreurs HTTP.

  ## Suppression

  * Suppression du paramètre privé `x_show_operation_stats`.

  ## Correctif

  * Correction de la journalisation incorrecte d’une exception &quot;wandb.Video requires moviepy [...]&quot; lors de l’utilisation de moviepy v2.
  * `wandb.setup()` démarre correctement le processus de service interne ; ce comportement avait été involontairement cassé dans la version 0.19.2.
  * Correction de `TypeError: Object of type ... is not JSON serializable` lors de l’utilisation de types numériques numpy comme valeurs.

  Consultez le [journal des modifications v0.19.7](https://github.com/wandb/wandb/releases/tag/v0.19.7) sur GitHub.
</Update>

<Update label="v0.19.6" description="5 février 2025">
  ## Ajouts

  * Prise en charge de l’API Prometheus pour la collecte de métriques GPU Nvidia DCGM.

  ## modification

  * Les compteurs ECC des GPU Nvidia sont passés du mode agrégé au mode volatile.

  ## Correctif

  * Correction d’un problème de performances entraînant une instanciation lente de `wandb.Artifact`, ce qui ralentissait à son tour la récupération des artefacts dans diverses méthodes d’API.
  * Certaines erreurs de `wandb.Api` ont désormais une meilleure représentation sous forme de chaîne.
  * Artifact.add&#95;reference, lorsqu’il était utilisé avec des URI de fichier pour un répertoire et le paramètre name, ajoutait incorrectement la valeur de `name` au chemin des références de fichier.
  * Correction d’un bug qui faisait que `Artifact.add_reference()` avec `checksum=False` enregistrait de nouvelles versions d’artefacts de référence locaux sans modification de l’URI de référence.

  Consultez le [journal des modifications v0.19.6](https://github.com/wandb/wandb/releases/tag/v0.19.6) sur GitHub.
</Update>

<Update label="v0.19.5" description="29 janvier 2025">
  ## Ajouts

  * Ajout de `wandb login --base-url {host_url}` comme alias de connexion à `wandb login --host {host_url}`.

  ## Modifications

  * Désactivation temporaire de la collecte des statistiques d’utilisation du CPU par cœur.

  ## Correctifs

  * Correction d’un bug qui amenait le mode `offline` à effectuer des requêtes réseau lors de la journalisation d’artefacts média. Si vous utilisez une ancienne version de W&amp;B Server qui ne prend pas en charge le téléversement d’artefacts hors ligne, utilisez le paramètre `allow_offline_artifacts=False` pour revenir à l’ancien comportement compatible.
  * Extension des règles de nettoyage du nom d’artefact de tableau journalisé afin d’autoriser les tirets et les points. Cette mise à jour met ces règles en conformité avec les règles actuelles de nommage des artefacts. (Lettres, chiffres, traits de soulignement, tirets et points sont autorisés).
  * Prise en charge correcte des paramètres de rembobinage de run `fork_from` et `resume_from`.

  Consultez le [journal des modifications v0.19.5](https://github.com/wandb/wandb/releases/tag/v0.19.5) sur GitHub.
</Update>

<Update label="v0.19.4" description="16 janvier 2025">
  ## Correctifs

  * Correction des nombres d’appareils signalés de manière incorrecte et des mesures de cycle d’utilisation pour les TPU avec un seul appareil par puce / plusieurs appareils sur l’hôte, et amélioration de la robustesse de l’échantillonnage des métriques TPU.
  * Prise en charge des ID d’appareil TPU non consécutifs dans le moniteur système.

  Consultez le [journal des modifications v0.19.4](https://github.com/wandb/wandb/releases/tag/v0.19.4) sur GitHub.
</Update>

<Update label="v0.19.3" description="13 janvier 2025">
  ## Correctifs

  * Correction d’une régression de mise à jour de `wandb.Settings` dans `wandb.integration.metaflow`.

  Consultez le [journal des modifications v0.19.3](https://github.com/wandb/wandb/releases/tag/v0.19.3) sur GitHub.
</Update>

<Update label="v0.19.2" description="07 janvier 2025">
  ## Ajouts

  * Prise en charge de l’authentification JWT dans wandb-core.
  * Ajout de la prise en charge de la journalisation de graphiques personnalisés imbriqués.

  ## Modifications

  * L’appel à `wandb.init(mode="disabled")` ne désactive plus tous les runs suivants par défaut. Utilisez plutôt `wandb.setup(settings=wandb.Settings(mode="disabled"))`, ou définissez explicitement `mode="disabled"` dans chaque appel à `wandb.init()`.

  ## Correctifs

  * Le bouton d’arrêt interrompt correctement les runs dont le thread Python principal exécute du code C, est en veille, etc.
  * Suppression d’un affichage involontaire lors de l’inspection de `wandb.Api().runs()`.
  * Correction du téléversement d’artefacts volumineux lors de l’utilisation d’Azure Blob Storage.
  * La commande `wandb offline` n’ajoute plus de paramètre non pris en charge à `wandb.Settings`, ce qui résout `ValidationError`.
  * Correction d’une erreur lors de la réinitialisation d’un run, causée par l’accès à un attribut supprimé.
  * Correction d’un interblocage occasionnel lors de l’utilisation de `multiprocessing` pour mettre à jour un seul run à partir de plusieurs processus.
  * Prévention des erreurs dues à des bugs dans d’anciennes versions de `botocore &lt;  1.5.76`.
  * Correction de diverses vérifications portant sur une valeur de paramètre `anonymous` non valide.

  ## Suppressions

  * Le paramètre `reset` de la fonction `wandb.wandb_sdk.wandb_setup._setup()` a été supprimé. Notez qu’il s’agit d’une fonction privée, même s’il semble qu’elle soit utilisée en dehors du dépôt. Utilisez `wandb.teardown()` à la place de `_setup(reset=True)`.
  * Dans le module privé `wandb.wandb_sdk.wandb_setup`, les symboles `logger` et `_set_logger` ont été supprimés.

  ## Sécurité

  * Mise à niveau de la version de `github.com/go-git/go-git` vers 5.13.0 pour corriger la CVE-2025-21613.
  * Mise à niveau de la version de `golang.org/x/net` vers 0.33.0 pour corriger la CVE-2024-45338.

  Consultez le [journal des modifications v0.19.2](https://github.com/wandb/wandb/releases/tag/v0.19.2) sur GitHub.
</Update>

<Update label="v0.19.1" description="13 décembre 2024">
  ## Correctif

  * Correction d’un bug où la définition de `WANDB__SERVICE_WAIT` entraînait une exception pendant `wandb.init`.

  ## Modification

  * `run.finish()` affiche des informations plus détaillées dans le terminal et dans les notebooks Jupyter.
  * Amélioration du message d’erreur pour les appels à `tensorboard.patch()` qui échouent afin d’indiquer qu’il est possible d’appeler d’abord `tensorboard.unpatch()`.
  * Ajout de `projectId` à la mutation `deleteFiles` si le serveur le prend en charge.

  ## Sécurité

  * Mise à niveau de la version de `golang.org/x/crypto` vers 0.31.0 pour corriger la CVE-2024-45337.

  Consultez le [journal des modifications de la v0.19.1](https://github.com/wandb/wandb/releases/tag/v0.19.1) sur GitHub.
</Update>

<Update label="v0.19.0" description="05 décembre 2024">
  ## Changements notables

  Cette version abandonne Python 3.7 et supprime la méthode `wandb.Run.plot_table`.
  Cette version ajoute `pydantic>=2.6,<33` comme dépendance.

  ## Modification

  * Le comportement par défaut est désormais de ne pas créer de job W&amp;B.
  * Ajout de `pydantic>=2.6,<33` comme dépendance (@dmitryduev dans https://github.com/wandb/wandb/pull/8649 &amp; estellazx.
    dans https://github.com/wandb/wandb/pull/8905)

  ## Suppression

  * Suppression de la méthode `wandb.Run.plot_table`. La fonctionnalité reste disponible et doit désormais être utilisée via `wandb.plot_table`, qui est la méthode recommandée pour cette fonctionnalité.
  * Fin de la prise en charge de Python 3.7.

  ## Correctif

  * Correction du rapport `ultralytics` lorsqu’il n’y a pas d’exemples positifs dans un lot de validation.
  * L’affichage de débogage de l’algorithme d’arrêt hyperband imprimait un caractère par ligne.
  * Ajout de l’argument `log_params` manquant lors de l’appel de la fonction `wandb_callback` de lightgbm.

  Consultez le [journal des modifications de la v0.19.0](https://github.com/wandb/wandb/releases/tag/v0.19.0) sur GitHub.
</Update>

<Update label="v0.18.7" description="13 novembre 2024">
  ## Ajouts

  * Ajout de `create_and_run_agent` à `__all__` dans `wandb/sdk/launch/__init__.py` afin de l’exposer comme une API publique.

  ## Modification

  * Lors de la synchronisation, les tableaux enregistrés en mode hors ligne utilisent désormais des clés mises à jour pointant vers les chemins d’artefact. Pour revenir à l’ancien comportement, utilisez le paramètre `allow_offline_artifacts = False`.

  ## Obsolète

  * L’argument `quiet` de `wandb.run.finish()` est obsolète ; utilisez `wandb.Settings(quiet=...)` pour le définir à la place.

  ## Correctif

  * Correction de `api.artifact()` pour transmettre correctement l’argument `enable_tracking` à la méthode `Artifact._from_name()`.

  Consultez le [journal des modifications de la v0.18.7](https://github.com/wandb/wandb/releases/tag/v0.18.7) sur GitHub.
</Update>

<Update label="v0.18.6" description="06 novembre 2024">
  ## Ajouts

  * Ajout d’un paramètre booléen `overwrite` à `Artifact.add()`/`Artifact.add_file()` pour permettre l’écrasement de fichiers d’artefact ajoutés précédemment.

  ## Correctif

  * Ajout des indications de type manquantes pour le module `wandb.plot` dans le stub du package.
  * Correction de la limitation des téléversements d’artefacts de référence Azure à `max_objects`.
  * Correction du téléchargement des artefacts de référence Azure avec `skip_cache=True`.
  * Correction des téléversements multipart pour les fichiers dont aucun type de contenu n’est défini dans les en-têtes.
  * Correction de tensorboard qui ne parvenait pas à se synchroniser lors de la journalisation de lots d’images.
  * Correction du comportement de `mode='x'`/`mode='w'` dans `Artifact.new_file()` pour le rendre conforme aux modes de fichier intégrés de Python.
  * Ne plus ignorer le paramètre `distribution` lors de la configuration des paramètres de balayage depuis le SDK.

  ## Modification

  * Ajout de la méthode interne `api._artifact()` pour récupérer des artefacts afin que des événements d’utilisation ne soient pas créés lorsqu’elle n’est pas appelée par un utilisateur externe.
  * Modification du `mode` par défaut dans `Artifact.new_file()` de `'w'` à `'x'` afin de refléter fidèlement le comportement par défaut existant.

  Consultez le [journal des modifications de la v0.18.6](https://github.com/wandb/wandb/releases/tag/v0.18.6) sur GitHub.
</Update>

<Update label="v0.18.5" description="17 octobre 2024">
  ## Correctif

  * Import de `Literal` depuis `typing_extensions` en Python 3.7 ; cassé dans la version 0.18.4.

  Consultez le [journal des modifications de la v0.18.5](https://github.com/wandb/wandb/releases/tag/v0.18.5) sur GitHub.
</Update>

<Update label="v0.18.4" description="17 octobre 2024">
  ## Ajouts

  * Suivi de métriques détaillées pour les systèmes Apple ARM, notamment l’utilisation du GPU, de l’eCPU et du pCPU, la consommation électrique, la température, ainsi que l’utilisation de la mémoire et du swap.
  * Possibilité, pour les utilisateurs, de lier des artefacts du registre sans saisir le nom de l’entité de l’organisation.
  * Ajout d’un message d’avertissement indiquant que l’argument `fps` sera ignoré lors de la création d’un objet `wandb.Video` à partir d’une chaîne représentant un chemin de fichier ou d’un objet bytes.
  * Mise à jour des docstrings des méthodes `logged_artifacts` et `used_artifacts` de la classe `Run`.
  * Le paramètre `_show_operation_stats` active un aperçu d’une meilleure expérience utilisateur pour `run.finish()`.

  ## Correctifs

  * Enregistrement de la consommation électrique sur les GPU de la série AMD MI300X.
  * Correction d’un problème de typage dans `wandb.Api`.
  * Garantie que les objets artefact sont entièrement mis à jour lors de `Artifact.save()`.

  ## Modifications

  * Mise à jour de la version minimale de `sentry-sdk` à 2.0.0 pour résoudre les avertissements de dépréciation.

  Consultez le [journal des modifications v0.18.4](https://github.com/wandb/wandb/releases/tag/v0.18.4) sur GitHub.
</Update>

<Update label="v0.18.3" description="01 octobre 2024">
  ## Ajouts

  * Ajout de la possibilité de surveiller les métriques d’utilisation des appareils Cloud TPU de Google.

  ## Correctifs

  * Capture des statistiques des GPU Nvidia sous Windows.
  * Correction d’une régression introduite dans la v0.18.2 qui affectait la capture des noms des GPU Nvidia.
  * `run.log_artifact()` ne bloque plus les autres téléversements de données jusqu’à la fin du téléversement de l’artefact.
  * Correction de la dépendance multimédia pour rdkit, mise à jour de `rdkit-pypi` vers `rdkit`.
  * L’enregistrement d’un artefact contenant de nombreux fichiers volumineux n’épuise plus les threads du système d’exploitation.

  ## Modifications

  * Après `artifact = run.log_artifact()`, vous devez utiliser `artifact.wait()` avant toute opération qui suppose que l’artefact a déjà été téléversé. Auparavant, ce n’était pas nécessaire dans certains cas, car `run.log_artifact()` bloquait les autres opérations du run.

  Consultez le [journal des modifications v0.18.3](https://github.com/wandb/wandb/releases/tag/v0.18.3) sur GitHub.
</Update>

<Update label="v0.18.2" description="27 septembre 2024">
  ## Ajouts

  * Ajout de la méthode `upsert_run_queue` à `wandb.Api`.
  * Ajout du paramètre `tags` à `wandb.Api.artifacts()` pour filtrer les artefacts par tag.

  ## Correctifs

  * Mise à jour de la signature et de la docstring de `wandb.api.public.runs.Run.log_artifact()` pour prendre en charge les tags d’artefact, comme pour les instances `Run` renvoyées par `wandb.init()`.
  * Ajout de la docstring de `wandb.watch` pour prendre en charge l’autocomplétion.
  * Correction de la correspondance glob dans define metric afin de fonctionner avec des clés enregistrées contenant `/`.
  * Autorisation de la syntaxe `a\.b` dans run.define&#95;metric pour se référer à un nom de métrique contenant des points.
    * REMARQUE : non corrigé si vous utilisez `wandb.require("legacy-service")`
  * Correction de l’erreur de format d’image inconnu lors du téléversement d’un gif via tensorboard.
  * Correction de `OSError` lors de l’appel à `Artifact.add_file` avec des chemins de fichier sur des systèmes de fichiers montés.
  * Compatibilité rétablie pour les versions de macOS &lt;== 10.15 pour wandb-core.

  Consultez le [journal des modifications v0.18.2](https://github.com/wandb/wandb/releases/tag/v0.18.2) sur GitHub.
</Update>

<Update label="v0.18.1" description="16 septembre 2024">
  ## Correctifs

  * Possibilité pour tous les utilisateurs de lire les fichiers de cache lorsque core est activé.
  * Les scalaires infinis enregistrés dans TensorBoard sont correctement téléversés au lieu d’être ignorés.
  * Respect correct de `WANDB_ERROR_REPORTING=false`. Cela corrige une régression introduite dans la 0.18.0.

  ## Modifications

  * Suppression de la journalisation sentry pour sendLinkArtifact.
  * Capture par défaut de requirements.txt dans Run.log&#95;code.

  Consultez le [journal des modifications v0.18.1](https://github.com/wandb/wandb/releases/tag/v0.18.1) sur GitHub.
</Update>

<Update label="v0.18.0" description="11 septembre 2024">
  ## Changements notables

  Cette version bascule `wandb` vers un nouveau backend en activant `wandb.require("core")` par défaut. Cela ne devrait pas constituer une modification incompatible, mais le nouveau backend peut présenter des différences de comportement inattendues pour certaines fonctionnalités héritées et dans de rares cas limites.

  ## Ajouts

  * Ajout de la prise en charge des tags d&#39;artefact via `Artifact.tags` et `Run.log_artifact()`.

  ## Correctif

  * Détection du nom du notebook dans le serveur Jupyter intégré de VS Code.

  ## modification

  * Le nouveau backend « core », auparavant activé avec `wandb.require("core")`, est désormais utilisé par défaut. Pour revenir au comportement hérité, ajoutez `wandb.require("legacy-service")` au début de votre script. Remarque : dans une prochaine version mineure, l&#39;option permettant de désactiver ce nouveau comportement sera supprimée.

  Consultez le [journal des modifications v0.18.0](https://github.com/wandb/wandb/releases/tag/v0.18.0) sur GitHub.
</Update>

<Update label="v0.17.9" description="05 septembre 2024">
  ## modification

  * L&#39;intervalle d&#39;échantillonnage par défaut des métriques système passe à 10 secondes, sans moyenne, avec la possibilité de définir des intervalles personnalisés via `wandb.init(settings=wandb.Settings(_stats_sampling_interval=...))`.

  ## Obsolète

  * `define_metric(summary='best', goal=...)` est obsolète et sera bientôt supprimé ; utilisez `define_metric(summary='min')` ou `define_metric(summary='min')` à la place.

  Consultez le [journal des modifications v0.17.9](https://github.com/wandb/wandb/releases/tag/v0.17.9) sur GitHub.
</Update>

<Update label="v0.17.8" description="28 août 2024">
  ## Ajouts

  * Capture de la fréquence d&#39;horloge SM (Streaming Multiprocessor), de la mémoire et du processeur graphique (MHz), du nombre d&#39;erreurs corrigées/non corrigées, de la vitesse du ventilateur (%) et de l&#39;utilisation de l&#39;encodeur sur les GPU Nvidia lors de l&#39;utilisation de core.
  * Possibilité d&#39;itérer sur `wandb.Config` comme sur un dictionnaire.
  * Les courbes PR, les images et les histogrammes sont pris en charge lors de l&#39;utilisation de TensorBoard avec core activé.
  * Ajout de `wandb.require("legacy-service")` comme option de désactivation équivalente à `wandb.require("core")`.

  ## Correctif

  * Évite de laisser le processus wandb-core en cours d&#39;exécution si l&#39;utilisateur appuie deux fois sur Ctrl+C.
  * Correction des avertissements de dépréciation liés à NumPy &gt;= 2.1 en supprimant l&#39;argument `newshape` de `numpy.reshape`.
  * Ignore le téléversement/téléchargement des entrées de manifeste d&#39;artefact de référence GCS correspondant à des dossiers.

  ## Obsolète

  * La possibilité de désactiver le processus de service (`WANDB__DISABLE_SERVICE`) est obsolète et sera supprimée dans la prochaine version mineure.

  Consultez le [journal des modifications v0.17.8](https://github.com/wandb/wandb/releases/tag/v0.17.8) sur GitHub.
</Update>

<Update label="v0.17.7" description="15 août 2024">
  ## Correctif

  * Garantit la capture des métriques GPU Nvidia si `libnvidia-ml.so` n&#39;est pas disponible lors de l&#39;utilisation de core.
  * Autorise `define_metric("x", step_metric="x")` lors de l&#39;utilisation de core.
  * Téléverse correctement les fichiers vides lors de l&#39;utilisation de core.
  * Corrige la panique occasionnelle « send on closed channel » lors de la finalisation d&#39;un run avec core.

  Consultez le [journal des modifications v0.17.7](https://github.com/wandb/wandb/releases/tag/v0.17.7) sur GitHub.
</Update>

<Update label="v0.17.6" description="08 août 2024">
  ## Ajouts

  * Spécifiez les schémas d&#39;entrée de job lors de l&#39;appel à manage&#95;config&#95;file ou manage&#95;wandb&#95;config afin de proposer une interface plus agréable lors du lancement du job.
  * Utilise le système de fichiers plutôt que des messages protobuf pour transporter les manifestes comportant plus de 100k entrées vers le processus core.
  * Ajoute le constructeur `box3d` pour `Box3D`.

  ## modification

  * `run.define_metric()` lève une erreur lorsqu&#39;on lui fournit des arguments superflus.
  * En mode désactivé, utilisez la classe `wandb.sdk.wandb_run.Run` au lieu de `wandb.sdk.wandb_run.RunDisabled`, qui a été supprimée.
  * Lorsque `WANDB_MODE = offline`, l&#39;appel à `artifact.download()` génère désormais une erreur au lieu de rester bloqué.

  ## Correctif

  * Gère correctement les stubs de répertoire lors du journal d&#39;un artefact externe dans un compte de stockage Azure avec Hierarchical Namespace activé.
  * Correction de la docstring de `api.runs()` concernant l&#39;ordre de tri par défaut, oubliée dans https://github.com/wandb/wandb/pull/7675.

  Consultez le [journal des modifications v0.17.6](https://github.com/wandb/wandb/releases/tag/v0.17.6) sur GitHub.
</Update>

<Update label="v0.17.5" description="19 juillet 2024">
  ## Ajouts

  * Avec wandb-core, prise en charge des téléversements multipart vers S3.

  ## Modification

  * `run.finish()` peut lever une exception dans des cas où il appelait auparavant `os._exit()`.
  * `run.link_artifact()` peut désormais faire remonter des erreurs du serveur.

  ## Correctif

  * Gestion des `path_prefix` qui ne correspondent pas à des noms de répertoire lors du téléchargement d&#39;artefacts.
  * Correction de la création ou de la mise à jour d&#39;un artefact avec l&#39;indicateur `incremental=True`.
  * Utilisation de macros `resource_arg` renseignées lors de la soumission de jobs W&amp;B Launch à AmazonSageMaker.

  Consultez le [journal des modifications v0.17.5](https://github.com/wandb/wandb/releases/tag/v0.17.5) sur GitHub.
</Update>

<Update label="v0.17.4" description="03 juillet 2024">
  ## Ajouts

  * Prise en charge des variables de modèle de file d&#39;attente dans les jobs du planificateur de balayage Launch.

  ## Correctif

  * Utilisation de `sys.exit()` au lieu de `os._exit()` si un sous-processus interne se termine avec un code non nul.
  * Correction d&#39;une condition de concurrence occasionnelle lors de l&#39;utilisation de `core`, qui pouvait affecter les journaux de run.
  * Correction de l&#39;OSError sur `Artifact.download(skip_cache=True)` en présence de systèmes de fichiers différents.

  Consultez le [journal des modifications v0.17.4](https://github.com/wandb/wandb/releases/tag/v0.17.4) sur GitHub.
</Update>

<Update label="v0.17.3" description="24 juin 2024">
  ## Correctif

  * Le fichier netrc est correctement nommé `_netrc` sous Windows.
  * Lorsque core est activé, les barres `tqdm` imbriquées s&#39;affichent correctement dans l&#39;onglet Logs.
  * Correction de la regex ECR du registre W&amp;B Launch qui séparait le tag sur la barre oblique et le point.

  Consultez le [journal des modifications v0.17.3](https://github.com/wandb/wandb/releases/tag/v0.17.3) sur GitHub.
</Update>

<Update label="v0.17.2" description="17 juin 2024">
  ## Ajouts

  * Ajout de runs antérieurs lors de la création d&#39;un balayage depuis la CLI.

  ## Correctif

  * Correction de problèmes de prise en charge de `numpy>=2`.
  * Correction de l&#39;erreur « UnicodeDecodeError: &#39;charmap&#39; » à l&#39;ouverture de fichiers HTML sous Windows en spécifiant l&#39;encodage UTF-8.
  * `Artifact.delete()` sur des artefacts liés supprime désormais uniquement le lien, et non l&#39;artefact source.
  * Les runs de balayage n&#39;apparaissent plus comme repris alors qu&#39;ils ne le sont pas, par @TimH98 https://github.com/wandb/wandb/pull/7684.

  ## Modification

  * Mise à niveau de github.com/vektah/gqlparser/v2 de 2.5.11 à 2.5.16.

  Consultez le [journal des modifications v0.17.2](https://github.com/wandb/wandb/releases/tag/v0.17.2) sur GitHub.
</Update>

<Update label="v0.17.1" description="07 juin 2024">
  ## Ajouts

  * Ajout de `api.runs().histories()` pour récupérer les métriques d&#39;historique des runs qui répondent aux conditions spécifiées.
  * Affichage d&#39;un avertissement lorsqu&#39;un pod Kubernetes ne peut pas être planifié.
  * Ajout de `ArtifactCollection.save()` pour permettre d&#39;enregistrer les modifications.
  * Ajout de la possibilité d&#39;écraser l&#39;historique de runs précédents à une étape arbitraire et de continuer la journalisation à partir de cette étape.
  * Ajout d&#39;une nouvelle API espace de travail pour modifier les Workspaces W&amp;B par programmation.
  * Ajout de `Artifact.unlink()` pour permettre de dissocier des artefacts par programmation.
  * Ajout d&#39;une prise en charge de base de TensorBoard lors de l&#39;exécution avec `wandb.require("core")`.
    * L&#39;onglet TensorBoard dans W&amp;B fonctionne.
    * Les graphiques s&#39;affichent dans W&amp;B, potentiellement mieux que lors d&#39;une exécution sans core.
    * Tous les types de données ne sont pas encore pris en charge. Les données non prises en charge ne sont pas affichées dans les graphiques.

  ## Correctif

  * Correction du comportement de `define_metric` pour plusieurs métriques en mode `shared`.
  * Transmission correcte du nom du projet à l&#39;API interne depuis le run lors de l&#39;appel à `run.use_artifact()`.
  * Téléversement correct des fichiers journal de sortie de console lors de la reprise de runs activés avec le paramètre `console_multipart` par @kptkin dans https://github.com/wandb/wandb/pull/7694 et @dmitryduev dans https://github.com/wandb/wandb/pull/7697.
  * Interprétation des chaînes non octales avec des zéros initiaux comme des chaînes et non comme des entiers dans les configurations de balayage par @KyleGoyette https://github.com/wandb/wandb/pull/7649.
  * Prise en charge du format d&#39;URI de dépôt Azure dans Launch @KyleGoyette https://github.com/wandb/wandb/pull/7664.
  * Correction de l&#39;analyse des chemins pour les artefacts avec des alias contenant des barres obliques.
  * Ajout des docstrings manquantes pour toutes les méthodes publiques de la classe `Api`.
  * Ajout correct du dernier alias aux jobs générés par le générateur de jobs @KyleGoyette https://github.com/wandb/wandb/pull/7727.

  ## Modifications

  * Option permettant de modifier le schéma de nommage des journaux de sortie de console, de `output.log` à `logs/YYYYMMDD_HHmmss.ms_output.log`.
  * `unsafe=True` est désormais requis dans les appels `use_model` susceptibles de charger et de désérialiser des fichiers pickle non sûrs par @anandwandb https://github.com/wandb/wandb/pull/7663.
  * Mise à jour de l&#39;ordre dans `api.runs()` en ordre croissant pour éviter les réponses dupliquées par @thanos-wandb https://github.com/wandb/wandb/pull/7675.
  * Élimination des erreurs d&#39;expiration d&#39;URL signée pendant le téléversement de fichiers d&#39;artefact dans core.

  ## Obsolète

  * `ArtifactCollection.change_type()` est obsolète au profit de `ArtifactCollection.save()`.

  Consultez le [journal des modifications v0.17.1](https://github.com/wandb/wandb/releases/tag/v0.17.1) sur GitHub.
</Update>

<Update label="v0.17.0" description="07 mai 2024">
  ## Changements notables

  `wandb.plots` a été renommé en `wandb.plot`, toutes les intégrations ont été renommées de `wandb.<name>` en `wandb.integration.<name>`, et l’extra `[async]` a été supprimé.

  Cette version intègre le binaire `wandb-core`, auparavant installé par le package Python `wandb-core` sur PyPI. Le package `wandb-core` n’est désormais plus utilisé et peut être désinstallé.

  ## Ajouts

  * Le package `wandb` inclut désormais le binaire `wandb-core`.
    * `wandb-core` est un nouveau backend amélioré pour le SDK W&amp;B, axé sur les performances, la polyvalence et la robustesse.
    * Actuellement, il doit être activé explicitement. Pour commencer à utiliser ce nouveau backend, ajoutez `wandb.require("core")` à votre script après avoir importé `wandb`.
  * `wandb-core` prend désormais en charge la mise en cache des fichiers d’Artifacts par @moredatarequired dans https://github.com/wandb/wandb/pull/7364 et https://github.com/wandb/wandb/pull/7366.
  * Ajout des méthodes artifact&#95;exists() et artifact&#95;collection&#95;exists() à Api pour vérifier si un artefact ou une collection existe.
  * `wandb launch -u <git-uri | local-path> ` crée et lance un job à partir du code source fourni.

  ## Correctifs

  * Empêche un plantage sur `run.summary` pour les runs terminés.
  * Signale correctement les erreurs de téléversement de fichiers lors de l’utilisation de `wandb-core`.
  * Met en œuvre une vérification plus stricte de la disponibilité des GPU AMD.
  * Correction de `run.save()` sous Windows.
  * Affiche un avertissement au lieu d’échouer lors de l’utilisation de registres autres que ECR et GAR avec le builder Kaniko.
  * Correction de la signature de type de `wandb.init()` pour inclure `None`.

  ## Modifications

  * Lors de l’utilisation de `wandb-core`, vous devez désormais spécifier un indicateur obligatoire (`wandb.require("core")`) pour l’activer, alors qu’auparavant il était activé automatiquement.
  * Utilise des ETags au lieu des hachages MD5 pour les artefacts de référence GCS.

  ## Suppressions

  * Suppression des fonctions obsolètes `wandb.plots.*` et des intégrations tierces de premier niveau `wandb.[catboost,fastai,keras,lightgbm,sacred,xgboost]`. Veuillez utiliser `wandb.plot` au lieu de `wandb.plots` et `wandb.integration.[catboost,fastai,keras,lightgbm,sacred,xgboost]` au lieu de `wandb.[catboost,fastai,keras,lightgbm,sacred,xgboost]`.
  * Suppression de l’extra `[async]` et du paramètre `_async_upload_concurrency_limit`.
  * Suppression des paramètres non documentés : `_except_exit` et `problem`.

  Consultez le [journal des modifications v0.17.0](https://github.com/wandb/wandb/releases/tag/v0.17.0) sur GitHub.
</Update>

<Update label="v0.16.6" description="03 avril 2024">
  ## Ajouts

  * Ajout de la prise en charge du remplacement des paramètres du builder kaniko dans la configuration de l’agent.
  * Ajout d’un lien vers l’espace de travail du projet d’un run dans le pied de page.
  * Ajout de la prise en charge du remplacement du délai de grâce des runs arrêtés dans la configuration de l’agent.
  * Ajout du paramètre (`_disable_update_check`) pour désactiver les vérifications de version pendant l’initialisation.
  * `WandbLogger.sync` dans l’intégration OpenAI Fine-Tuning reçoit un nouvel argument booléen `log_datasets` pour désactiver la journalisation automatique des jeux de données dans Artifacts.
  * Réduction de la fréquence d’affichage par défaut du statut de l’agent Launch. Ajout de contrôles de verbosité permettant d’augmenter la fréquence d’affichage du statut et d’imprimer les informations de débogage sur stdout.

  ## Modifications

  * Limitation de l’option de politique sur le `put()` de la CLI artifact aux choix [&quot;mutable&quot;, &quot;immutable&quot;].
  * Mise à jour des méthodes de l’API publique des artefacts pour gérer le champ `Project` nullable sur le type `ArtifactSequence/ArtifactCollection`, sur la base des changements du serveur gorilla.

  ## Correctifs

  * Correction de `run.save()` qui ne fonctionnait pas avec les fichiers dans `run.dir`, introduit dans la version précédente.
  * Correction d’une panique rare lors de téléversements volumineux d’artefacts.
  * Correction de `wandb.login` qui empêchait l’association des runs à la file d’attente Launch.
  * Correction d’un échec silencieux du téléchargement de l’artefact de job, provoquant le plantage du run lors de l’utilisation de W&amp;B Launch par @KyleGoyette https://github.com/wandb/wandb/pull/7285.
  * Correction de la gestion de l’enregistrement des fichiers d’entraînement dans Artifacts dans l’intégration OpenAI Fine-Tuning.

  Consultez le [journal des modifications v0.16.6](https://github.com/wandb/wandb/releases/tag/v0.16.6) sur GitHub.
</Update>

<Update label="v0.16.5" description="25 mars 2024">
  ## Ajouts

  * Ajout d’une fonctionnalité permettant de déplacer les fichiers de staging vers le cache (au lieu de les copier) pour le téléversement de fichiers d’artefacts mutables lorsque la mise en cache est activée.
  * Ajout de la prise en charge permettant d’ignorer la mise en cache des fichiers sur le système de fichiers local lors du téléversement de fichiers vers des artefacts.
  * Ajout de la prise en charge permettant d’ignorer le staging des fichiers d’artefacts pendant le téléversement en sélectionnant une stratégie de stockage.
  * Prise en charge préliminaire du fork d’un run à l’aide de `wandb.init(fork_from=...)`.
  * `run.save()` accepte les valeurs `pathlib.Path`.

  ## Modifications

  * Lors de l’affichage du lien du run, celui-ci pointe désormais explicitement vers l’espace de travail.

  ## Correctif

  * En cas de problèmes transitoires du serveur lors de la création du secret Kubernetes pour la clé API wandb, jusqu’à 5 nouvelles tentatives seront effectuées.

  ## Suppressions

  * Lors de l’affichage des informations du run dans le terminal, suppression des liens vers les jobs.

  Consultez le [journal des modifications de la v0.16.5](https://github.com/wandb/wandb/releases/tag/v0.16.5) sur GitHub.
</Update>

<Update label="v0.16.4" description="05 mars 2024">
  ## Ajouts

  * Ajout de la possibilité de modifier les types de collections d’artifacts.
  * Ajout de la prise en charge de l’installation des dépendances à partir de pyproject.toml.
  * Prise en charge du build kaniko avec un PVC et une configuration Docker fournis par l’utilisateur.
  * Ajout de la possibilité d’importer des runs entre des instances W&amp;B.

  ## Modifications

  * Wandb-core limite le débit des requêtes vers le backend et respecte les en-têtes RateLimit-*.
    par @timoffex dans https://github.com/wandb/wandb/pull/7065

  ## Correctif

  * Correction de la transmission des variables de modèle dans le planificateur sweeps-on-launch.
  * Liaison de l’artefact de job à un run afin qu’il puisse être spécifié comme entrée.
  * Correction de l’entrypoint SageMaker pour utiliser l’entrypoint fourni.
  * Analyse correcte des en-têtes de téléversement.
  * Propagation correcte des erreurs du serveur.
  * Accélération du téléversement des fichiers grâce au parallélisme.
  * Ne pas envoyer les données git si elles ne sont pas renseignées.
  * Correction de la reprise de la journalisation dans la console afin d’éviter l’écrasement.
  * Suppression de la validation du nom d’hôte lors de l’utilisation de --host avec wandb login.
  * Ne pas supprimer les visualisations précédentes lors de la reprise d’un run.
  * Éviter les nouvelles tentatives en cas de code de statut de conflit.
  * Correction de la fusion de la configuration de visualisation pour les runs repris dans wandb-core.
  * Remplacement de l’utilisation du module json de la bibliothèque standard par celui de `segmentio`.
  * Suppression de stderr comme writer pour les journaux.
  * Désactivation des étapes négatives à l’initialisation.
  * Correction du chargement des rapports dans pydantic26.
  * Annulation de « make upload request async to support progress reporting (#6497) ».
  * Correction de la spécification de l’entrypoint lors de l’utilisation d’un Dockerfile.wandb.
  * Correction de la libération trop précoce du handle de sonde du flux.
  * Toujours tenter de récupérer la dernière image pour le conteneur local.

  Consultez le [journal des modifications de la v0.16.4](https://github.com/wandb/wandb/releases/tag/v0.16.4) sur GitHub.
</Update>

<Update label="v0.16.3" description="06 février 2024">
  ## Améliorations

  * Générer les informations de type de données dans le cœur.
  * Ajouter la prise en charge de Launch.
  * Ajout d&#39;une option pour contrôler le nombre de runs échantillonnés groupés dans les rapports.
  * Ajouter un mode partagé pour permettre à plusieurs rédacteurs indépendants d&#39;écrire dans le même run.
  * Réduire la latence de téléchargement des artefacts grâce à une copie facultative du cache et à des threads.
  * Ajouter le téléchargement partiel de fichiers via un préfixe de répertoire.
  * Mettre à jour l&#39;intégration Diffusers.
  * Mettre à jour l&#39;intégration Ultralytics.
  * Ajouter le Logger Fabric de PyTorch Lightning.
  * Mettre à jour les packages Go.

  ## Correctifs

  * Supprimer les champs projet et runner de la configuration de l&#39;agent.
  * Reconnaître les jobs k8s supprimés comme ayant échoué.
  * Signaler les champs supplémentaires dans le bloc d&#39;environnement par un avertissement au lieu de générer une erreur.
  * Corriger un bug de remplacement d&#39;entité où ENVVAR est prioritaire sur kwargs.
  * Le runner de conteneur local n&#39;ignore pas les arguments de remplacement.
  * Fusionner la configuration avec la configuration balayage/launch.
  * Corriger la logique de nouvelle tentative dans wandb-core et le conftest de system&#95;tests.
  * Utiliser des verrous RW dans la gestion des ressources du moniteur système.
  * Définir le contexte de build sur le répertoire de l&#39;entrypoint s&#39;il contient Dockerfile.wandb.
  * Sécurité (Launch) : monter la clé API wandb dans les pods de job Launch à partir d&#39;un secret k8s.
  * Wandb job create ne doit pas rechercher requirements.txt si Dockerfile.wandb se trouve à côté de l&#39;entrypoint.
  * Corriger &#95;parse&#95;path lorsque seul l&#39;id est transmis à wandb.Api().run().
  * Mettre à jour video.py : corriger le bug de fps.
  * Nettoyer les dossiers temporaires.
  * Corriger les chemins d&#39;artefacts trop longs sous Windows.
  * Mettre à jour l&#39;API Report pour qu&#39;elle fonctionne avec pydantic2.6.
  * Récupérer tous les commits pour permettre le checkout par sha.
  * Ne pas masquer les exceptions dans pyagent.
  * Ajouter la progression du téléversement des fichiers d&#39;artefact dans nexus.
  * Faire preuve de prudence dans le moniteur système lorsque rocm-smi est installé sur un système sans GPU AMD.
  * Corriger une faute de frappe dans cli.py.
  * Supprimer deadsnakes de l&#39;étape de build de l&#39;accélérateur.

  Consultez le [journal des modifications v0.16.3](https://github.com/wandb/wandb/releases/tag/v0.16.3) sur GitHub.
</Update>

<Update label="v0.16.2" description="09 janvier 2024">
  ## Améliorations

  * Refactoriser la logique du store et ajouter un writer de store.
  * Ajouter la surveillance des GPU AMD.
  * Ajouter le téléversement du fichier de journal de la console.
  * Ajouter un champ d&#39;URI de registre aux builders.
  * Ajouter la fonctionnalité `wandb beta sync` pour téléverser des runs vers W&amp;B.
  * La CLI prend en charge les paramètres de file d&#39;attente autorisés.
  * Ajouter la prise en charge de requirements et de patch.diff.
  * Capturer les variables d&#39;environnement liées à SLURM dans les métadonnées.
  * Ajouter l&#39;indicateur --priority à la commande `wandb launch` pour spécifier la priorité lors de la mise en file d&#39;attente des jobs.
  * Ajouter une fonctionnalité de vérification à wandb login.
  * Sweeps sur Launch respecte la priorité de job sélectionnée pour les runs de balayage.
  * Lancer l&#39;opération SDKrab.
  * Rendre la requête de téléversement asynchrone pour permettre le suivi de la progression.
  * Ajouter un observateur du répertoire de journal TensorBoard.
  * Téléverser les fichiers wandb-summary.json et config.yaml.

  ## Correctifs

  * Mettre à jour le message d&#39;erreur et supprimer extra.
  * Corriger les problèmes liés à l&#39;envoi de fichiers.
  * Ajouter de la gigue à la stratégie de backoff des clients de nouvelle tentative.
  * Ignorer le téléchargement du fichier uniquement si le condensat correspond.
  * Corriger la reprise et ajouter des tests.
  * Capturer les erreurs lors de la création d&#39;un job k8s à partir d&#39;un fichier yaml.
  * Obtenir l&#39;entité par défaut avant de vérifier les variables de modèle.
  * Supprimer la création de run pour les téléchargements d&#39;artefacts si le cœur n&#39;est pas utilisé.
  * Récupérer l&#39;ETag pour ObjectVersion au lieu de Object pour les buckets versionnés.
  * Annuler #6759 et lire l&#39;ETag de la version d&#39;objet sur place.
  * Rétablir le nombre de fichiers téléversés.
  * Ajouter l&#39;envoi d&#39;une requête d&#39;annulation à l&#39;expéditeur.
  * Vérifier les erreurs dans la surveillance de la mémoire.
  * Ajouter l&#39;indicateur job&#95;type dans la CLI pour permettre de remplacer job&#95;type.
  * Gérer le refactoring de l&#39;import des utilitaires ultralytics.
  * Télécharger les artefacts avec wandb-core sans run actif.
  * Corriger la journalisation des erreurs du graphique de dispersion matplotlib si la version minimale de la bibliothèque plotly n&#39;est pas respectée.
  * Autoriser le remplacement de l&#39;emplacement .netrc par défaut avec la variable d&#39;environnement NETRC.

  Consultez le [journal des modifications v0.16.2](https://github.com/wandb/wandb/releases/tag/v0.16.2) sur GitHub.
</Update>

<Update label="v0.16.1" description="05 décembre 2023">
  ## Améliorations

  * Supprime le téléchargement récursif.
  * Ajoute un résumé avec debounce dans le gestionnaire.
  * Corrige un bug dans l&#39;import d&#39;ultralytics et l&#39;épinglage de version.
  * Prend en charge les variables de modèle lors de la mise en file d&#39;attente des runs Launch.
  * Ajoute l&#39;option --skip-console à la commande cli de synchronisation hors ligne.
  * Ajoute un mécanisme de gestion des versions graphql de base.
  * Ajoute la surveillance des statistiques GPU Apple M*.
  * Ajoute un utilitaire pour charger wandb.Config à partir des variables d&#39;environnement.
  * Adapte OpenAI WandbLogger à openai-python v1.0.
  * Corrige la vérification de version pour OpenAI WandbLogger.
  * Logger automatique pour Diffusers.
  * Ajoute un paramètre à l&#39;image pour spécifier le type de fichier : jpg, png, bmp ou gif.

  ## Correctifs

  * Fait fonctionner correctement la synchronisation hors ligne.
  * Corrige la vérification de l&#39;existence du run afin qu&#39;elle ne dépende pas des fichiers téléversés dans le run.
  * La vérification de l&#39;URI de stockage Gcp échouait à cause d&#39;un encapsulage asynchrone incorrect.
  * La mise à jour du résumé avec des `dict` imbriqués ne génère plus d&#39;erreur.
  * Ajoute un mode de priorisation à la création de RunQueue.
  * Crée un lien symbolique vers les journaux du serveur dans le dossier runs.
  * Corrige un problème de valeur unique dans wandb/wandb&#95;torch.py::log&#95;tensor&#95;stats.
  * Corrige Tmin vs tmax dans wandb/wandb&#95;torch.py::log&#95;tensor&#95;stats.
  * Correction mineure pour les cas sans serveur.
  * Rend les anciens paramètres plus robustes.
  * Gère les tags lors de la reprise d&#39;un run.

  Consultez le [journal des modifications v0.16.1](https://github.com/wandb/wandb/releases/tag/v0.16.1) sur GitHub.
</Update>

<Update label="v0.16.0" description="07 novembre 2023">
  ## Améliorations

  * Ajoute la prise en charge de la configuration imbriquée.
  * Finalise le mécanisme de sauvegarde des artefacts.
  * Ajoute le nombre de fichiers synchronisés dans le pied de page.
  * Implémente la surveillance de répertoire et les fonctionnalités associées.
  * Exécute l&#39;agent sur une boucle d&#39;événements.
  * Ajoute l&#39;historique échantillonné.
  * Prototype pour l&#39;api Models.
  * Enregistre les agents virtuels du planificateur de balayage de façon asynchrone.
  * Fait fonctionner les téléversements de fichiers avec local.
  * Permet aux utilisateurs d&#39;enregistrer des tableaux de graphiques personnalisés dans une autre section.
  * Généralise l&#39;outil de téléversement en gestionnaire de fichiers pour permettre les téléchargements.
  * Met fin à la prise en charge de python 3.6.
  * Supprime les fichiers de staging dans Nexus.
  * Configure les téléchargements d&#39;artefacts pour utiliser sdk nexus core.
  * Ajoute la progression du téléversement de fichiers et transforme le rappel de fin en liste de rappels.
  * Ajoute le label wandb.ai/run-id aux jobs.

  ## Correctifs

  * Garantit que l&#39;intégralité de run.config est capturée dans SageMaker.
  * Améliore la gestion des erreurs de gitlib pour les FileNotFoundErrors.
  * Augmente la taille maximale des messages et gère les erreurs.
  * L&#39;agent équilibre mieux plusieurs files d&#39;attente.
  * Supprime la vérification de l&#39;activation de la gestion des versions dans le gestionnaire de références GCS.
  * Ajoute google-cloud-aiplatform au shard Launch.
  * Ajoute use&#95;artifact comme message de transmission directe.
  * Ajuste les variables d&#39;environnement vertex.
  * Met à jour les permissions du fichier cache des artefacts pour NamedTemporaryFile.
  * Corrige plusieurs problèmes.
  * L&#39;enregistrement d&#39;une figure mlp dans un tampon puis sa lecture avec PIL doit spécifier un format.
  * Envoie content-length et vérifie le code de statut de la réponse dans l&#39;outil de téléversement.
  * Corrige la logique d&#39;étape lors de la reprise de runs sans métriques enregistrées.
  * Hook&#95;handle est défini comme une liste au lieu d&#39;un dict lors du unhook.
  * Vérifie les identifiants gcp avant de créer le job vertex.
  * Ajoute l&#39;option load à docker buildx.
  * Corrige les régressions de performance dans les téléchargements d&#39;artefacts et l&#39;emplacement de téléchargement des fichiers.
  * Ajoute un avertissement lorsque `log_code` ne trouve aucun fichier.
  * Ajuste les hooks ipython pour v8.17.

  Consultez le [journal des modifications v0.16.0](https://github.com/wandb/wandb/releases/tag/v0.16.0) sur GitHub.
</Update>

<Update label="v0.15.12" description="03 octobre 2023">
  ## Améliorations

  * Implémente un mécanisme de debounce pour la configuration.
  * Corrige l&#39;inversion des canaux dans la tâche de classification ultrlytics.
  * Implémente l&#39;interface cpp alpha nexus.
  * Expose les métriques système dans l&#39;objet run (PoC).
  * Épingle la prise en charge de la version ultralytics à `v8.0.186`.

  ## Correctifs

  * Récupère les journaux des pods k8s en échec.
  * Permet d&#39;ajouter un bucket s3 comme artefact de référence.
  * Corrige une condition de concurrence lors du nettoyage du thread de l&#39;agent.
  * Ne suppose pas que le run et ses artefacts d&#39;entrée/sortie se trouvent dans le même projet.
  * Corrige wandb.Api().run(run&#95;name).log&#95;artifact(artifact).
  * ValueError avec une valeur None dans le balayage.
  * Corrige une faute de frappe dans le gestionnaire s3.
  * Corrige l&#39;ordre des arguments pour le nouvel argument target&#95;fraction.
  * Corrige une condition de concurrence potentielle dans le debounceur de configuration.
  * Corrige le mappage des types graphql.
  * Corrige la limite de concurrence dans l&#39;outil de téléversement.

  Consultez le [journal des modifications v0.15.12](https://github.com/wandb/wandb/releases/tag/v0.15.12) sur GitHub.
</Update>

<Update label="v0.15.11" description="21 septembre 2023">
  ## Améliorations

  * Ajouter la prise en charge de l’enregistrement du code en mode script.
  * Ajouter la prise en charge de `policy=end` dans `wandb.save`.
  * Ajouter les informations système aux métadonnées.
  * Ajouter les informations système du GPU NVIDIA aux métadonnées.
  * Supprimer les files d’attente de runs via l’API publique.
  * Introduire la prise en charge d’un proxy personnalisé pour le trafic HTTP(S) de wandb.

  ## Correctifs

  * Corriger le logger lors de la journalisation d’une exception de filestream.
  * Utiliser l’API watch pour surveiller les CRD lancées.
  * Interdire la mise en file d’attente d’images Docker sans projet cible.
  * Ajouter l’import Twitter manquant pour les utilisateurs de l’API.
  * Obtenir les ID de version S3 à partir des références de répertoire.
  * Faire en sorte que les flux watch se rétablissent après une réinitialisation de connexion.
  * Utiliser json.loads(..., strict=False) pour ignorer les caractères UTF-8 non valides et les caractères de contrôle dans api.Run.load.
  * Identifier correctement Colab comme un environnement de type Jupyter dans les paramètres.
  * Améliorer la gestion de la mémoire pour les mises à jour du summary.
  * Ajouter une variable d’environnement pour configurer la taille de lot des URL de téléchargement.
  * Faire échouer rqis si aucun run n’est créé.

  ## Nettoyage

  * Mettre à jour README.md.

  Consultez le [journal des modifications de la version v0.15.11](https://github.com/wandb/wandb/releases/tag/v0.15.11) sur GitHub.
</Update>

<Update label="v0.15.10" description="06 septembre 2023">
  ## Améliorations

  * Ajouter la prise en charge asynchrone à l’API `Autologger` et l’activer pour OpenAI.
  * Ajouter la prise en charge officielle de Python 3.11 et abandonner celle de Python 3.6.
  * Implémenter la journalisation réseau et un délai d’expiration pour le pousseur de fichiers à des fins de débogage.
  * Définir le TTL (time to live) pour les versions d’artefact.
  * Ajouter la prise en charge de define metric.
  * Inclure la version de l’agent lors de la création de l’agent Launch.
  * Les jobs git sans run peuvent utiliser requirements.txt dans les répertoires parents.
  * Récupérer la collection parente à partir d’un Artifact.
  * Améliorer les téléversements de fichiers.
  * Ajouter une option d’ID de run à la méthode artifact put pour enregistrer des artefacts dans des runs existants.
  * Ajouter la propriété metadata à l’objet Run.
  * Prendre en charge la définition d’un Dockerfile personnalisé dans les overrides Launch.
  * Ajouter la ressource GPU Nvidia au moniteur système.
  * Activer la suppression des collections d’artefacts depuis le SDK.
  * Ajouter le paramètre CLI dockerfile et utiliser Dockerfile.wandb par défaut s’il est présent.
  * Étendre le nettoyage du cache pour permettre de spécifier une fraction cible.
  * Ajouter un repr évaluable à ArtifactManifestEntry.
  * Activer la création de wheels basée sur Docker pour Nexus.
  * Ajouter la ressource GPU Nvidia au moniteur système.
  * Effacer le cache à l’ajout pour éviter tout débordement.
  * Capturer l’utilisation des E/S disque dans les métriques système.
  * Ajouter des compteurs d’E/S disque aux métriques de surveillance.
  * Rendre configurables les chemins utilisés pour la surveillance de l’utilisation du disque.
  * Utiliser `WANDB_SWEEP_ID` pour inclure un run dans un balayage existant.
  * Gérer les appels LinkArtifact effectués vers Nexus Core.
  * Corriger la logique de nouvelle tentative des clients HTTP et permettre sa personnalisation par l’utilisateur.
  * Prendre en charge les en-têtes définis par l’utilisateur dans le transport du client gql.
  * Activer les types set dans wandb.Config.
  * Visualiser les images avec des superpositions de boîtes englobantes pour `ultralytics`.
  * Ajouter un utilitaire d’échantillonnage à décroissance exponentielle pour line&#95;plot.
  * Toujours afficher la trace de pile dans le gestionnaire de contexte `wandb.init`.
  * Ajouter un paramètre pour désactiver la capture automatique des informations sur la machine.

  ## Correctifs

  * Étendre le try dans la boucle de l’agent pour couvrir toute la gestion des jobs.
  * Se prémunir contre un délai d’expiration filestream non défini.
  * Produire une erreur si le job sous-jacent à l’artefact de code a été supprimé.
  * Utiliser un nom unique pour l’artefact créé par `verify`.
  * Utiliser resume=allow lors de la remise en file d’attente automatique.
  * Corriger le chemin de l’entrypoint à partir du subir du dépôt git désactivé.
  * Remplacer individuellement les resource&#95;args du job.
  * Corriger un problème d’import pour prendre en charge Python 3.6.
  * Corriger le remplacement de l’entrypoint lors de l’utilisation de balayages sur Launch sans job de planification.
  * Corriger la référence resume lorsqu’elle est nil.
  * Accélérer encore le temps d’import.
  * Corriger le montage du secret dans l’exemple de manifeste d’agent Kubernetes.
  * Supprimer un import inutilisé.
  * Observer pour obtenir les statuts des runs Kubernetes.
  * Interdire l’enregistrement d’artefacts dans un projet différent de leur artefact de base.
  * Exiger que les artefacts existants soient enregistrés dans leur entité/projet source.
  * Ajuster les fonctions de démarrage et d’arrêt du moniteur système.
  * Supprimer les caractères suspects lorsque la création du répertoire échoue.
  * Le comportement d’exclusion par défaut de log&#95;code gère désormais correctement `wandb` dans le préfixe du chemin racine.
  * Interdire la création de files d’attente de projet.
  * Intercepter toutes les erreurs de définition d’état de balayage.
  * Create&#95;job fonctionne désormais depuis un notebook Jupyter.
  * Corriger une condition de concurrence pour defer et update control.
  * Améliorer la gestion et la journalisation des types tensor.
  * La commande CLI Launch doit se terminer avec un statut non nul si le run lancé sous-jacent se termine avec un statut non nul.
  * Corriger le comportement du mode hors ligne.
  * API Reports - corriger le chemin JSON de media&#95;keys.
  * Permettre d’enregistrer des images uint8 en tant que wandb.Image().
  * Revenir à /tmp/username/.config/wandb dans les anciens paramètres.
  * Utiliser UpsertBucketRetryPolicy dans tous les appels gql.UpsertBucket.
  * Mettre à jour la validation et l’encodage de l’ID de rapport.
  * Ajouter la prise en charge de la propagation des messages depuis le processus interne.

  ## Nettoyage

  * Correction liée aux nouveaux ajouts de la règle ruff E721.

  Consultez le [journal des modifications v0.15.10](https://github.com/wandb/wandb/releases/tag/v0.15.10) sur GitHub.
</Update>

<Update label="v0.15.9" description="28 août 2023">
  ## Améliorations

  * Lancer des planificateurs de balayage vers les files d’attente d’équipe depuis l’interface utilisateur.
  * Rendre Vertex Launcher plus personnalisable.
  * Utiliser par défaut le builder noop si Docker n’est pas installé.

  ## Correctifs

  * Utiliser les commandes intégrées d’entrypoint et d’arguments pour SageMaker.
  * Copier les informations du projet source parent vers le nouvel artefact brouillon.
  * Éviter une erreur à la fin du run avec les bigints.
  * Les jobs d’image créés manuellement peuvent être relancés correctement.

  Consultez le [journal des modifications v0.15.9](https://github.com/wandb/wandb/releases/tag/v0.15.9) sur GitHub.
</Update>

<Update label="v0.15.8" description="01 août 2023">
  ## Améliorations

  * Utiliser la mutation createRunFiles pour obtenir les uploadUrls.
  * Ajouter create&#95;run&#95;queue à l’API publique.
  * Ajouter une option masquée pour utiliser orjson au lieu de json.
  * Améliorer le message d’erreur lors d’un build avec noop builder.
  * La création d’un agent Launch inclut la configuration de l’agent si elle est présente.
  * Vérifier que les ingrédients du job existent avant de créer le job.
  * Gérer proprement l’erreur 404 de Kubernetes.

  ## Correctifs

  * Créer un nouveau projet uniquement s’il n’existe pas déjà.
  * Prendre en charge namespace dans la clé metadata des arguments de ressource.
  * Utiliser &quot;&quot; au lieu de None pour le kwarg project lorsqu’aucun projet n’est fourni.
  * Ajouter + à la regex torch cpu + tests.
  * Implémenter un délai d’expiration pour file&#95;stream et ajouter des journaux de débogage.
  * Corriger le filtrage de collection lors de la récupération des alias.
  * Remplacer les paramètres `dir_watcher` par SettingsStatic.
  * Définir la base correcte pour les artefacts incrémentiels.
  * Supprimer https des registres Azure pour garantir la compatibilité avec la macro ${image_uri}.
  * Gérer correctement une description définie sur None.
  * Ne pas créer de secret k8s s’il existe déjà.
  * Supprimer la vérification de gestion des versions du bucket S3.
  * Accélérer le temps d’importation et corriger l’avertissement d’obsolescence `pkg_resources`.

  Consultez le [journal des modifications v0.15.8](https://github.com/wandb/wandb/releases/tag/v0.15.8) sur GitHub.
</Update>

<Update label="v0.15.7" description="25 juillet 2023">
  ## Correctifs

  * Les images ne se synchronisent pas avant la fin du run (annulation de #5777).

  Consultez le [journal des modifications v0.15.7](https://github.com/wandb/wandb/releases/tag/v0.15.7) sur GitHub.
</Update>

<Update label="v0.15.6" description="24 juillet 2023">
  ## Améliorations

  * Ajouter un lien vers le job dans le pied de page de wandb.
  * Réimplémenter la remise en file d’attente des jobs, corriger le comportement d’annulation.
  * Créer manuellement des jobs depuis la CLI.
  * Permettre aux utilisateurs de spécifier le nom du job via le paramètre `job_name`.
  * Ajouter une API de trace simplifiée pour journaliser les traces de prompt.
  * Prendre en charge le format de modèle `.keras` avec `WandbModelCheckpoint`, compatible avec TF 2.13.0.
  * Prise en charge initiale de la migration des runs et des rapports W&amp;B entre instances.

  ## Correctifs

  * Rendre le callback LightGBM compatible avec 4.0.0.
  * Utiliser les paramètres par défaut pour la récupération du projet s’ils sont disponibles.

  Consultez le [journal des modifications v0.15.6](https://github.com/wandb/wandb/releases/tag/v0.15.6) sur GitHub.
</Update>

<Update label="v0.15.5" description="05 juillet 2023">
  ## Améliorations

  * Améliorer la gestion des noms et des tags de jobs d’image Docker.
  * Prendre en charge les builds kaniko sur AKS.
  * Permettre aux builds kaniko de s’exécuter dans d’autres espaces de noms.
  * Prendre en charge la clé d’accès pour les références Azure.
  * Ajouter des informations aux éléments de la file d’attente des runs en échec et prendre en charge les avertissements pour ces éléments.
  * Permettre la configuration directe de l’URI du registre pour tous les registres.
  * Améliorer le processus de récupération des URL de téléchargement avec une logique de traitement par lots et de nouvelle tentative.
  * Ajouter un indicateur pour ignorer les références S3 manquantes dans `Artifact.download`.
  * Implémenter la remise en file d’attente des jobs lorsqu’un pod se déconnecte.
  * Ajouter un paramètre pour désactiver setproctitle.

  ## Correctifs

  * Gérer les schémas d’URI dans LogicalPath.
  * Mettre à jour le stockage d’objets pour inclure la référence et empêcher la réutilisation des ID.
  * Mettre à jour les exigences de version du package click.
  * Améliorer les imports lazy pour qu’ils soient sûrs vis-à-vis des threads.
  * Corriger une faute de frappe dans le nom de l’image kaniko.
  * Corriger l’analyse de date dans la configuration SageMaker.
  * Rendre le build docker non interactif pour éviter les questions liées à la région.
  * Mettre à jour le chemin de l’image de base &quot;cuda&quot; vers celui de l’image de base &quot;accelerator&quot;.
  * Remplacer le nom de l’artefact par un espace réservé pour ignorer la validation.
  * Empêcher les jobs avec de grandes sorties de se bloquer sur local-container.
  * S’assurer que Reprendre n’envoie pas d’informations sensibles.
  * Corriger la gestion des références lors du téléchargement.
  * Corriger l’auteur du sweep Launch en nom d’utilisateur personnel.
  * Rendre privées les méthodes et les attributs de l’artefact.

  Consultez le [journal des modifications v0.15.5](https://github.com/wandb/wandb/releases/tag/v0.15.5) sur GitHub.
</Update>

<Update label="v0.15.4" description="06 juin 2023">
  ## Améliorations

  * Définir la source du job dans les paramètres.
  * Lancer des sweeps contrôlés par wandb run.
  * Ajouter l&#39;autolog pour le SDK Python de Cohere.
  * Prendre en charge le lancement d&#39;objets k8s personnalisés.
  * Réduire l&#39;utilisation de la mémoire lors du hachage des fichiers.
  * Ajouter la méthode new&#95;draft pour modifier les artefacts enregistrés et les journaliser comme nouvelle version.
  * Ne pas installer les dépendances figées s&#39;il existe un fichier de dépendances.
  * Ne pas supprimer par défaut les fichiers temporaires du cache des artefacts.
  * Ajouter source&#95;entity et mettre à jour la gestion de sequenceName.
  * Ajouter &#39;remove&#39; à l&#39;API Artifacts.
  * Ajouter le planificateur Optuna pour les sweeps au lancement.
  * Prendre en charge la création de jobs notebook.
  * Activer les macros Launch pour tous les runners.
  * Ajouter l&#39;autologging pour les pipelines Hugging Face pris en charge.
  * Ajouter les métriques d&#39;utilisation et la journalisation de tableaux à l&#39;autologger OpenAI.
  * Ajouter la prise en charge de la surveillance des métriques système des GPU AMD.
  * Capturer l&#39;allocation absolue de mémoire GPU.

  ## Correctifs

  * S&#39;assurer que wandb peut être utilisé dans AWS Lambda.
  * Permettre à `LogicalPath` de supprimer les barres obliques finales.
  * Faire preuve de prudence lors de la création du fichier ~/.config/wandb/settings.
  * Mettre à jour la gestion des requêtes de graphiques personnalisés et ajouter un constructeur alternatif pour les graphiques basés sur des tableaux.
  * Ajouter le téléversement multipart S3 pour les fichiers d&#39;artefact.
  * Gérer les chaînes de nom d&#39;artefact incompatibles.
  * Forcer systématiquement le pull dans le runner Docker pour les jobs basés sur des images.
  * Améliorer la gestion des erreurs lors de l&#39;installation des paquets.
  * Faire en sorte que les objets k8s personnalisés respectent les remplacements de command/args.
  * Supprimer entity et project des propriétés valides et ajuster la gestion de name.
  * Utiliser une variable d&#39;environnement pour l&#39;URL de base de l&#39;agent Launch.
  * Écrire dans le cache de manière défensive (intercepter OSError).
  * Gérer l&#39;exception dans finish&#95;thread&#95;id et faire échouer les éléments de la file d&#39;attente de runs.
  * Ajouter des pull secrets pour les images prédéfinies lorsqu&#39;un registre est spécifié.
  * Lire le nom du compte de service du pod kaniko depuis une variable d&#39;environnement.
  * Correctifs GCP divers.
  * Prendre en charge la déclaration de l&#39;environnement local et du registre.
  * Prendre en charge les URL Git SSH et les sous-modules dans l&#39;agent.
  * Mettre à jour la gestion du dépôt Git pour les cas d&#39;échec et le renommer en gitlib.
  * Unifier les modes hors ligne et en ligne lors de l&#39;initialisation et corriger l&#39;attachement multiprocessus.
  * Éviter les erreurs en vérifiant `wandb.run` dans l&#39;intégration Gym.
  * Corriger le problème de synchronisation wandb tfevent.

  Consultez le [journal des modifications v0.15.4](https://github.com/wandb/wandb/releases/tag/v0.15.4) sur GitHub.
</Update>

<Update label="v0.15.3" description="17 mai 2023">
  ## Correctifs

  * Autoriser le SDK à fonctionner si le jeton SA ne peut pas être lu.
  * Nettoyer la logique de découverte du jeton k8s dans util.py::image&#95;id&#95;from&#95;k8s.
  * Mettre à jour `WandbTracer` pour qu&#39;il fonctionne avec la nouvelle version de langchain.
  * Mettre à jour le résumé uniquement pour les clés modifiées.

  Consultez le [journal des modifications v0.15.3](https://github.com/wandb/wandb/releases/tag/v0.15.3) sur GitHub.
</Update>

<Update label="v0.15.2" description="05 mai 2023">
  ## Correctifs

  * Mettre à jour WandbTracer pour la nouvelle version de langchain par @parambharat @tssweeney dans https://github.com/wandb/wandb/pull/5467.
  * Corriger le message d&#39;erreur dans la vérification de version de langchain wandb&#95;tracer.

  Consultez le [journal des modifications v0.15.2](https://github.com/wandb/wandb/releases/tag/v0.15.2) sur GitHub.
</Update>

<Update label="v0.15.1" description="02 mai 2023">
  ## Améliorations

  * Implémenter un nouveau schéma de configuration pour le runner Kubernetes.
  * Autoriser la redéfinition de la plateforme pour le build Docker.
  * Obtenir le nom complet de l&#39;artefact pour en faciliter la récupération.
  * Rendre configurable le répertoire racine par défaut pour le téléchargement des artefacts.
  * Ajouter un gestionnaire de stockage Azure dans le SDK.
  * Ajouter une méthode pour convertir `wandb.Table` en `pandas.DataFrame`.
  * Les arguments de commande Launch pour sweeps sont transmis comme paramètres.

  ## Correctifs

  * Ne pas supposer que les clés dans args et config désignent la même chose.
  * Faire en sorte que l&#39;environnement ElasticContainerRegistry gère &quot;ImageNotFoundException&quot; correctement.
  * Désactiver les nouvelles tentatives du builder kaniko.
  * Affiner le message d&#39;erreur en cas d&#39;erreur d&#39;authentification.
  * Le runner Kubernetes ne respecte pas les arguments de redéfinition.
  * Autoriser les attr-dicts comme configurations de sweeps.
  * Calculer la somme de contrôle de la copie de staging en lecture seule au lieu du fichier original.
  * Ignorer la récupération des informations du run si le run se termine correctement ou s&#39;il provient d&#39;une autre entité.
  * Utiliser par défaut le projet &quot;uncategorized&quot; au lieu de &quot;None&quot; lors de la récupération des artefacts.
  * Ajouter une vérification de l&#39;activation à gym VideoRecorder.
  * Corriger la gestion du projet et de l&#39;entité par défaut.
  * Mettre à jour import&#95;hook.py avec les dernières modifications du dépôt wrapt.
  * Corriger la prise en charge des URL locales dans l&#39;agent Launch k8s.
  * Améliorer la détection et les tests de l&#39;environnement notebook.
  * Implémenter un utilitaire de vérification récursive avec isinstance pour l&#39;objet Settings.
  * Analyser correctement les cas limites dans les définitions de filtre OpenMetrics dans System Monitor.
  * Mettre à jour les journaux de débogage pour inclure la version du SDK.
  * Filtrer les métriques AWS Trainium par rang local si l&#39;exécution se fait avec torchrun.
  * Informer les utilisateurs de l&#39;incompatibilité de WandbTracer avec LangChain &gt; 0.0.153.

  Consultez le [journal des modifications v0.15.1](https://github.com/wandb/wandb/releases/tag/v0.15.1) sur GitHub.
</Update>

<Update label="v0.15.0" description="19 avril 2023">
  ## Améliorations

  * Ajouter la prise en charge du type de média LangChain.
  * Ajouter l&#39;autolog pour la bibliothèque Python d&#39;OpenAI par @dmitryduev @parambharat @kptkin @raubitsj dans https://github.com/wandb/wandb/pull/5362.

  ## Correctifs

  * Ajouter un wrapper de signature de fonction aux méthodes OpenAI patchées.
  * Ajuster l&#39;API publique de l&#39;autolog OpenAI pour améliorer l&#39;expérience utilisateur par @dmitryduev @kptkin @raubitsj dans https://github.com/wandb/wandb/pull/5381.

  Consultez le [journal des modifications v0.15.0](https://github.com/wandb/wandb/releases/tag/v0.15.0) sur GitHub.
</Update>

<Update label="v0.14.2" description="07 avril 2023">
  ## Correctifs

  * Corriger la régression de `wandb sync`.

  Consultez le [journal des modifications v0.14.2](https://github.com/wandb/wandb/releases/tag/v0.14.2) sur GitHub.
</Update>

<Update label="v0.14.1" description="05 avril 2023">
  ## Améliorations

  * Améliorer run.log&#95;artifact() avec le type par défaut et les références de chemin.
  * Ajouter la prise en charge optionnelle du téléversement asynchrone d&#39;artefacts.
  * Mettre à jour le résumé uniquement pour les clés modifiées.
  * Utiliser un objet de session persistant pour les requêtes GraphQL.
  * Autoriser la définition d&#39;en-têtes supplémentaires pour le client gql.
  * Autoriser le filtrage des métriques en fonction des points de terminaison OpenMetrics.

  ## Correctifs

  * Message plus informatif en cas d&#39;échec de la création du répertoire d&#39;artefact de staging.
  * Définir la valeur par défaut de Kubernetes backoffLimit sur 0.
  * Supprimer le tri par défaut lors de l&#39;export de la configuration dans un fichier yaml.
  * Corriger l&#39;encodage des types html sous Windows.
  * Nettoyer l&#39;état de reprise automatique lors de l&#39;initialisation d&#39;un nouveau run.
  * Renforcer la gestion des erreurs de `wandb.init()` en cas d&#39;erreurs du backend.
  * Corriger la logique d&#39;arrêt du moniteur système.
  * Autoriser les utilisateurs à spécifier des versions épinglées dans requirements.txt.
  * Faire en sorte que `wandb.log()` gère correctement les valeurs de chaîne vide.
  * Lever une exception lors de l&#39;accès aux méthodes et attributs d&#39;un run terminé.

  ## Nettoyage

  * Utiliser le module &#39;secrets&#39; au lieu d&#39;un générateur personnalisé de jetons aléatoires.
  * Déplacer &#95;manifest&#95;json&#95;from&#95;proto vers sender.py.

  Consultez le [journal des modifications v0.14.1](https://github.com/wandb/wandb/releases/tag/v0.14.1) sur GitHub.
</Update>

<Update label="v0.14.0" description="14 mars 2023">
  ## Améliorations

  * Prise en charge des images de base CUDA pour les runs Launch.
  * Avertissement indiquant quels packages n&#39;ont pas pu être installés pendant le processus de build.
  * Ajout de la prise en charge de l&#39;importation de runs depuis MLflow.
  * Les runs en file d&#39;attente qui ne parviennent pas à se lancer sont marqués comme `FAILED`.

  ## Correctifs

  * Suppression temporaire de la validation locale de la clé API.
  * L&#39;agent Launch supprime correctement le thread lorsqu&#39;une exception se produit.
  * Message d&#39;erreur explicite lorsqu&#39;il est impossible de se connecter au démon Docker.
  * Prise en charge des rôles d&#39;instance EKS dans Launch.
  * Messages d&#39;erreur plus clairs lorsque Launch rencontre des erreurs Docker, avec échec propre.
  * Hachage des images Docker basé sur la version du job et le contenu du Dockerfile.
  * Security(launch) : avertissement lorsqu&#39;un agent démarre le polling sur une file d&#39;attente d&#39;équipe.
  * Ajout de télémétrie lors de la synchronisation des fichiers tfevents.
  * Correction d&#39;une régression empêchant l&#39;arrêt d&#39;un run de fonctionner.
  * Instructions destinées à l&#39;utilisateur pour gérer l&#39;import Kubernetes manquant lors de l&#39;utilisation du runner Kubernetes ou du builder Kaniko.
  * Masquage des options CLI Launch non prises en charge.
  * Le builder d&#39;image Launch installe désormais correctement PyTorch avec ses dépendances sur différents matériels.

  Consultez le [journal des modifications v0.14.0](https://github.com/wandb/wandb/releases/tag/v0.14.0) sur GitHub.
</Update>

<Update label="v0.13.11" description="07 mars 2023">
  ## Améliorations

  * Amélioration de la journalisation de l&#39;agent Launch.
  * `run_cap` de sweep fonctionne désormais pour les sweeps Launch.
  * Lancement de jobs de sweep à partir de `image_uri`.
  * Ajout du paramètre `num_workers` à la section scheduler dans `launch_config`.
  * Levée de `ArtifactNotLoggedError` à la place de `ValueError`.
  * L&#39;agent Launch utilise un pool de threads pour exécuter les jobs.
  * Les runners et builders utilisent désormais les classes Environnement et Registre.
  * Ajout de la prise en charge d&#39;OpenMetrics pour les métriques système.
  * Ajout de la possibilité de filtrer les métriques système consommées depuis les endpoints OpenMetrics.
  * Ajout de la prise en charge de la surveillance des environnements gymnasium, en plus de gym.
  * Ajout de la clé `max_scheduler` à la configuration de l&#39;agent Launch.
  * Ajout d&#39;une intégration avec la bibliothèque `ultralytics` pour YOLOv8.

  ## Correctifs

  * Nettoyage de l&#39;avertissement de dépréciation du widget d&#39;IPython.
  * Ajout d&#39;exceptions spéciales pour la logique du gestionnaire lors d&#39;une tentative de connexion à un service indisponible.
  * Correction d&#39;un problème où le répertoire global de configuration devait être accessible en écriture pour utiliser Api.
  * Message d&#39;erreur plus exploitable pendant l&#39;initialisation du run et correction d&#39;une exception non interceptée.
  * Ajout de la méthode spéciale deepcopy à la classe Run.
  * Suppression du choix systématique du projet par défaut dans sweep.
  * Une erreur est désormais renvoyée lors de la tentative de création d&#39;un sweep Launch sans job spécifié.
  * `Mkdir_exists_ok` vérifie désormais (à nouveau) les permissions si le répertoire existe déjà.
  * Journalisation uniquement du job reçu lors du lancement d&#39;un élément provenant d&#39;un job.
  * Correction d&#39;un problème où les runs en file d&#39;attente provenant d&#39;images disparaissaient dans l&#39;URI.
  * Ajout des permissions d&#39;écriture aux artefacts copiés.
  * Amélioration de l&#39;analyse de l&#39;argument `queue` dans la commande CLI `sweep`.
  * En mode désactivé, le service n&#39;est pas démarré.
  * Correction de la prise en charge des images Docker avec un entrypoint spécifié par l&#39;utilisateur dans le conteneur local.
  * API - ArtifactFiles ne renvoie plus d&#39;erreur lors de l&#39;accès à un élément.
  * Vérification de l&#39;existence du job avant de démarrer le scheduler sweeps.
  * Gestion des métriques système nécessitant des étapes supplémentaires de configuration et de nettoyage.
  * Correction d&#39;une faute de frappe dans `CONTRIBUTING.md`.
  * Détection correcte du nom du notebook et correction de l&#39;enregistrement du code dans Colab.
  * Autorisation jusqu&#39;à max&#95;artifacts (correction d&#39;une erreur de décalage de 1).
  * Prudence accrue lors du démarrage des threads de surveillance des ressources.
  * Correction d&#39;un bug où la dépendance boto3 plante à l&#39;import lorsque downl….
  * Vérification de la validité de l&#39;argument CLI `num_workers`, avec une valeur par défaut de 8 sinon.
  * Correction de la référence de fichier ajoutée à l&#39;artefact de vérification.
  * Gestion spéciale du scheduler sweeps dans l&#39;agent.
  * Les fichiers ne sont retéléchargés ou écrasés qu&#39;en cas de modification.
  * Évitement de l&#39;introspection en mode hors ligne.
  * Ordonnancement topologique de `wandb.Settings`.
  * Désactivation du chargement lazy pour le patching TensorBoard.

  ## Nettoyage

  * Correction de bugbear B028 avec ajout de stacklevel.
  * Déplacement des erreurs Launch au plus près du code.
  * Déplacement de l&#39;erreur de boîte aux lettres au plus près du code.
  * Ajout d&#39;un type d&#39;erreur non pris en charge.
  * Ajout de la prise en charge du linter ruff.
  * Modifications cosmétiques pour améliorer la lisibilité.
  * Introduction de classes abstraites d&#39;environnement et de registre.
  * Correction d&#39;un type union inutilisé dans l&#39;agent Launch.
  * Suppression de l&#39;artefact du manifeste.
  * Activation de la vérification de types pour interface.artifacts et ajout d&#39;annotations de type / casts.
  * Ajout d&#39;annotations de type à `wandb_setup.py`.
  * Suppression des directives #noqa inutilisées.
  * Désactivation du suivi Sentry pendant les tests.

  Consultez le [journal des modifications v0.13.11](https://github.com/wandb/wandb/releases/tag/v0.13.11) sur GitHub.
</Update>

<Update label="v0.13.10" description="07 février 2023">
  ## Améliorations

  * Réutiliser la session pour les requêtes de téléversement de fichiers.
  * Exposer le point de terminaison de liste des alias pour les collections d’artefacts.
  * Inclure le nom d’utilisateur de l’auteur du run dans les variables d’environnement.
  * Ajouter la prise en charge des arguments de ressource `local-container`.
  * Ajouter la possibilité de compléter un run avec `wandb sync --append`.
  * Ajouter une option de secours (`disable_job_creation`) pour désactiver la création automatique de jobs.

  ## Correctifs

  * Supprimer les traits de soulignement du nom de job généré dans le runner Kubernetes.
  * Les arguments de la commande de sweep peuvent à nouveau être de type int.
  * S’assurer que les artefacts préparés ont l’alias `latest`.
  * Intercepter FileNotFoundError et PermissionError pendant cache.cleanup().
  * Corriger l’ordre de résolution de l’exécutable Python.
  * Corriger la gestion de la console lors d’un fork et lorsque stdout==stderr.
  * Corriger le problème où les artefacts de job étaient enregistrés sans l’alias `latest`.
  * S’assurer que les noms de job ne dépassent pas la longueur maximale autorisée pour les artefacts.

  ## Dév

  * S’assurer que la version du manifeste est vérifiée.
  * Ajouter des tests pour les certificats SSL personnalisés et la désactivation de SSL.
  * Corriger les builds Docker nocturnes.
  * Ne pas créer de package universel py2/py3.
  * Corriger flake8-bugbear B028 et ignorer B017.
  * Corriger la version du SDK gcloud demandée dans les tests nocturnes.
  * Supprimer les paramètres inutilisés dans `StorageHandler.load_{path,file,reference}`.
  * Scinder les tests en tests système et véritables tests unitaires.
  * Gérer le déplacement du serveur fixture du port 9010 au port 9015 dans local-testcontainer.
  * Ajouter des alias à la réponse à la requête ac.
  * Exécuter la suite de régression chaque nuit.
  * Corriger le test Lightning défaillant.
  * Activer la vérification de type pour wandb&#95;init.py.
  * Déprécier l’utilisation implicite de la file d’attente par défaut dans la commande launch-agent.
  * Ajouter un test unitaire pour le runner Kubernetes avec des annotations.
  * Corriger le test train&#95;gpu&#95;ddp.
  * Corriger l’image de test Docker pour récupérer la version amd64.
  * Corriger codeowners après la restructuration des tests.
  * Corriger les échecs des tests md5 sous Windows.
  * Isoler le serveur relay afin qu’il puisse être partagé avec yea-wandb.
  * Corriger un avertissement flake8 dans un test.
  * Corriger plusieurs tests d’import.
  * Ne pas utiliser de liens symboliques pour les ressources de test SSL, à cause de Windows.
  * Ajouter des tests unitaires pour filesync.Stats.
  * Ajouter une logique de nouvelle tentative asynchrone.
  * Renforcer les tests pour ArtifactSaver et StepUpload.
  * L’agent consigne la trace complète de la pile lors de l’interception d’une exception.
  * Masquer l’avertissement affiché par neuron-ls.
  * Épingler pip et tox dans les environnements de développement.

  ## Nettoyage

  * Renforcer les tests de StepUpload ; rendre la gestion des exceptions plus exhaustive dans upload/commit.
  * Refactoriser la requête Artifact pour récupérer l’entité et le projet.
  * Remplacer davantage d’appels à communicate par deliver.
  * Utiliser Future en interne pour communiquer le succès/l’échec de commit, et non threading.Event.
  * Utiliser ThreadPoolExecutor de la bibliothèque standard dans StepUpload au lieu de gérer le nôtre.

  Consultez le [journal des modifications v0.13.10](https://github.com/wandb/wandb/releases/tag/v0.13.10) sur GitHub.
</Update>

<Update label="v0.13.9" description="11 janvier 2023">
  ## Correctifs

  * Faire preuve d’une prudence accrue lors de la vérification de la disponibilité d’AWS Trainium sur le système.
  * Restaurer &#39;util.generate&#95;id&#39; pour le code hérité / utilisateur.
  * Remplacer `release` par `abandon` lors de la libération du handle de boîte aux lettres pendant l’initialisation.

  Consultez le [journal des modifications v0.13.9](https://github.com/wandb/wandb/releases/tag/v0.13.9) sur GitHub.
</Update>

<Update label="v0.13.8" description="10 janvier 2023">
  ## Améliorations

  * Conserver les téléversements non validés dans une zone de staging distincte.
  * Améliorer la gestion des descripteurs de fichiers.
  * Utiliser par défaut le projet model-registry pour l&#39;agent et launch&#95;add.
  * Ajouter `exist_ok=False` à `file.download()`.
  * Créer automatiquement des artefacts de job à partir des runs disposant des éléments requis.
  * Ajouter un modèle généralisé d&#39;injection de réponse pour les tests.
  * Remplacer les `multiprocessing.Queue` par des `queue.Queue`.
  * Utiliser le journal des transactions pour plafonner l&#39;utilisation de la mémoire.
  * Prendre en charge les métriques système pour AWS Trainium.

  ## Correctifs

  * Corriger l&#39;annotation de type pour wandb.run.
  * Reprendre la collecte des métriques système au redémarrage de l&#39;objet.
  * Corriger la gestion de env et de node&#95;selector.
  * Corriger l&#39;utilisation par Job.call() du mauvais mot-clé (queue vs queue&#95;name) lors de l&#39;appel à launch&#95;add.
  * Les ordonnanceurs sweeps gèrent les paramètres composés de plusieurs mots.
  * Autoriser les espaces dans le fichier requirements et supprimer le fichier bootstrap wandb dupliqué.
  * Gérer correctement les références à des fichiers locaux encodées dans l&#39;URL.
  * Obtenir le digest directement au lieu de le récupérer à partir du manifeste des manifests.
  * Artifact.version doit être l&#39;index de version de la collection associée.
  * Supprimer les fonctions generate&#95;id en double et remplacer shortuuid par secrets.
  * Corriger la vérification de type pour jax.Array introduit dans jax==0.4.1.
  * Corriger le blocage après l&#39;échec de wandb.init (ajout de cancel).
  * Autoriser les utilisateurs à fournir le chemin vers des exécutables personnalisés.
  * Corriger le TypeError lors d&#39;une tentative de slicing sur un objet Paginator.
  * Ajouter `AttributeError` à la liste des exceptions gérées lors de l&#39;enregistrement d&#39;un modèle keras.
  * Supprimer les arguments des jobs.

  ## Dév

  * Mettre à jour t2&#95;fix&#95;error&#95;cond&#95;feature&#95;importances pour installer scikit-learn.
  * Mettre à jour les images Docker de base pour les tests nocturnes.
  * Remplacer sklearn par scikit-learn dans le test fonctionnel sacred.
  * Ajouter une vérification d&#39;erreur pour `--build` lorsque resource=local-process.
  * Mettre à jour la gestion des ressources du scheduler et de l&#39;agent pour autoriser le remplacement DRC.
  * Exiger une revue de l&#39;équipe SDK pour l&#39;ajout ou la suppression de haut niveau….
  * Supprimer l&#39;exigence imposant que le projet cible corresponde à la file d&#39;attente.
  * Améliorer le processus de tests cloud nocturnes.
  * Mettre à jour le modèle de pull request.
  * Renvoyer le runSpec mis à jour après la requête pushToRunQueue.
  * Corriger la gestion de run spec dans le SDK.
  * Supprimer la dépendance de test à l&#39;ancien package fastparquet.
  * Corriger le dtype np.float (qui n&#39;existe pas) et le remplacer par le float Python.
  * Corriger &#39;exclude&#39; en &#39;ignore-paths&#39; dans .pylintrc.
  * Utiliser pytest tmp&#95;path afin de pouvoir inspecter les échecs.
  * Réinitialiser la commande build après la build.
  * Relancer les tests instables dans la CI avec pytest-rerunfailures.
  * Supprimer le code mort de la logique filesync.
  * Supprimer les champs inutilisés d&#39;un message filesync.
  * Refactoriser la logique de nouvelle tentative pour utiliser des globales au lieu de les injecter comme dépendances.
  * Ajouter des tests unitaires pour filesync.StepUpload.
  * Ajouter des tests pour Api.upload&#95;file&#95;retry.
  * Supprimer la ressource de secours lorsqu&#39;elle n&#39;est pas spécifiée pour une file d&#39;attente.
  * Améliorer la couverture des tests du gestionnaire de stockage.
  * Corriger les tests d&#39;importation.
  * Rendre MetricsMonitor moins verbeux en cas d&#39;erreur.
  * Prendre en compte le déplacement du serveur fixture du port 9003 vers 9010 dans local-testcontainer.
  * Intégrer promise==2.3.0 pour supprimer sans ambiguïté la dépendance à six.
  * Autoriser la définition du répertoire de cache des artefacts dans wandb.init(...).
  * Réduire temporairement le tampon réseau pour les tests.
  * Ajouter de la télémétrie si l&#39;utilisateur s&#39;exécute dans un environnement pex.
  * Ajouter davantage de télémétrie de contrôle de flux.
  * Ajouter des paramètres et des informations de débogage pour les problèmes de démarrage du service (wait&#95;for&#95;ports).
  * Corriger le test AWS Trainium.
  * Corriger le problème de thread du vérificateur de statut lorsque le processus utilisateur se termine sans finish().
  * Ajouter de la télémétrie pour l&#39;utilisation avec service désactivé.

  ## Nettoyage

  * Utiliser la même syntaxe à chaque levée d&#39;exception.
  * Combiner &#95;safe&#95;mkdirs avec mkdir&#95;exist&#95;ok.
  * Utiliser une fixture pytest pour le cache d&#39;artefacts.
  * Utiliser directement ArtifactEntry au lieu de créer une sous-classe.
  * Consolider les utilitaires de hachage dans lib.hashutil.
  * Formater le fichier public correctement.
  * Installer tox dans le bon environnement dans l&#39;outil de configuration de l&#39;environnement de développement.
  * Nettoyer la logique d&#39;initialisation et de run.

  Consultez le [journal des modifications v0.13.8](https://github.com/wandb/wandb/releases/tag/v0.13.8) sur GitHub.
</Update>

<Update label="v0.13.7" description="14 décembre 2022">
  ## Correctifs

  * Annuler la modification `Circular reference detected` pour résoudre `Object of type Tensor is not JSON serializable`.

  Consultez le [journal des modifications v0.13.7](https://github.com/wandb/wandb/releases/tag/v0.13.7) sur GitHub.
</Update>

<Update label="v0.13.6" description="06 décembre 2022">
  ## Améliorations

  * Ajout de `Sweep.expected_run_count` à l&#39;API publique.
  * Prise en charge des montages de volumes et des contextes de sécurité dans le runner Kubernetes.
  * Ajout d&#39;un nouveau flag `--build` pour faire le build de l&#39;image puis la pousser vers une file d&#39;attente.
  * Ajout de la possibilité de consigner le taux d&#39;apprentissage avec WandbMetricsLogger.
  * Amélioration de l&#39;API des rapports en préparation de la GA.

  ## Correctifs

  * Ajout d&#39;un filtre pour `artifact_version` afin de ne récupérer que les artefacts commités.
  * Stabilisation de `wandb verify`.
  * Correction du type des arguments de surcharge transmis à un LaunchProject depuis un Job.
  * Suppression des deux-points superflus dans le préfixe du journal.
  * Ajout de la prise en charge d&#39;un service s&#39;exécutant dans un environnement basé sur pex.
  * Correction du sondage des informations IPU statiques.
  * Remplacement de `artifactSequence` par `artifactCollection` dans les requêtes GQL publiques.
  * Correction des problèmes de compatibilité TF avec `WandbModelCheckpoint`.
  * Rendu de Keras WandbCallback compatible avec la version TF &gt;= 2.11.0.
  * Mise à jour de l&#39;intégration gym pour l&#39;aligner sur la dernière version.
  * Renforcement de la gestion interne des threads dans SystemMetrics.

  ## Dév

  * Amélioration du temps de récupération Git en spécifiant un `refspec` et `depth=1`.
  * Correction de la règle linguist pour ignorer les fichiers générés par grpc.
  * Nouveau shard pour les tests Launch.
  * Mise à niveau des GitHub Actions basées sur Node 12.
  * Ignorer le test instable `artifact_metadata_save`.
  * Remplacement des temporisations par flush lors de l&#39;attente de l&#39;écriture d&#39;un fichier.
  * Utilisation de la fixture `tmp_path` au lieu d&#39;écrire des fichiers locaux pendant les tests.
  * Correction d&#39;un test de file d&#39;attente défaillant.
  * `skip` au lieu de `xfail` pour le test `test_artifact_metadata_save`.
  * Ajout de nombreux tests pour InternalApi.upload&#95;file.
  * Ajout d&#39;un mécanisme de repli pour artifact Sequence sur les anciens serveurs.
  * Exigences de version protobuf rendues plus granulaires.

  ## Nettoyage

  * Lors du commit des artefacts, ne pas réessayer les erreurs 409 Conflict.
  * Ajout de l&#39;ajout/suppression programmatique d&#39;alias depuis le SDK sur les artefacts.
  * Suppression de `wandb.sklearn.plot_decision_boundaries`, qui contient une logique morte.
  * Ajout d&#39;une option pour forcer le pull de la dernière version d&#39;une image dev-container de test.
  * Builder no-op.
  * Suppression d&#39;un attribut inutilisé.
  * Mise à jour de `mypy` vers 0.991.
  * Ajout d&#39;une analyse des URI plus robuste.
  * Activation du linting pour internal&#95;api.py.
  * Suppression de la dépendance à six en modifiant les bibliothèques intégrées.

  Consultez le [journal des modifications v0.13.6](https://github.com/wandb/wandb/releases/tag/v0.13.6) sur GitHub.
</Update>

<Update label="v0.13.5" description="03 novembre 2022">
  ## Améliorations

  * Ajout d&#39;une option pour téléverser des références d&#39;image.

  ## Correctifs

  * Génération de noms d&#39;image plus lisibles.
  * Utilisation de hash(`etag`+`url`) au lieu de seulement `etag` comme clé dans le cache des artefacts.
  * Attente du commit de l&#39;artefact avant d&#39;indiquer à l&#39;utilisateur qu&#39;il est prêt lors de l&#39;utilisation de `wandb artifact put`.
  * Ajout d&#39;un préfixe à la bibliothèque watchdog intégrée.
  * Correction de l&#39;erreur `Circular reference detected` lors de la mise à jour des métadonnées avec un tableau numpy de plus de 32 éléments.
  * Ajout d&#39;une chaîne aléatoire à run&#95;id sur SageMaker pour ne pas casser le mode DDP.

  ## Dév

  * Vérifier que le cluster de test n&#39;est pas arrêté avant la récupération des résultats.
  * Ajout d&#39;un test d&#39;artefact autonome à la suite nightly CPU.
  * Renommage de la branche par défaut en `main`.
  * Mise à jour de l&#39;extension mypy pour la vérification des types protobuf.
  * Mise à jour du nom de branche dans codeql-analysis.yml.
  * Déplacement des tests d&#39;import fonctionnels vers nightly et extension de la couverture des versions Python.
  * Ajout d&#39;une notification Slack pour les échecs des tests d&#39;import nightly.
  * Correction de tests CLI défaillants qui tentaient de téléverser des artefacts inexistants.

  ## Nettoyage

  * Création de job via use&#95;artifact au lieu de log&#95;artifact.
  * Ajout d&#39;une action GH pour automatiser certaines parties du processus de version.
  * Les nuages de points 3D sont désormais visibles dans l&#39;interface utilisateur dans tous les cas.
  * Les URL Git échouaient si fsmonitor est activé.
  * Ignorer les nouveaux répertoires de fichiers générés proto.
  * Correction d&#39;un bug empêchant la suppression de Run Queue dans le SDK.
  * Ajout de la prise en charge de la mutation `pushToRunQueueByName`.
  * Refactorisation de la surveillance et du sondage des métriques système.
  * Correction de gitattribute pour les fichiers générés par protobuf.

  Consultez le [journal des modifications v0.13.5](https://github.com/wandb/wandb/releases/tag/v0.13.5) sur GitHub.
</Update>

<Update label="v0.13.4" description="05 octobre 2022">
  ## Améliorations

  * Afficher l’entité et le projet dans les noms de job k8s.
  * Ajouter une macro de commande de balayage pour les variables d’environnement.
  * Ajouter au type de données `Object3D` des constructeurs `from_*`, ainsi que des scores de confiance pour la caméra de scène et la boîte englobante.
  * Ajouter un indicateur de progression simple pour les téléchargements d’artefacts.
  * Ajouter le callback `WandbMetricsLogger` - un callback `Keras` dédié à la journalisation des métriques.
  * Ajouter le callback `WandbModelCheckpoint` - un callback `Keras` pour les points de contrôle du modèle.
  * Ajouter le callback `WandbEvalCallback` - un callback `Keras` pour journaliser les prédictions du modèle sous forme de tableaux W&amp;B.

  ## Correctifs

  * Convertir en entier l’attribut `max_jobs` de la configuration de l’agent.
  * Corriger le chemin affiché vers `debug-cli.log` (journal de débogage).
  * Gérer les délais d’expiration des requêtes réessayables lors du téléversement d’artefacts vers AWS.
  * Améliorer les retours à l’utilisateur pour les appels longs : résumé, fin.
  * Corriger l’erreur RuntimeError lors de l’utilisation de `keras.WandbCallback` avec `tf.MirroredStrategy`.

  ## Dev

  * Ajouter l’analyse du code avec `codeql`.
  * Valider les titres de PR pour garantir leur conformité aux directives Conventional Commits.
  * Renforcer Launch en épinglant les versions de build de `kaniko` et `launch-agent-dev`.
  * Ajouter la télémétrie pour le package `mmengine`.
  * Ajouter le type `build` à notre configuration Conventional Commits.
  * Ajouter la dépendance `tensorflow_datasets` au shard `imports12`.
  * Corriger le test sb3 en épinglant la dépendance upstream.
  * Rendre le SDK compatible avec protobuf v4.
  * Corriger la coloration de la sortie de flake8.
  * Corriger le répertoire des ressources de test pour les références d’artefact.

  ## Nettoyage

  * Corriger l’indication de type pour l’argument `filters` dans `public_api.runs`.
  * Améliorer les annotations de type liées à artifact-file-creation.
  * Améliorer les annotations de type et la configuration VSCode pour l’API publique.
  * Rendre les annotations de type plus faciles à parcourir dans VSCode.
  * Introduire des `NewTypes` basés sur `str` et les utiliser pour divers champs d’Artifact.
  * Ajouter des annotations de type pour obtenir de meilleurs indices dans l’IDE lors de l’utilisation de boto3.

  Consultez le [journal des modifications de la version v0.13.4](https://github.com/wandb/wandb/releases/tag/v0.13.4) sur GitHub.
</Update>

<Update label="v0.13.3" description="08 septembre 2022">
  ## Amélioration

  * Ajoute des `exemples` / `tests` pour `raytune`.
  * Remanie les `tests` unitaires `pytest` pour les exécuter sur un véritable `wandb server`.
  * Ajoute la prise en charge des taints et des tolerations pour Launch sur `kubernetes`.
  * Ajoute Sweeps sur Launch sur Kubernetes.
  * Ajoute du parallélisme aux tests fonctionnels.
  * Met à niveau `mypy` vers la `version` `0.971`.
  * Ajoute une communication interne asynchrone entre processus pour Mailbox.
  * Implémente la recherche de jobs Launch dans la configuration de balayage.
  * Améliore les performances lors de l&#39;envoi de messages volumineux.
  * Intègre la dernière version de `nvidia-ml-py-11.515.48`.
  * Améliore les performances en augmentant la taille de réception sur le socket du service.
  * Ajoute la prise en charge de isort avec le profil black.
  * Implémente l&#39;envoi des résultats de tests vers CircleCI pour les tests nocturnes.
  * Ajoute un mode débogage pour les `tests` unitaires `pytest`.
  * Ajoute la prise en charge des arguments dans les jobs Launch.
  * Ajoute la requête FetchRunQueueItemById.
  * Ajoute la télémétrie pour keras-cv.
  * Ajoute le suivi de session sentry.
  * Ajoute la possibilité de journaliser un artefact tout en le liant à un modèle enregistré.

  ### Nettoyage

  * Interrompt les hooks de gradient et de paramètres.
  * Ajoute un message d&#39;erreur explicite pour les doublons uri/docker-image.
  * Vérifie que les arguments de la fixture wandb&#95;init sont synchronisés avec wandb.init().
  * Met à niveau le cluster GKE utilisé pour les tests nocturnes vers `n1-standard-8`.
  * Déplace l&#39;arrêt du service à la fin des tests.
  * Réduit le parallélisme du job `pytest` de 10 à 6.
  * Supprime la documentation utilisateur du service.
  * Déplace la logique `_timestamp` vers le processus interne.
  * Ajoute un message d&#39;erreur pour Launch `gitversion`.
  * Met à jour l&#39;image de VM KFP dans CircleCI.
  * Met à niveau sweeps vers la dernière version.
  * Implémente le nettoyage du planificateur Sweep et améliore les tests.
  * Ajoute l&#39;obligation d&#39;obtenir l&#39;approbation de l&#39;équipe sdk pour les changements d&#39;API.
  * Ajoute davantage de temps pour le commit d&#39;artefact.
  * Implémente une configuration tox avec résolution dynamique.
  * Supprime l&#39;épinglage de version `buildx` pour les builds nocturnes.
  * Déplace les configurations de run Launch de entrypoint vers params.
  * Supprime l&#39;utilisation de l&#39;orbe Slack du job Win sur CircleCI.
  * Ajoute l&#39;analyse des heartbeat pour les arguments de run Launch à l&#39;aide de l&#39;agent hérité.
  * Améliore la gestion des erreurs lors de l&#39;arrêt du service.
  * Nettoie le pipeline de création de job Launch.
  * Ajoute des détails au message d&#39;erreur lors du téléversement d&#39;un artefact avec le mauvais type.
  * Ajoute un paramètre de délai d&#39;expiration facultatif à artifacts wait().
  * Assainit les génériques numpy dans les clés.
  * Supprime la réaffectation de la fonction run dans l&#39;API publique.
  * Rend facultative la récupération des sweeps lors de l&#39;utilisation de l&#39;API publique pour interroger les runs.
  * Met à jour la documentation de référence de `wandb.init` afin de fournir plus d&#39;informations sur les caractères spéciaux.

  ## Correction de bug

  * Corrige l&#39;exigence de Sweeps sur les jobs Launch.
  * Corrige l&#39;encodage JSON des métadonnées Artifact pour accepter davantage de types.
  * Ajuste la logique de traitement du paramètre `root_dir`.
  * Empêche run.log() de modifier les arguments transmis.
  * Corrige le test `05-batch5.py`.
  * Permet aux utilisateurs de contrôler le `run_id` via la spécification Launch.
  * Corrige un écrasement accidentel dans `config.yml`.
  * Garantit la propagation de `base_url` surchargé lors de l&#39;initialisation de l&#39;API publique.
  * Corrige la configuration de lancement CLI de Sweeps sur Launch, relpath.
  * Corrige le message d&#39;erreur d&#39;API key défaillant de Launch.
  * Marque le test sweeps instable comme xfail.
  * Corrige le message d&#39;erreur Launch `gitversion`.
  * Corrige `yea-wandb` dev release -&gt; release.
  * Nettoie les problèmes restants après le renommage client-&gt;wandb.
  * Corrige le test precision recall.
  * Corrige le test fonctionnel sklearn.
  * Corrige le blocage provoqué par une interruption clavier sous Windows.
  * Corrige le tag de conteneur de test par défaut.
  * Corrige la gestion du résumé dans conftest.py.
  * Corrige quelques petites fautes de frappe dans la sortie CLI.
  * Corrige le problème déclenché par la mise à jour de colab en utilisant le fichier par défaut et en interceptant les exceptions.
  * Corrige le problème de verrouillage de mailbox.
  * Corrige l&#39;inclusion d&#39;une variable dans la chaîne de journal.
  * Corrige l&#39;attribut `wandb.Artifacts.artifact.version`.
  * Corrige la transmission des arguments docker par l&#39;agent Launch.
  * Corrige RecursionError lors de l&#39;affichage de l&#39;objet User de l&#39;API publique sans récupération de l&#39;e-mail.
  * Corrige la désérialisation des noms de colonne numériques.

  Consultez le [journal des modifications v0.13.3](https://github.com/wandb/wandb/releases/tag/v0.13.3) sur GitHub.
</Update>

<Update label="v0.13.2" description="22 août 2022">
  ## Correction de bug

  * Corrige le problème déclenché par la mise à jour de colab en utilisant le fichier par défaut et en interceptant les exceptions.

  Consultez le [journal des modifications v0.13.2](https://github.com/wandb/wandb/releases/tag/v0.13.2) sur GitHub.
</Update>

<Update label="v0.13.1" description="05 août 2022">
  ## Correction de bugs

  * Empêche `run.log()` de modifier les arguments passés.

  Consultez le [journal des modifications v0.13.1](https://github.com/wandb/wandb/releases/tag/v0.13.1) sur GitHub.
</Update>

<Update label="v0.13.0" description="04 août 2022">
  ## Amélioration

  * Active le service par défaut.
  * Ajoute une logique de prise en charge pour gérer les messages envoyés par le serveur.
  * Permet aux runs de produire des jobs à la fin.
  * Ajoute Job, QueuedRun et la gestion des jobs dans Launch.
  * Ajoute à l&#39;agent Launch la prise en charge des rôles d&#39;instance dans EC2 et EKS.
  * Ajoute le comportement par défaut au callback Keras : toujours enregistrer les points de contrôle du modèle comme artefacts.
  * Assainit le nom de l&#39;artefact dans KerasCallback pour l&#39;enregistrement des artefacts de modèle.
  * Améliore la journalisation dans la console en déplaçant l&#39;émulateur vers le processus de service.
  * Corrige un problème de corruption des données lors de la journalisation de grands volumes de données.
  * Ajoute l&#39;état à la représentation du balayage dans l&#39;API publique.
  * Ajoute une option pour spécifier un répertoire racine Git différent à l&#39;aide des paramètres ou de variables d&#39;environnement.
  * Ajoute une option pour transmettre `remote url` et `commit hash` comme arguments aux paramètres ou comme variables d&#39;environnement.
  * Améliore la résolution temporelle des métriques suivies et des métriques système.
  * Utilise par défaut le nom du projet de la configuration de balayage lorsque le projet n&#39;est pas spécifié dans l&#39;appel `wandb.sweep()`.
  * Ajoute la prise en charge de l&#39;utilisateur défini au niveau de l&#39;espace de noms par l&#39;agent Launch.
  * Ajoute de la télémétrie pour suivre les cas où un run pourrait être écrasé.
  * Ajoute un outil pour exporter l&#39;historique de `wandb` vers `sqlite`.
  * Remplace certains types `Mapping[str, ...]` par des `NamedTuples`.
  * Ajoute un hook d&#39;import pour la télémétrie des runs.
  * Implémente la prise en charge du profilage pour les IPU.

  ## Correction de bugs

  * Corrige l&#39;agent de balayage avec le service.
  * Corrige le cas où un type vide équivaut à un type non valide, ainsi que la façon dont les dictionnaires d&#39;artefacts sont gérés.
  * Corrige l&#39;objet `wandb.Config` pour prendre en charge les valeurs par défaut lors de l&#39;accès à un attribut.
  * Supprime la configuration par défaut des jobs.
  * Corrige un problème où patch vaut `None`.
  * Corrige l&#39;analyse de requirements.txt dans les vérifications d&#39;installation nightly du SDK.
  * Corrige la gestion des conflits 409 lorsque les requêtes GraphQL expirent.
  * Corrige la gestion de l&#39;arrêt du service si le processus utilisateur a été terminé.
  * Ajoute `storage_path` et corrige `artifact.files`.
  * Corrige un problème de performances lors de la synchronisation de runs comportant un grand nombre de fichiers média.

  ### Nettoyage

  * Ajoute un mécanisme de secours pour désactiver le service.
  * Annote `wandb/docker` et annule la modification du fixture Docker.
  * Corrige GFLOPS en GFLOPs dans le `WandbCallback` Keras.
  * Ajoute des annotations de type pour `file_stream.py`.
  * Renomme le dépôt de `client` en `wandb`.
  * Met à jour la documentation : ajout de `--report_to wandb` pour HuggingFace Trainer.
  * Rend les alias facultatifs dans link&#95;artifact.
  * Renomme `wandb local` en `wandb server`.
  * Met à jour les badges du README.

  Consultez le [journal des modifications v0.13.0](https://github.com/wandb/wandb/releases/tag/v0.13.0) sur GitHub.
</Update>

<Update label="v0.12.21" description="05 juillet 2022">
  ## Amélioration

  * Corrige le fait que la configuration ne s&#39;affiche pas tant que le run n&#39;est pas terminé.
  * Ajoute de nouveaux types au TypeRegistry pour gérer les objets artefact dans les jobs et les configurations de run.
  * Ajoute une nouvelle requête à l&#39;API interne pour obtenir l&#39;état du run.
  * Remplace les chargeurs YAML non sécurisés par yaml.safe&#95;load.
  * Améliore l&#39;outillage de test en permettant de spécifier des shards dans les tests manuels.
  * Corrige les courbes ROC et PR dans l&#39;intégration sklearn en stratifiant l&#39;échantillonnage.
  * Corrige la zone de saisie dans les notebooks qui dépassait l&#39;espace de la cellule.
  * Permet de passer une chaîne de caractères comme alias pour link&#95;model.
  * Ajoute la prise en charge du calcul des FLOPS dans le `WandbCallback` de `keras`.
  * Étend l&#39;édition des rapports Python.

  ## Correction de bugs

  * Corrige le logger de statistiques afin qu&#39;il puisse trouver tous les bons GPU dans les processus enfants.
  * Corrige une régression dans le téléversement de références S3 pour les dossiers.
  * Corrige la logique de commit des artefacts pour gérer les collisions dans le backend.
  * Vérifie la présence d&#39;une réponse `None` dans la logique de nouvelle tentative (contrôle de sécurité).
  * Ajoute les Sweeps par-dessus Launch (actuellement en MVP).
  * Renomme le répertoire et les fichiers des tests fonctionnels.

  ### Nettoyage

  * Corrige l&#39;ordre des conditions du helper `_to_dict`.
  * Corrige le lien cassé du journal des modifications vers la PR 3709.
  * Corrige la requête de l&#39;API publique (QueuedJob Api).
  * Renomme les runners locaux en local-container et local-process.
  * Ajoute des annotations de type aux fichiers dans le répertoire wandb/filesync.
  * Réorganise tous les répertoires de test pour qu&#39;ils aient un répertoire racine commun.
  * Corrige la configuration des tests et ajoute une machine plus puissante sur `CircleCI`.
  * Corrige une faute de frappe dans le fichier README de `wandb-service-user`.
  * Corrige un test d&#39;artefact défaillant lié à une régression.
  * Supprime les fichiers inutilisés (liés à `py27`) et la déclaration `submodules` vide.
  * Ajoute un extra pour la dépendance cloudpickle de model reg.
  * Remplace les alias de threading obsolètes.
  * Met à jour le README du `sdk` avec les commandes renommées (local -&gt; server).

  Consultez le [journal des modifications v0.12.21](https://github.com/wandb/wandb/releases/tag/v0.12.21) sur GitHub.
</Update>

<Update label="v0.12.20" description="29 juin 2022">
  ## Correction de bugs

  * Relance `commit_artifact` en cas d&#39;erreur de conflit.

  Consultez le [journal des modifications v0.12.20](https://github.com/wandb/wandb/releases/tag/v0.12.20) sur GitHub.
</Update>

<Update label="v0.12.19" description="22 juin 2022">
  ## Correction de bugs

  * Corrige une régression dans le téléversement de références S3 pour les dossiers.

  Consultez le [journal des modifications v0.12.19](https://github.com/wandb/wandb/releases/tag/v0.12.19) sur GitHub.
</Update>

<Update label="v0.12.18" description="09 juin 2022">
  ## Amélioration

  * Launch : BareRunner basé sur LocalRunner.
  * Ajout de la possibilité de spécifier une clé API pour l&#39;API publique.
  * Ajout de la prise en charge, dans les artefacts, des fichiers Unicode sous Windows.
  * Ajout de la télémétrie pour les nouveaux packages.
  * Amélioration de la gestion des clés API.
  * Ajout d&#39;informations sur `wandb server` lors de la connexion.

  ## Correction de bugs

  * Prise en charge native des horodatages dans les types de tableau Python.
  * Ajout de la prise en charge de magic avec service.
  * Ajout de tests unitaires pour DirWatcher et les classes associées.
  * Amélioration de `DirWatcher.update_policy` : O(1) au lieu de O(nombre de fichiers téléversés).
  * Ajout d&#39;un argument pour contrôler ce qui est journalisé dans le callback SB3.
  * Amélioration du nommage des paramètres dans l&#39;intégration sb3.
  * Ajustement des exigences de configuration de l&#39;environnement de développement sur Mac M1.
  * Launch : correction des clés Linux de l&#39;image de base NVIDIA.
  * Correction de la gestion de la file d&#39;attente des runs Launch à partir du fichier de configuration.
  * Correction d&#39;un problème où les tfevents n&#39;étaient pas toujours traités.
  * [Snyk] Correctif pour 8 vulnérabilités.
  * Correction du gestionnaire de stockage s3 pour téléverser des dossiers lorsque les noms de clé entrent en collision.
  * Chargement correct des horodatages à partir des tableaux dans les artefacts.
  * Exiger `protobuf&lt;44`.
  * Permettre aux conteneurs créés via launch d&#39;être relancés comme jobs de conteneur.
  * Correction de l&#39;intégration tensorboard qui ignorait certaines étapes à `finish()`.
  * Renommage de `wandb local` en `wandb server`.
  * Correction de la commande `docker inspect` défectueuse.
  * Ajout d&#39;un sentry dédié à wandb.
  * Le type Image doit gérer correctement les anciens paramètres de type.

  ### Nettoyage

  * Intégration de FileEventHandler.synced dans la seule méthode où il est utilisé.
  * Utilisation de l&#39;argument size transmis pour faire de `PolicyLive.min_wait_for_size` une méthode de classe.
  * Transformation de FileEventHandler en ABC et suppression de certaines implémentations de méthode « par défaut » qui n&#39;étaient utilisées qu&#39;une seule fois.
  * Suppression d&#39;un champ inutilisé de DirWatcher.
  * Utilisation de sweeps comme extra au lieu de l&#39;inclure directement.
  * Ajout de tests CI nocturnes.
  * Amélioration de keras et de la documentation de référence des types de données.
  * Mise à jour des exigences de version de `pytorch` dans les environnements de développement.
  * Nettoyage de la configuration CircleCI.
  * Ajout des tests `py310` dans la CI.
  * Suppression de `dateutil` des exigences.
  * Ajout de la chaîne obsolète à `Table.add_row`.

  Consultez le [journal des modifications v0.12.18](https://github.com/wandb/wandb/releases/tag/v0.12.18) sur GitHub.
</Update>

<Update label="v0.12.17" description="26 mai 2022">
  ## Correction de bugs

  * Mise à jour des exigences pour corriger l&#39;incompatibilité avec protobuf &gt;= 4.

  Consultez le [journal des modifications v0.12.17](https://github.com/wandb/wandb/releases/tag/v0.12.17) sur GitHub.
</Update>

<Update label="v0.12.16" description="03 mai 2022">
  ## Amélioration

  * Amélioration du pied de page W&amp;B en alignant le résumé et l&#39;historique dans l&#39;environnement notebook.
  * Activation de la journalisation expérimentale des étapes de l&#39;historique dans les artefacts.
  * Ajout de la macro `args_no_boolean_flags` à la configuration de balayage.
  * Ajout de la prise en charge de la journalisation pour `jax.bfloat.bfloat16`.
  * Lever une exception lorsque la taille de Table dépasse la limite.
  * Ajout du builder kaniko k8s pour wandb launch.
  * Ajout du paramètre de délai d&#39;expiration de wandb.init().
  * Ne pas supposer qu&#39;un exécutable est disponible pour les points d&#39;entrée donnés avec wandb launch.
  * Les environnements Jupyter ne collectent plus les arguments de commande.
  * Ajout de la prise en charge du format TensorFlow/Keras SavedModel.

  ## Correction de bugs

  * Prise en charge des ID de version dans les références d&#39;artefact et correction des références s3/gcs sous Windows.
  * Correction de la prise en charge de plusieurs finish pour un seul run avec wandb-service.
  * Correction des backtraces dupliquées lors de l&#39;utilisation de wandb-service.
  * Correction de l&#39;entité incorrecte affichée dans le message de connexion.
  * Correction du blocage lorsque `wandb.init` est interrompu en cours de configuration avec wandb-service.
  * Correction de la gestion des interruptions clavier pour éviter les blocages lorsque wandb-service est activé.
  * Correction de la journalisation dans la console avec des sorties print très longues lors de l&#39;utilisation de wandb-service.
  * Correction d&#39;une chaîne d&#39;artefact incorrecte dans la configuration d&#39;initialisation de launch.

  ### Nettoyage

  * Correction d&#39;une faute de frappe dans la docstring de wandb.log().
  * Nettoyage du code de graphique personnalisé et ajout d&#39;annotations de type aux fonctions de graphique.
  * Amélioration de `wandb.init(settings=)` pour gérer l&#39;objet `Settings` de manière similaire au paramètre `dict`.
  * Ajout d&#39;une note dans la documentation sur api.viewer dans api.user() et api.users().
  * Préciser explicitement dans setup.py que seule la version py3+ est prise en charge.
  * Ajout d&#39;annotations de type à DirWatcher.
  * Amélioration de la docstring de wandb.log() pour utiliser le bon nom d&#39;argument.

  Consultez le [journal des modifications v0.12.16](https://github.com/wandb/wandb/releases/tag/v0.12.16) sur GitHub.
</Update>

<Update label="v0.12.15" description="21 avril 2022">
  ## Amélioration

  * Optimiser la journalisation de wandb.Image lorsqu’elle est liée à un artefact.

  Consultez le [journal des modifications de v0.12.15](https://github.com/wandb/wandb/releases/tag/v0.12.15) sur GitHub.
</Update>

<Update label="v0.12.14" description="08 avril 2022">
  ## Correction de bugs

  * Corriger une régression : désactiver l’enregistrement de l’étape d’historique dans les artefacts.

  Consultez le [journal des modifications de v0.12.14](https://github.com/wandb/wandb/releases/tag/v0.12.14) sur GitHub.
</Update>

<Update label="v0.12.13" description="07 avril 2022">
  ## Correction de bugs

  * Annuler le renforcement de la validation de api&#95;key.

  Consultez le [journal des modifications de v0.12.13](https://github.com/wandb/wandb/releases/tag/v0.12.13) sur GitHub.
</Update>

<Update label="v0.12.12" description="05 avril 2022">
  ## Amélioration

  * Autoriser le passage d’objets run à d’autres processus lors de l’utilisation de wandb-service.
  * Ajouter create user à l’API publique.
  * Prendre en charge la journalisation depuis plusieurs processus avec wandb-service.
  * Ajouter l’indicateur gpus pour le runner Launch local avec cuda.
  * Améliorer l’agent Launch déployable.
  * Ajouter l’intégration Kubernetes à Launch.
  * KFP : ajouter l’utilitaire de visualisation wandb.
  * KFP : ajouter un lien de retour vers l’interface Kubeflow.
  * Ajouter une macro d’argument pour indicateur booléen.

  ## Correction de bugs

  * Améliorer la validation de host / WANDB&#95;BASE&#95;URL.
  * Corriger tempfile/insecure.
  * Corriger les avertissements excessifs si le WANDB&#95;DIR/root&#95;dir demandé n’est pas accessible en écriture.
  * Corriger line&#95;series pour tracer un tableau de chaînes.
  * Gérer correctement les arguments de ligne de commande avec le service.
  * Améliorer la validation de api&#95;key.
  * Corriger plusieurs problèmes de performances dus à la non-utilisation de defaultdict.
  * Activer inf max jobs sur l’agent de lancement.
  * Corriger la commande colab pour qu’elle fonctionne avec Launch.
  * Corriger une faute de frappe dans la docstring de Config.
  * Ne plus faire de l’enregistrement du code une politique, et conserver la logique personnalisée précédente.
  * Corriger la journalisation des images de séquence avec le service.
  * Ajouter le nom d’utilisateur au fichier journal de debug-cli pour éviter les conflits entre plusieurs utilisateurs.
  * Corriger l’agent de balayage Python pour les utilisateurs de wandb service / pytorch-lightning.
  * Supprimer les vérifications inutiles des dépendances de Launch.
  * Contournement pour le Writer non fermé de MoviePy.
  * Améliorer la gestion des objets Run lorsque le service n’est pas activé.

  Consultez le [journal des modifications de v0.12.12](https://github.com/wandb/wandb/releases/tag/v0.12.12) sur GitHub.
</Update>

<Update label="v0.12.11" description="01 mars 2022">
  ## Amélioration

  * Ajouter des légendes à Molecules.
  * Ajouter l’intégration CatBoost.
  * Launch : intégration AWS Sagemaker.
  * Launch : supprimer repo2docker et ajouter la prise en charge des GPU.
  * Ajouter l’inférence de Timestamp depuis Python pour Weave.
  * Launch : intégration GCP Vertex.
  * Utiliser Artifacts lorsqu’ils sont placés dans la configuration du run. Accepter une chaîne pour représenter un artefact dans la configuration du run.
  * Améliorer `wandb_callback` de xgboost (#2929).
  * Ajouter une prise en charge initiale de kubeflow pipeline.

  ## Correction de bugs

  * Corriger la journalisation d’images avec des caractères spéciaux dans la clé.
  * Corriger la logique de nouvelle tentative de téléversement vers Azure Blob.
  * Corriger le champ program pour les scripts exécutés comme module Python.
  * Corriger le problème où `sync_tensorboard` pouvait échouer sur de grands histogrammes.
  * Corriger un problème de performances du service wandb lors de l’arrêt du run.
  * Corriger l’inclusion de gql et graphql.
  * Vider les données du journal sans finish avec le service.
  * Corriger le blocage du service wandb lorsque celui-ci plante.
  * Corriger le problème de journalisation d’images avec &quot;/&quot; sous Windows.
  * Ajouter les noms de fichiers d’image à images/separated media.
  * Ajouter setproctitle à requirements.txt.
  * Corriger le problème où les identifiants de run Sagemaker cassent les files d’attente de run.
  * Corriger l’exception d’encodage lors de l’utilisation de la magic %%capture.

  Consultez le [journal des modifications de v0.12.11](https://github.com/wandb/wandb/releases/tag/v0.12.11) sur GitHub.
</Update>

<Update label="v0.12.10" description="01 février 2022">
  ## Amélioration

  * Amélioration de la validation lors de la création de Tables à partir de dataframes avec des colonnes non valides.
  * Activation de la déduplication des empreintes pour les appels `use_artifact()`.
  * Prototype initial de prise en charge du téléversement vers Azure Blob Storage.

  ## Correction de bugs

  * Correction de wandb launch avec les versions de développement de Python.
  * Correction du chargement des tableaux enregistrés avec des types mixtes.
  * Correction de `ResourceWarning` lors de l&#39;appel à wandb.log.
  * Correction du curseur manquant dans ProjectArtifactCollections.
  * Correction d&#39;un problème avec les classes de journalisation des tableaux sous Windows.
  * Gestion correcte des libellés de chaîne dans wandb.sklearn.plot.classifier.calibration&#95;curve.
  * Suppression de l&#39;avertissement de connexion lors de l&#39;appel à wandb.sweep().

  ### Nettoyage

  * Suppression des dépendances de rétroportage Python 2 (enum34, subprocess32, configparser).
  * Refactorisation des paramètres.

  Consultez le [journal des modifications v0.12.10](https://github.com/wandb/wandb/releases/tag/v0.12.10) sur GitHub.
</Update>

<Update label="v0.12.9" description="16 décembre 2021">
  ## Correction de bugs

  * Correction d&#39;une régression dans le gestionnaire d&#39;exceptions de `upload_file()`.

  Consultez le [journal des modifications v0.12.9](https://github.com/wandb/wandb/releases/tag/v0.12.9) sur GitHub.
</Update>

<Update label="v0.12.8" description="16 décembre 2021">
  ## Amélioration

  * Mise à jour du guide de contribution et de l&#39;outil de configuration de l&#39;environnement de développement.
  * Amélioration de `wandb_callback` pour LightGBM (#2945).

  ## Correction de bugs

  * Réduction de l&#39;utilisation de la mémoire GPU lors de la génération de l&#39;histogramme des poids du modèle.
  * Prise en charge des classes mixtes dans les couches d&#39;annotation des boîtes englobantes et des masques d&#39;image.
  * Ajout des arguments max-jobs et launch async.
  * Prise en charge des listes d&#39;objets Summary encodés sous forme de chaînes dans wandb.tensorboard.log.
  * Correction de la gestion des tableaux np à 0 dimension.
  * Correction de la gestion d&#39;un fichier de configuration par défaut vide.
  * Ajout d&#39;un backend de service utilisant des sockets (prise en charge de fork).
  * Envoi du port git avec l&#39;URL lors de l&#39;envoi du dépôt git.
  * Ajout de la prise en charge des adresses IP brutes pour launch.
  * Tables ne sérialise ni ne masque plus les NDArrays 1D.
  * Correction du téléversement des fichiers d&#39;artefact vers les stockages S3.
  * Envoi de la liste des fichiers téléversés lors des heartbeats du flux de fichiers.
  * Ajout de la prise en charge des couches expérimentales de keras.
  * Correction de `from wandb import magic` pour ne plus nécessiter tensorflow.
  * Correction d&#39;une erreur d&#39;autorisation dans launch.

  Consultez le [journal des modifications v0.12.8](https://github.com/wandb/wandb/releases/tag/v0.12.8) sur GitHub.
</Update>

<Update label="v0.12.7" description="18 novembre 2021">
  ## Correction de bugs

  * Correction d&#39;un problème où la diffusion des journaux de console provoquait un trafic réseau excessif.
  * Metaflow : les dépendances facultatives sont désormais réellement facultatives.
  * Correction des docstrings pour wandb.watch et ValidationDataLogger.
  * Empêchement de l&#39;envoi de runs par l&#39;agent Launch vers un autre projet ou une autre entité.
  * Correction de la journalisation de pr&#95;curves via tensorboard.
  * Empêchement du signalement de métriques non valides par la surveillance TPU lorsqu&#39;elles ne sont pas disponibles.
  * Renforcement de la robustesse des dépendances d&#39;ordre d&#39;importation pour WandbCallback.
  * Correction d&#39;un bug dans le tracé de l&#39;importance des variables afin de gérer des matrices de formes différentes.
  * Correction de la gestion de l&#39;URL de base pour autoriser un `/` final.
  * Empêchement de l&#39;envoi d&#39;un trop grand nombre de heartbeats par wandb.agent(), ce qui affectait les limites de débit.
  * Masquage des informations sensibles dans les journaux de débogage.

  ## Amélioration

  * Ajout de la prise en charge de wandb.Molecule pour les formats pris en charge par rdkit.
  * Ajout de docstrings au niveau du module pour les modules de documentation de référence.
  * Stockage des métadonnées launch dans un fichier.
  * Ajout de l&#39;appel d&#39;API publique Project.sweeps() pour afficher tous les balayages d&#39;un projet.
  * Garantie que l&#39;invite de clé API reste captive lorsque l&#39;utilisateur ne saisit rien.
  * Refactorisation de wandb.sklearn en sous-modules.
  * Prise en charge des artefacts de code dans wandb launch.
  * Amélioration de l&#39;agent Launch (async, arrêt, mises à jour heartbeat).
  * Amélioration de l&#39;utilisation et des messages d&#39;erreur pour le mode anonyme.
  * Ajout d&#39;un exemple montrant comment trouver des runs avec wandb.Api().runs(...) correspondant à une regex.

  Consultez le [journal des modifications v0.12.7](https://github.com/wandb/wandb/releases/tag/v0.12.7) sur GitHub.
</Update>

<Update label="v0.12.6" description="27 octobre 2021">
  ## Correction de bugs

  * Correction d&#39;un problème avec sklearn `plot_calibration_curve()` qui cassait le modèle fourni.
  * Correction de `CondaEnvExportError` en redirigeant stderr.
  * Correction de `use_artifact()` lors de la spécification d&#39;un artefact provenant d&#39;un autre projet.

  ## Amélioration

  * Ajout des noms de métriques aux graphiques de courbe PR dans tensorboard.

  Consultez le [journal des modifications v0.12.6](https://github.com/wandb/wandb/releases/tag/v0.12.6) sur GitHub.
</Update>

<Update label="v0.12.5" description="19 octobre 2021">
  ## correction de bug

  * Signale les erreurs dues à des caractères non valides dans les clés des médias enregistrés sous Windows.
  * Gère les erreurs lors de la détection des TPU dans des environnements non pris en charge.
  * Corrige un bug où `%%wandb`, suivi de wandb.init(), n&#39;affiche pas les liens de run.
  * Corrige api.runs() pour renvoyer correctement tous les runs de l&#39;entité/projet actuel.

  ## Amélioration

  * Ajoute `wandb.require(experiment="service")` pour améliorer la prise en charge du multiprocessing.
  * Ajoute la prise en charge des artefacts interchangeables dans le contexte Launch.
  * Ajoute la prise en charge de `wandb.login(timeout=)` pour les environnements Jupyter.
  * Ajoute la possibilité de désactiver l&#39;enregistrement de la référence Git avec `WANDB_DISABLE_GIT`.
  * Prend en charge des versions plus récentes de pytest-mock et PyYAML.
  * Ajoute la possibilité de supprimer des artefacts avec leurs alias : `artifact.delete(delete_aliases=True)`.
  * Ajoute la méthode `unwatch()` à l&#39;objet Run.

  Consultez le [journal des modifications de la v0.12.5](https://github.com/wandb/wandb/releases/tag/v0.12.5) sur GitHub.
</Update>

<Update label="v0.12.4" description="05 octobre 2021">
  ## correction de bug

  * Corrige une régression introduite dans la version 0.12.2 qui provoquait un accès réseau lorsque `WANDB_MODE=offline`.

  Consultez le [journal des modifications de la v0.12.4](https://github.com/wandb/wandb/releases/tag/v0.12.4) sur GitHub.
</Update>

<Update label="v0.12.3" description="30 septembre 2021">
  ## correction de bug

  * Corrige la condition d&#39;arrêt de la recherche sur grille dans le contrôleur local.

  ## Amélioration

  * Ajoute une nouvelle commande magique Jupyter pour afficher les runs, les balayages et les projets : `%wandb path/to/run -h 1024`.
  * N&#39;affiche plus l&#39;iframe du run par défaut dans Jupyter ; ajoutez `%%wandb` à une cellule pour afficher un run.
  * Fait en sorte que la demande de clé API se répète indéfiniment en cas de saisie mal formée.
  * Permet d&#39;inviter des utilisateurs dans des équipes via l&#39;API `api.team("team_name").invite("username_or_email")`.
  * Permet de supprimer des utilisateurs d&#39;une équipe via l&#39;API `api.team("team_name").members[0].delete()`.
  * Permet de créer des comptes de service via l&#39;API `api.team("team_name").create_service_account("Description")`.
  * Permet de gérer les clés API via l&#39;API `api.user("username_or_email").generate_api_key()`.
  * Ajoute la prise en charge des traces de profilage PyTorch avec `wandb.profiler.torch_trace_handler()`.

  Consultez le [journal des modifications de la v0.12.3](https://github.com/wandb/wandb/releases/tag/v0.12.3) sur GitHub.
</Update>

<Update label="v0.12.2" description="15 septembre 2021">
  ## correction de bug

  * Corrige tensorboard&#95;sync pour gérer les fichiers tfevents éphémères de SageMaker.
  * Corrige la requête Reports depuis l&#39;API publique (pagination défaillante et chemin du rapport).
  * Corrige `wandb.login()` lorsque relogin est spécifié (force la connexion une seule fois).

  ## Amélioration

  * Nettoie la sortie de bas de page des métriques de résumé et d&#39;historique.
  * Nettoie le message d&#39;erreur de `wandb sweep --update`.
  * Ajoute un avertissement pour les utilisateurs de `wandb local` afin qu&#39;ils mettent à jour Docker.
  * Ajoute l&#39;argument facultatif log&#95;learning&#95;curve à wandb.sklearn.plot&#95;classifier().
  * Restaure les versions figées des paquets pip lors de l&#39;utilisation de `wandb launch`.
  * Ajoute la prise en charge des notebooks Jupyter dans Launch.
  * Ajoute l&#39;option de délai d&#39;expiration à `wandb.login()`.

  Consultez le [journal des modifications de la v0.12.2](https://github.com/wandb/wandb/releases/tag/v0.12.2) sur GitHub.
</Update>

<Update label="v0.12.1" description="26 août 2021">
  ## correction de bug

  * Corrige l&#39;absence de journalisation des exemples de validation avec tensorflow/keras 2.6.
  * Corrige l&#39;absence de prise en charge du temps sur l&#39;axe des x pour les métriques journalisées via tensorboard.
  * Corrige la gestion de la variable d&#39;environnement `WANDB_IGNORE_GLOBS`.
  * Corrige la gestion de sys.stdout lorsqu&#39;il est configuré avec un journaliseur personnalisé.
  * Corrige le fait que les graphiques d&#39;importance des variables de sklearn ne correspondent pas correctement aux noms des variables.
  * Corrige un problème où les URL Colab n&#39;étaient pas capturées.
  * Enregistre la ligne de commande du programme si l&#39;exécutable du run se trouvait en dehors du cwd.

  ## Amélioration

  * Ajoute l&#39;intégration Prodigy pour téléverser des jeux de données annotés vers W&amp;B Tables.
  * Ajoute une prise en charge initiale de Metaflow.
  * Ajoute la prise en charge expérimentale de wandb launch.
  * Ajoute des avertissements indiquant que les requêtes de l&#39;API publique expirent et permet de les outrepasser.
  * Améliore la gestion des erreurs dans le moteur de balayages du contrôleur local.

  Consultez le [journal des modifications de la v0.12.1](https://github.com/wandb/wandb/releases/tag/v0.12.1) sur GitHub.
</Update>

<Update label="v0.12.0" description="10 août 2021">
  ### N’est plus pris en charge

  * Suppression de la prise en charge de Python 3.5.

  ## Correction de bugs

  * Correction d’un problème pouvant entraîner l’échec du téléversement d’artefacts si les fichiers d’artefact étaient en cours de modification.
  * Correction d’un problème où `wandb.restore()` ne fonctionnait pas avec les runs issus d’un balayage.

  ## Amélioration

  * Amélioration du calcul du temps d’exécution des runs.

  consultez le [journal des modifications de la version v0.12.0](https://github.com/wandb/wandb/releases/tag/v0.12.0) sur GitHub.
</Update>

<Update label="v0.11.2" description="02 août 2021">
  ## Correction de bugs

  * Restauration de la bibliothèque graphql-core intégrée en raison d’une régression réseau.

  consultez le [journal des modifications de la version v0.11.2](https://github.com/wandb/wandb/releases/tag/v0.11.2) sur GitHub.
</Update>

<Update label="v0.11.1" description="29 juillet 2021">
  ## Obsolète

  * Python 3.5 ne sera plus pris en charge à partir de `wandb==0.12.0`.

  ## Correction de bugs

  * Réduction de l’empreinte mémoire des images dans les tableaux.
  * Ajout d’une dépendance à graphql-core&gt;=2.3.0.
  * Suppression de l’épinglage d’urllib3 pour éviter les conflits ; si vous voyez des erreurs liées à urllib3, exécutez `pip install --upgrade urllib3`.
  * Amélioration des messages d’erreur HTTP de l’API publique.
  * Définition de run.dir sur le nom du répertoire généré en mode désactivé.

  ## Amélioration

  * Ajout de la prise en charge de la journalisation native des tableaux JAX.
  * Les tableaux prennent désormais en charge le type de données Molecule.
  * Amélioration de l’API Stable-Baselines3 en journalisant automatiquement le nom du modèle et en téléversant systématiquement les modèles à la fin de l’entraînement.
  * Implémentation du contrôleur local de balayage à l’aide de wandb/sweeps.

  consultez le [journal des modifications de la version v0.11.1](https://github.com/wandb/wandb/releases/tag/v0.11.1) sur GitHub.
</Update>

<Update label="v0.11.0" description="15 juillet 2021">
  ### N’est plus pris en charge

  * Suppression de la prise en charge de Python 2.7.

  ## Correction de bugs

  * Correction d’un problème où `wandb.watch()` empêchait l’enregistrement des modèles dans PyTorch.
  * Correction d’un problème où les paramètres de balayage uniformes étaient analysés comme int&#95;uniform.
  * Correction d’un problème où le thread file&#95;stream était interrompu en cas d’erreurs 4xx.

  ## Amélioration

  * Amélioration des performances de la journalisation d’artefacts en la rendant non bloquante.
  * Ajout de l’intégration wandb pour Stable-Baselines3.
  * Amélioration de la logique d’inférence du callback Keras pour la journalisation de la validation.
  * Exposition de l’état du balayage via l’API publique.
  * Amélioration des performances de récupération des runs de balayage via l’API.

  consultez le [journal des modifications de la version v0.11.0](https://github.com/wandb/wandb/releases/tag/v0.11.0) sur GitHub.
</Update>

<Update label="v0.10.33" description="28 juin 2021">
  ## Correction de bugs

  * Correction d’un problème où `wandb restore` renvoyait une erreur 404 si le run ne comportait pas de fichier diff.patch.
  * Correction d’un problème où wandb.log levait une Exception après avoir tenté de journaliser un dataframe pandas.
  * Correction d’un problème où des runs pouvaient être marqués comme terminés avant la fin du téléversement des fichiers.

  ## Amélioration

  * Désactivation du rechargement des métadonnées du run (telles que la commande) dans les runs repris.
  * Autorisation de la journalisation de dataframes pandas par conversion automatique en tableaux W&amp;B.
  * Correction de la fonction d’exclusion de `log_code()` pour gérer le répertoire .wandb.
  * Amélioration de la gestion de la topologie des modèles PyTorch.
  * Augmentation du délai d’anti-rebond de la configuration à 30 s pour réduire la charge sur WB/backend.
  * Amélioration de la fiabilité de la CLI pour générer des Sweeps avec des noms, des programmes et des paramètres.

  consultez le [journal des modifications de la version v0.10.33](https://github.com/wandb/wandb/releases/tag/v0.10.33) sur GitHub.
</Update>

<Update label="v0.10.32" description="10 juin 2021">
  ## Correction de bugs

  * Renforcement de la résilience de `log_artifact()` face aux erreurs réseau.
  * Suppression des dépendances d’artefact en double.
  * Contournement d’un problème urllib3 sous Windows.
  * Correction d’une régression où ipython se bloquait.
  * Autorisation de la journalisation des valeurs à virgule flottante haute précision de numpy.
  * Réduction du risque de collisions pour les médias adossés à des fichiers ou les objets d’artefact.
  * Correction d’une régression de wandb.watch() lors de la journalisation de graphes PyTorch.

  ## Amélioration

  * Ajout de la prise en charge de la journalisation de tableaux joints et partitionnés.
  * Gestion des avertissements de validation de schéma pour les configurations de balayage.
  * Amélioration de wandb sync pour mieux gérer les erreurs.
  * Ajout de la possibilité d’étiqueter les scripts et dépôts qui utilisent wandb.

  consultez le [journal des modifications de la version v0.10.32](https://github.com/wandb/wandb/releases/tag/v0.10.32) sur GitHub.
</Update>

<Update label="v0.10.31" description="27 mai 2021">
  ## Correction de bug

  * Wandb.login() ne conservait pas correctement le paramètre host.
  * Correction d’un problème où les informations d’étape n’étaient pas correctement synchronisées lors de la synchronisation de répertoires tensorboard.
  * Correction de problèmes Unicode avec python2.7.
  * Correction d’un bug dans `plot_calibration_curve` pour ComplementNB.
  * Utilisation d’un mode de repli sans SendFile sur certains systèmes Linux.
  * Correction de problèmes de console où des lignes étaient tronquées.
  * Correction de problèmes de console où la journalisation vers la console pouvait se bloquer.

  ## Amélioration

  * Ajout de la prise en charge des Sweeps préemptibles.
  * Ajout d’une commande de ligne de commande pour contrôler les Sweeps.
  * Ajout de la prise en charge du chargement des propriétés des collections d’artefacts.

  consultez le [journal des modifications de la version v0.10.31](https://github.com/wandb/wandb/releases/tag/v0.10.31) sur GitHub.
</Update>

<Update label="v0.10.30" description="07 mai 2021">
  ## Correction de bug

  * Identification et correction des derniers problèmes qui faisaient que des runs étaient marqués comme plantés pendant des pannes.
  * Amélioration des performances pour les utilisateurs de `define_metric`, de pytorch-lightning et de l’enregistrement agressif de la configuration.
  * Correction d’un problème lors de la tentative d’enregistrer un tenseur CUDA dans config ou summary.
  * Suppression de la dépendance à torch `backward_hooks` pour calculer le graphe.
  * Correction d’un problème empêchant la reprise des runs sur sagemaker.
  * Correction de problèmes empêchant pdb de fonctionner de manière fiable avec wandb.
  * Correction d’un avertissement de dépréciation dans une bibliothèque embarquée (contribution d’un utilisateur).
  * Correction d’un comportement de journalisation où la bibliothèque affichait accidentellement des journaux dans la console.
  * Correction du mode désactivé pour éviter la création du répertoire wandb et des fichiers journal.
  * Renommage des types en préparation du lancement de Tables.

  ## Amélioration

  * Autoriser le renommage des groupes avec l’API publique.

  consultez le [journal des modifications de la version v0.10.30](https://github.com/wandb/wandb/releases/tag/v0.10.30) sur GitHub.
</Update>

<Update label="v0.10.29" description="03 mai 2021">
  ## Correction de bug

  * Correction d’autres problèmes de gestion du réseau qui faisaient que des runs étaient marqués comme plantés (utilisez wandb sync pour récupérer).
  * Amélioration de la journalisation et de la gestion des exceptions afin de mieux signaler et journaliser les processus plantés.

  consultez le [journal des modifications de la version v0.10.29](https://github.com/wandb/wandb/releases/tag/v0.10.29) sur GitHub.
</Update>

<Update label="v0.10.28" description="28 avril 2021">
  ## Correction de bug

  * Correction d’un problème de gestion du réseau qui faisait que des runs étaient marqués comme plantés (utilisez wandb sync pour récupérer).
  * Utilisation de `register_full_backward_hook` pour prendre en charge les modèles avec des sorties de type dict.
  * Autoriser les points dans les colonnes de tableau.
  * Correction des collisions dans le cache d’artefacts lors de l’utilisation de processus forkés.
  * Correction d’un problème où les graphiques personnalisés ne s’affichaient pas correctement avec pytorch-lightning.

  ## Amélioration

  * Ajout d’une prise en charge expérimentale des artefacts incrémentiels.
  * Amélioration des avertissements lorsque la journalisation est limitée par le débit.

  consultez le [journal des modifications de la version v0.10.28](https://github.com/wandb/wandb/releases/tag/v0.10.28) sur GitHub.
</Update>

<Update label="v0.10.27" description="19 avril 2021">
  ## Correction de bug

  * Correction d’un cas dans tensorboard&#95;sync où les métriques à la fin d’un run court étaient ignorées.
  * Correction de `wandb sync` lorsque des fichiers tensorboard sont détectés.
  * Correction de l’invite de clé API dans le notebook databricks.

  ## Amélioration

  * Intégration de DSViz à Keras WandbCallback.
  * Ajout de la prise en charge des dépendances conda (contribution d’un utilisateur).

  consultez le [journal des modifications de la version v0.10.27](https://github.com/wandb/wandb/releases/tag/v0.10.27) sur GitHub.
</Update>

<Update label="v0.10.26" description="13 avril 2021">
  ## Correction de bug

  * Correction d’un problème de gestion du réseau où la synchronisation s’arrêtait (utilisez wandb sync pour récupérer).
  * Correction d’un problème d’authentification lors de l’utilisation conjointe des intégrations sagemaker et hugginface.
  * Correction de la gestion des valeurs NaN dans les tableaux contenant des valeurs non flottantes.
  * Chargement lazy de l’objet API pour éviter les accès inutiles aux fichiers lors du chargement du module.

  ## Amélioration

  * Amélioration des messages d’erreur lors de l’utilisation des accesseurs d’historique de l’API publique.

  consultez le [journal des modifications de la version v0.10.26](https://github.com/wandb/wandb/releases/tag/v0.10.26) sur GitHub.
</Update>

<Update label="v0.10.25" description="05 avril 2021">
  ## correction de bug

  * Corrige une possible condition de concurrence dans le cache d’artefacts lors de lectures parallèles d’artefacts.
  * Corrige la référence d’artefact lorsque `checksum=False`.

  ## amélioration

  * Publie `run.define_metric()` pour simplifier la définition d’un axe x personnalisé, entre autres.
  * Ajoute les opérateurs de colonne `add_column`, `get_column`, `get_index` à `wandb.Table()`.

  consultez le journal des modifications de la version [v0.10.25](https://github.com/wandb/wandb/releases/tag/v0.10.25) sur GitHub.
</Update>

<Update label="v0.10.24" description="30 mars 2021">
  ## correction de bug

  * Correctifs importants pour la journalisation de la console stdout/stderr.
  * Évite une utilisation excessive du réseau lors de l’enregistrement de fichiers avec `policy=live`.
  * Corrige les erreurs lors de l’envoi de mises à jour volumineuses (le plus souvent avec `wandb sync`).

  ## amélioration

  * Génère automatiquement l’artefact `run_table` pour les tableaux enregistrés.
  * Ajoute la notation entre crochets aux artefacts.
  * Améliore la validation de l’URL lors de la spécification de l’URL du serveur pour `wandb login`.

  consultez le journal des modifications de la version [v0.10.24](https://github.com/wandb/wandb/releases/tag/v0.10.24) sur GitHub.
</Update>

<Update label="v0.10.23" description="22 mars 2021">
  ## correction de bug

  * Rend les artefacts enregistrés accessibles après `wait()`.
  * Corrige l’intégration avec spell.run.
  * Corrige les performances de synchronisation des journaux de console avec retours chariot.
  * Corrige la matrice de confusion avec les noms de classes et les données non étiquetées.

  ## amélioration

  * Ajoute la possibilité d’enregistrer des artefacts sans créer de run.
  * Ajoute les références de tableaux externes à `wandb.Table`.
  * Permet d’enregistrer le même objet d’exécution dans plusieurs artefacts.
  * Ajoute une prise en charge expérimentale de `run._define_metric()`.
  * Avertit et ignore les appels `wandb.log()` multiprocessus non pris en charge.

  consultez le journal des modifications de la version [v0.10.23](https://github.com/wandb/wandb/releases/tag/v0.10.23) sur GitHub.
</Update>

<Update label="v0.10.22" description="09 mars 2021">
  ## correction de bug

  * Corrige la fréquence d’enregistrement des métriques système dans la série 0.10.x.
  * Corrige un problème de référence externe pour Audio.
  * Corrige les runs courts avec tensorboard&#95;sync.
  * Ignore `wandb.init(id=)` lors de l’exécution d’un balayage.
  * Nettoie les métadonnées d’artefact si nécessaire.

  ## amélioration

  * Permet de synchroniser les tfevents avec `wandb sync --sync-tensorboard`.

  consultez le journal des modifications de la version [v0.10.22](https://github.com/wandb/wandb/releases/tag/v0.10.22) sur GitHub.
</Update>

<Update label="v0.10.21" description="02 mars 2021">
  ## correction de bug

  * Corrige la régression de `artifact.get()` depuis la version 0.10.18.
  * Autorise les artefacts de 0 octet.
  * Corrige l’enregistrement du code et le rapport du nom du programme.

  ## amélioration

  * Ajoute la prise en charge des fichiers glb pour `wandb.Object3D()`.
  * Ajoute la prise en charge des références externes pour `wandb.Audio()`.
  * Ajoute la prise en charge du plugin tensorboard `pr_curves` pour les graphiques personnalisés.
  * Permet d’enregistrer l’intégralité du répertoire de code dans un artefact.

  consultez le journal des modifications de la version [v0.10.21](https://github.com/wandb/wandb/releases/tag/v0.10.21) sur GitHub.
</Update>

<Update label="v0.10.20" description="22 février 2021">
  ## correction de bug

  * `Wandb.login()` respecte désormais le mode désactivé.
  * Gère l’exception lors de la tentative d’enregistrement des TPU dans Colab.

  ## amélioration

  * Ajoute `WANDB_START_METHOD=thread` pour prendre en charge les environnements sans multiprocessing.
  * Ajoute `group` et `job_type` à l’objet Run dans l’API d’exportation.
  * Améliore les docstrings des artefacts.

  consultez le journal des modifications de la version [v0.10.20](https://github.com/wandb/wandb/releases/tag/v0.10.20) sur GitHub.
</Update>

<Update label="v0.10.19" description="14 février 2021">
  ## correction de bug

  * Corrige les fichiers manifestes d’artefacts incorrectement nommés avec le suffixe de correctif.

  consultez le journal des modifications de la version [v0.10.19](https://github.com/wandb/wandb/releases/tag/v0.10.19) sur GitHub.
</Update>

<Update label="v0.10.18" description="08 février 2021">
  ## Amélioration

  * Ajout de la suppression de runs et de fichiers dans l’API publique.
  * Alignement des étapes entre `tensorboard_sync` et l’historique de wandb.log().
  * Ajout de `WANDB_START_METHOD` pour permettre aux systèmes POSIX d’utiliser fork.
  * Prise en charge des types mixtes dans wandb.Table() avec `allow_mixed_types`.

  ## Correction de bugs

  * Correction d’une fuite de fichier potentielle due à un journal qui n’était pas correctement fermé.
  * Amélioration de `wandb verify` pour mieux gérer les problèmes réseau et signaler les erreurs.
  * Téléchargements de fichiers rendus plus déterministes vis-à-vis des caches du système de fichiers.

  consultez le [journal des modifications de la version v0.10.18](https://github.com/wandb/wandb/releases/tag/v0.10.18) sur GitHub.
</Update>

<Update label="v0.10.17" description="01 février 2021">
  ## Correction de bugs

  * Correction d’une régression observée avec Python 3.5.
  * Suppression des avertissements du watchdog embarqué sur mac.

  consultez le [journal des modifications de la version v0.10.17](https://github.com/wandb/wandb/releases/tag/v0.10.17) sur GitHub.
</Update>

<Update label="v0.10.16" description="01 février 2021">
  ## Amélioration

  * Artifacts prend désormais en charge les écritures parallèles pour les grands flux de travail distribués.
  * Artifacts prend en charge les tableaux distribués pour la visualisation des jeux de données.
  * Améliorations des modèles de PR.
  * Ajout de davantage d’annotations de type.
  * Intégration de watchdog 0.9.0, supprimé ainsi des dépendances.
  * Nouveau générateur de documentation.
  * L’API publique dispose désormais de `file.direct_url` pour éviter les redirections pour les URL signées.

  ## Correction de bugs

  * Autorisation de l’écrasement de `config-defaults.yaml` lors de l’exécution de Sweeps.
  * Correctifs de bugs généraux et améliorations de `wandb verify`.
  * Désactivation des widgets dans l’IDE Spyder.
  * Correction de WANDB&#95;SILENT dans l’IDE Spyder.
  * Les artefacts de référence file:// respectent l’attribut `name`.

  consultez le [journal des modifications de la version v0.10.16](https://github.com/wandb/wandb/releases/tag/v0.10.16) sur GitHub.
</Update>

<Update label="v0.10.15" description="24 janvier 2021">
  ## Amélioration

  * Ajout de `wandb verify` pour faciliter le dépannage des installations locales.

  ## Correction de bugs

  * Correction d’un problème de tensorboard&#95;sync lors de l’écriture vers s3.
  * Empêchement du stockage des secrets git.
  * Désactivation des messages verbeux dans la console lors de l’utilisation de moviepy.
  * Correction des artefacts avec points de contrôle pour les rendre plus robustes lors de l’écrasement de fichiers.
  * Correction d’un problème de réutilisation d’identifiants d’artefacts.

  consultez le [journal des modifications de la version v0.10.15](https://github.com/wandb/wandb/releases/tag/v0.10.15) sur GitHub.
</Update>

<Update label="v0.10.14" description="15 janvier 2021">
  ## Amélioration

  * Ajouter la prise en charge de wandb.Audio dans Artifacts.

  ## Correction de bugs

  * Correction des régressions de configuration wandb introduites dans la version 0.10.13.
  * Annulation des changements de prise en charge des médias avec des barres obliques dans les clés.

  consultez le [journal des modifications de la version v0.10.14](https://github.com/wandb/wandb/releases/tag/v0.10.14) sur GitHub.
</Update>

<Update label="v0.10.13" description="11 janvier 2021">
  ## Amélioration

  * Ajouter la prise en charge de la surveillance du GPU sur Mac M1.
  * Ajouter la prise en charge de la surveillance des TPU.
  * Ajout d’un paramètre pour désactiver l’intégration SageMaker.

  ## Correction de bugs

  * Correction de tensorboard&#95;sync avec tensorboardX et tf1.
  * Correction des problèmes de journalisation des images avec des barres obliques.
  * Correction des problèmes liés aux graphiques personnalisés.
  * Amélioration des messages d’erreur avec `wandb pull`.
  * Amélioration des messages d’erreur avec `wandb.Table()`.
  * Vérification que le mode silencieux est bien silencieux.
  * Correction de `wandb online` pour réactiver la journalisation.
  * Multiples correctifs sur les artefacts.

  consultez le [journal des modifications de la version v0.10.13](https://github.com/wandb/wandb/releases/tag/v0.10.13) sur GitHub.
</Update>

<Update label="v0.10.12" description="03 décembre 2020">
  ## Améliorations

  * Ajout de Artifact.used&#95;by et Artifact.logged&#95;by.
  * Validation de la cohérence des types lors de la journalisation des artefacts.
  * Amélioration de JoinedTable pour ne plus nécessiter d’artefacts téléchargés.
  * Ajout de la possibilité de télécharger récursivement les artefacts dépendants.
  * Prise en charge de la journalisation des gradients avec keras et tf2+.
  * Vérification que les modèles PyTorch sont bien transmis à wandb.watch().
  * Amélioration des docstrings des méthodes / objets publics.
  * Avertissement si des séquences d’images sont journalisées avec des tailles différentes.

  ## Correction de bugs

  * Correction des noms de fichiers générés incorrectement dans le résumé.
  * Correction du mode anonyme pour inclure la clé API dans les URL.
  * Correction d’un problème de pickle avec le mode désactivé.
  * Correction de la requête from&#95;id d’artefact.
  * Correction de la gestion des Tables avec des chemins d’image différents.

  consultez le journal des modifications de la version [v0.10.12](https://github.com/wandb/wandb/releases/tag/v0.10.12) sur GitHub.
</Update>

<Update label="v0.10.11" description="18 novembre 2020">
  ## Améliorations

  * Désactiver la journalisation wandb avec `wandb disabled` ou `wandb.init(mode="disabled")`.
  * Prise en charge du clonage d’un artefact lors de l’enregistrement d’un wandb.Image().

  ## Correction de bugs

  * Multiples améliorations des artefacts multimédias et refactorisation interne.
  * Amélioration de la gestion des erreurs liées aux artefacts.
  * Correction d’un problème où le nom du notebook était ignoré.
  * Extension du mode silencieux à la journalisation Jupyter.
  * Correction d’un problème où les bibliothèques embarquées interféraient avec le chemin Python.
  * Correction de diverses exceptions (division par zéro, conversion en int, TypeError).

  consultez le journal des modifications de la version [v0.10.11](https://github.com/wandb/wandb/releases/tag/v0.10.11) sur GitHub.
</Update>

<Update label="v0.10.10" description="09 novembre 2020">
  ## Améliorations

  * Ajout d’un graphique de matrice de confusion.
  * Amélioration des messages Jupyter avec wandb.init()/reinit/finish.

  ## Correction de bugs

  * Correction pour fastai 2.1.5 (suppression de log&#95;args).
  * Correction de la journalisation des médias lorsque les répertoires sont modifiés.

  consultez le journal des modifications de la version [v0.10.10](https://github.com/wandb/wandb/releases/tag/v0.10.10) sur GitHub.
</Update>

<Update label="v0.10.9" description="04 novembre 2020">
  ## Améliorations

  * Ajout de la journalisation des médias d’artefact (alpha).
  * Ajout d’alertes scriptables.
  * Ajout de l’attribut url pour l’API publique des balayages.
  * Mise à jour des docstrings des fonctions du SDK wandb.

  ## Correction de bugs

  * Correction des cas où le mode hors ligne établissait des connexions réseau.
  * Correction de problèmes avec les Sweeps Python et l’arrêt des runs.
  * Correction d’un problème de journalisation qui pouvait afficher accidentellement une clé API.
  * Correction des problèmes de connexion wandb avec des hôtes mal formés.
  * Possibilité d’appeler wandb.restore() sans wandb.init().
  * Correction de la reprise (réutilisation de run&#95;id) avec un résumé vide.
  * Correction d’un problème de téléchargement d’artefact.
  * Ajout de la fonction wandb.unwatch() manquante.
  * Éviter la création de répertoires wandb superflus.
  * Correction d’un problème d’import de collections lors de l’utilisation d’une ancienne version de six.

  consultez le journal des modifications de la version [v0.10.9](https://github.com/wandb/wandb/releases/tag/v0.10.9) sur GitHub.
</Update>

<Update label="v0.10.8" description="22 octobre 2020">
  ## Améliorations

  * Autoriser la sérialisation des callables.

  ## Correction de bugs

  * Correction d’un problème de compatibilité avec Python 3.9.
  * Correction de l’échec de `wandb sync` introduit dans la version 0.10.6.
  * Amélioration de la gestion, par l’agent Python, des runs en échec.
  * Correction d’un cas rare où la reprise des runs ne fonctionne pas.
  * Amélioration de la gestion des liens symboliques lors d’un appel dans un contexte de thread.
  * Correction de problèmes lors d’un changement de répertoire avant l’appel de wandb.init().

  consultez le journal des modifications de la version [v0.10.8](https://github.com/wandb/wandb/releases/tag/v0.10.8) sur GitHub.
</Update>

<Update label="v0.10.7" description="15 octobre 2020">
  ## Correction de bugs

  * Correction d’un problème lors de la vérification des nouvelles versions sur PyPI.

  consultez le journal des modifications de la version [v0.10.7](https://github.com/wandb/wandb/releases/tag/v0.10.7) sur GitHub.
</Update>

<Update label="v0.10.6" description="15 octobre 2020">
  ## Correction de bugs

  * Assurez-vous que l’enregistrement du code est activé dans les environnements Jupyter après la connexion.
  * Les agents de balayage disposent d’un délai d’expiration plus long pour les configurations de balayage volumineuses.
  * Prend en charge la variable d’environnement WANDB&#95;SILENT.
  * Avertit lorsqu’un package Python est manquant lors de la journalisation d’images.
  * Corrige wandb.restore() pour appliquer un patch différentiel.
  * Améliore les messages d’erreur des artefacts.
  * Corrige le chargement de config-defaults.yaml et de la liste spécifiée de fichiers de configuration YAML.

  consultez le journal des modifications de la version [v0.10.6](https://github.com/wandb/wandb/releases/tag/v0.10.6) sur GitHub.
</Update>

<Update label="v0.10.5" description="07 octobre 2020">
  ## Amélioration

  * Ajoute de nouveaux graphiques personnalisés : `wandb.plot.*`.
  * Ajoute un nouvel agent de balayage Python : `wandb.agent()`.

  ## Correction de bugs

  * Correctifs du journal de console (tqdm sous Windows, correction des exceptions à la fermeture).
  * Ajoute davantage d’attributs à l’objet Run (group, job&#95;type, urls).
  * Corrige les problèmes de connexion à SageMaker.
  * Corrige un problème où les graphiques n’étaient pas téléversés avant la fin du run.

  consultez le journal des modifications de la version [v0.10.5](https://github.com/wandb/wandb/releases/tag/v0.10.5) sur GitHub.
</Update>

<Update label="v0.10.4" description="29 septembre 2020">
  ## Correction de bugs

  * Corrige un problème où wandb.init(allow&#95;val&#95;change=) déclenche une exception.

  consultez le journal des modifications de la version [v0.10.4](https://github.com/wandb/wandb/releases/tag/v0.10.4) sur GitHub.
</Update>

<Update label="v0.10.3" description="29 septembre 2020">
  ## Amélioration

  * Ajoute un avertissement lors d’une tentative de synchronisation de répertoires de run antérieurs à la version 0.10.0.
  * Améliore la prise en charge de Jupyter pour les informations de synchronisation des runs wandb.

  ## Correction de bugs

  * Corrige les problèmes de téléchargement d’artefacts.
  * Corrige plusieurs problèmes avec tensorboard&#95;sync.
  * Corrige plusieurs problèmes avec les Sweeps Jupyter/Python.
  * Corrige un problème où la connexion expirait.
  * Corrige un problème où la configuration était écrasée lors de la reprise des runs.
  * Porte l’observateur Sacred vers la version 0.10.x.
  * Corrige un problème où les boîtes englobantes prédites étaient écrasées par les boîtes de vérité terrain.
  * Ajoute le paramètre save&#95;code manquant à wandb.init().

  consultez le journal des modifications de la version [v0.10.3](https://github.com/wandb/wandb/releases/tag/v0.10.3) sur GitHub.
</Update>

<Update label="v0.10.2" description="20 septembre 2020">
  ## Amélioration

  * Ajoute upload&#95;file à l’API.
  * Wandb.finish() peut être appelé sans wandb.init() correspondant.

  ## Correction de bugs

  * Corrige un problème où des fichiers étaient journalisés dans les mauvais runs parallèles.
  * Corrige les propriétés/méthodes manquantes -- as&#95;dict(), sweep&#95;id.
  * Corrige un problème où wandb.summary.update() ne mettait pas à jour toutes les clés.
  * L’enregistrement du code n’était pas correctement activé en fonction des paramètres de l’interface utilisateur.
  * Tensorboard journalise désormais les images avant la fin du programme.
  * Corrige les problèmes de reprise liés à la configuration et aux métriques de résumé.

  consultez le journal des modifications de la version [v0.10.2](https://github.com/wandb/wandb/releases/tag/v0.10.2) sur GitHub.
</Update>

<Update label="v0.10.1" description="16 septembre 2020">
  ## Amélioration

  * Ajoute à sync&#95;tensorboard la possibilité de gérer les fichiers S3 et GCS.
  * Ajoute la possibilité de spécifier l’hôte lors de la connexion.
  * Améliore l’API des artefacts pour permettre de modifier les attributs.

  ## Correction de bugs

  * Corrige l’enregistrement du code afin de respecter les paramètres du serveur.
  * Corrige un problème lors de l’exécution de wandb.init() sur des réseaux restreints.
  * Corrige un problème où les modifications des paramètres étaient ignorées.
  * Corrige les problèmes de téléchargement d’artefacts.

  consultez le journal des modifications de la version [v0.10.1](https://github.com/wandb/wandb/releases/tag/v0.10.1) sur GitHub.
</Update>

<Update label="v0.10.0" description="11 septembre 2020">
  ## Amélioration

  * Ajoute des mini-graphiques de l’historique à la fin du run.
  * Améliorations des artefacts et API de création de liens.
  * Améliore la prise en charge hors ligne et la synchronisation.
  * Prise en charge de base du mode noop pour simplifier les tests.
  * Améliore la prise en charge de Windows/PyCharm.
  * L’objet Run dispose de davantage de propriétés modifiables.
  * L’API publique prend en charge l’attachement d’artefacts à des runs historiques.

  ## Correction de bugs

  * De nombreux bugs ont été corrigés grâce à une simplification de la logique.

  consultez le journal des modifications de la version [v0.10.0](https://github.com/wandb/wandb/releases/tag/v0.10.0) sur GitHub.
</Update>

<Update label="v0.9.7" description="08 septembre 2020">
  ## amélioration

  * Nouvel observateur sacred disponible dans wandb.sacred.WandbObserver.
  * Amélioration du suivi des références d&#39;artefacts pour les URL HTTP.

  ## Correction de bugs

  * Affiche un message d&#39;erreur explicite lorsque les runs sont interrogés avec `summary` au lieu de `summary_metrics`.

  Consultez le [journal des modifications de la v0.9.7](https://github.com/wandb/wandb/releases/tag/v0.9.7) sur GitHub.
</Update>

<Update label="v0.9.6" description="28 août 2020">
  ## amélioration

  * Les sous-chemins des artefacts exposent désormais un argument facultatif de répertoire racine pour download().
  * Artifact.new&#95;file accepte un argument `mode` facultatif.
  * Suppression de l&#39;ancienne documentation fastai, car le package inclut désormais fastai v2 !

  ## Correction de bugs

  * Correction de la logique de gestion des erreurs d&#39;analyse YAML.
  * Correction d&#39;une faute d&#39;orthographe dans une docstring de torch, merci à @mkkb473.

  Consultez le [journal des modifications de la v0.9.6](https://github.com/wandb/wandb/releases/tag/v0.9.6) sur GitHub.
</Update>

<Update label="v0.9.5" description="17 août 2020">
  ## amélioration

  * Suppression du paramètre `y_probas` inutilisé dans les graphiques sklearn, merci à @dreamflasher.
  * Nouvelles API de suppression pour les artefacts.

  ## Correction de bugs

  * Correction de `wandb restore` lorsque vous n&#39;êtes pas connecté.
  * Correction des chemins de téléchargement des artefacts sous Windows.
  * Nouvelle tentative en cas d&#39;erreur 408 lors du téléversement.
  * Correction des types numériques des masques, merci à @numpee.
  * Correction d&#39;une confusion dans le nommage des références d&#39;artefacts.

  Consultez le [journal des modifications de la v0.9.5](https://github.com/wandb/wandb/releases/tag/v0.9.5) sur GitHub.
</Update>

<Update label="v0.9.4" description="24 juillet 2020">
  ## amélioration

  * La fréquence par défaut de journalisation des histogrammes PyTorch passe de 100 à 1 000 étapes.

  ## Correction de bugs

  * Correction des invites de connexion multiples lors de l&#39;utilisation de la ligne de commande.
  * Correction de l&#39;erreur « no method rename&#95;file ».
  * Correction du calcul des histogrammes dans certains cas limites sous PyTorch.
  * Correction d&#39;une erreur dans Jupyter lors de l&#39;enregistrement de l&#39;historique de session.
  * Retour correct des métadonnées d&#39;artefact dans l&#39;API publique.
  * Correction d&#39;une erreur de rendu matplotlib / plotly.

  Consultez le [journal des modifications de la v0.9.4](https://github.com/wandb/wandb/releases/tag/v0.9.4) sur GitHub.
</Update>

<Update label="v0.9.3" description="10 juillet 2020">
  ## amélioration

  * Nouvelles commandes de ligne de commande pour les artefacts !

  ```shell
  wandb artifact put path_file_or_ref
  wandb artifact get artifact:version
  wandb artifact ls project_name
  ```

  * Nouvelles commandes d&#39;API pour les artefacts !

  ```python
  wandb.log_artifact()
  wandb.use_artifact()
  wandb.Api().artifact_versions()
  wandb.Api().run.used_artifacts()
  wandb.Api().run.logged_artifacts()
  wandb.Api().Artifact().file()
  ```

  * Amélioration de la synchronisation des gros fichiers wandb-history.jsonl avec wandb sync.
  * Nouvelle méthode Artifact.verify pour garantir l&#39;intégrité des artefacts locaux.
  * Amélioration de l&#39;infrastructure de test pour les commandes d&#39;API.
  * Le répertoire du run stocke désormais l&#39;heure locale au lieu de l&#39;heure UTC dans son nom, merci à @aiyolo !
  * Amélioration générale de nos docstrings.
  * Wandb.Table prend désormais en charge un argument `dataframe` pour enregistrer des dataframes sous forme de tableaux !

  ## Correction de bugs

  * Artifacts fonctionne avec Python 2.
  * Les emplacements de téléchargement par défaut d&#39;Artifacts fonctionnent sous Windows.
  * Les références GCS sont désormais correctement mises en cache / téléchargées, merci à @yoks !
  * Correction de l&#39;encodage des tableaux numpy en JSON.
  * Correction du message d&#39;erreur de comparaison de chaînes.

  Consultez le [journal des modifications de la v0.9.3](https://github.com/wandb/wandb/releases/tag/v0.9.3) sur GitHub.
</Update>

<Update label="v0.9.2" description="29 juin 2020">
  ## amélioration

  * Refonte majeure de la mise en cache des artefacts.
  * Répertoire de cache configurable pour les artefacts.
  * Répertoire de téléchargement configurable pour les artefacts.
  * Nouvelle méthode Artifact.verify pour garantir l&#39;intégrité des artefacts locaux.
  * Use&#95;artifact ne nécessite plus `type`.
  * Les artefacts supprimés peuvent désormais être validés de nouveau.
  * Les scènes lidar prennent désormais en charge les vecteurs.

  ## Correction de bugs

  * Correction d&#39;un problème entraînant des erreurs lors du téléchargement d&#39;artefacts.
  * Les masques de segmentation gèrent désormais les données autres que unint8.
  * Correction de la logique d&#39;analyse des chemins dans `api.runs()`.

  Consultez le [journal des modifications de la v0.9.2](https://github.com/wandb/wandb/releases/tag/v0.9.2) sur GitHub.
</Update>

<Update label="v0.9.1" description="09 juin 2020">
  ## Correction de bugs

  * Corrige un problème à cause duquel les fichiers étaient toujours enregistrés dans le run le plus récent d’un projet.
  * Corrige un problème où l’URL n’était pas affichée lors du premier appel à wandb.init.

  Consultez le [journal des modifications v0.9.1](https://github.com/wandb/wandb/releases/tag/v0.9.1) sur GitHub.
</Update>

<Update label="v0.9.0" description="05 juin 2020">
  ## Correction de bugs

  * Gère les initialisations multiples dans Jupyter.
  * Gère les `ValueError` lors de la capture des signaux, merci à @jsbroks.
  * L’agent wandb gère correctement la limite de débit.

  ## amélioration

  * wandb.Artifact est désormais disponible de façon générale !
  * Feature&#95;importances prend désormais en charge CatBoost, merci à @neomatrix369.

  Consultez le [journal des modifications v0.9.0](https://github.com/wandb/wandb/releases/tag/v0.9.0) sur GitHub.
</Update>

<Update label="v0.8.36" description="11 mai 2020">
  ## Correction de bugs

  * Capture toutes les exceptions lors de l’enregistrement des sessions Jupyter.
  * `validation_data` est automatiquement défini dans TF &gt;= 2.2.
  * Les hooks *implements** sont désormais implémentés dans le callback Keras pour TF &gt;= 2.2.

  ## amélioration

  * L’enregistrement brut du code source est désormais désactivé par défaut.
  * Les paramètres globaux au démarrage sont désormais pris en charge pour activer l’enregistrement du code sur le serveur.
  * Nouvel argument `code_save=True` dans wandb.init pour activer manuellement l’enregistrement du code.

  Consultez le [journal des modifications v0.8.36](https://github.com/wandb/wandb/releases/tag/v0.8.36) sur GitHub.
</Update>

<Update label="v0.8.35" description="01 mai 2020">
  ## Correction de bugs

  * Garantit que les cellules ne restent pas bloquées une fois l’exécution terminée.
  * Corrige l’intégration Jupyter dans les shells PyCharm.
  * L’enregistrement de l’historique de session gère désormais les métadonnées `None` dans les sorties.

  Consultez le [journal des modifications v0.8.35](https://github.com/wandb/wandb/releases/tag/v0.8.35) sur GitHub.
</Update>

<Update label="v0.8.34" description="28 avril 2020">
  ## amélioration

  * Enregistre l’historique de session dans les notebooks Jupyter.
  * Notification d’activation d’Internet dans Kaggle.
  * Étend wandb.plots.feature&#95;importances pour fonctionner avec davantage de types de modèles, merci à @neomatrix369 !

  ## Correction de bugs

  * L’enregistrement du code pour les notebooks Jupyter a été rétabli.
  * Corrige les erreurs de thread dans Jupyter.
  * Garantit que les dernières lignes de l’historique ne sont pas perdues dans Jupyter.

  Consultez le [journal des modifications v0.8.34](https://github.com/wandb/wandb/releases/tag/v0.8.34) sur GitHub.
</Update>

<Update label="v0.8.33" description="24 avril 2020">
  ## amélioration

  * Ajoute des étiquettes de classe par défaut pour la segmentation sémantique.
  * Améliore l’API des boîtes englobantes pour la rapprocher de l’API de segmentation sémantique.

  ## Correction de bugs

  * Augmente le nombre de lignes du tableau des médias pour améliorer la journalisation des courbes ROC/PR.
  * Corrige un problème où les histogrammes pré-binés n’étaient pas correctement gérés.
  * Gère les valeurs `nan` dans les histogrammes PyTorch.
  * Corrige la gestion des masques d’image binaires.

  Consultez le [journal des modifications v0.8.33](https://github.com/wandb/wandb/releases/tag/v0.8.33) sur GitHub.
</Update>

<Update label="v0.8.32" description="14 avril 2020">
  ## amélioration

  * Améliore la journalisation des masques d’image pour la segmentation sémantique.

  Consultez le [journal des modifications v0.8.32](https://github.com/wandb/wandb/releases/tag/v0.8.32) sur GitHub.
</Update>

<Update label="v0.8.31" description="19 mars 2020">
  ## amélioration

  * Ferme tous les fichiers ouverts pour éviter les `ResourceWarning`, merci à @CrafterKolyan !

  ## Correction de bugs

  * Analyse les protobufs &quot;tensor&quot;, ce qui corrige des problèmes de synchronisation TensorBoard dans la version 2.1.

  Consultez le [journal des modifications v0.8.31](https://github.com/wandb/wandb/releases/tag/v0.8.31) sur GitHub.
</Update>

<Update label="v0.8.30" description="19 mars 2020">
  ## amélioration

  * Ajouter ROC, precision&#95;recall, HeatMap, explainText, POS et NER à wandb.plots.
  * Ajouter la journalisation avec wandb.Molecule().
  * Capturer les runs Kaggle pour les métriques.
  * Ajouter la possibilité d&#39;effectuer l&#39;observation depuis l&#39;objet run.

  ## Correction de bugs

  * Éviter de récupérer accidentellement des journaux de débogage globaux.

  Consulter le [journal des modifications v0.8.30](https://github.com/wandb/wandb/releases/tag/v0.8.30) sur GitHub.
</Update>

<Update label="v0.8.29" description="05 mars 2020">
  ## amélioration

  * Améliorer les annotations des boîtes englobantes.
  * Journaliser les métriques système actives du GPU.
  * Écrire le fichier wandb/settings uniquement si wandb init est appelé.
  * Améliorations de la commande wandb local.

  ## Correction de bugs

  * Corriger la journalisation GPU sur certains appareils sans métriques de puissance.
  * Corriger la gestion de la commande de configuration de balayage.
  * Corriger la journalisation des chaînes de caractères TensorFlow.

  Consulter le [journal des modifications v0.8.29](https://github.com/wandb/wandb/releases/tag/v0.8.29) sur GitHub.
</Update>

<Update label="v0.8.28" description="21 février 2020">
  ## amélioration

  * Ajout de l&#39;enregistrement du code du module Python principal.
  * Ajout de la possibilité de spécifier des métadonnées pour les boîtes englobantes et les masques de segmentation.

  ## Correction de bugs

  * Corriger les cas où les données non validées de wandb.log() ne sont pas conservées.

  Consulter le [journal des modifications v0.8.28](https://github.com/wandb/wandb/releases/tag/v0.8.28) sur GitHub.
</Update>

<Update label="v0.8.27" description="11 février 2020">
  ## Correction de bugs

  * Corriger le conflit de dépendances avec les nouvelles versions du package six.

  Consulter le [journal des modifications v0.8.27](https://github.com/wandb/wandb/releases/tag/v0.8.27) sur GitHub.
</Update>

<Update label="v0.8.26" description="10 février 2020">
  ## amélioration

  * Ajouter la meilleure métrique et l&#39;époque au résumé du run avec le callback Keras.
  * Ajout de wandb.run.config&#95;static pour les environnements nécessitant une configuration sérialisée.

  ## Correction de bugs

  * Correction d&#39;une régression provoquant des échecs avec wandb.watch() et DataParallel.
  * Compatibilité améliorée avec Python 3.8.
  * Corriger la journalisation du modèle sous Windows.

  Consulter le [journal des modifications v0.8.26](https://github.com/wandb/wandb/releases/tag/v0.8.26) sur GitHub.
</Update>

<Update label="v0.8.25" description="04 février 2020">
  ## Correction de bugs

  * Corriger l&#39;exception lors de l&#39;utilisation de wandb.watch() dans un notebook.
  * Améliorer la prise en charge de la journalisation des gradients de tenseurs creux sur les GPU.

  Consulter le [journal des modifications v0.8.25](https://github.com/wandb/wandb/releases/tag/v0.8.25) sur GitHub.
</Update>

<Update label="v0.8.24" description="03 février 2020">
  ## Correction de bugs

  * Assouplir la dépendance de version de PyYAML pour les utilisateurs ayant d&#39;anciens environnements.

  Consulter le [journal des modifications v0.8.24](https://github.com/wandb/wandb/releases/tag/v0.8.24) sur GitHub.
</Update>

<Update label="v0.8.23" description="03 février 2020">
  ## amélioration

  * Ajout de la prise en charge de scikit-learn.
  * Ajout de la possibilité de spécifier/exclure des clés spécifiques lors de la création de wandb.config.

  ## Correction de bugs

  * Corriger wandb.watch() sur les tenseurs creux.
  * Corriger l&#39;incompatibilité avec ray 0.8.1.
  * Corriger la dépendance manquante pyyaml.
  * Corriger les problèmes « W&amp;B process failed to launch ».
  * Améliorer la capacité à journaliser de grands graphiques de modèle et tracés.

  Consulter le [journal des modifications v0.8.23](https://github.com/wandb/wandb/releases/tag/v0.8.23) sur GitHub.
</Update>

<Update label="v0.8.22" description="24 janvier 2020">
  ## amélioration

  * Ajout de la possibilité de configurer la ligne de commande de l&#39;agent à partir de la configuration de balayage.

  ## Correction de bugs

  * Corriger la journalisation des prédictions fast.ai.
  * Corriger la journalisation des tenseurs TensorFlow eager.
  * Corriger les problèmes Jupyter liés à la journalisation du nom du notebook et à wandb.watch().

  Consulter le [journal des modifications v0.8.22](https://github.com/wandb/wandb/releases/tag/v0.8.22) sur GitHub.
</Update>

<Update label="v0.8.21" description="15 janvier 2020">
  ## amélioration

  * Ignorer le projet et l’entité spécifiés dans wandb.init() lors de l’exécution d’un balayage.

  ## Correction de bug

  * Corriger la détection du « flapping » de l’agent.
  * Corriger le problème empêchant le contrôleur local de démarrer lorsqu’un balayage est en attente.

  Consulter le [journal des modifications de la v0.8.21](https://github.com/wandb/wandb/releases/tag/v0.8.21) sur GitHub.
</Update>

<Update label="v0.8.20" description="10 janvier 2020">
  ## amélioration

  * Ajout de la prise en charge de LightGBM.
  * Ajout de la prise en charge du board local (expérimental).
  * Ajout de la possibilité de modifier la configuration du balayage.
  * Ajout de la journalisation de la consommation GPU dans les métriques système.

  ## Correction de bug

  * Empêcher l’agent de balayage d’échouer en continu lorsqu’il est mal configuré.

  Consulter le [journal des modifications de la v0.8.20](https://github.com/wandb/wandb/releases/tag/v0.8.20) sur GitHub.
</Update>

<Update label="v0.8.19" description="18 décembre 2019">
  ## amélioration

  * Ajout de la prise en charge bêta de la stratégie de recherche ray/tune hyperopt.
  * Ajout de la possibilité de spécifier un nombre maximal de runs par agent.
  * Amélioration de l’expérience de démarrage d’un balayage sans projet préalablement créé.

  ## Correction de bug

  * Correction des appels répétés à wandb.Api().Run(id).scan&#95;history() pour récupérer des données à jour.
  * Correction de early&#95;terminate/hyperband dans les environnements notebook/python.

  Consulter le [journal des modifications de la v0.8.19](https://github.com/wandb/wandb/releases/tag/v0.8.19) sur GitHub.
</Update>

<Update label="v0.8.18" description="04 décembre 2019">
  ## amélioration

  * Ajout de min&#95;step et max&#95;step à run.scan&#95;history pour récupérer des sous-sections de métriques.
  * Wandb.init(reinit=True) appelle désormais automatiquement wandb.join() pour mieux prendre en charge plusieurs runs par processus.

  ## Correction de bug

  * Wandb.init(sync&#95;tensorboard=True) fonctionne à nouveau avec TensorFlow 2.0.

  Consulter le [journal des modifications de la v0.8.18](https://github.com/wandb/wandb/releases/tag/v0.8.18) sur GitHub.
</Update>

<Update label="v0.8.17" description="02 décembre 2019">
  ## amélioration

  * Gérer les tags transmis sous forme de chaîne.

  ## Correction de bug

  * Épingler graphql-core &lt;  3.0.0 pour corriger les erreurs d’installation.
  * Les barres de progression TQDM mettent correctement à jour les journaux.
  * Les journaux de résumé ou d’historique trop volumineux sont désormais ignorés, ce qui évite les blocages lors des nouvelles tentatives.

  Consulter le [journal des modifications de la v0.8.17](https://github.com/wandb/wandb/releases/tag/v0.8.17) sur GitHub.
</Update>

<Update label="v0.8.16" description="21 novembre 2019">
  ## Correction de bug

  * Correction d’une régression dans la synchronisation de certaines versions de Tensorboard depuis la version 0.8.13.
  * Correction d’une erreur réseau dans Jupyter.

  Consulter le [journal des modifications de la v0.8.16](https://github.com/wandb/wandb/releases/tag/v0.8.16) sur GitHub.
</Update>

<Update label="v0.8.15" description="05 novembre 2019">
  ## Correction de bug

  * Correction des appels multiples à wandb.init avec sync&#95;tensorboard dans Jupyter.
  * Correction d’une condition de concurrence RuntimeError lors de l’utilisation de threads et de l’appel à wandb.log.
  * Ne pas initialiser Sentry lorsque le signalement d’erreurs est désactivé.

  ## amélioration

  * Ajout de best&#95;run() aux objets d’API publique de wandb.sweep().
  * Suppression des clés de suivi internes des objets wandb.config dans l’API publique.

  Consulter le [journal des modifications de la v0.8.15](https://github.com/wandb/wandb/releases/tag/v0.8.15) sur GitHub.
</Update>

<Update label="v0.8.14" description="01 novembre 2019">
  ## Correction de bug

  * Améliore l&#39;avertissement sur les objets volumineux lorsque les valeurs atteignent la taille maximale.
  * Avertit lorsque wandb.save n&#39;est pas appelé avec une chaîne de caractères.
  * L&#39;arrêt des runs depuis l&#39;interface utilisateur fonctionne de nouveau après la régression introduite en 0.8.12.
  * La restauration d&#39;un fichier qui existe déjà localement fonctionne.
  * Correction d&#39;un problème où TensorBoard plaçait certaines clés à la mauvaise étape depuis la version 0.8.10.
  * Wandb.Video accepte désormais uniquement uint8 au lieu de convertir incorrectement en flottants.
  * La détection de l&#39;environnement SageMaker est désormais plus robuste.
  * La reprise renseigne correctement la configuration.
  * Wandb.restore respecte root lorsque run.dir est défini #658.
  * L&#39;appel de wandb.watch plusieurs fois applique correctement des espaces de noms aux histogrammes et aux graphes.

  ## amélioration

  * Les balayages fonctionnent désormais sous Windows !
  * Ajout de l&#39;attribut balayage à Run dans l&#39;API publique.
  * Ajout d&#39;un lien de balayage à la sortie Jupyter et au terminal.
  * La journalisation TensorBoard stocke désormais des horodatages corrects lors de l&#39;importation de résultats historiques.
  * La journalisation TensorBoard prend désormais en charge la configuration de rate&#95;limits et le filtrage des types d&#39;événements.
  * Utilise une sortie simple pour dupliquer stdout lorsqu&#39;il n&#39;a pas de descripteur de fichier.
  * Écrit les fichiers meta de wandb dans le répertoire temporaire du système si le répertoire local n&#39;est pas accessible en écriture.
  * Ajout de la version bêta de api.reports à l&#39;API publique.
  * Ajout de wandb.unwatch pour supprimer les hooks des modèles pytorch.
  * Stocke le framework utilisé dans config.&#95;wandb.

  Consultez le [journal des modifications v0.8.14](https://github.com/wandb/wandb/releases/tag/v0.8.14) sur GitHub.
</Update>

<Update label="v0.8.13" description="15 octobre 2019">
  ## Correction de bug

  * Crée un répertoire imbriqué lorsque des vidéos sont enregistrées depuis des espaces de noms TensorBoard.
  * Corrige une condition de concurrence lors de l&#39;utilisation de wandb.log `async=True`.
  * Run.summary se comporte comme un véritable dictionnaire.
  * Les sous-dictionnaires de Run.summary s&#39;affichent correctement.
  * Gère None lors du passage de class&#95;colors pour les masques de segmentation.
  * Gère le cas où tensorflow2 n&#39;a pas de SessionHook.
  * Échappe correctement les arguments sous Windows.
  * Corrige le blocage de la connexion en mode anonyme.
  * Le patch Keras Tf2 gère désormais les arguments de callbacks manquants.

  ## amélioration

  * Met à jour la documentation générée automatiquement à partir des docstrings dans /docs.
  * Wandb.init(config=config&#95;dict) ne met pas à jour les paramètres spécifiés par balayage.
  * L&#39;objet Wandb.config dispose désormais d&#39;une méthode setdefaults permettant une meilleure prise en charge du balayage.
  * Améliore les messages du terminal et de Jupyter avec des emojis !
  * Autorise l&#39;appel de wandb.watch plusieurs fois sur différents modèles.
  * Améliore la prise en charge de la surveillance de plusieurs fichiers tfevent.
  * Windows ne nécessite plus `wandb run`, exécutez simplement `python script_name.py`.
  * `wandb agent` fonctionne désormais sous Windows.
  * Message d&#39;erreur clair lorsque wandb.log est appelé sans dict.
  * Le callback Keras inclut désormais un nouveau paramètre `log_batch_frequency` pour enregistrer les métriques tous les N lots.

  Consultez le [journal des modifications v0.8.13](https://github.com/wandb/wandb/releases/tag/v0.8.13) sur GitHub.
</Update>

<Update label="v0.8.12" description="20 septembre 2019">
  ## Correction de bug

  * Corrige un problème de compatibilité avec Python 2.7 et d&#39;anciennes dépendances pip.

  ## amélioration

  * Améliore le flux d&#39;intégration lors de la création de nouveaux comptes et de la saisie de api&#95;key.

  Consultez le [journal des modifications v0.8.12](https://github.com/wandb/wandb/releases/tag/v0.8.12) sur GitHub.
</Update>

<Update label="v0.8.11" description="19 septembre 2019">
  ## Correction de bug

  * Corrige le renvoi de données incorrectes par l&#39;API publique lorsque la valeur de configuration est 0 ou False.
  * Les runs repris n&#39;écrasent plus les noms de run avec l&#39;ID du run.

  ## amélioration

  * Ajout de l&#39;enregistrement de l&#39;ID spell.run dans la configuration.

  Consultez le [journal des modifications v0.8.11](https://github.com/wandb/wandb/releases/tag/v0.8.11) sur GitHub.
</Update>

<Update label="v0.8.10" description="13 septembre 2019">
  ## Correction de bug

  * Wandb magic gère correctement les cas où tf.keras et keras sont chargés.
  * La journalisation TensorBoard ne perd plus d&#39;étapes lorsque plusieurs enregistreurs ont des global&#95;steps différents.
  * La journalisation des gradients Keras fonctionne avec la dernière version de tf.keras.
  * `validation_data` de Keras est correctement défini dans TensorFlow 2.
  * La commande `wandb pull` crée les répertoires s&#39;ils n&#39;existent pas, merci à @chmod644.
  * Le téléversement de fichiers par lots impose désormais une taille minimale.
  * balayage fonctionne à nouveau avec Python 2.
  * `scan_history` parcourt désormais l&#39;ensemble complet des points.
  * Jupyter exécute le mode local si les identifiants ne peuvent pas être obtenus.

  ## amélioration

  * balayage peut désormais être exécuté depuis Jupyter / directement depuis Python ! https://docs.wandb.com/sweeps/python.
  * La nouvelle intégration OpenAI Gym journalise automatiquement les vidéos, lorsqu&#39;elle est activée avec l&#39;argument nommé `monitor_gym` de `wandb.init`.
  * Callback de journalisation Ray Tune dans `wandb.ray.WandbLogger`.
  * Nouveau fichier de configuration global dans `~/.config/wandb` pour les paramètres globaux.
  * Ajout de tests pour fastai, merci à @borisdayma.
  * Améliorations des performances de l&#39;API publique.
  * `username` est obsolète au profit de `entity` dans l&#39;API publique, pour plus de cohérence.
  * La prise en charge de la connexion anonyme est activée par défaut.
  * Nouvelle méthode `wandb.login` à utiliser dans Jupyter pour permettre les connexions anonymes.
  * Messages d&#39;erreur améliorés pour les dépendances des dataframes.
  * Intégration initiale avec spell.run.
  * Toutes les images sont désormais rendues en PNG pour éviter les artefacts JPEG.
  * L&#39;API publique dispose désormais d&#39;un champ `projects`.

  Consulter le [journal des modifications v0.8.10](https://github.com/wandb/wandb/releases/tag/v0.8.10) sur GitHub.
</Update>

<Update label="v0.8.9" description="19 août 2019">
  ## Correction de bug

  * Les mises à jour de `Run.summary` fonctionnent dans Jupyter avant l&#39;appel à `log`.
  * Il n&#39;est plus nécessaire d&#39;avoir numpy installé.
  * La définition de clés imbriquées dans `summary` fonctionne.
  * Les notebooks situés dans des répertoires imbriqués sont correctement enregistrés.
  * Plus de nouvelle tentative sur les erreurs 404 / meilleurs messages d&#39;erreur côté serveur.
  * Supprime les barres obliques initiales lors du chargement des chemins dans l&#39;API publique.

  ## amélioration

  * Les petits fichiers sont téléversés par lots sous forme d&#39;archives tar compressées avec gzip.
  * Les GIF TensorBoardX sont enregistrés dans wandb.

  Consulter le [journal des modifications v0.8.9](https://github.com/wandb/wandb/releases/tag/v0.8.9) sur GitHub.
</Update>

<Update label="v0.8.8" description="13 août 2019">
  ## Correction de bug

  * `Wandb.init` gère correctement les défaillances réseau au démarrage.
  * Le callback Keras n&#39;enregistre des exemples que si `data_type` ou `input_type` est défini.
  * Corrige un bug de journalisation du modèle PyTorch dans un cas limite.
  * Gère l&#39;application du patch TensorBoard plusieurs fois dans Jupyter.
  * balayage récupère `config.yaml` depuis le répertoire du run.
  * Les dataframes gèrent les libellés entiers.
  * Gère les JSON invalides lors de l&#39;interrogation des serveurs Jupyter.

  ## amélioration

  * Fastai utilise une seed fixe pour la journalisation des exemples.
  * Augmentation du nombre maximal d&#39;images pour le callback fastai.
  * Nouveau tag `wandb.Video` pour la journalisation de vidéos.
  * L&#39;argument `Sync=False` de `wandb.log` déplace la journalisation vers un thread.
  * Nouveau contrôleur de balayage local pour une logique de recherche personnalisée.
  * Prise en charge de la connexion anonyme pour une prise en main plus facile.
  * Appeler `wandb.init` plusieurs fois dans Jupyter ne provoque plus d&#39;erreur.

  Consulter le [journal des modifications v0.8.8](https://github.com/wandb/wandb/releases/tag/v0.8.8) sur GitHub.
</Update>

<Update label="v0.8.7" description="07 août 2019">
  ## Correction de bug

  * Le callback Keras ne déduit plus `input_type` pour les données 2D.
  * `Wandb.Image` gère les images d&#39;une hauteur de 1 px.

  ## amélioration

  * L&#39;API publique Wandb dispose désormais de `run.scan_history` pour renvoyer toutes les lignes de l&#39;historique.
  * `Wandb.config` affiche des erreurs utiles s&#39;il est utilisé avant l&#39;appel à `init`.
  * `Wandb.summary` affiche des erreurs utiles s&#39;il est utilisé avant l&#39;appel à `init`.
  * L&#39;API Filestream pointe vers une nouvelle URL côté backend.

  Consulter le [journal des modifications v0.8.7](https://github.com/wandb/wandb/releases/tag/v0.8.7) sur GitHub.
</Update>

<Update label="v0.8.6" description="31 juillet 2019">
  ## Correction de bug

  * Le callback Fastai utilise le moniteur par défaut au lieu de supposer `val_loss`.
  * L’introspection du notebook gère les cas d’erreur et n’affiche pas de stack trace en cas d’échec.
  * N’affiche plus l’avertissement sur la description lors de la définition du nom.
  * Correction d’une erreur de journalisation de dataframe avec le callback Keras.
  * Correction des décalages de ligne dans les journaux lors de la reprise des runs.
  * wandb.config convertit les types non natifs avant de les écrire en yaml.
  * Intégration de backports.tempfile pour corriger l’absence du package à l’installation.

  ## amélioration

  * Ajout de `api.sweep` à l’API d’export Python pour interroger les Sweeps.
  * Ajout de `WANDB_NOTEBOOK_NAME` pour spécifier le nom du notebook dans les cas où il ne peut pas être déduit.
  * Ajout de `WANDB_HOST` pour remplacer les noms d’hôte.
  * Stocke l’information indiquant si un run a été exécuté dans Jupyter.
  * Wandb prend désormais en charge l’arrêt des runs depuis l’interface web.
  * Gestion des nombres flottants passés comme étape à `wandb.log`.
  * Wandb.config prend entièrement en charge Unicode.
  * Synchronise le fichier principal avec wandb si l’enregistrement du code est activé et qu’il n’est pas suivi par git.
  * Callback XGBoost : wandb.xgboost.wandb&#95;callback().

  Consultez le [journal des modifications v0.8.6](https://github.com/wandb/wandb/releases/tag/v0.8.6) sur GitHub.
</Update>

<Update label="v0.8.5" description="12 juillet 2019">
  ## Correction de bug

  * Correction du rendu des graphiques plotly avec de grands tableaux numpy.
  * `wandb docker` fonctionne lorsque nvidia est disponible.
  * Message d’erreur amélioré lorsque des clés non textuelles sont envoyées au journal.
  * Assouplissement de la dépendance pyyaml pour corriger les installations AMI.
  * Magic fonctionne dans les notebooks Jupyter.

  ## amélioration

  * Nouvelle version préliminaire des dataframes automatiques pour Keras.
  * Ajout de input&#95;type et output&#95;type au callback Keras pour une configuration simplifiée.
  * L’API publique prend en charge la récupération de clés spécifiques et d’un axe x personnalisé.

  Consultez le [journal des modifications v0.8.5](https://github.com/wandb/wandb/releases/tag/v0.8.5) sur GitHub.
</Update>

<Update label="v0.8.4" description="08 juillet 2019">
  ## Correction de bug

  * WANDB&#95;IGNORE&#95;GLOBS est respecté lors de l’analyse finale des fichiers.
  * Uniformisation de run.id, run.name et run.notes dans toutes les API.
  * Gestion des tailles de terminal atypiques lors de la configuration de notre pseudo-TTY.
  * Correction de la logique d’introspection des notebooks Jupyter.
  * Run.summary.update() conserve les modifications sur le serveur.
  * La synchronisation TensorBoard est robuste face aux histogrammes invalides et aux fichiers tronqués.

  ## amélioration

  * Version préliminaire de magic : l’appel à wandb.init(magic=True) devrait automatiquement suivre la configuration et les métriques lorsque c’est possible.
  * La CLI prend désormais en charge les installations locales du backend.
  * Le callback Fastai prend en charge la journalisation d’images d’exemple.

  Consultez le [journal des modifications v0.8.4](https://github.com/wandb/wandb/releases/tag/v0.8.4) sur GitHub.
</Update>

<Update label="v0.8.3" description="26 juin 2019">
  ## Correction de bug

  * La journalisation d’images fonctionne sous Windows.
  * Wandb sync gère les tfevents avec une seule étape temporelle.
  * Correction de la commande incorrecte sur la page d’aperçu pour exécuter des runs.
  * Gestion des histogrammes avec plus de 512 compartiments lors du streaming de TensorBoard.
  * Message d’erreur amélioré lors de l’appel à wandb sync sur un fichier au lieu d’un répertoire.

  ## amélioration

  * Nouvelle fonction utilitaire pour la gestion des hyperparamètres dans les balayages `wandb.config.user_items()`.
  * Mocking amélioré pour faciliter les tests.

  Consultez le [journal des modifications v0.8.3](https://github.com/wandb/wandb/releases/tag/v0.8.3) sur GitHub.
</Update>

<Update label="v0.8.2" description="20 juin 2019">
  ## Correction de bug

  * L’entité est désormais conservée dans wandb.run lorsqu’elle est récupérée depuis le serveur.
  * Les fichiers temporaires utilisent toujours le répertoire temporaire pour éviter la synchronisation.
  * Une erreur est levée si le fichier rétrécit pendant le téléversement.
  * Les images sont correctement journalisées sous Windows.
  * La version requise de pyyaml a été mise à jour pour corriger la CVE.
  * L’historique des lignes n’est plus stocké afin d’éviter une fuite de mémoire.

  ## amélioration

  * Le résumé prend désormais en charge le nouveau format de dataframe.
  * La variable d’environnement WANDB&#95;SILENT écrit tous les messages de wandb dans debug.log.
  * Messages d’erreur améliorés pour Windows et la journalisation TensorBoard.
  * Output.log est téléversé à la fin de chaque run.
  * Les métadonnées, les dépendances et les patchs sont téléversés au début d’un run.
  * Lorsqu’aucune exécution n’a lieu depuis un dépôt git, le fichier Python principal est stocké.
  * Ajout de WANDB&#95;DISABLE&#95;CODE pour empêcher la génération des diffs et l’enregistrement du code.
  * Lors de l’exécution dans Jupyter, le nom du notebook est stocké.
  * Prise en charge de la connexion automatique pour Colab.
  * Stockage de l’URL du notebook Colab.
  * Stockage de la version de cette bibliothèque dans la configuration.
  * Stockage de sys.executable dans les métadonnées.
  * Le callback Fastai ne nécessite plus de chemin.
  * Wandb.init accepte désormais un argument notes.
  * L’interface en ligne de commande a remplacé l’argument message par notes et name.

  Consultez le [journal des modifications v0.8.2](https://github.com/wandb/wandb/releases/tag/v0.8.2) sur GitHub.
</Update>

<Update label="v0.8.1" description="23 mai 2019">
  ## Correction de bug

  * Wandb sync gère les embeddings TensorBoard.
  * Wandb sync gère correctement les images dans TensorBoard.
  * Tf.keras gère correctement les modèles fonctionnels à entrée unique.
  * Wandb.Api().runs renvoie un itérateur réutilisable.
  * WANDB&#95;DIR dans un répertoire caché n’empêche pas la synchronisation.
  * Run.files() parcourt tous les fichiers.
  * Correction d’une erreur de récursion trop profonde avec PyTorch.

  ## amélioration

  * Wandb sync accepte un argument --ignore avec des globs pour ignorer certains fichiers.
  * Run.summary dispose désormais d’une méthode items() pour parcourir toutes les clés.

  Consultez le [journal des modifications v0.8.1](https://github.com/wandb/wandb/releases/tag/v0.8.1) sur GitHub.
</Update>

<Update label="v0.8.0" description="17 mai 2019">
  ## Correction de bug

  * Messages d’erreur améliorés en cas d’accès refusé.
  * Messages d’erreur améliorés lorsque des packages facultatifs ne sont pas installés.
  * Les URL affichées dans le terminal sont échappées.
  * Les événements TensorBoard avec espace de noms fonctionnent avec les histogrammes.
  * L’API publique réessaie désormais en cas d’échec et réutilise le pool de connexions.
  * Les erreurs git sont interceptées lorsque les dépôts distants n’ont pas été poussés vers origin.
  * La collection du graphe Keras a été déplacée vers on&#95;train&#95;begin pour gérer les modèles non construits.
  * Davantage de cas d’impossibilité d’enregistrer les poids sont pris en charge.
  * Les mises à jour du résumé après reprise sont conservées.
  * Correction de la journalisation PyTorch histc dans la version 0.4.1.
  * Correction de l’import TensorBoard de `wandb sync`.

  ## amélioration

  * Wandb.init(tensorboard=True) fonctionne avec Tensorflow 2 et Eager Execution.
  * Wandb.init(tensorboard=True) fonctionne désormais avec tb-nightly et PyTorch.
  * Journalisation automatique des exemples avec tf.keras par ajout de la validation&#95;data manquante.
  * Le socket se lie uniquement à localhost afin d’améliorer la sécurité et d’éviter les avertissements du pare-feu sous OSX.
  * Ajout de l’objet utilisateur à l’API publique pour obtenir l’utilisateur source.
  * Ajout de run.display&#95;name à l’API publique.
  * Affichage du nom d’affichage dans la sortie de la console.
  * Ajout de --tags, --job&#95;group et --job&#95;type à `wandb run`.
  * Ajout d’une variable d’environnement définissant la durée minimale d’exécution avant de considérer qu’un run a planté.
  * Ajout des tests flake8 à la CI, merci @cclauss !

  Consultez le [journal des modifications v0.8.0](https://github.com/wandb/wandb/releases/tag/v0.8.0) sur GitHub.
</Update>

<Update label="v0.7.3" description="15 avril 2019">
  ## Correction de bug

  * Wandb-docker-run accepte les empreintes d’image.
  * Le callback Keras fonctionne dans tensorflow2-alpha0.
  * Le graphe du modèle Keras place désormais la couche d’entrée en premier.

  ## amélioration

  * Ajout de la fréquence de journalisation PyTorch pour les gradients et les poids.
  * Améliorations des performances de la journalisation PyTorch.
  * Wandb.init accepte désormais un paramètre name pour nommer les runs.
  * Wandb.run.name reflète les noms d’affichage personnalisés.
  * Améliorations des valeurs de résumé imbriquées.
  * wandb.Table.add&#95;row est obsolète au profit de wandb.Table.add&#95;data.
  * Prise en charge initiale d’un callback fast.ai grâce à @borisdayma !

  Consultez le [journal des modifications v0.7.3](https://github.com/wandb/wandb/releases/tag/v0.7.3) sur GitHub.
</Update>

<Update label="v0.7.2" description="19 mars 2019">
  ## Correction de bug

  * Run.get&#95;url résout l’entité par défaut si aucune n’est spécifiée.
  * wandb restore accepte les chemins de run composés uniquement de barres obliques.
  * Correction des avertissements de dépréciation de PyYAML.
  * Ajout du script shell d’entrée au manifeste.
  * Suppression des sauts de ligne de la version de CUDA.

  Consultez le [journal des modifications v0.7.2](https://github.com/wandb/wandb/releases/tag/v0.7.2) sur GitHub.
</Update>

<Update label="v0.7.1" description="14 mars 2019">
  ## Correction de bug

  * Gère les identifiants Docker insensibles à la casse.
  * Correction de app&#95;url pour le flux de connexion au cloud privé.
  * N’effectue pas de nouvelle tentative après des erreurs 404 lors du démarrage des agents de balayage.

  Consultez le [journal des modifications v0.7.1](https://github.com/wandb/wandb/releases/tag/v0.7.1) sur GitHub.
</Update>

<Update label="v0.7.0" description="28 février 2019">
  ## Correction de bug

  * Garantit que les échecs de résolution DNS ne peuvent pas empêcher le démarrage.
  * Centralisation de la journalisation de débogage.
  * wandb agent attend plus longtemps avant d’envoyer un SIGKILL après avoir envoyé un SIGINT.

  ## amélioration

  * Prise en charge de la journalisation des images Docker avec la variable d’environnement WANDB&#95;DOCKER.
  * WANDB&#95;DOCKER est automatiquement défini lors d’une exécution dans Kubernetes.
  * Nouvelle commande wandb-docker-run pour définir automatiquement les variables d’environnement et monter le code.
  * Wandb.restore prend en charge le lancement de Docker pour les runs qui l’utilisaient.
  * Les paquets Python sont désormais consignés et enregistrés dans un fichier requirements.txt.
  * Cpu&#95;count, gpu&#95;count, gpu, os et la version de Python sont stockés dans wandb-metadata.json.
  * L’API d’export prend désormais en charge les chemins de type Docker, par ex. username/project:run&#95;id.
  * Amélioration des messages de première utilisation et des informations de connexion.

  Consultez le [journal des modifications v0.7.0](https://github.com/wandb/wandb/releases/tag/v0.7.0) sur GitHub.
</Update>

<Update label="v0.6.35" description="29 janvier 2019">
  ## Correction de bug

  * Amélioration du signalement des erreurs pour les Sweeps.

  Consultez le [journal des modifications v0.6.35](https://github.com/wandb/wandb/releases/tag/v0.6.35) sur GitHub.
</Update>

<Update label="v0.6.35" description="29 janvier 2019">
  ## Correction de bug

  * Amélioration du signalement des erreurs pour les Sweeps.

  Consultez le [journal des modifications v0.6.35](https://github.com/wandb/wandb/releases/tag/v0.6.35) sur GitHub.
</Update>

<Update label="v0.6.35" description="29 janvier 2019">
  ## Correction de bug

  * Amélioration du signalement des erreurs pour les Sweeps.

  Consultez le [journal des modifications v0.6.35](https://github.com/wandb/wandb/releases/tag/v0.6.35) sur GitHub.
</Update>

<Update label="v0.6.35" description="29 janvier 2019">
  ### Correction de bug

  * Amélioration du signalement des erreurs pour les Sweeps.

  Consultez le [journal des modifications v0.6.35](https://github.com/wandb/wandb/releases/tag/v0.6.35) sur GitHub.
</Update>

<Update label="v0.6.35" description="29 janvier 2019">
  ## Correction de bug

  * Amélioration du signalement des erreurs pour les Sweeps.

  Consultez le [journal des modifications v0.6.35](https://github.com/wandb/wandb/releases/tag/v0.6.35) sur GitHub.
</Update>

<Update label="v0.6.35" description="29 janvier 2019">
  ## Correction de bug

  * Amélioration du signalement des erreurs pour les Sweeps.

  Consultez le [journal des modifications v0.6.35](https://github.com/wandb/wandb/releases/tag/v0.6.35) sur GitHub.
</Update>

<Update label="v0.6.34" description="23 janvier 2019">
  ## Correction de bug

  * Correction de la journalisation dans Jupyter, sans modifier le niveau du logger.
  * Correction de la reprise dans Jupyter.

  ## amélioration

  * Wandb.init gère désormais correctement le cas où un utilisateur n&#39;est pas connecté à wandb.
  * Ajout d&#39;un indicateur **force** à wandb.init pour exiger qu&#39;une machine soit connectée.
  * La journalisation Tensorboard et TensorboardX est désormais instrumentée automatiquement lorsqu&#39;elle est activée.
  * Ajout d&#39;un paramètre **tensorboard** à wandb.init, qui applique un patch à tensorboard pour la journalisation.
  * Wandb.save accepte désormais un chemin de base pour des fichiers situés dans des sous-répertoires.
  * Wandb.tensorflow et wandb.tensorboard sont désormais accessibles sans import direct.
  * `wandb sync` parcourt désormais un répertoire de run wandb et synchronise tous les runs.

  Consultez le [journal des modifications v0.6.34](https://github.com/wandb/wandb/releases/tag/v0.6.34) sur GitHub.
</Update>

<Update label="v0.6.34" description="23 janvier 2019">
  ## Correction de bug

  * Correction de la journalisation dans Jupyter, sans modifier le niveau du logger.
  * Correction de la reprise dans Jupyter.

  ## amélioration

  * Wandb.init gère désormais correctement le cas où un utilisateur n&#39;est pas connecté à wandb.
  * Ajout d&#39;un indicateur **force** à wandb.init pour exiger qu&#39;une machine soit connectée.
  * La journalisation Tensorboard et TensorboardX est désormais instrumentée automatiquement lorsqu&#39;elle est activée.
  * Ajout d&#39;un paramètre **tensorboard** à wandb.init, qui applique un patch à tensorboard pour la journalisation.
  * Wandb.save accepte désormais un chemin de base pour des fichiers situés dans des sous-répertoires.
  * Wandb.tensorflow et wandb.tensorboard sont désormais accessibles sans import direct.
  * `wandb sync` parcourt désormais un répertoire de run wandb et synchronise tous les runs.

  Consultez le [journal des modifications v0.6.34](https://github.com/wandb/wandb/releases/tag/v0.6.34) sur GitHub.
</Update>

<Update label="v0.6.34" description="23 janvier 2019">
  ## Correction de bug

  * Correction de la journalisation dans Jupyter, sans modifier le niveau du logger.
  * Correction de la reprise dans Jupyter.

  ## amélioration

  * Wandb.init gère désormais correctement le cas où un utilisateur n&#39;est pas connecté à wandb.
  * Ajout d&#39;un indicateur **force** à wandb.init pour exiger qu&#39;une machine soit connectée.
  * La journalisation Tensorboard et TensorboardX est désormais instrumentée automatiquement lorsqu&#39;elle est activée.
  * Ajout d&#39;un paramètre **tensorboard** à wandb.init, qui applique un patch à tensorboard pour la journalisation.
  * Wandb.save accepte désormais un chemin de base pour des fichiers situés dans des sous-répertoires.
  * Wandb.tensorflow et wandb.tensorboard sont désormais accessibles sans import direct.
  * `wandb sync` parcourt désormais un répertoire de run wandb et synchronise tous les runs.

  Consultez le [journal des modifications v0.6.34](https://github.com/wandb/wandb/releases/tag/v0.6.34) sur GitHub.
</Update>

<Update label="v0.6.34" description="23 janvier 2019">
  ### Correction de bug

  * correction de la journalisation dans Jupyter, sans modifier le niveau du logger.
  * correction de la reprise dans Jupyter.

  ### amélioration

  * wandb.init gère désormais correctement le cas où un utilisateur n&#39;est pas connecté à wandb.
  * ajout d&#39;un indicateur **force** à wandb.init pour exiger qu&#39;une machine soit connectée.
  * La journalisation Tensorboard et TensorboardX est désormais instrumentée automatiquement lorsqu&#39;elle est activée.
  * ajout d&#39;un paramètre **tensorboard** à wandb.init, qui applique un patch à tensorboard pour la journalisation.
  * wandb.save accepte désormais un chemin de base pour des fichiers situés dans des sous-répertoires.
  * wandb.tensorflow et wandb.tensorboard sont désormais accessibles sans import direct.
  * `wandb sync` parcourt désormais un répertoire de run wandb et synchronise tous les runs.

  Consultez le [journal des modifications v0.6.34](https://github.com/wandb/wandb/releases/tag/v0.6.34) sur GitHub.
</Update>

<Update label="v0.6.34" description="23 janvier 2019">
  ## Correction de bugs

  * Correction de la journalisation dans Jupyter, sans modifier le niveau du logger.
  * Correction de la reprise dans Jupyter.

  ## amélioration

  * Wandb.init gère désormais correctement le cas où un utilisateur n&#39;est pas connecté à wandb.
  * Ajout d&#39;un indicateur **force** à wandb.init pour exiger qu&#39;une machine soit connectée.
  * La journalisation de Tensorboard et TensorboardX est désormais instrumentée automatiquement lorsqu&#39;elle est activée.
  * Ajout d&#39;un paramètre **tensorboard** à wandb.init, qui applique un patch à tensorboard pour la journalisation.
  * Les handles de Wandb.save acceptent désormais un chemin de base vers des fichiers dans des sous-répertoires.
  * Wandb.tensorflow et wandb.tensorboard sont désormais accessibles sans import direct.
  * `wandb sync` parcourt désormais un répertoire de run wandb et synchronise tous les Runs.

  Consultez le [journal des modifications v0.6.34](https://github.com/wandb/wandb/releases/tag/v0.6.34) sur GitHub.
</Update>

<Update label="v0.6.34" description="23 janvier 2019">
  ## Correction de bugs

  * Correction de la journalisation dans Jupyter, sans modifier le niveau du logger.
  * Correction de la reprise dans Jupyter.

  ## amélioration

  * Wandb.init gère désormais correctement le cas où un utilisateur n&#39;est pas connecté à wandb.
  * Ajout d&#39;un indicateur **force** à wandb.init pour exiger qu&#39;une machine soit connectée.
  * La journalisation de Tensorboard et TensorboardX est désormais instrumentée automatiquement lorsqu&#39;elle est activée.
  * Ajout d&#39;un paramètre **tensorboard** à wandb.init, qui applique un patch à tensorboard pour la journalisation.
  * Les handles de Wandb.save acceptent désormais un chemin de base vers des fichiers dans des sous-répertoires.
  * Wandb.tensorflow et wandb.tensorboard sont désormais accessibles sans import direct.
  * `wandb sync` parcourt désormais un répertoire de run wandb et synchronise tous les Runs.

  Consultez le [journal des modifications v0.6.34](https://github.com/wandb/wandb/releases/tag/v0.6.34) sur GitHub.
</Update>

<Update label="v0.6.33" description="22 janvier 2019">
  ## Correction de bugs

  * Correction d&#39;une condition de concurrence pouvant bloquer le processus wandb à la fin d&#39;un run.

  Consultez le [journal des modifications v0.6.33](https://github.com/wandb/wandb/releases/tag/v0.6.33) sur GitHub.
</Update>

<Update label="v0.6.33" description="22 janvier 2019">
  ## Correction de bugs

  * Correction d&#39;une condition de concurrence pouvant bloquer le processus wandb à la fin d&#39;un run.

  Consultez le [journal des modifications v0.6.33](https://github.com/wandb/wandb/releases/tag/v0.6.33) sur GitHub.
</Update>

<Update label="v0.6.33" description="22 janvier 2019">
  ## Correction de bugs

  * Correction d&#39;une condition de concurrence pouvant bloquer le processus wandb à la fin d&#39;un run.

  Consultez le [journal des modifications v0.6.33](https://github.com/wandb/wandb/releases/tag/v0.6.33) sur GitHub.
</Update>

<Update label="v0.6.33" description="22 janvier 2019">
  ### Correction de bugs

  * Correction d&#39;une condition de concurrence pouvant bloquer le processus wandb à la fin d&#39;un run.

  Consultez le [journal des modifications v0.6.33](https://github.com/wandb/wandb/releases/tag/v0.6.33) sur GitHub.
</Update>

<Update label="v0.6.33" description="22 janvier 2019">
  ## Correction de bugs

  * Correction d&#39;une condition de concurrence pouvant bloquer le processus wandb à la fin d&#39;un run.

  Consultez le [journal des modifications v0.6.33](https://github.com/wandb/wandb/releases/tag/v0.6.33) sur GitHub.
</Update>

<Update label="v0.6.33" description="22 janvier 2019">
  ## Correction de bugs

  * Correction d&#39;une condition de concurrence pouvant bloquer le processus wandb à la fin d&#39;un run.

  Consultez le [journal des modifications v0.6.33](https://github.com/wandb/wandb/releases/tag/v0.6.33) sur GitHub.
</Update>

<Update label="v0.6.32" description="22 décembre 2018">
  ## Correction de bug

  * Corrige la reprise dans Jupyter lors du redémarrage du noyau.
  * Wandb.save garantit que les fichiers sont envoyés même s&#39;ils continuent à grossir.

  ## amélioration

  * Ajout du mot-clé replace=True à init pour la reprise automatique.
  * Nouvelle propriété run.resumed, utilisable pour détecter si une reprise est en cours.
  * Nouvelle propriété run.step à utiliser pour définir une époque initiale lors de la reprise.
  * Le callback Keras enregistre désormais le meilleur modèle au fil des améliorations.

  Consultez le [journal des modifications v0.6.32](https://github.com/wandb/wandb/releases/tag/v0.6.32) sur GitHub.
</Update>

<Update label="v0.6.31" description="20 décembre 2018">
  ## Correction de bug

  * N&#39;exige vraiment pas numpy.
  * Message d&#39;erreur amélioré si wandb.log est appelé avant wandb.init.
  * Empêche d&#39;appeler wandb.watch plusieurs fois.
  * Gère les attributs datetime dans les journaux / plotly.

  ## amélioration

  * Ajoute l&#39;environnement aux Sweeps.
  * Active l&#39;étiquetage dans l&#39;API publique et dans wandb.init.
  * Nouveau type de média wandb.Html pour journaliser du HTML arbitraire.
  * Ajout de la méthode api.create&#95;run de l&#39;API publique pour les intégrations personnalisées.
  * Ajout de la prise en charge des glob dans wandb.save, les fichiers étant enregistrés au fur et à mesure de leur écriture.
  * Ajout de wandb.restore pour récupérer les fichiers lors de la reprise.

  Consultez le [journal des modifications v0.6.31](https://github.com/wandb/wandb/releases/tag/v0.6.31) sur GitHub.
</Update>

<Update label="v0.6.30" description="06 décembre 2018">
  ## Correction de bug

  * Ajout d&#39;un délai d&#39;expiration pour générer les diffs sur les grands dépôts.
  * Correction d&#39;un cas limite où la synchronisation des fichiers pouvait se bloquer.
  * Garantit que toutes les modifications de fichiers sont capturées avant la sortie.
  * Gère les cas de sys.exit où aucun code n&#39;est transmis.
  * N&#39;exige pas numpy.

  ## amélioration

  * Nouvelle commande `wandb sync` qui envoie un répertoire local vers le cloud.
  * Prise en charge de la synchronisation du fichier tfevents pendant l&#39;entraînement.
  * Détecte l&#39;exécution en tant que TFJob et groupe automatiquement.
  * Nouveau module Kubeflow avec des assistants initiaux pour les pipelines.

  Consultez le [journal des modifications v0.6.30](https://github.com/wandb/wandb/releases/tag/v0.6.30) sur GitHub.
</Update>

<Update label="v0.6.29" description="26 novembre 2018">
  ## Correction de bug

  * Correction d&#39;un bug dans history / summary.

  Consultez le [journal des modifications v0.6.29](https://github.com/wandb/wandb/releases/tag/v0.6.29) sur GitHub.
</Update>

<Update label="v0.6.28" description="24 novembre 2018">
  ## amélioration

  * Prise en charge initiale d&#39;AWS SageMaker.
  * `hook_torch` renommé en `watch` avec un avertissement de dépréciation.
  * Les Projects sont automatiquement créés s&#39;ils n&#39;existent pas.
  * Ajout de la métrique GPU memory&#95;allocated.
  * Le graphique Keras stocke les arêtes.

  ## Correction de bug

  * L&#39;analyse du graphique PyTorch est plus robuste.
  * Correction de la prise en charge de PyTorch 0.3.
  * L&#39;API de téléchargement de fichiers prend en charge l&#39;authentification WANDB&#95;API&#95;KEY.

  Consultez le [journal des modifications v0.6.28](https://github.com/wandb/wandb/releases/tag/v0.6.28) sur GitHub.
</Update>

<Update label="v0.6.27" description="13 novembre 2018">
  ## amélioration

  * Sweeps fonctionne avec le nouveau backend (version préliminaire).
  * Summary suit toutes les métriques de l&#39;historique, sauf si elles sont remplacées par une écriture directe
    dans summary.
  * Prise en charge des fichiers dans l&#39;API de données.

  ## Correction de bug

  * Affiche les téléversements de fichiers média en cours dans la progression finale du téléversement.

  Consultez le [journal des modifications v0.6.27](https://github.com/wandb/wandb/releases/tag/v0.6.27) sur GitHub.
</Update>

<Update label="v0.6.26" description="09 novembre 2018">
  ## amélioration

  * Wandb.Audio prend en charge la durée.

  ## Correction de bug

  * Transmet l&#39;en-tête du nom d&#39;utilisateur dans l&#39;API filestream.

  Consultez le [journal des modifications v0.6.26](https://github.com/wandb/wandb/releases/tag/v0.6.26) sur GitHub.
</Update>

<Update label="v0.6.25" description="08 novembre 2018">
  ## amélioration

  * Nouveau type de données wandb.Audio.
  * Nouvel argument mot-clé `step` lors de la journalisation des métriques.
  * Possibilité de spécifier le groupe de run et le type de job lors de l’appel à wandb.init() ou via
    des variables d’environnement. Cela permet de regrouper automatiquement les runs d’entraînement distribués
    dans l’interface utilisateur.
  * Possibilité de remplacer le nom d’utilisateur lors de l’utilisation d’une clé API de compte de service.

  ## Correction de bugs

  * Gère les environnements non TTY dans Python 2.
  * Gère l’absence du binaire git.
  * Corrige un problème où la même image était parfois journalisée deux fois pendant une étape Keras.

  Consultez le [journal des modifications v0.6.25](https://github.com/wandb/wandb/releases/tag/v0.6.25) sur GitHub.
</Update>

<Update label="v0.6.23" description="19 octobre 2018">
  ## amélioration

  * PyTorch.
    * Ajout d’une nouvelle méthode `wandb.hook_torch` qui enregistre le graphe ainsi que les gradients et paramètres des modèles PyTorch
    * `wandb.Image` détecte les tenseurs PyTorch et utilise **torchvision.utils.make&#95;grid** pour effectuer le rendu de l’image.

  ## Correction de bugs

  * `wandb restore` gère le cas où il n’est pas exécuté depuis un dépôt git.

  Consultez le [journal des modifications v0.6.23](https://github.com/wandb/wandb/releases/tag/v0.6.23) sur GitHub.
</Update>

<Update label="v0.6.22" description="18 octobre 2018">
  ## Correction de bugs

  * Nous ouvrons désormais stdout et stderr en mode brut dans Python 2, ce qui garantit le bon fonctionnement d’outils comme bpdb.

  Consultez le [journal des modifications v0.6.22](https://github.com/wandb/wandb/releases/tag/v0.6.22) sur GitHub.
</Update>

<Update label="v0.6.21" description="12 octobre 2018">
  ## amélioration

  * Les erreurs fatales sont désormais signalées à Sentry, sauf si WANDB&#95;ERROR&#95;REPORTING est défini sur false.
  * Amélioration de la gestion des erreurs et des messages au démarrage.

  Consultez le [journal des modifications v0.6.21](https://github.com/wandb/wandb/releases/tag/v0.6.21) sur GitHub.
</Update>

<Update label="v0.6.20" description="05 octobre 2018">
  ## Correction de bugs

  * La première image enregistrée lors d’un appel à wandb.log n’était pas écrite ; c’est désormais corrigé.
  * `wandb.log` et `run.summary` suppriment désormais les espaces des clés.

  Consultez le [journal des modifications v0.6.20](https://github.com/wandb/wandb/releases/tag/v0.6.20) sur GitHub.
</Update>

<Update label="v0.6.19" description="05 octobre 2018">
  ## Correction de bugs

  * Intégration de prompt&#95;toolkit &lt;  1.0.15, car la dernière version d’ipython est épinglée à &gt; 2.0.
  * Chargement lazy de wandb.h5 uniquement si `summary` est consulté, afin d’améliorer les performances de la Data API.

  ## amélioration

  * Jupyter.
    * `wandb.monitor` est obsolète au profit du démarrage automatique des métriques système après le premier appel à wandb.log
    * Ajout de la nouvelle commande magique Jupyter **%%wandb** pour afficher les résultats en direct
    * Suppression de l’iframe de description Jupyter
  * La Data API prend désormais en charge les options `per_page` et `order` pour la méthode `api.runs`.
  * Prise en charge initiale de la journalisation de wandb.Table.
  * Prise en charge initiale de la journalisation avec matplotlib.

  Consultez le [journal des modifications v0.6.19](https://github.com/wandb/wandb/releases/tag/v0.6.19) sur GitHub.
</Update>