---
title: Gérer les utilisateurs, les groupes et les rôles avec SCIM
description: "Utilisez l'API SCIM pour gérer les utilisateurs, les groupes et les rôles personnalisés dans une organisation W&B grâce au provisionnement automatisé."
---

<Note>
  Regardez une [vidéo présentant SCIM en action](https://www.youtube.com/watch?v=Nw3QBqV0I-o) (12 min)
</Note>

<div id="overview">
  ## Aperçu
</div>

L’API System for Cross-domain Identity Management (SCIM) permet aux administrateurs de l’instance ou de l’organisation de gérer les utilisateurs, les groupes et les rôles personnalisés au sein de leur organisation W&amp;B. Les groupes SCIM correspondent aux Teams de W&amp;B.

L’API SCIM de W&amp;B est compatible avec les principaux fournisseurs d’identité, notamment Okta, ce qui permet le provisionnement et le déprovisionnement automatisés des utilisateurs. Pour la configuration du SSO avec Okta et d’autres fournisseurs d’identité, voir la [documentation SSO](/fr/platform/hosting/iam/sso/).

Pour obtenir des exemples pratiques en Python montrant comment interagir avec l’API SCIM, consultez notre dépôt [`wandb-scim`](https://github.com/wandb/examples/tree/master/wandb-scim).

<div id="supported-features">
  ### Fonctionnalités prises en charge
</div>

* **Filtrage** : l’API prend en charge le filtrage pour les points de terminaison `/Users` et `/Groups`
* **Opérations PATCH** : prise en charge de PATCH pour les mises à jour partielles des ressources
* **Prise en charge des ETags** : mises à jour conditionnelles à l’aide d’ETags pour détecter les conflits
* **Authentification par compte de service** : les comptes de service de l’organisation peuvent accéder à l’API

<Note>
  Si vous êtes administrateur de plusieurs organisations Enterprise [SaaS multilocataire](/fr/platform/hosting/hosting-options/multi_tenant_cloud), vous devez configurer l’organisation vers laquelle les requêtes d’API SCIM sont envoyées afin de garantir que les requêtes d’API SCIM envoyées avec votre clé API s’appliquent à la bonne organisation. Cliquez sur votre image de profil, puis sur **Paramètres utilisateur**, puis vérifiez le paramètre **Default API organization**.

  L’option d’hébergement choisie détermine la valeur de l’espace réservé `<host-url>` utilisé dans les exemples de cette page.

  De plus, les exemples utilisent des ID utilisateur comme `abc` et `def`. Dans les requêtes et réponses réelles, les ID utilisateur sont des valeurs hachées.
</Note>

<div id="authentication">
  ## Authentification
</div>

Choisissez de vous authentifier à l’aide d’une identité utilisateur ou d’un compte de service, après avoir examiné les principales différences.

<div id="key-differences">
  ### Différences clés
</div>

* Qui doit l’utiliser : les Users conviennent mieux aux actions d’administration interactives et ponctuelles ; les comptes de service conviennent mieux à l’automatisation et aux intégrations (CI/CD, outils de provisionnement).
* Informations d’identification : les Users envoient un nom d’utilisateur et une clé API ; les comptes de service envoient uniquement une clé API (sans nom d’utilisateur).
* Contenu de l’en-tête Authorization : les Users encodent `username:API-KEY` ; les comptes de service encodent `:API-KEY` (avec un deux-points initial).
* Portée et autorisations : les deux nécessitent des privilèges d’administration ; les comptes de service sont limités à l’organisation et sans interaction utilisateur, ce qui offre des traces d’audit plus claires pour l’automatisation.
* Où obtenir les informations d’identification : les Users copient leur clé API dans les Paramètres utilisateur ; les clés de compte de service se trouvent dans l’onglet Service account de l’organisation.
* Cloud mutualisé : si vous avez accès à plusieurs organisations du Cloud mutualisé, vous devez définir la Default API organization pour vous assurer que les appels à l’API SCIM sont acheminés vers l’organisation voulue.

<div id="users">
  ### Users
</div>

Utilisez vos identifiants personnels d’administrateur lorsque vous effectuez des tâches d’administration interactives. Construisez l’en-tête HTTP `Authorization` au format `Basic <base64(username:API-KEY)>`.

Par exemple, authentifiez-vous avec `demo:p@55w0rd` :

```bash
Authorization: Basic ZGVtbzpwQDU1dzByZA==
```

<div id="service-accounts">
  ### Comptes de service
</div>

Utilisez un compte de service limité à l’organisation pour l’automatisation ou les intégrations. Formez l’en-tête HTTP `Authorization` comme suit : `Basic <base64(:API-KEY)>` (notez le caractère deux-points initial et le nom d’utilisateur vide). Trouvez les clés API des comptes de service dans le tableau de bord de l’organisation, sous l’onglet **Service account**. Référez-vous à [Comptes de service limités à l’organisation](/fr/platform/hosting/iam/service-accounts/#organization-scoped-service-accounts).

Par exemple, authentifiez-vous avec la clé API `sa-p@55w0rd` :

```bash
Authorization: Basic OnNhLXBANTV3MHJk
```

<div id="user-management">
  ## Gestion des utilisateurs
</div>

La ressource utilisateur SCIM correspond aux utilisateurs de W&amp;B. Utilisez ces points de terminaison pour gérer les utilisateurs de votre organisation.

<div id="get-user">
  ### Obtenir un utilisateur
</div>

Récupère les informations d’un utilisateur spécifique au sein de votre organisation.

<Note>Cette opération ne récupère pas les comptes de service.</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: GET

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                          |
| --------- | -------------------- | ------ | ------------------------------------ |
| `id`      | chaîne de caractères | Oui    | L&#39;ID unique de l&#39;utilisateur |

#### Exemple

<Tabs>
  <Tab title="Obtenir la requête de l’utilisateur">
    ```bash
    GET /scim/Users/abc
    ```
  </Tab>

  <Tab title="Obtenir la réponse de l’utilisateur">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": true,
        "daysActive": 42,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "lastActiveAt": "2023-10-15T14:32:10Z",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1"
    }
    ```

    La réponse contient des détails sur l’activité de l’utilisateur dans l’organisation :

    * **`daysActive`** : Nombre total de jours pendant lesquels l’utilisateur a été actif dans l’organisation.
    * **`lastActiveAt`** : Horodatage ISO 8601 de l’activité la plus récente de l’utilisateur. Renvoie `null` si l’utilisateur n’a jamais été actif.

    La définition de « actif » diffère selon le type de déploiement :

    * **Cloud dédié / Autogéré** : Un utilisateur est actif s’il se connecte, ouvre n’importe quelle page dans W&amp;B App, journalise des Runs, utilise le SDK ou interagit de quelque manière que ce soit avec le serveur W&amp;B.
    * **Cloud mutualisé** : Un utilisateur est actif s’il effectue, dans la portée de l’organisation, une action traçable après le 8 mai 2025. Voir [Audit logging actions](/fr/platform/hosting/monitoring-usage/audit-logging#actions) pour la liste complète.
  </Tab>
</Tabs>

<div id="list-users">
  ### Liste des utilisateurs
</div>

Récupère la liste de tous les utilisateurs de votre organisation.

<Note>Cette opération ne récupère pas les comptes de service.</Note>

<div id="filter-users">
  #### Filtrer les utilisateurs
</div>

Le point de terminaison `/Users` prend en charge le filtrage des utilisateurs par nom d’utilisateur ou par adresse e-mail :

* `userName eq "value"` - Filtrer par nom d’utilisateur
* `emails.value eq "value"` - Filtrer par adresse e-mail

<div id="example">
  ##### Exemple
</div>

```bash
GET /scim/Users?filter=userName eq "john.doe"
GET /scim/Users?filter=emails.value eq "john@example.com"
```

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL** : `<host-url>/scim/Users`
* **Méthode** : GET

#### Exemple

<Tabs>
  <Tab title="Requête de liste des utilisateurs">
    ```bash
    GET /scim/Users
    ```
  </Tab>

  <Tab title="Réponse de liste des utilisateurs">
    ```bash
    (Statut 200)
    ```

    ```json
    {
        "Resources": [
            {
                "active": true,
                "daysActive": 42,
                "displayName": "Dev User 1",
                "emails": {
                    "Value": "dev-user1@example.com",
                    "Display": "",
                    "Type": "",
                    "Primary": true
                },
                "id": "abc",
                "lastActiveAt": "2023-10-15T14:32:10Z",
                "meta": {
                    "resourceType": "User",
                    "created": "2023-10-01T00:00:00Z",
                    "lastModified": "2023-10-01T00:00:00Z",
                    "location": "Users/abc"
                },
                "schemas": [
                    "urn:ietf:params:scim:schemas:core:2.0:User"
                ],
                "userName": "dev-user1"
            }
        ],
        "itemsPerPage": 9999,
        "schemas": [
            "urn:ietf:params:scim:api:messages:2.0:ListResponse"
        ],
        "startIndex": 1,
        "totalResults": 1
    }
    ```

    La réponse contient des détails sur l’activité de chaque utilisateur dans l’organisation :

    * **`daysActive`** : Nombre total de jours pendant lesquels l’utilisateur a été actif dans l’organisation.
    * **`lastActiveAt`** : Horodatage ISO 8601 de l’activité la plus récente de l’utilisateur. Renvoie `null` si l’utilisateur n’a jamais été actif.

    La définition de « actif » varie selon le type de déploiement :

    * **Cloud dédié / Autogéré** : Un utilisateur est actif s’il se connecte, ouvre une page dans la W&amp;B App, consigne des runs, utilise le SDK ou interagit de quelque manière que ce soit avec le serveur W&amp;B.
    * **Cloud mutualisé** : Un utilisateur est actif s’il effectue, après le 8 mai 2025, une action auditable au niveau de l’organisation. Voir [Audit logging actions](/fr/platform/hosting/monitoring-usage/audit-logging#actions) pour la liste complète.
  </Tab>
</Tabs>

<div id="create-user">
  ### Créer un utilisateur
</div>

Crée un nouvel utilisateur au sein de votre organisation.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users`
* **Méthode**: POST

<div id="parameters">
  #### Paramètres
</div>

| Paramètre  | Type                 | Requis | Description                                                         |
| ---------- | -------------------- | ------ | ------------------------------------------------------------------- |
| `emails`   | array                | oui    | Tableau d’objets e-mail. Doit inclure une adresse e-mail principale |
| `userName` | chaîne de caractères | oui    | Nom d’utilisateur du nouvel utilisateur                             |

#### Exemple

<Tabs>
  <Tab title="Requête de création d’utilisateur (Dédié/Autogéré)">
    ```bash
    POST /scim/Users
    ```

    ```json
    {
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "emails": [
            {
                "primary": true,
                "value": "dev-user2@example.com"
            }
        ],
        "userName": "dev-user2"
    }
    ```
  </Tab>

  <Tab title="Requête de création d’utilisateur (Multilocataire)">
    ```bash
    POST /scim/Users
    ```

    ```json
    {
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User",
            "urn:ietf:params:scim:schemas:extension:teams:2.0:User"
        ],
        "emails": [
            {
                "primary": true,
                "value": "dev-user2@example.com"
            }
        ],
        "userName": "dev-user2",
        "urn:ietf:params:scim:schemas:extension:teams:2.0:User": {
            "teams": ["my-team"]
        }
    }
    ```
  </Tab>
</Tabs>

<div id="response">
  #### Réponse
</div>

<Tabs>
  <Tab title="Réponse à la création d’un utilisateur (Dédié/Autogéré)">
    ```bash
    (Statut 201)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 2",
        "emails": {
            "Value": "dev-user2@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "def",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "location": "Users/def"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user2"
    }
    ```
  </Tab>

  <Tab title="Réponse à la création d’un utilisateur (Multilocataire)">
    ```bash
    (Statut 201)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 2",
        "emails": {
            "Value": "dev-user2@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "def",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "location": "Users/def"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User",
            "urn:ietf:params:scim:schemas:extension:teams:2.0:User"
        ],
        "userName": "dev-user2",
        "organizationRole": "member",
        "teamRoles": [
            {
                "teamName": "my-team",
                "roleName": "member"
            }
        ],
        "groups": [
            {
                "value": "my-team-id"
            }
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="delete-user">
  ### Supprimer un utilisateur
</div>

<Warning>
  **Conservez l’accès administrateur**

  Vous devez vous assurer qu’au moins un utilisateur administrateur est présent en permanence dans votre instance ou votre organisation. Sinon, aucun utilisateur ne pourra configurer ou administrer le compte W&amp;B de votre organisation. Si une organisation utilise SCIM ou un autre processus automatisé pour déprovisionner des utilisateurs de W&amp;B, une opération de déprovisionnement pourrait supprimer par inadvertance le dernier administrateur restant de l’instance ou de l’organisation.

  Pour obtenir de l’assistance afin de définir des procédures opérationnelles, ou pour rétablir l’accès administrateur, contactez [l’assistance](mailto:support@wandb.com).
</Warning>

Supprime complètement un utilisateur de votre organisation.

<Note>Cette opération fonctionne uniquement pour les utilisateurs, pas pour les comptes de service. Supprimez un compte de service dans les paramètres de l’équipe W&amp;B.</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: DELETE

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                            |
| --------- | -------------------- | ------ | -------------------------------------- |
| `id`      | chaîne de caractères | oui    | ID unique de l’utilisateur à supprimer |

#### Exemple

<Tabs>
  <Tab title="Requête de suppression d’utilisateur">
    ```bash
    DELETE /scim/Users/abc
    ```
  </Tab>

  <Tab title="Réponse de suppression d’utilisateur">
    ```bash
    (Statut 204)
    ```
  </Tab>
</Tabs>

<Note>
  Pour désactiver temporairement l’utilisateur, reportez-vous à l’API [Désactiver l’utilisateur](#deactivate-user), qui utilise le point de terminaison `PATCH`.
</Note>

<div id="update-user-email">
  ### Mettre à jour l’adresse e-mail d’un utilisateur
</div>

Met à jour l’adresse e-mail principale d’un utilisateur.
**Non pris en charge sur le Cloud mutualisé**, où le compte d’un utilisateur n’est pas géré par l’organisation.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                              |
| --------- | -------------------- | ------ | ---------------------------------------- |
| `id`      | chaîne de caractères | oui    | L’ID unique de l’utilisateur             |
| `op`      | chaîne de caractères | oui    | `replace`                                |
| `path`    | chaîne de caractères | oui    | `emails`                                 |
| `value`   | array                | oui    | Tableau contenant le nouvel objet e-mail |

#### Exemple

<Tabs>
  <Tab title="Requête de mise à jour de l’adresse e-mail">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "emails",
                "value": [
                    {
                        "value": "newemail@example.com",
                        "primary": true
                    }
                ]
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse de mise à jour de l’adresse e-mail">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dév User 1",
        "emails": {
            "Value": "newemail@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1"
    }
    ```
  </Tab>
</Tabs>

<div id="update-user-display-name">
  ### Mettre à jour le Display name d’un utilisateur
</div>

Met à jour le Display name d’un utilisateur.
**Non pris en charge dans le Cloud mutualisé**, où le compte d’un utilisateur n’est pas géré par l’organisation.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                  |
| --------- | -------------------- | ------ | ---------------------------- |
| `id`      | chaîne de caractères | oui    | L’ID unique de l’utilisateur |
| `op`      | chaîne de caractères | oui    | `replace`                    |
| `path`    | chaîne de caractères | oui    | `displayName`                |
| `value`   | chaîne de caractères | oui    | Nouveau Display name         |

#### Exemple

<Tabs>
  <Tab title="Requête de mise à jour du Display name">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "displayName",
                "value": "John Doe"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse de mise à jour du Display name">
    ```bash
    (Statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "John Doe",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2025-7-01T00:00:00Z",
            "lastModified": "2025-7-01T00:00:00Z",
            "location": "users/dev-user1"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1"
    }
    ```
  </Tab>
</Tabs>

<div id="deactivate-user">
  ### Désactiver un utilisateur
</div>

Désactive un utilisateur dans votre organisation. Le résultat varie selon le type de déploiement :

* **Cloud dédié** / **Autogéré** : définit le champ `active` de l’utilisateur sur `false`. Pour rétablir l’accès d’un utilisateur désactivé à votre organisation, voir [Réactiver un utilisateur](#reactivate-user).
* **Cloud mutualisé** : supprime l’utilisateur de l’organisation. Pour rétablir l’accès de l’utilisateur, ajoutez-le de nouveau à votre organisation. Voir [Créer un utilisateur](#create-user-request-multi-tenant). Dans le Cloud mutualisé, le compte d’un utilisateur n’est pas géré par l’organisation.

<Note>Cette opération fonctionne uniquement pour les utilisateurs, pas pour les comptes de service. La désactivation d’un compte de service n’est pas prise en charge. Gérez les comptes de service de l’équipe dans les paramètres de l’équipe W&amp;B.</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                               |
| --------- | -------------------- | ------ | ----------------------------------------- |
| `id`      | chaîne de caractères | Oui    | L’ID unique de l’utilisateur à désactiver |
| `op`      | chaîne de caractères | Oui    | `replace`                                 |
| `value`   | objet                | Oui    | Objet contenant `{"active": false}`       |

#### Exemple

<Tabs>
  <Tab title="Requête de désactivation d’utilisateur (Dédié/Autogéré)">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "value": {"active": false}
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Requête de désactivation d’utilisateur (Multilocataire)">
    ```bash
    PATCH /scim/Users
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "value": {"active": false}
            }
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="response">
  #### Réponse
</div>

<Tabs>
  <Tab title="Réponse de désactivation d’utilisateur (Dédié/Autogéré)">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": false,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1"
    }
    ```
  </Tab>

  <Tab title="Réponse de désactivation d’utilisateur (multilocataire)">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "value": {"active": true}
            }
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="reactivate-user">
  ### Réactiver l’utilisateur
</div>

Réactive un utilisateur précédemment désactivé dans votre organisation.

<Note>
  * La réactivation d’un utilisateur s’applique uniquement aux utilisateurs, et non aux comptes de service. Elle n’est pas prise en charge pour les comptes de service. Gérez les comptes de service dans les paramètres de l’équipe W&amp;B.

  * La réactivation d’un utilisateur n’est pas prise en charge dans [Cloud mutualisé](/fr/platform/hosting/hosting-options/multi_tenant_cloud). Pour rétablir l’accès de l’utilisateur, ajoutez-le de nouveau à votre organisation. Voir [Créer un utilisateur](#create-user-request-multi-tenant). Dans Cloud mutualisé, le compte d’un utilisateur n’est pas géré par l’organisation. Toute tentative de réactiver un utilisateur entraîne une erreur HTTP `400` :
    ```json
    {
        "schemas": [
            "urn:ietf:params:scim:api:messages:2.0:Error"
        ],
        "detail": "Les opérations de réactivation d’utilisateur ne sont pas prises en charge dans SaaS Cloud",
        "status": "400"
    }
    ```
</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                            |
| --------- | -------------------- | ------ | -------------------------------------- |
| `id`      | chaîne de caractères | oui    | ID unique de l’utilisateur à réactiver |
| `op`      | chaîne de caractères | oui    | `replace`                              |
| `value`   | objet                | oui    | Objet contenant `{"active": true}`     |

#### Exemple

<Tabs>
  <Tab title="Requête de réactivation de l’utilisateur">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "value": {"active": true}
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse de réactivation de l’utilisateur">
    ```bash
    (Statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1"
    }
    ```
  </Tab>
</Tabs>

<div id="assign-organization-role">
  ### Attribuer un rôle d’organisation
</div>

Attribue à un utilisateur un rôle au niveau de l’organisation.

<Note>Cette opération fonctionne uniquement pour les utilisateurs, pas pour les comptes de service. Les rôles personnalisés ne sont pas pris en charge pour les comptes de service.</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                       |
| --------- | -------------------- | ------ | --------------------------------- |
| `id`      | chaîne de caractères | Oui    | L’ID unique de l’utilisateur      |
| `op`      | chaîne de caractères | Oui    | `replace`                         |
| `path`    | chaîne de caractères | Oui    | `organizationRole`                |
| `value`   | chaîne de caractères | Oui    | Nom du rôle (`admin` ou `member`) |

<Note>
  Le rôle `viewer` limité à l’organisation est obsolète et ne peut plus être attribué dans l’interface utilisateur. Si vous utilisez SCIM pour attribuer le rôle `viewer` à un utilisateur :

  * Le rôle `member` lui est attribué dans l’organisation.
  * Une licence `viewer` Models lui est attribuée au lieu d’une licence `complet`. Cela donne un accès en lecture seule à Models et un accès complet au registre. Si aucune licence Models n’est disponible, une erreur `Seat limit reached` est enregistrée et le membre est ajouté sans accès à Models. Cette attribution peut être mise à jour ultérieurement si une licence devient disponible.
  * Une licence `viewer` Weave lui est attribuée au lieu d’une licence `complet`. Cela donne un accès en lecture seule à Weave. Si aucune licence Weave n’est disponible, une erreur `Seat limit reached` est enregistrée et le membre est ajouté sans accès à Weave. Cette attribution peut être mise à jour ultérieurement si une licence devient disponible.
  * Le rôle `viewer` du registre lui est attribué dans les registres visibles au niveau de l’organisation.
</Note>

#### Exemple

<Tabs>
  <Tab title="Requête d’attribution du rôle d’organisation">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "organizationRole",
                "value": "admin"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse d’attribution du rôle d’organisation">
    ```bash
    (Statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1",
        "teamRoles": [
            {
                "teamName": "team1",
                "roleName": "admin"
            }
        ],
        "organizationRole": "admin"
    }
    ```
  </Tab>
</Tabs>

<div id="assign-team-role">
  ### Attribuer un rôle d’équipe
</div>

Attribue à un utilisateur un rôle d’équipe.

<Note>Cette opération fonctionne uniquement pour les utilisateurs, pas pour les comptes de service. Les rôles personnalisés ne sont pas pris en charge pour les comptes de service.</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                                             |
| --------- | -------------------- | ------ | ------------------------------------------------------- |
| `id`      | chaîne de caractères | Oui    | L&#39;ID unique de l&#39;utilisateur                    |
| `op`      | chaîne de caractères | Oui    | `replace`                                               |
| `path`    | chaîne de caractères | Oui    | `teamRoles`                                             |
| `value`   | array                | Oui    | Tableau d&#39;objets contenant `teamName` et `roleName` |

#### Exemple

<Tabs>
  <Tab title="Requête d’attribution d’un rôle d’équipe">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "teamRoles",
                "value": [
                    {
                        "roleName": "admin",
                        "teamName": "team1"
                    }
                ]
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse d’attribution d’un rôle d’équipe">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1",
        "teamRoles": [
            {
                "teamName": "team1",
                "roleName": "admin"
            }
        ],
        "organizationRole": "admin"
    }
    ```
  </Tab>
</Tabs>

<div id="add-to-registry">
  ### Ajouter au registre
</div>

Ajoute un utilisateur à un registre en lui attribuant un rôle au niveau du registre.

<Note>Cette opération fonctionne uniquement pour les Users, pas pour les comptes de service. Les rôles personnalisés ne sont pas pris en charge pour les comptes de service.</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                                             |
| --------- | -------------------- | ------ | ------------------------------------------------------- |
| `id`      | chaîne de caractères | oui    | L’ID unique de l’utilisateur                            |
| `op`      | chaîne de caractères | oui    | `add`                                                   |
| `path`    | chaîne de caractères | oui    | `registryRoles`                                         |
| `value`   | array                | oui    | Tableau d’objets contenant `registryName` et `roleName` |

#### Exemple

<Tabs>
  <Tab title="Requête d’ajout au registre">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "registryRoles",
                "value": [
                    {
                        "roleName": "admin",
                        "registryName": "hello-registry"
                    }
                ]
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse d’ajout au registre">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1",
        "registryRoles": [
            {
                "registryName": "hello-registry",
                "roleName": "admin"
            }
        ],
        "organizationRole": "admin"
    }
    ```
  </Tab>
</Tabs>

<div id="remove-from-registry">
  ### Retirer du registre
</div>

Retire un utilisateur d’un registre.

<Note>
  * Les opérations de suppression suivent les spécifications du protocole SCIM RFC 7644. Utilisez la syntaxe de filtre `"registryRoles[registryName eq \"{registry_name}\"]"` pour retirer un utilisateur d’un registre spécifique, ou `"registryRoles"` pour retirer l’utilisateur de tous les registres.
  * Cette opération fonctionne uniquement pour les Users, pas pour les comptes de service. Supprimez les comptes de service d’un registre dans les paramètres de l’équipe W&amp;B.
</Note>

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Users/{id}`
* **Méthode**: PATCH

<div id="parameters">
  #### Paramètres
</div>

| Paramètre | Type                 | Requis | Description                                                                 |
| --------- | -------------------- | ------ | --------------------------------------------------------------------------- |
| `id`      | chaîne de caractères | oui    | L’ID unique de l’utilisateur                                                |
| `op`      | chaîne de caractères | oui    | `remove`                                                                    |
| `path`    | chaîne de caractères | oui    | `"registryRoles[registryName eq \"{registry_name}\"]"` ou `"registryRoles"` |

#### Exemple

<Tabs>
  <Tab title="Requête de suppression du registre">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "registryRoles[registryName eq \"goodbye-registry\"]"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse de suppression du registre">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1",
        "registryRoles": [
            {
                "registryName": "hello-registry",
                "roleName": "admin"
            }
        ],
        "organizationRole": "admin"
    }
    ```
  </Tab>
</Tabs>

<Tabs>
  <Tab title="Requête de suppression de TOUS les registres">
    ```bash
    PATCH /scim/Users/abc
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "replace",
                "path": "registryRoles"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse de suppression de TOUS les registres">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "active": true,
        "displayName": "Dev User 1",
        "emails": {
            "Value": "dev-user1@example.com",
            "Display": "",
            "Type": "",
            "Primary": true
        },
        "id": "abc",
        "meta": {
            "resourceType": "User",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Users/abc"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:User"
        ],
        "userName": "dev-user1",
        "organizationRole": "admin"
    }
    ```
  </Tab>
</Tabs>

<div id="group-resource">
  ## Ressource de groupe
</div>

Lorsque vous créez un groupe SCIM dans votre IAM, une équipe W&amp;B est créée et associée à ce groupe, et les autres opérations de groupe SCIM s&#39;appliquent à cette équipe. Pour configurer un stockage personnalisé lors de la création de l&#39;équipe, incluez `storageBucket` dans la requête.

<div id="service-accounts">
  ### Comptes de service
</div>

Lorsqu&#39;une équipe W&amp;B est créée à l&#39;aide de SCIM, tous les comptes de service au niveau de l&#39;organisation sont automatiquement ajoutés à l&#39;équipe afin de préserver l&#39;accès du compte de service aux ressources de l&#39;équipe.

<div id="filtering-groups">
  ### Filtrage des groupes
</div>

Le point de terminaison `/Groups` prend en charge le filtrage pour rechercher des équipes spécifiques :

<div id="supported-filters">
  #### Filtres pris en charge
</div>

* `displayName eq "value"` - Filtrer par le Display name de l’équipe

#### Exemple

```bash
GET /scim/Groups?filter=displayName eq "engineering-team"
```

<div id="get-team">
  ### Obtenir l&#39;équipe
</div>

Récupérez les informations de l&#39;équipe à partir de son ID unique.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL** : `<host-url>/scim/Groups/{id}`
* **Méthode** : GET

#### Exemple

<Tabs>
  <Tab title="Requête">
    ```bash
    GET /scim/Groups/ghi
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "displayName": "acme-devs",
        "id": "ghi",
        "members": [
            {
                "Value": "abc",
                "Ref": "",
                "Type": "",
                "Display": "dev-user1"
            }
        ],
        "meta": {
            "resourceType": "Group",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Groups/ghi"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:Group"
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="list-teams">
  ### Liste des équipes
</div>

Récupérez la liste des équipes.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL** : `<host-url>/scim/Groups`
* **Méthode** : GET

#### Exemple

<Tabs>
  <Tab title="Requête">
    ```bash
    GET /scim/Groups
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "Resources": [
            {
                "displayName": "acme-devs",
                "id": "ghi",
                "members": [
                    {
                        "Value": "abc",
                        "Ref": "",
                        "Type": "",
                        "Display": "dev-user1"
                    }
                ],
                "meta": {
                    "resourceType": "Group",
                    "created": "2023-10-01T00:00:00Z",
                    "lastModified": "2023-10-01T00:00:00Z",
                    "location": "Groups/ghi"
                },
                "schemas": [
                    "urn:ietf:params:scim:schemas:core:2.0:Group"
                ]
            }
        ],
        "itemsPerPage": 9999,
        "schemas": [
            "urn:ietf:params:scim:api:messages:2.0:ListResponse"
        ],
        "startIndex": 1,
        "totalResults": 1
    }
    ```
  </Tab>
</Tabs>

<div id="create-team">
  ### Créer une équipe
</div>

* **Point de terminaison** : **`<host-url>/scim/Groups`**
* **Méthode** : POST
* **Description** : Créer une nouvelle ressource d&#39;équipe.
* **Champs pris en charge** :

| Champ           | Type                        | Requis                                                                   |
| --------------- | --------------------------- | ------------------------------------------------------------------------ |
| `displayName`   | Chaîne de caractères        | Oui                                                                      |
| `members`       | Tableau à valeurs multiples | Oui (`value` est un sous-champ requis et correspond à un ID utilisateur) |
| `storageBucket` | Objet                       | Non                                                                      |

Vous pouvez configurer [Bring your own bucket (BYOB)](/fr/platform/hosting/data-security/secure-storage-connector) au niveau de l&#39;équipe lors de sa création en incluant un objet `storageBucket`. S&#39;il est omis, l&#39;équipe utilise le stockage par défaut ou le stockage au niveau de l&#39;instance. Provisionnez le bucket (stratégie, CORS, identifiants) et déterminez le format de l&#39;adresse de stockage selon le fournisseur à l&#39;aide du guide BYOB. L&#39;objet `storageBucket` comporte les sous-champs suivants :

* **Requis** : `name` (nom du bucket), `provider` (une des valeurs suivantes : `coreweave`, `aws`, `gcp`, `azure`, `minio`). Respecte la casse.
* **Facultatif** : `path` (préfixe de chemin dans le bucket), `kmsKeyId` (clé KMS pour le chiffrement, par exemple pour AWS), `awsExternalId` (accès AWS intercomptes), `azureTenantId` (ID du locataire Azure), `azureClientId` (ID client de l&#39;identité managée Azure).

W&amp;B vérifie que le bucket existe et est accessible avant de créer l&#39;équipe. Si la validation échoue, la requête SCIM échoue et l&#39;équipe n&#39;est pas créée.

Une valeur `provider` non valide renvoie `400 Bad Request` avec une erreur SCIM qui répertorie les valeurs autorisées.

<div id="examples">
  #### Exemples
</div>

Ces exemples montrent comment créer une équipe sans stockage personnalisé et avec un stockage BYOB chez un fournisseur spécifique. Sélectionnez l’onglet correspondant à la configuration de stockage souhaitée pour afficher un exemple de requête, puis sélectionnez l’onglet **Réponse** pour afficher un exemple de réponse.

<Tabs>
  <Tab title="Requête (sans BYOB)">
    ```bash
    POST /scim/Groups
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Group"],
        "displayName": "wandb-support",
        "members": [
            {
                "value": "def"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="CoreWeave">
    ```bash
    POST /scim/Groups
    Content-Type: application/scim+json
    ```

    ```json
    {
      "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Group"],
      "displayName": "ml-training-team",
      "members": [
        {
          "value": "user@example.com",
          "display": "user@example.com"
        }
      ],
      "storageBucket": {
        "name": "wandb-coreweave-bucket",
        "provider": "coreweave",
        "path": "ml-training/experiments"
      }
    }
    ```
  </Tab>

  <Tab title="AWS S3">
    ```bash
    POST /scim/Groups
    Content-Type: application/scim+json
    ```

    ```json
    {
      "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Group"],
      "displayName": "ml-team",
      "members": [
        {
          "value": "user@example.com",
          "display": "user@example.com"
        }
      ],
      "storageBucket": {
        "name": "my-company-wandb-data",
        "provider": "s3",
        "path": "ml-team/experiments",
        "kmsKeyId": "arn:aws:kms:us-east-1:123456789012:key/12345678-1234-1234-1234-123456789012",
        "awsExternalId": "wandb-external-id-abc123"
      }
    }
    ```
  </Tab>

  <Tab title="GCS">
    ```bash
    POST /scim/Groups
    Content-Type: application/scim+json
    ```

    ```json
    {
      "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Group"],
      "displayName": "data-science-team",
      "members": [
        {
          "value": "VXNlcjox",
          "display": "jane.doe@example.com"
        },
        {
          "value": "VXNlcjoy",
          "display": "john.smith@example.com"
        }
      ],
      "storageBucket": {
        "name": "my-gcs-bucket",
        "provider": "gcs",
        "path": "data-science/runs"
      }
    }
    ```
  </Tab>

  <Tab title="Azure">
    ```bash
    POST /scim/Groups
    Content-Type: application/scim+json
    ```

    ```json
    {
      "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Group"],
      "displayName": "research-team",
      "members": [],
      "storageBucket": {
        "name": "wandbstorage",
        "provider": "azure",
        "path": "research/artifacts",
        "azureTenantId": "12345678-1234-1234-1234-123456789012",
        "azureClientId": "87654321-4321-4321-4321-210987654321"
      }
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 201)
    ```

    ```json
    {
        "displayName": "wandb-support",
        "id": "jkl",
        "members": [
            {
                "Value": "def",
                "Ref": "",
                "Type": "",
                "Display": "dev-user2"
            }
        ],
        "meta": {
            "resourceType": "Group",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:00:00Z",
            "location": "Groups/jkl"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:Group"
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="update-team">
  ### Mettre à jour l’équipe
</div>

* **Point de terminaison** : **`<host-url>/scim/Groups/{id}`**
* **Méthode** : PATCH
* **Description** : Mettre à jour la liste des membres d’une équipe existante.
* **Opérations prises en charge** : `add` membre, `remove` membre, `replace` membres

<Note>
  - Les opérations de suppression suivent les spécifications du protocole SCIM RFC 7644. Utilisez la syntaxe de filtre `members[value eq "{user_id}"]` pour supprimer un utilisateur spécifique, ou `members` pour supprimer tous les Users de l’équipe.

    **Identification de l’utilisateur** : le `{user_id}` dans les opérations sur les membres peut être :

    * Un ID utilisateur W&amp;B
    * Une adresse e-mail (par exemple, &quot;user@example.com&quot;)
  - Ces opérations fonctionnent uniquement pour les Users, pas pour les comptes de service. Mettez à jour les comptes de service d’une équipe dans les paramètres de l’équipe W&amp;B.
</Note>

<Info>
  Remplacez `{team_id}` par l’ID réel de l’équipe et `{user_id}` par l’ID utilisateur réel ou l’adresse e-mail dans vos requêtes.
</Info>

<div id="replace-team-members">
  ### Remplacer les membres de l&#39;équipe
</div>

Remplace tous les membres d&#39;une équipe par ceux d&#39;une nouvelle liste.

<Note>Cette opération fonctionne uniquement pour les Users, pas pour les comptes de service. Gérez les comptes de service dans les paramètres de l&#39;équipe W&amp;B.</Note>

* **Point de terminaison** : **`<host-url>/scim/Groups/{id}`**
* **Méthode** : PUT
* **Description** : Remplace l&#39;intégralité de la liste des membres de l&#39;équipe.

<Tabs>
  <Tab title="Requête">
    ```bash
    PUT /scim/Groups/{team_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Group"],
        "displayName": "acme-devs",
        "members": [
            {
                "value": "{user_id_1}"
            },
            {
                "value": "{user_id_2}"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "displayName": "acme-devs",
        "id": "ghi",
        "members": [
            {
                "Value": "user_id_1",
                "Ref": "",
                "Type": "",
                "Display": "user1"
            },
            {
                "Value": "user_id_2",
                "Ref": "",
                "Type": "",
                "Display": "user2"
            }
        ],
        "meta": {
            "resourceType": "Group",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:01:00Z",
            "location": "Groups/ghi"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:Group"
        ]
    }
    ```
  </Tab>
</Tabs>

**Ajout d&#39;un utilisateur à une équipe**

Ajout de `dev-user2` à `acme-devs` :

<Note>Cette opération fonctionne uniquement pour les Users, pas pour les comptes de service. Gérez les comptes de service dans les paramètres de l&#39;équipe W&amp;B.</Note>

<Tabs>
  <Tab title="Requête">
    ```bash
    PATCH /scim/Groups/{team_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "add",
                "path": "members",
                "value": [
                    {
                        "value": "{user_id}"
                    }
                ]
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "displayName": "acme-devs",
        "id": "ghi",
        "members": [
            {
                "Value": "abc",
                "Ref": "",
                "Type": "",
                "Display": "dev-user1"
            },
            {
                "Value": "def",
                "Ref": "",
                "Type": "",
                "Display": "dev-user2"
            }
        ],
        "meta": {
            "resourceType": "Group",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:01:00Z",
            "location": "Groups/ghi"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:Group"
        ]
    }
    ```
  </Tab>
</Tabs>

**Suppression d&#39;un utilisateur spécifique d&#39;une équipe**

Suppression de `dev-user2` d&#39;`acme-devs` :

<Note>Cette opération fonctionne uniquement pour les Users, pas pour les comptes de service. Gérez les comptes de service dans les paramètres de l&#39;équipe W&amp;B.</Note>

<Tabs>
  <Tab title="Requête">
    ```bash
    PATCH /scim/Groups/{team_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "remove",
                "path": "members[value eq \"{user_id}\"]"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "displayName": "acme-devs",
        "id": "ghi",
        "members": [
            {
                "Value": "abc",
                "Display": "dev-user1"
            }
        ],
        "meta": {
            "resourceType": "Group",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:01:00Z",
            "location": "Groups/ghi"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:Group"
        ]
    }
    ```
  </Tab>
</Tabs>

**Suppression de tous les Users d’une équipe**

Pour supprimer tous les Users de `acme-devs` :

<Note>Cette opération fonctionne uniquement pour les Users, pas pour les comptes de service. Gérez les comptes de service dans les paramètres de l’équipe W&amp;B.</Note>

<Tabs>
  <Tab title="Requête">
    ```bash
    PATCH /scim/Groups/{team_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "remove",
                "path": "members"
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (statut 200)
    ```

    ```json
    {
        "displayName": "acme-devs",
        "id": "ghi",
        "members": null,
        "meta": {
            "resourceType": "Group",
            "created": "2023-10-01T00:00:00Z",
            "lastModified": "2023-10-01T00:01:00Z",
            "location": "Groups/ghi"
        },
        "schemas": [
            "urn:ietf:params:scim:schemas:core:2.0:Group"
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="delete-team">
  ### Supprimer l&#39;équipe
</div>

* La suppression des équipes n&#39;est actuellement pas prise en charge par l&#39;API SCIM, car des données supplémentaires y sont associées. Supprimez les équipes depuis l&#39;application pour confirmer que vous souhaitez tout supprimer.

<div id="role-resource">
  ## Ressource de rôle
</div>

La ressource de rôle SCIM correspond aux rôles personnalisés de W&amp;B. Comme indiqué précédemment, les points de terminaison `/Roles` ne font pas partie du schéma SCIM officiel ; W&amp;B ajoute des points de terminaison `/Roles` pour permettre la gestion automatisée des rôles personnalisés dans les organisations W&amp;B.

<div id="get-custom-role">
  ### Obtenir un rôle personnalisé
</div>

Récupérez les informations sur un rôle personnalisé en fournissant l&#39;ID unique du rôle.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Roles/{id}`
* **Méthode**: GET

#### Exemple

<Tabs>
  <Tab title="Requête">
    ```bash
    GET /scim/Roles/abc
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Status 200)
    ```

    ```json
    {
        "description": "A sample custom role for example",
        "id": "Um9sZTo3",
        "inheritedFrom": "member", // indique le rôle prédéfini
        "meta": {
            "resourceType": "Role",
            "created": "2023-11-20T23:10:14Z",
            "lastModified": "2023-11-20T23:31:23Z",
            "location": "Roles/Um9sZTo3"
        },
        "name": "Sample custom role",
        "organizationID": "T3JnYW5pemF0aW9uOjE0ODQ1OA==",
        "permissions": [
            {
                "name": "artifact:read",
                "isInherited": true // hérité du rôle prédéfini membre
            },
            ...
            ...
            {
                "name": "project:update",
                "isInherited": false // autorisation personnalisée ajoutée par l’administrateur
            }
        ],
        "schemas": [
            ""
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="list-custom-roles">
  ### Lister les rôles personnalisés
</div>

Récupérez les informations sur tous les rôles personnalisés de l&#39;organisation W&amp;B.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL** : `<host-url>/scim/Roles`
* **Méthode** : GET

#### Exemple

<Tabs>
  <Tab title="Requête">
    ```bash
    GET /scim/Roles
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Statut 200)
    ```

    ```json
    {
       "Resources": [
            {
                "description": "A sample custom role for example",
                "id": "Um9sZTo3",
                "inheritedFrom": "member", // indique le rôle prédéfini dont le rôle personnalisé hérite
                "meta": {
                    "resourceType": "Role",
                    "created": "2023-11-20T23:10:14Z",
                    "lastModified": "2023-11-20T23:31:23Z",
                    "location": "Roles/Um9sZTo3"
                },
                "name": "Sample custom role",
                "organizationID": "T3JnYW5pemF0aW9uOjE0ODQ1OA==",
                "permissions": [
                    {
                        "name": "artifact:read",
                        "isInherited": true // hérité du rôle prédéfini member
                    },
                    ...
                    ...
                    {
                        "name": "project:update",
                        "isInherited": false // autorisation personnalisée ajoutée par l'administrateur
                    }
                ],
                "schemas": [
                    ""
                ]
            },
            {
                "description": "Another sample custom role for example",
                "id": "Um9sZToxMg==",
                "inheritedFrom": "viewer", // indique le rôle prédéfini dont le rôle personnalisé hérite
                "meta": {
                    "resourceType": "Role",
                    "created": "2023-11-21T01:07:50Z",
                    "location": "Roles/Um9sZToxMg=="
                },
                "name": "Sample custom role 2",
                "organizationID": "T3JnYW5pemF0aW9uOjE0ODQ1OA==",
                "permissions": [
                    {
                        "name": "launchagent:read",
                        "isInherited": true // hérité du rôle prédéfini viewer
                    },
                    ...
                    ...
                    {
                        "name": "run:stop",
                        "isInherited": false // autorisation personnalisée ajoutée par l'administrateur
                    }
                ],
                "schemas": [
                    ""
                ]
            }
        ],
        "itemsPerPage": 9999,
        "schemas": [
            "urn:ietf:params:scim:api:messages:2.0:ListResponse"
        ],
        "startIndex": 1,
        "totalResults": 2
    }
    ```
  </Tab>
</Tabs>

<div id="create-custom-role">
  ### Créer un rôle personnalisé
</div>

* **Point de terminaison**: **`<host-url>/scim/Roles`**
* **Méthode**: POST
* **Description**: Créer un nouveau rôle personnalisé dans l&#39;organisation W&amp;B.
* **Champs pris en charge**:

| Champ           | Type                 | Requis                                                                                                                                                                                                                                              |
| --------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `name`          | chaîne de caractères | Nom du rôle personnalisé                                                                                                                                                                                                                            |
| `description`   | chaîne de caractères | Description du rôle personnalisé                                                                                                                                                                                                                    |
| `permissions`   | Tableau d&#39;objets | Tableau d&#39;objets d&#39;autorisation, où chaque objet comprend un champ chaîne `name` dont la valeur est de la forme `w&bobject:operation`. Par exemple, pour une autorisation de suppression de W&amp;B Runs, la valeur de `name` serait `run:delete`. |
| `inheritedFrom` | chaîne de caractères | Rôle prédéfini dont le rôle personnalisé hérite. Il peut s&#39;agir de `member` ou de `viewer`.                                                                                                                                                     |

#### Exemple

<Tabs>
  <Tab title="Requête">
    ```bash
    POST /scim/Roles
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Role"],
        "name": "Exemple de rôle personnalisé",
        "description": "Un exemple de rôle personnalisé",
        "permissions": [
            {
                "name": "project:update"
            }
        ],
        "inheritedFrom": "member"
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Statut 201)
    ```

    ```json
    {
        "description": "Un exemple de rôle personnalisé",
        "id": "Um9sZTo3",
        "inheritedFrom": "member", // indique le rôle prédéfini
        "meta": {
            "resourceType": "Role",
            "created": "2023-11-20T23:10:14Z",
            "lastModified": "2023-11-20T23:31:23Z",
            "location": "Roles/Um9sZTo3"
        },
        "name": "Exemple de rôle personnalisé",
        "organizationID": "T3JnYW5pemF0aW9uOjE0ODQ1OA==",
        "permissions": [
            {
                "name": "artifact:read",
                "isInherited": true // hérité du rôle prédéfini member
            },
            ...
            ...
            {
                "name": "project:update",
                "isInherited": false // autorisation personnalisée ajoutée par l'administrateur
            }
        ],
        "schemas": [
            ""
        ]
    }
    ```
  </Tab>
</Tabs>

<div id="update-custom-role">
  ### Mettre à jour le rôle personnalisé
</div>

<div id="add-permissions-to-role">
  #### Ajouter des autorisations au rôle
</div>

* **Point de terminaison**: **`<host-url>/scim/Roles/{id}`**
* **Méthode**: PATCH
* **Description**: Ajouter des autorisations à un rôle personnalisé existant.

<Tabs>
  <Tab title="Requête">
    ```bash
    PATCH /scim/Roles/{role_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "add",
                "path": "permissions",
                "value": [
                    {
                        "name": "project:delete"
                    },
                    {
                        "name": "run:stop"
                    }
                ]
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Status 200)
    ```

    Renvoie le rôle mis à jour avec les nouvelles autorisations ajoutées.
  </Tab>
</Tabs>

<div id="remove-a-permission-from-a-role">
  #### Supprimer une autorisation d’un rôle
</div>

* **Point de terminaison** : **`<host-url>/scim/Roles/{id}`**
* **Méthode** : PATCH
* **Description** : Supprime des autorisations d’un rôle personnalisé existant.

<Tabs>
  <Tab title="Requête">
    ```bash
    PATCH /scim/Roles/{role_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
        "Operations": [
            {
                "op": "remove",
                "path": "permissions",
                "value": [
                    {
                        "name": "project:update"
                    }
                ]
            }
        ]
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Status 200)
    ```

    Renvoie le rôle mis à jour, dont les autorisations spécifiées ont été supprimées.
  </Tab>
</Tabs>

<div id="replace-custom-role">
  ### Remplacer le rôle personnalisé
</div>

* **Point de terminaison** : **`<host-url>/scim/Roles/{id}`**
* **Méthode** : PUT
* **Description** : Remplace entièrement la définition d’un rôle personnalisé.

<Tabs>
  <Tab title="Requête">
    ```bash
    PUT /scim/Roles/{role_id}
    ```

    ```json
    {
        "schemas": ["urn:ietf:params:scim:schemas:core:2.0:Role"],
        "name": "Updated custom role",
        "description": "Updated description for the custom role",
        "permissions": [
            {
                "name": "project:read"
            },
            {
                "name": "run:read"
            },
            {
                "name": "artifact:read"
            }
        ],
        "inheritedFrom": "viewer"
    }
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Status 200)
    ```

    Renvoie la définition du rôle entièrement remplacée.
  </Tab>
</Tabs>

<div id="delete-custom-role">
  ### Supprimer le rôle personnalisé
</div>

Supprimez un rôle personnalisé dans l’organisation W&amp;B. **Utilisez cette fonctionnalité avec prudence**. Le rôle prédéfini dont le rôle personnalisé a hérité est désormais attribué à tous les utilisateurs auxquels le rôle personnalisé était attribué avant l’opération.

<div id="endpoint">
  #### Point de terminaison
</div>

* **URL**: `<host-url>/scim/Roles/{id}`
* **Méthode**: DELETE

#### Exemple

<Tabs>
  <Tab title="Requête">
    ```bash
    DELETE /scim/Roles/abc
    ```
  </Tab>

  <Tab title="Réponse">
    ```bash
    (Statut 204 : aucun contenu)
    ```
  </Tab>
</Tabs>

<div id="advanced-features">
  ## Fonctionnalités avancées
</div>

<div id="etag-support">
  ### Prise en charge des ETags
</div>

L’API SCIM prend en charge les ETags pour les mises à jour conditionnelles afin d’éviter les conflits de modification concurrente. Les ETags sont renvoyés dans l’en-tête de réponse `ETag` et le champ `meta.version`.

<div id="etags">
  #### ETags
</div>

Pour utiliser les ETags :

1. **Obtenir l’ETag actuel**: Lorsque vous effectuez un GET sur une ressource, notez l’en-tête ETag dans la réponse
2. **Mise à jour conditionnelle**: Incluez l’ETag dans l’en-tête `If-Match` lors de la mise à jour

#### Exemple

```
# Obtenir l'utilisateur et noter l'ETag
GET /scim/Users/abc
# La réponse inclut : ETag: W/"xyz123"

# Mettre à jour avec l'ETag
PATCH /scim/Users/abc
If-Match: W/"xyz123"

{
    "schemas": ["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
    "Operations": [
        {
            "op": "replace",
            "path": "organizationRole",
            "value": "admin"
        }
    ]
}
```

Une réponse d’erreur `412 Precondition Failed` indique que la ressource a été modifiée depuis que vous l’avez récupérée.

<div id="error-handling">
  ### Gestion des erreurs
</div>

L’API SCIM renvoie des réponses d’erreur SCIM standard :

| Code de statut | Description                                                     |
| -------------- | --------------------------------------------------------------- |
| `200`          | Succès                                                          |
| `201`          | Créé                                                            |
| `204`          | Aucun contenu (suppression réussie)                             |
| `400`          | Requête incorrecte - paramètres ou corps de requête non valides |
| `401`          | Non autorisé - échec de l’authentification                      |
| `403`          | Interdit - permissions insuffisantes                            |
| `404`          | Introuvable - la ressource n’existe pas                         |
| `409`          | Conflit - la ressource existe déjà                              |
| `412`          | Précondition échouée - incompatibilité ETag                     |
| `500`          | Erreur interne du serveur                                       |

<div id="implementation-differences-per-deployment-type">
  ### Différences d’implémentation par type de déploiement
</div>

W&amp;B maintient deux implémentations distinctes de l’API SCIM, et les fonctionnalités diffèrent entre elles :

| Fonctionnalité                                 | Cloud dédié | Autogéré |
| ---------------------------------------------- | ----------- | -------- |
| Mettre à jour l’e-mail de l’utilisateur        | -           | ✓        |
| Mettre à jour le Display name de l’utilisateur | -           | ✓        |
| Désactivation de l’utilisateur                 | ✓           | ✓        |
| Réactivation de l’utilisateur                  | -           | ✓        |
| E-mails multiples par utilisateur              | ✓           | -        |

<div id="limitations">
  ## Limites
</div>

* **Résultats maximum** : 9999 éléments par requête.
* **Environnements monolocataires** : ne prennent en charge qu’un seul e-mail par utilisateur.
* **Suppression d’équipe** : non prise en charge via SCIM (utilisez l’interface web W&amp;B).
* **Réactivation d’utilisateur** : non prise en charge dans les environnements Cloud mutualisé.
* **Limites de licences** : les opérations peuvent échouer si les limites de licences de l’organisation sont atteintes.