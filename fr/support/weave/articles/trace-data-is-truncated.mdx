---
title: "Les données de trace sont tronquées"
keywords: ["Données de trace"]
---

Parfois, des données de trace volumineuses sont partiellement tronquées dans l’interface Weave. Ce problème survient parce que la sortie de trace par défaut est un objet Python brut et personnalisé que Weave ne sait pas sérialiser.

Pour éviter que des données de trace volumineuses ne soient tronquées, définissez un dictionnaire de chaînes comme valeur de retour afin de renvoyer l’ensemble des données de trace.

```python
import weave

class MyObj:
    def __init__(self, x: int):
        self.x = x

    def __repr__(self):
        return f"MyObj(x={self.x})"

    def to_dict(self):
        return {"x": self.x}

@weave.op()
def make_my_obj():
    x = "s" * 10_000
    return MyObj(x)
```

***

{/* ---- GÉNÉRÉ AUTOMATIQUEMENT : badges d'onglet ----
  Géré par scripts/knowledgebase-nav/generate_tags.py à partir des mots-clés du front matter.
  Ne modifiez pas manuellement le contenu entre ces marqueurs.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Données de trace](/fr/support/weave/tags/trace-data)</Badge>

{/* ---- FIN GÉNÉRÉ AUTOMATIQUEMENT : badges d'onglet ---- */}
