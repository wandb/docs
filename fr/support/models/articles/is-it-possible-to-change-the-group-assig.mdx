---
title: "Est-il possible de modifier le groupe attribué à un run une fois celui-ci terminé ?"
keywords: ["Runs"]
---

Vous pouvez modifier le groupe attribué à un run terminé à l’aide de l’API. Cette fonctionnalité n’est pas disponible dans l’interface web. Utilisez le code suivant pour mettre à jour le groupe :

```python
import wandb

api = wandb.Api()
run = api.run("<ENTITY>/<PROJECT>/<RUN_ID>")
run.group = "NEW-GROUP-NAME"
run.update()
```

***

{/* ---- GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet ----
  Géré par scripts/knowledgebase-nav/generate_tags.py à partir des mots-clés du front matter.
  Ne modifiez pas manuellement entre ces marqueurs.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Runs](/fr/support/models/tags/runs)</Badge>

{/* ---- FIN GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet ---- */}
