---
title: "Comment supprimer plusieurs runs en une seule fois plutôt qu’un par un ?"
keywords: ["Projects", "Runs"]
---

Dans l’onglet **Runs** de votre Workspace, cochez la case à côté de chaque run à supprimer, puis cliquez sur **Supprimer**.

Utilisez l’[API publique](/fr/models/ref/python/public-api/api) pour supprimer plusieurs runs en une seule opération :

```python
import wandb

api = wandb.Api()
runs = api.runs('<entity>/<project>')
for run in runs:
    if <condition>:
        run.delete()
```

***

{/* GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet */}

<Badge stroke shape="pill" color="orange" size="md">[Projects](/fr/support/models/tags/projects)</Badge><Badge stroke shape="pill" color="orange" size="md">[Runs](/fr/support/models/tags/runs)</Badge>

{/* FIN GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet */}