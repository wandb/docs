---
title: "Peut-on définir des variables booléennes comme hyperparamètres ?"
keywords: ["Sweeps"]
---

Utilisez la macro `${args_no_boolean_flags}` dans la section `command` de la configuration pour transmettre les hyperparamètres sous forme d&#39;options booléennes. Cette macro inclut automatiquement les paramètres booléens sous forme d&#39;options. Si `param` vaut `True`, la commande reçoit `--param`. Si `param` vaut `False`, l&#39;option est omise.

***

{/* ---- GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet ----
  Géré par scripts/knowledgebase-nav/generate_tags.py à partir des mots-clés du front matter.
  Ne modifiez pas manuellement le contenu entre ces marqueurs.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Sweeps](/fr/support/models/tags/sweeps)</Badge>

{/* ---- FIN GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglets ---- */}
