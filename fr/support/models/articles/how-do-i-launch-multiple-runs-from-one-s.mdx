---
title: "Comment puis-je lancer plusieurs runs à partir d’un script ?"
keywords: ["Experiments"]
---

Terminez les runs précédents avant d’en démarrer de nouveaux afin de journaliser plusieurs runs dans
un même script.

La méthode recommandée est d’utiliser `wandb.init()` comme gestionnaire de contexte,
car cela termine le run et le marque comme ayant échoué si votre script lève une
exception :

```python
import wandb

for x in range(10):
    with wandb.init() as run:
        for y in range(100):
            run.log({"metric": x + y})
```

Vous pouvez aussi appeler explicitement `run.finish()` :

```python
import wandb

for x in range(10):
    run = wandb.init()

    try:
        for y in range(100):
            run.log({"metric": x + y})

    except Exception:
        run.finish(exit_code=1)
        raise

    finally:
        run.finish()
```

<div id="multiple-active-runs">
  ## Plusieurs runs actifs
</div>

À partir de wandb 0.19.10, vous pouvez définir le paramètre `reinit` sur `"create_new"`
pour créer plusieurs runs actifs en même temps.

```python
import wandb

with wandb.init(reinit="create_new") as tracking_run:
    for x in range(10):
        with wandb.init(reinit="create_new") as run:
            for y in range(100):
                run.log({"x_plus_y": x + y})

            tracking_run.log({"x": x})
```

Voir [Plusieurs runs par processus](/fr/models/runs/initialize-run)
pour en savoir plus sur `reinit="create_new"`, y compris les mises en garde concernant les
intégrations W&amp;B.

***

{/* ---- GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet ----
  Géré par scripts/knowledgebase-nav/generate_tags.py à partir des mots-clés du front matter.
  Ne modifiez pas manuellement le contenu entre ces marqueurs.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Experiments](/fr/support/models/tags/experiments)</Badge>

{/* ---- FIN GÉNÉRÉ AUTOMATIQUEMENT: badges d’onglets ---- */}
