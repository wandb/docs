---
title: "Quelles sont les bonnes pratiques pour gérer les erreurs de W&B Inférence ?"
keywords: ["Inférence"]
---

Suivez ces bonnes pratiques pour gérer correctement les erreurs de W&amp;B Inférence et garantir la fiabilité de vos applications.

<div id="1-always-implement-error-handling">
  ## 1. Mettez toujours en place une gestion des erreurs
</div>

Encapsulez les appels d’API dans des blocs try-except :

```python
import openai

try:
    response = client.chat.completions.create(
        model="meta-llama/Llama-3.1-8B-Instruct",
        messages=messages
    )
except Exception as e:
    print(f"Error: {e}")
    # Gérer l'erreur de façon appropriée
```

<div id="2-use-retry-logic-with-exponential-backoff">
  ## 2. Utiliser une logique de réessai avec backoff exponentiel
</div>

```python
import time
from typing import Optional

def call_inference_with_retry(
    client, 
    messages, 
    model: str,
    max_retries: int = 3,
    base_delay: float = 1.0
) -> Optional[str]:
    for attempt in range(max_retries):
        try:
            response = client.chat.completions.create(
                model=model,
                messages=messages
            )
            return response.choices[0].message.content
        except Exception as e:
            if attempt == max_retries - 1:
                raise
            
            # Calculer le délai avec un backoff exponentiel
            delay = base_delay * (2 ** attempt)
            print(f"Tentative {attempt + 1} échouée, nouvelle tentative dans {delay}s...")
            time.sleep(delay)
    
    return None
```

<div id="3-monitor-your-usage">
  ## 3. Surveillez votre utilisation
</div>

* Suivez l’utilisation des crédits sur la page de facturation de W&amp;B
* Configurez des alertes avant d’atteindre les limites
* Consignez l’utilisation de l’API dans le journal de votre application

<div id="4-handle-specific-error-codes">
  ## 4. Gérez les codes d’erreur spécifiques
</div>

```python
def handle_inference_error(error):
    error_str = str(error)
    
    if "401" in error_str:
        # Authentification invalide
        raise ValueError("Vérifiez votre clé API et la configuration du projet")
    elif "402" in error_str:
        # Crédits insuffisants
        raise ValueError("Crédits insuffisants")
    elif "429" in error_str:
        # Limite de débit atteinte
        return "retry"
    elif "500" in error_str or "503" in error_str:
        # Erreur serveur
        return "retry"
    else:
        # Erreur inconnue
        raise
```

<div id="5-set-appropriate-timeouts">
  ## 5. Définissez des délais d’expiration adaptés
</div>

Configurez des délais d’expiration raisonnables pour votre cas d’usage :

```python
# Pour les réponses plus longues
client = openai.OpenAI(
    base_url='https://api.inference.wandb.ai/v1',
    api_key="your-api-key",
    timeout=60.0  # délai d'expiration de 60 secondes
)
```

<div id="additional-tips">
  ## Conseils supplémentaires
</div>

* Consignez les erreurs avec des horodatages pour faciliter le débogage
* Utilisez des opérations asynchrones pour mieux gérer la concurrence
* Implémentez des mécanismes de disjoncteur pour les systèmes de production
* Mettez les réponses en cache lorsque c’est pertinent afin de réduire les appels à l’API

***

{/* ---- GÉNÉRÉ AUTOMATIQUEMENT: badges d’onglet ----
  Géré par scripts/knowledgebase-nav/generate_tags.py à partir des mots-clés du front matter.
  Ne modifiez pas manuellement le contenu entre ces marqueurs.
  ---- */}

<Badge stroke shape="pill" color="orange" size="md">[Inférence](/fr/support/models/tags/inference)</Badge>

{/* ---- FIN GÉNÉRÉ AUTOMATIQUEMENT : badges d’onglet ---- */}
