---
title: "wandb online"
description: "Switch W&B back to online mode after using wandb offline."
---
{/*
  To add introductory content for this command:
  1. Create the snippet file: /snippets/en/_includes/cli/wandb-online.mdx
  2. Add your intro content to that file
  3. Delete this entire comment block and keep only the two lines below:

import WandbOnline from "/snippets/en/_includes/cli/wandb-online.mdx";

<WandbOnline/>

  The snippet will be auto-detected on the next regeneration.
*/}

Re-enable cloud syncing for W&B runs.

Clear the offline mode setting so subsequent runs in this directory sync data to the W&B cloud.

Undoes a previous call to `wandb offline`.

For example, to re-enable cloud syncing after working offline:

```bash
$ wandb online
```

To re-enable cloud syncing, then run a training script:

```bash
$ wandb online && python train.py
```

## Usage

```bash
wandb online
```
