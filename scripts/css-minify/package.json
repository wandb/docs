{
  "name": "wandb-docs-css-minify",
  "version": "1.0.0",
  "private": true,
  "description": "Bundles and minifies the W&B docs CSS partials (./*.css) into a single css/styles.css served by Mintlify.",
  "scripts": {
    "build": "cleancss --inline all main.css -o ../../css/styles.css",
    "build:dev": "cleancss --format beautify --inline all main.css -o ../../css/styles.css",
    "watch": "npm run build:dev && chokidar './*.css' -c 'npm run build:dev'"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "clean-css-cli": "^5.6.3"
  }
}
