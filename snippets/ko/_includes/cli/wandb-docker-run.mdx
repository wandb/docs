---
title: Wandb Docker Run
---

`docker run`을 감싸고 WANDB&#95;API&#95;KEY 및 WANDB&#95;DOCKER 환경 변수를 추가합니다.

시스템에 nvidia-docker 실행 파일이 있고 `--runtime`이 설정되지 않은 경우 런타임도 nvidia로 설정합니다.

자세한 내용은 `docker run --help`를 참조하세요.