---
title: カスタムサマリーメトリクスを記録する
---

```python
"""
カスタムサマリーメトリクスをW&Bに記録する。
"""
import wandb
import random 

with wandb.init(project="<project>") as run:
    # 1から10のランダムな整数値でカスタムサマリーメトリクスを記録する
    run.summary["<metric_name>"] = random.randint(1, 10)
```
