---
title: Experiments の作成
---

```python
"""
W&B で実験を作成します。プロジェクトが存在しない場合、W&B が自動的に作成します。

このファイルは実験の初期化のみを行います。`with` ブロック内にメトリクスや
アーティファクトなどをログするコードを追加できます。
"""
import wandb

# W&B の run を初期化する
with wandb.init(project="<project>") as run:
    # 実験コードをここに記述する
    pass
```
