---
title: 既存の Artifacts にエイリアスを追加
---

```python
"""
既存の W&B Artifacts にエイリアスを追加します。
"""
import wandb

# 既存の Artifacts を取得してエイリアスを追加する
artifact = wandb.Api().artifact("entity/project/artifact:version")
artifact.aliases = ["<new-alias>"]
artifact.save()
```
