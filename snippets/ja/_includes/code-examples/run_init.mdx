---
title: "run の初期化"
---

```python
"""
W&B の run を初期化します。

プロジェクトが存在しない場合、W&B は自動的に作成します。このファイルは
実験を初期化するだけです。メトリクス、アーティファクトなどをログに記録する
コードは `with` ブロック内に追加できます。
"""
import wandb

# リソースを適切に管理するために `with` 文を使用していることに注意してください。
with wandb.init(project="<project>") as run:
    # トレーニングおよびロギングのコードをここに記述します
    pass
```
