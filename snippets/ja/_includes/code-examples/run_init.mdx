---
title: run の初期化
---

```python
"""
W&B の run を初期化します。

プロジェクトが存在しない場合、W&B は自動的に作成します。このファイルは
実験を初期化するだけです。`with` ブロック内にメトリクスやアーティファクト等を
ログするコードを追加できます。
"""
import wandb

# リソースを適切に管理するため、`with` 文を使用しています。
with wandb.init(project="<project>") as run:
    # 学習およびログのコードをここに記述します
    pass
```
