---
title: Wandb Docker Run
---

`docker run` をラップし、WANDB&#95;API&#95;KEY と WANDB&#95;DOCKER の環境変数を追加します。

システムに nvidia-docker 実行ファイルがあり、かつ --runtime が設定されていない場合は、ランタイムも nvidia に設定されます。

詳しくは `docker run --help` を参照してください。