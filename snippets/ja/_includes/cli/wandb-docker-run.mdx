---
title: Wandb Docker Run
---

`docker run` をラップし、環境変数 WANDB_API_KEY と WANDB_DOCKER を追加します。

また、システムに nvidia-docker 実行ファイルが存在し、かつ --runtime が指定されていない場合は、runtime を nvidia に設定します。

詳細については、`docker run --help` を参照してください。